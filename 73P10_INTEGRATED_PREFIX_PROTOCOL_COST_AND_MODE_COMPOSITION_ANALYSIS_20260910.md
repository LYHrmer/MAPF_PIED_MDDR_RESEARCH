# 73P10：整合前缀协议、费用与模式组合的独立建设分析

日期：2026-09-10。NO-SKILL / CONSTRUCTIVE / NONBALLOT；不是正式独立逻辑审查、资格投票或根验收。全文输入为73_PIE_SPATIAL_EVIDENCE_TERMINAL_HANDOFF_AND_PAID_SERVICE_PREEXPERIMENT_DRAFT_20260908.md，SHA256 **0beccea730aced26203406f8ffa69f5ffa93247f80cec64f00ec9de265065965**，155562 bytes / 531 LF。已实际读取全部531行；两处工具输出截断的相应窗口已再次读取补齐。未读其他新审查意见，未重做P7、L7、QEMU或外部来源审计。以下行号均指该固定输入。

本轮判断：多区间计数、组内早扩展、付费FINISHED后退栏的核心包含关系成立；未找到满足正文全部明确守卫的界内碰撞前缀。最需要直接补稿的是**跨原MOVE的控制代际作用域及验证/实际处理的唯一写入点**，它目前不能仅凭字段名字决定。另有WHOLE_EDGE初始C赋法的局部文字冲突应修正。本件只给一个主要可构造的接口歧义见证，不为凑满三个而把已经明示的有限日历或停滞负例再算漏洞。全部结论保留HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT。

## 1. 多区间计数与收费：成立的具体理由

L66–80明确把每个资源的交集投影精确合并成有限闭区间，允许非凸资源多次进入。对其中单个[a,d]且q≤b，有

**1{a≤b}−1{d<q}=1{a≤b且d≥q}**。

证明只需a≤d：若d<q，则a≤d<q≤b，减项必对应已加项；其余情况按是否a≤b区分。逐区间相加得到n_r≥0且n_r>0当且仅当I_r与[q,b]相交。d=q仍相交，故退出必须严格；a=b属于进入，不能两端统一用开区间。这个论证不要求I_r只有一段，也不要求r只退休一次。q跨过早段后n_r可归零，b以后进入晚段又变正，必须重新检查owner和所有围栏；正文L76、78、106已要求这一检查，不能把旧tid曾经拥有r当作重新取得它的权利。

局部枚举界的适用域也已收窄正确：成功提交历史内q、b各单调，入口/退出指针各跨每个合并区间至多一次；L80把失败重扫、固定更新费用、区间构造/合并/排序、owner/围栏、位复杂度和所有重试明确另计。因此不能用一次失败后重扫反驳其已经限定的O(c_upd+M_int)，也不能把该局部界再扩写为总运行界。

付费准备与提交必须保留下列具体不变量，建议直接作为L78之后的短释义：已提交(q,b,n,两指针,owner,相关需求版本)是同一快照的同一事务；私有候选失败时丢弃的是未发布增量，已发生费用、日志和保留内存不退；下一候选从已提交指针重新开始，除非同一仍有效私有作业继续且其费用和引用完整保留。成功发布后不再重复兑现旧事件。复制整张计数表或大整数操作若实际发生，同样属于已列费用，不能靠“私有副本”一词免费化。

F_cap/E0保持q_release=0的口径前后一致；F_cap的q_evidence不代替实际退休。E0仍须为它真正使用的组/cap几何、owner与围栏检查付费，但不被迫建立退出事件或D评分索引。不能把已公开的精度合同当作一次免费实测q；GROUP_ADMIT只用该服务能力合同做B_i证明，实际退休仍走付费POSITION。

## 2. GROUP_ADMIT、RETIRE与条件进展：无需新ACK的证明

L94–110和L126–140构成可用的顺序：全组A/B及外国责任/围栏检查完成后，中心一次提交准入、各tid初始cap、持久累计授权和F_G；初始A_i含parent resident并两两不交，因此各agent安装和START可以异步。未完成事务只保留各旧resident，没有半组已发权限。中心单计算/待发布槽排除了“计算期间别的中心作业先改owner”的交错；物理继续推进不改变未授resident的保持证明。

组内早扩展的包含式可逐项成立。当前S_i=Mask(U_i(q_i,b_i))，x>b_i；凡属于新目标而不属于S_i的资源，必由[b_i,x]段带来，故

**Mask(U_i(q_i,x))减S_i ⊆ Mask(U_i(b_i,x)) ⊆ B_i**，

因为b_i≥c_i≥l_i。这里只是包含，不误写为等式；一个资源可能同时触到旧段和新段。早扩展因此不需要等待全员达到c_i，但仍须逐个实际owner/围栏检查并先预留后签发。q退休与扩展各自完整提交，不能把未成功扩展的指针推进发布出去。

对成员j的完整剩余扩展需求，外国阻塞资源r不属于j已拥有的S_j，所以r属于B_j。若holder i已经q_release,i≥l_i，其现有责任只在B_i；若中心已FINISHED，其endpoint也在B_i。B_i与B_j不交，故这些已清holder不可能阻塞j的剩余新增集合。未清但物理已END的holder在中心完成终点链前仍为Active；正文没有把物理END自动计作中心清除。由此可解释L519–521的“相关正分只来自尚未清的非endpoint责任”，并保留无组外额外正分竞争的限定；不能推出跨组纯D公平。

条件进展还确实需要L517列明的实际后继链：到达分析时刻T以后重新CAPTURE，合法精度使q_sample达到l_i，随后完整付费交付和退休；或本地END、证书、中心FINISHED完整完成。早采样晚交付、无后继QUERY、PROCESS_SLICE不够、只有原消息最终返回均不满足此条件。每次有效清除使有限未清集合严格减少，但查询次数和重传费用不由组员数界定。原END以后到真实Q还要任务/服务行/全实体谓词；本证明没有跨越这些层次。

GROUP_RETIRE只要以**该组固定成员的原tid及已核END_PROOF支撑的中心FINISHED**为依据，就不需要额外安装、到cap、START或READY消费ACK。L126的END已经在s=ell、v_ref=0处永久关闭原入口并提升g_i；L130–132才允许由其证书执行FINISHED；L134的READY消费在后。退栏时尚有未消费READY不使原tid恢复可执行。退栏只去掉额外准入禁令，不删除endpoint；新MOVE还须本地RESIDENT_OPEN、c/parent和PRECHECK，故不会因退栏先于READY消费而提前进入。

建议在L110补一句可判定释义：退栏凭已验证END_PROOF所蕴含的原tid永久关闭事实，不读取本地当前C/s或待ACTUATOR列表，也不以READY已发送/已消费作为替代；组记录在退栏完成以前持续引用各成员关闭事实和接续gamma_prime，禁止GC先删去唯一证明。这里是把既有跨角色证据链写清，不是新增ACK、server或证明采样。

围栏确有未来排他能力，故不能称完全FREE；它不负责代替owner覆盖实体。全部成员FINISHED之前禁止各成员下一原MOVE，正是保持B_i证明域所需的屏障。允许合法READY消费和原顶点WAIT不会产生新空间需求。永久no-start/中心停止、无退栏token等会长期锁栏，正文已把这种开销和负结果保留。E0/F_cap仍有相同cap/维护机会；它们在指定环中的不能完成首先来自q_release=0与唯一owner，并非删掉扩展机会，不能将该特定否定外推为所有无途中观测方法均无执行解。

## 3. 唯一主要接口歧义见证：控制代际跨tid与两阶段写入

L88将control_epoch列在“中心活动记录”，L118说“初始中心control_epoch”含RUN_ALLOWED；L120却要求“本地保持最大已验证control_epoch”，且只有低代际不得覆盖高代际，没有明确最大值按(session,i)还是按tid取、换原MOVE时如何续接。L120同时说认证通过的新尝试在“实际处理点”登记水位；L226把这些字段归executor，L266把实际接纳归共同ACTUATOR容量。认证完成、Srv_E后继发布和ACTUATOR是不同事件，最大已验证代际究竟在哪一步产生效果尚未唯一。

最小区分前缀不需要碰撞：某原tid经历合法BRAKE/RESUME，gate保存较高代际h；它后来真正END、中心FINISHED，所在组全员完成且付费退栏，READY也已合法消费。下一原tid完整准入、cap安装，并获得正常的CONTROL_RETRY RUN。若新活动记录重置其初始代际为小于h的值，而本地最大值跨tid保留，所有该新tid普通RUN都可被低代际规则拒绝；若记录按agent连续增长，完全相同业务历史可以START。正文尚未选择两种解释中的唯一一种。前者是条件协议停滞，**不是**满足P1全部前提后的反例，也不能称已证碰撞；它说明研究映射不能靠实现者私下选择作用域来定义。

同一问题还影响先验证后处理：旧RUN已验证待ACTUATOR，新STOP在Srv_E验证完成但尚未ACTUATOR时，“最大已验证代际已经有效”与“仅实际处理才更新代际”会给旧RUN不同命运。正文只承诺MODE在后继ACTUATOR才作用，因此应明确采用后者，不能在认证阶段隐含提前实施STOP，同时又声称全部控制语义在ACTUATOR才发生。

建议只采用下面这一套最小纸面补全，不保留多个运行时备选：

- 中心control_epoch改为同session、同agent的持久单调控制代际，跨原tid、FINISHED和退栏均不重置。每次初始普通/组GRANT提交为该agent取得严格后继代际并设RUN_ALLOWED意图；它只创建意图，不新增自动RUN。MODE BRAKE/RESUME仅对当前GRANTED原tid取后继代际并改变意图，无合法tid则付费NO_MATCH且不修改后来tid的意图。累计cap扩展/重发不另推进代际。几何/控制器实现版本与此可变意图代际严格区分。
- 控制尝试完整绑定session/agent/tid/slot/parent、上述代际与模式、唯一公共机会的全序位置及来源kind。SOURCE_REQUEST modulo 3和MODE共用这一公共次序，不各起一个可能碰撞的局部计数器。n_i按公共出现次数派生，空结果也消耗该次出现；“不改变相位”释为不额外插入/跳过后续预定角色。
- Srv_E只对控制envelope认证、复制及发布准备好的候选，不在验证阶段改有效控制代际/已处理水位或运动模式；把L120字段改称“最大已实际处理控制代际及该代际内已处理公共尝试位置”。ACTUATOR按已发布候选的固定入列次序一次处理一条，在同一有费用保障的原子接纳中核完整原身份/closed/g_i、代际、尝试位置和必要守卫，登记新有效水位，再施加一次作用或无作用结果。相同代际更旧尝试不能以后复活；更低代际不能覆盖。已闭/其他tid包永不更新当前活tid的水位，旧closed事实已经足以使它永久无效，无需无限旧尝试位图。
- 未认证消息仍不推进任何控制水位。对身份合格且当前代际的新尝试，缺grant、no-start、C=s、运行中无作用或BRAKE尚未到零均只消费本次尝试，不留自动启动钩子。候选队列入列与实际消费分别形成不可变记录/单调消费事实，后继Srv_E提交不得用旧快照覆盖ACTUATOR已写的水位；发布/接纳仍遵守同一本地事件全序。需要的队列/引用/幂等字段、每次拒绝和整数位长均使用现有本地/共同控制容量付费，不借新ACK、额外机会或全局锁。

这套补全不声称任何剩余有限O都含足够RUN；它只是让每条实际机会及消息有唯一含义。若根选择不同代际作用域，也必须同时替换整套初始赋代、换tid和旧消息处理规则，不能只补一句“保持单调”而继续混用两个域。

## 4. 模式、角色与整边残留的具体核对

SOURCE_REQUEST的公共n_i modulo 3与GROUP_ADMIT/RETIRE各自独立token可以在同Srv_C FIFO组合；没有发现必须增设优先级或在一次token内做两件事的依赖。角色调度必须发生在公共出现次序，业务选择才在收费中心快照；不能根据no-start或缺cap ACK改变下一个子角色。GROUP_RETIRE后只解除组屏障，下一SOURCE_OR_EXTEND仍等合法Kc/PRECHECK。中心不需要持槽等待本地ACK。

建议把模式许可压成明确的稿内表述：ACTIVE允许既定查询与新准入；SHARED-OFF仅停止新的途中POSITION，仍保留原声明的其他能力；DRAIN不允许任何尚UNGRANTED的原请求取得首次正cap，也不允许GROUP_ADMIT或新规划承诺，但保留已GRANTED原tid的合法扩展、cap/控制重试、终点/退栏和合法本地消费；BRAKE/RESUME是独立的agent控制意图，不通过切换查询模式取消已发cap。这样L170的UNGRANTED重试分支必须先服从DRAIN守卫，不能误把“已创建请求”当“已承诺运动”。QUERY_ON若承担恢复ACTIVE，应明确记作这次付费模式提交；不能从名称猜测DRAIN只切查询位。

L529的WHOLE_EDGE参照写“初始b=C=ell”，与L88无活动时b/C为空授权状态、L116累计包实际安装后才改变C的共享接口不一致。若只是指两端都完成合法接纳后的状态，直接改为“普通中心准入提交b=ell；本地C仍待合法grant安装后才为ell，丢包/未安装期间不得预置C”。WHOLE_EDGE省去部分cap/组工作可保留；不能给它免费、即时安装或额外START。此为残留表述冲突，不据一句简称判定整个主GROUP_PREFIX已有违规赋值。

未发现L64主责任仍把M0当初始已持有、L78仍用单lambda退休、L164用整边退出阈值误判当前前缀endpoint、或L176把DRAIN等同可保证排空的残留。L355“不做无用途lambda预计算”措辞可换成“不构建无用途退出事件/退休索引”，因为主算法已不是单lambda组织；它不改变费用事实。主F_cap与旧F的区别在L8及L529已声明，不应继续使用旧整边2×2因子名称解释现在结果。

## 5. C9真实意见与不用skill逐条复算

本新题实际执行一次，functions cell109→exec29561，最终chunk455020、exit0；CLI session 7fa8583c-34b7-4062-a7d6-bb61f0808613，duration_ms175504。完整返回明确modelUsage为claude-opus-5、costUSD0.33605375，未重试。完整JSON与未改动result分别保存在73C9_GROUP_RETIRE_CONTROL_MODE_RAW_RESPONSE_20260910.json及73C9_GROUP_RETIRE_CONTROL_MODE_COMPLETE_RECEIPT_20260910.md。提示不灌入本件第3节结论，只给候选身份、必要原文与问题。意见实际成功取得不等于观点成立。

**主反例否定。** C9第5步由“FINISHED先于READY消费”推“本地原tid未闭、仍在中点HOLD”。这个推导错误。L126的本地END先关闭原入口、提升g_i，再产生EndRecord；L130–132的中心FINISHED必须验证这个证据。中点C<ell、s<C不能产生该END。READY消费改变c与resident身份，不创造或撤回END。其第7步还把已安装cap覆盖的资源r称为无owner；在END以前P2/P3与q≤s保证(s,C]仍在当前责任内，围栏从来不是这段实体覆盖的唯一屏障。因此第5步和第7步均不属于合法前缀，不能认定物理安全漏洞。

**所谓二分否定。** 中心FINISHED蕴含历史本地END，不蕴含本地READY消费。END_PROOF正是正文已许可、完整付费的跨角色证据；它不属于被禁止的cap安装/到达中点/START ACK，也无需中心跨writer持锁读取当前C。不存在“要么未知当前控制，要么必须等待消费”的二选一。

**修复1–3不采用。** 在中心FINISHED新写关闭标记、再把它携往本地，不能修复一个旧在途RUN对后来标记尚不知情的问题；签发时的旧envelope不会被后来的中心事实自动改变。当前方案安全的真正方向是本地原END先不可逆关闭，再以证据通知中心。可以采用的是把退栏守卫明确引用这个已验证历史事实，而不是改成仅中心标记已发布。

**修复4不采用其新增机制。** 原owner责任已独立于F_G，终点提交保留endpoint、清余下原tid资源需要已核END；这是现有P3，不需要额外“lineage消灭”闸。已签发cap在本地closed后永久失效，但审计/在途引用仍可存在，不能因引用尚存而否定安全退栏，也不能提前GC引用。

**修复5否定。** C9要求补偿只能中心→executor、永不反向，与已有END_PROOF、CURSOR等合法付费证据路线冲突。无安装ACK不等于禁止一切反向信息。本件不建议新ACK。

**其最小定义清单分别处理。** FINISHED证据、永久关闭writer、退栏后owner条件在完整候选已有；提示摘录没有逐字展开，不表示正文不存在。已验证未处理命令的代际/水位总顺序确可加强，由第3节给唯一补全。空结果是否推进n_i由“公开出现次序”已经决定，补释防歧义足够。C9说换新tid后STOP_LATCHED必持续且必须MODE RESUME，也不是当前“初始RUN_ALLOWED”已规定的唯一含义；这正是应明确跨tid域的原因，不能把其猜测当合法等待定论。

## 6. 完成范围与保留项

本件完成新整合接口的有限纸面核对及一个新Opus任务的完整实际终态保存。计数、早扩展、FINISHED支撑退栏与E0同能力在所列前提下可以直接承接；跨tid控制代际及ACTUATOR写入点须根作明确选择，WHOLE_EDGE初始C措辞应修正。没有证明所有主roster均可准入/推进、纯D全局公平、有限窗口必有收益或外部SYSTEM已经同能力闭合，也没有用新顾问意见替代正式顺序审查。

只以apply_patch新增本件、C9提示、完整JSON及终态回执；未改73、旧报告、行政页、Git或桌面。未实施任何代码/伪代码、构建、研究测试、仿真、调参、实例化O、保护参数赋值或载荷/结果生成；未读35–38及受限载荷。六门仍保持原有效裁决2 PASS / 3 UNKNOWN / 1 FAIL，HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT。
