# 73E10：根构造的非抢占规划服务备选与子代回收接口

2026-09-09，/root直接分析，NO-SKILL / CONSTRUCTIVE / NONBALLOT。当前73实核SHA256为1881a85c7a21e5e5fe55e602e246cc057b964df4114dc6c8ab4bbde42f200a54，130281 bytes /466行；E8已实际完整根读。本件不是偷偷改变已送审候选，也不把新模型当当前73已有的执行后端。没有实现、伪代码、构建、研究测试、仿真、运行规划核、调参、实验载荷或结果。保护参数原义不变且未赋值，保持HOLD。

## 1. 需要作出的选择

现73将固定PROCESS_SLICE容量、源每一步收费及公共行读钟绑定。E8已给出具体不相容点：普通墙钟暂停通知不能保证工人在最后一个已授权步骤之前停止。根不将“事后计时再补费”伪装为同一模型的实现。

本件提出另一个明确命名的备选 **NP-PLANNER**：只把Srv_P改为FIFO、一次一个、非抢占的隔离规划作业服务，其占站时长取有证据的实际经过时间；中心/可信采样/网络等仍保留各自付费权限与发布边界。它不使用原Srv_P逐指令脉冲容量，也不声称逐输出与该模型相同。若选用，必须修改全稿涉及Srv_P供给、源时钟、随机环境、失败和推断的合同；不能只给backend字段换名字。

该备选仍完整研究大规模lifelong闭环：规划期间物理控制、任务服务、中心和其他agent继续，缓慢/失败规划不重开评价窗。改变的是计算服务域，不是将任务缩为离线单次路径或不计规划成本。

## 2. 非抢占作业的因果接口

中心已完成收费冻结输入/版本检查后，规划作业才可进入Srv_P队列。作业实际开始点为t_start；同时绑定不可变输入I、源状态基准S、原预算槽、源/依赖/外部输入版本和唯一job_id。Srv_P在前一作业完整释放之前不能启动下一作业；不存在用两个实际worker交错却只算一个站的路径。

从这个开始点到封存，worker只读I和自己的可变副本。当前真实任务、实体、中心owner、尚未投送消息、后来的K_task/Kc均不可见，任何普通日志或共享引用也不能旁路进入。允许外部接口仅为明确的时钟、该作业私有输入/输出和控制终止信号；未知外部输入仍不可认证。

设该次实际服务以经过量d_body完成并封存结果Y及候选状态S_prime。最早可用点为t_seal=t_start+d_body；管理器接收/检验/复制与中心CAS另按原收费链，故Y不能在t_seal之前供任何算法读取，亦不因已在宿主计算出来而提早可见。中心仍用接纳时最新合法知识复核PROJECT/目标/cut及版本，拒收后状态不继承，全部费用保留。

这里可以按离散事件顺序预计算这个私有作业的返回及耗时，再把结果放在t_seal之后的不可见封存对象中；但仅当返回过程没有未来世界输入时才合法。形式上，给定(I,S,本作业执行环境轨迹)，(Y,S_prime,d_body)是该局部过程的结果；虚拟系统在t_seal以前的算法状态不能依赖Y的值。以事件全序归纳，队列登记、物理演化和合法快照与该私有返回隔离，就不因宿主先后计算顺序产生未来信息通道。

这项因果论证不使耗时自动确定，也不允许边算规划边读取后续真值。中心若提前看Y是否成功来决定何时进行QUERY，或根据尚不可见d_body改变公共机会/派单，均违反此接口。已知的公共未来FAULT只能由封闭控制层按规定施加，不能作为规划输入改善输出。

## 3. 时钟需要真实改写，不能混用

一个可供独立审查的唯一经过钟映射是：以受监管的宿主单调经过钟h为服务时基，记录h_start；worker活动期间原chrono接口返回t_start+(h_now−h_start)，按固定duration period及原转换方式表示。它保留该次服务的实际经过量、原局部起点/重置及比较符号；排队发生在本次开始以前，跨作业保存的源时钟值与新的t_start共同反映中间间隔。

这不同于73“同一公共PROCESS_SLICE行内now可同值”的映射，因此必须标为新适配域。用CPU时间替代这里的经过钟会忽略被调度器停顿等经过量，不能仍称这个模型。CPU clock日志另用该执行域实际CPU计量和明确的基准偏移，全部失败worker的CPU总量在外部费用账中分别保留，不能靠状态拒收回滚费用。

根实际读了Linux官方手册的CLOCK_MONOTONIC、CLOCK_MONOTONIC_RAW和CLOCK_PROCESS_CPUTIME_ID段：单调经过钟与进程CPU钟是不同接口，连续单调读数也可能相同；RAW不受NTP渐进修正。此备选选择RAW作为宿主经过时基，仍需未来实际绑定其可用性、精度与暂停制度。[clock_gettime官方说明](https://man7.org/linux/man-pages/man3/clock_gettime.3.html)。本次页面实际标识为man-pages 5.07/2020版，未冒称最新平台源码已经核验。

源clock接口适配必须完整覆盖实际链接路径，包含被固定ABI采用的chrono入口；不能只拦截一个函数名就声称库、vDSO和系统调用都已覆盖。若进程CPU计数因fork重置，源所见累计CPU域须有明确偏移；不能拿宿主自动计数冒充同一长期worker。尚未完整取得的外部调用表依然是资格前提。

## 4. 供给和成本如何改变

NP-PLANNER的共同供给是同样的单工人服务能力、FIFO及固定可开始机会，而不是每个公共行可消耗的精确指令数。开始之后持续占有该站直至封存、必要控制与完整释放；其他臂同站采用相同规则。等待队列的作业没有求解进展，但输入管理/保留内存的实际开销仍记账。

必须分报至少：源体经过时间、实际CPU、父子创建/页表与COW、冻结输入复制、候选状态保留、日志/输出缓冲、中心验证、失败处理和确认回收。d_body不能同时被当作精确CPU量、整个端到端延迟和所有管理开销；从中心输入CAPTURE到接纳终态的elapsed另记。已发生的失败/过期/重复成本不能被丢弃。

如果保留STRICT预付语义，预付的是整个固定观察期中该共同服务能力的供给，并另列实际工作；不强制各臂跑无用规划来凑相同耗时。原NATURAL的逐指令工作向量不能原样继承，应给新量纲和费用映射；源指令计数最多为另行核得的描述项，不是这个模型的授权闸门。

物理/任务时钟继续走自己的共同世界全序。某次宿主规划运行占用了真实评估器进程时间，不意味着虚拟机器人在那段世界时间HOLD；不能在计算返回后从同一旧时刻重开物理世界或重新评分。对每个已产生的服务终点按合法行序插入可见结果，插入规则须成为公共事件文法的一部分，不能让未声明的即时回调穿过原发布边界。

## 5. 故障与截止是仍须显式处理的接口

普通目标变动不打断源函数：它只可能令后继CAS拒收。中心fail-stop使其不再接纳/发布结果，原物理责任继续；worker已经实际发生的工作仍计费。非抢占并不授权失去所有退出控制，也不保证任意源会正常返回。

对于已规定的WORKER_FAIL、初始化截止或全臂Stop，需要监管器按映射后的相应时点撤销发布资格并发出真实终止动作；信号发送至确认退出之间仍可能有尾部工作，该尾部单列收费且持续占站，不声称恰在一个指令边界中断。所有末端动作只有已依法发布的结果才可作用，Stop之后不补任务服务。原plan内部预算及其增量/重置规则不能改写为一个新的未声明硬截止。

不能只在完整规划算完以后按结果把耗时截成E−t_start：那会遗漏真正的中断行为/费用，也可能保留本来应丢弃的后状态。若使用离散事件预计算，监管终止的实际轨迹、测得终态与虚拟截止必须一致；不能证明则该次服务成本/源语义未认证，不拿主机“后来终于返回”补一个在窗内完成事件。

依旧不存在“kill成功即可复用”规则。未知退出、日志不足或宿主故障必须保留原分类及UNVERIFIABLE/INFERENCE_UNAVAILABLE边界，不能通过丢块维持漂亮结果。这里给出了尾部可超出正常服务段的合法位置，尚未证明具体监管接口覆盖所有源失败路径。

## 6. 子代提升：用已核Linux接口关闭一个具体缺口

E8指出，基准B fork出的事务T仍是B的孩子，管理器M的业务CAS不能令M直接wait任意孙代。根本次另核 **PR_SET_CHILD_SUBREAPER**：孤儿在直接父亲退出后交给最近存活的祖先subreaper，后者收到终止通知并可wait取终态；该属性不由fork子代继承。[Linux subreaper官方手册](https://man7.org/linux/man-pages/man2/PR_SET_CHILD_SUBREAPER.2const.html)。

因此可给出有条件的明确结构：M事前成功建立subreaper身份并保持存活；B与T处于其后代链、没有另一个更近的subreaper/不明逃逸子树。拒收T时，在B存活且有明确回收路径下确认T退出，再继续保留B。接纳T时，先将T保持在封存边界，中心合法接纳候选状态后才使B受控退出；M实际确认B退出和T被重新接续后，T才作为下一基准进入可派新事务状态。不能只发退出信号或改句柄便开始下一事务。

若T先于B异常退出，它的状态及未确认收据不等于可接纳基准；若M死亡、重收养失败或出现未登记后代，资格失败并保留未回收责任。直到全部必要退出/父子责任明晰才释放相应站和资源，所有管理等待/内存成本照计。

这只解决已给Linux前提下的父子回收所有权入口。fork仍共享某些外部文件描述符状态；stdio缓冲、映射、PRNG、线程/atfork钩子和动态库必须满足独立快照合同。不能从subreaper推出内存/外部状态已经可回滚，更不能将此根构造改名为已完成实现。

## 7. 对公平推断的实质代价

NP-PLANNER耗时含宿主执行环境影响。相同block根/源种子不再单独确定结果时间，时钟驱动的搜索输出也可能变化。必须明确新的环境输入层、实际机器/绑定/运行顺序与隔离证据、原始时间和完整费用；不能继续声称单靠seed即可逐结果重放。

原73的独立block与配对推断要重新对应实际数据生成方式。随机化臂运行顺序有助于避免系统性顺序偏差，但本身不证明宿主噪声跨block独立；同机器、同输入也不证明反事实服务轨迹相同。需要要么有可核的独立环境创新模型与对应获取制度，要么选择适用于实际依赖/随机化设计的推断；没有此证据时只可描述，不套原置信区间。

不得提前采集CPU基准、生成耗时表、设速度倍率或用保护预算去填这个缺口。本件没有任何实际测量或性能结果，也没有把该备选称为MAPF领域已经认可的本项目实验制度。是否选择它，应比较可复现性、实际费用解释、全流程实现负担和公平闭环，而不只比较哪条路线更容易写成PASS。

## 8. 当前根裁决

- NP-PLANNER具有明确的冻结输入/延后可见因果接口，保留lifelong闭环目标；它确实避开“每个源指令之前精确暂停”的要求。
- 代价是改变Srv_P供给、源读钟、失败尾部和统计环境域。当前不能当作现73的即插即用后端，不能把它与精确脉冲模型混报为同一profile。
- subreaper为E8的子代提升回收缺口提供了实际官方接口，但只在已列条件下成立；外部状态与监管完整性仍待核。
- 根保持它为实质备选，与E9正在有界核验的指令执行器比较后再作全稿选择；不是宣布主准备闭合，也不是退回不计计算成本的研究。

本次只新增此研究文档。外部一手阅读限上述两个官方手册，subreaper主体段全文、clock_gettime相关时钟/返回/实现说明窗口；未取源码、实现、数据载荷或实验表。以上公式与接口均为声明式设计，不是可执行算法或生成的实验事件表。HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT。
