# 73L26：图表骨架设计与其期刊惯例依据

2026-09-18；NONBALLOT。本件为 `MANUSCRIPT_PREEXPERIMENT.md` §6 表格中五项工件设计骨架，依据是对同域已发表论文图注的实际抽取，而非通用制图建议。本件**不产生任何数值、不预设任何结果方向**；骨架只规定面板、轴、编码与图注义务。设计参数（agent 阶梯、密度、窗口长度）一律不在此选定，理由见第 5 节。

## 1. 证据来源与抽取方式

对下列 7 份文件用 `pdftotext -layout` 抽全文文本，再提取 `Fig./Figure/TABLE/Table` 起始的图表标题原文。**只读图注与其邻接正文，未逐图检查像素，未复算任何数值。**

| 来源 | 文本行数 | 位置 |
|---|---|---|
| Zhong 等，Autonomous Robots 2026（主动观测） | 1339 | 既有阅读包 `02` |
| Chen 等，AAAI 2021（S2M2） | 625 | 既有阅读包 `03` |
| Yan 等，WinkTPG（arXiv 2508.01495v2） | 980 | 既有阅读包 `07` |
| Li 等，AAAI 2021（RHCR） | 584 | 既有阅读包 `10` |
| Cao，SCALE（arXiv 2607.00591v2） | 864 | 既有阅读包 `11` |
| Chen 等，AAAI 2024（Traffic Flow） | 615 | 既有阅读包 `12` |
| Berndt 等，T-RO 2024 作者预印本 | 1391 | 73L24 §3 所核 arXiv:2312.04190v1 |

既有阅读包位于仓库之外、不受版本控制，其字节核验以该包 `99` 文档为准。

## 2. 抽取到的通行做法（每条附出处原文）

1. **横轴为 agent 数。** RHCR 表 1–3 与 Traffic Flow 表 1 均按 agent 数成列（`with number of agents varies from 200 to 1000`）；SCALE `Fig. 6: Success rate versus the number of robots`。
2. **主指标吞吐、次指标响应时间，上下双面板。** Traffic Flow `Figure 4: Lifelong MAPF. Average throughput (top) and response time in second (bottom).`；`Figure 5: Average tasks finished (top) and average response time (bottom)`。
3. **离差用阴影带，不用误差棒。** Traffic Flow 图 4 图注 `Shaded regions show standard deviation.`；SCALE `shaded bands`。
4. **成功率单列报告。** WinkTPG `Fig. 7: ... and success rate (line plots in even-numbered columns)`；SCALE 图 6 与 `Fig. 7: Computation time of successful MRPP instances in Fig. 6`。
5. **超时以记号保留而非删样本。** RHCR `Table 2: ... "-" indicates that it takes more ...`。
6. **扰动与故障设置单独成表。** SCALE `TABLE I: Settings for External Disturbances in Experiments`，列为 `Item | Probability | Pattern`，行如 `Packet loss | 1% / planning cycle | State update loss`。
7. **多面板网格中列位置编码指标、线型编码方法。** WinkTPG 图 7 以奇偶列分置次优度／运行时与成功率；图 8、9、11 以 `ADG (solid lines) and ...` 用线型区分方法。
8. **成本—收益权衡用帕累托前沿。** Zhong `Fig. 6: A Pareto frontier of schedules`。
9. **执行策略比较单独成表并含等待时间。** SCALE `TABLE III: Comparison of different execution strategies of task response time, measured as the waiting time between ...`。

## 3. 本项目必须偏离通行做法的三处

这三处不是风格选择，是本稿已定义的推断结构所强制的。若照通行做法画，图会与 §4.5 的主张不符。

**（一）阴影带不得表示标准差。** 第 2 节第 3 条是同域最常见做法，但本稿主量是**预注册权重下的配对差点估计**，其不确定性由 §4.5 的加权 Hoeffding 半径给出，与块间标准差不是同一对象。图中所绘区间必须是 §4.5 的预注册区间，且图注须明写“所示区间为预注册加权区间，非样本标准差带；两个区间不声明同时覆盖”。这一条 §6 表格原已要求，此处落到图注义务。

**（二）成功率与失败率的分母必须是固定 roster。** 通行做法常报“成功实例中的”指标（SCALE 图 7 明写 `successful MRPP instances`）。本稿 §4.3 规定比例以固定 roster 为分母、不筛成功初始化者、状态未知保留为未知。因此不得出现任何以“共同成功样本”为分母的面板；确需展示成功者子集时，须与固定分母版本并列。

**（三）横轴不能是自选的 agent 阶梯。** 见第 5 节。

## 4. 五项工件的骨架

编号对应 §6 表格五行。所有轴范围、刻度与序列数量待绑定后填写，此处只定结构。

### 工件一（表）实验来源与臂定义

沿 SCALE `TABLE II`（机器人参数）与 `TABLE I`（扰动设置）的两表分置做法，拆为两张：

- **表 1a 来源与臂**：行为臂（D-S、R-S、E0、D-F_cap、R-F_cap、H19E0、H19R-S），列为「提议来源 | 查询策略 | 实际释放制度 | 几何实现 | 计费制度 | 共同能力 | 资格状态」。资格状态取 `已绑定／UNKNOWN／不可估／不可推断` 四值之一，不留空白格。
- **表 1b 模型条件与扰动**：列仿 SCALE 的 `Item | 取值或域 | 作用位置`。行含 δ 的登记敏感性域、`a_launch ≤ a_hi` 准入约束、丢包与普通误报设置、no-start 条件。**δ 一行给域不给点值**（§4.5 已定）。

### 工件二（图）固定窗口真实任务效应 —— 主图

- **面板结构**：上下两行，仿 Traffic Flow 图 4。上：块级配对差 \(d_b=(Q_{D\text{-}S,b}-Q_{R\text{-}S,b})/E_b\) 的散点；下：D-S−E0 的同类散点。两行共享横轴。
- **叠加**：每行叠加预注册权重点估计（横线）与 §4.5 区间（区间带），**带的图注按第 3 节（一）声明**。
- **纵轴**：真实可证服务率之差，单位「服务／单位暴露」。**不得标为 throughput**——本稿的量经真实服务判定，与未区分判定的吞吐不同类（D5 维度的区别）。
- **附栏**：右侧或下方附固定 roster 的评分可用情况条形，分母固定，仿第 2 节第 5 条以记号保留不可估项。

### 工件三（表）外部系统比较

- 单表并列 D-S−H19E0 与 D-S−H19R-S，仿 SCALE `TABLE III` 的执行策略比较表并加等待时间列。
- 列：「比较对 | 点估计 | 区间（α/2）| 所需评分是否齐备 | 比较域是否合格 | 系统级解释限制」。
- 表注须写：两比较构成单独推断族、按并合界控制同时覆盖、不与主比较合并；缺任一必需评分则整族不可估。

### 工件四（图）已登记机制分析

- 仿 WinkTPG 图 7 的多面板网格：行为机制维度（D/R × S/F_cap 的任务效应；同策略下 G-GEOM/N-GEOM 的工作量与等待），列为指标。**线型编码方法、位置编码指标**，不用颜色单独承载信息（须黑白可读）。
- 另设一子图用帕累托前沿（仿 Zhong 图 6）：横轴为模型内已计费工作量，纵轴为真实服务率。该子图是本稿「付费是否值得」的自然呈现形式，且同域有先例。
- 图注须写：退休事件数与局部提前放行**不代替**任务量；机制面板用于解释，主效应由工件二决定。

### 工件五（表）成本、安全与失败

- 分栏：模型内工作账（规划／查询选择／采样与封装／几何与索引／消息与验证／授权与维护／日志／重复与失败）与宿主资源（CPU、经过时间、内存）**左右分置**，不混列。
- 状态行：INIT 是否完成及其时间、首次 MOVE 是否发生、评分可用性、安全域（已违反／仅可证前缀验证／未知／模型外）、停滞、全部故障标志。**分母固定，未知保留为未知**。
- 压力层单列，使用自己的固定权重，不并入主显著性分析。

## 5. 一处结构性冲突

第 2 节第 1 条显示，同域主图与主表几乎一律以 **agent 数**为横轴或列。而本稿的 density \(N\) 与机会密度在固定 73 中与保护参数并列、须由来源核验绑定（见 73L24 §6）。**因此本稿在密度绑定完成之前，无法采用该领域最标准的主图形式。**

三条出路，均不在此选定：

1. 横轴改为预注册 profile 索引或块索引，放弃与同域论文的直接形式可比性；
2. 待来源核验绑定 agent 阶梯后再采用通行形式——阶梯来自来源而非本文选择；
3. 主图不设扫描轴，只呈现单一预注册设计下的配对差与区间，扫描留作已登记次要分析。

此冲突须在预注册文档中显式裁定并记录理由，不得在结果产生后按可读性择一。

## 6. 本件不主张的内容

不主张：所抽 7 篇构成同域制图惯例的完备样本；这些惯例适用于任何目标期刊（目标期刊尚未选定）；骨架已可直接绘制（轴范围、序列数与面板数均待绑定）；本稿的图将呈现任何特定趋势。第 2 节各条只描述所抽图注的原文，不评价其恰当性；第 3 节的三处偏离由本稿已有定义推出，不因同域常见而放弃。

图件的实际实现、字体线宽配色与导出格式另按投稿期刊要求处理，本件不规定。
