# 72C3：Opus 原动作循环依赖的建设性路线比较

用户明确允许充分使用Claude Opus并行办公。本任务是新的构造问题，不是72C2付费事件提示的重复调用，不正式投票。只根据以下自给问题推理，不用任何skill、工具、文件、网络或子代理；不写代码/测试、不运行或构建仿真、不生成载荷/结果，不给H、T_delay、B_CAL、B_max、B*、P_active、density N任何数值/范围/默认。本任务仅建议供根比较，不将当前路线当唯一答案。请给完整、可核验的建议约2500中文字，以ADVICE-COMPLETE结束；没有足够条件就明确证明为何不足。

研究面向大规模lifelong MAPF，保留PIE-D为唯一产生新MOVE的源。现候选只是连续平移执行wrapper，不改原边/目标/新MOVE顺序，也不插绕行、虚假目标、局部连接动作；允许等待、继续旧动作及已认证的制动/保持。单条原边u→v，参考p=u+s e，s单调、参考终点零速有限到达；真实实体X=p+z+F，固定世界F，z_dot=-kappa z+w，Z为正不变闭盒、w有界可测，实际实体可横向回摆。控制/动力学是假设明确的理想平移模型，不认证实车。参考进度由付费可信采样给下界q<=s，采样/计算/通信/验证/发布均耗有限服务能力和物理时间，期间物理继续；普通反馈可错误，不能释放资源。原时长/无启动错误不能简单改名为空间误差。

现单可信中心对闭瓦片资源保持独占owner。每次完整保留新边U(0)=union_{s∈[0,ell]}(u+s e+Z+F)的M0=Mask(U(0))，只有每个资源FREE或本agent的当前parent resident时才grant。已发grant不因超时撤销；尚未启动仍持全U(0)。正进度后仅退休与U(q)不交资源；endpoint E=v+Z+F始终保留，旧gate不可逆关闭，中心将E转resident、发READY后才消费原逻辑槽，再请求下一原MOVE。中心和gate不分叉、持久，中心fail-stop不清账本/不重启重分配。此规则可条件性证明安全，不能证明全局活性。

关键问题：四邻格的合法联合MAPF环形轮转中，各agent下一边的终点是另一agent的当前resident。所有原MOVE可能在同步离散语义合法，却无法逐个获得独占全M0。即使较稀疏，也可能出现局部循环等待。增大安全裕量、查询进度或多WAIT本身不消除该循环。现计划把这些失败保留全roster，不能靠排除循环、只挑获益图证明大规模有效。主查询比较D-S vs RR-S、必报不主动途中查询而做共同付费终点确认的E0，只识别既有安全底座内的增量，不掩盖此共用失败。

请给四部分：
1. 用最小符号环论证现独占全M0准入为何无法启动，区分保守瓦片冲突、真正连续几何碰撞和离散同步可解；不能把三者混同。
2. 在不产生新MOVE条件下，独立比较最多两种具体改进路线。例如联合接纳/共享事务并不自动安全：必须覆盖不同启动、no-start、制动、参考误差和消息延迟时的连续状态集合。请给必要状态、接纳规则、持续不变量、失败回退及至少一个实际不可行反例；缺必要条件时明确“尚不能构成可用算法”。不要把免费同步屏障或联合立即运动当已证事实。
3. 分析保留现较窄路线vs为循环改进扩大控制/信息假设的科研取舍：最小可以承诺什么效果、哪种反事实对照能证伪，如何避免新共同控制器掩盖查询机制收益。不得声称首次或必有发表价值，不编论文。
4. 给根一份有顺序、可在实验前用推导/源阅读推进的最小待办，不要求现在生成实例或实验。具体指出哪项若不成立应放弃该扩展，而不是无限补协议。

只有实际完整回执才算建议返回；本任务未读取完整候选72或来源全文。请保留NONBALLOT和未知边界。
