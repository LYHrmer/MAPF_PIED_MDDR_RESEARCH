# C26: construct an implementable deterministic compute model

You are a constructive co-designer of a research protocol, not a reviewer voting PASS/FAIL. The user expressly asks for substantive Claude Opus assistance. Use only this complete statement; no tools, skills, browsing, commands, file operations, code or implementation pseudocode. Do not generate experimental payloads or assign experimental parameters. Produce a coherent proposed design in at most 1,500 English words, ending END-C26-20260912. Show the actual construction and its justification, not a catalog of possible backends. It is acceptable to reject part of the proposed route with a precise reason, but offer the smallest coherent alternative. The root will independently verify source/API facts.

## Scientific problem and preserved commitments

The study is large-scale lifelong multi-agent path finding with genuine bounded transverse tracking error and paid observations/processing. A discrete planner proposes four-neighbour MOVE/WAIT paths. A shared adapter preserves each original MOVE and its task/slot, permits only certified starts, monotone within-MOVE progress caps, braking/HOLD, and release of resources proved behind the same MOVE. It cannot invent detours or goals. A single resource authority and per-agent controllers preserve ownership and irreversible END; finishing the physical MOVE, informing the centre, consuming READY, and moving the logical cursor are distinct events.

D selects an active agent by how many distinct legally admissible pending requests its releasable resources block; R is a strong stable-agent round robin with identical information, cap, safety and release abilities. E0 buys no in-motion POSITION but keeps paid terminal notification and the same useful execution capabilities. D-S versus R-S is primary; D-S versus E0 is mandatory. Two named external H19 systems use their own ECBS/ADG planning and otherwise declared common execution abilities. Internal ablations distinguish spatial release from holding the cap reservation. All actual querying, planning, geometry, scoring, failed attempts, communication, index maintenance and first/reused preparation must cost resources. No system gets free business information.

A fixed finite public calendar O orders rows by virtual time and row identity. It includes agent policies, actuator opportunities, task service, source requests, finite computation supply at each server, communication, mode/fault opportunities and cutoff. Its values and all physical/experimental parameter bindings are still unassigned. There is one central application server, one per-agent application server, one persistent planner source server, a task-snapshot server and declared directed communication servers. FIFO and at most one compute/pending publication slot per writer are specified. New work/results become visible only on an originally permitted strict successor row. Public row arrival creates an opaque token, not a free query answer. Physical evolution and true task service proceed at their fixed virtual times while software waits. An independent ideal simulator computes world truth and reports actual host cost separately; algorithms cannot read its truth or future inputs.

Each full paired block evaluates all required methods for the same fixed virtual window E_b. Q counts true world task completions, including legal completions during software initialization. Delta=sum_b w_b (Q_D,b-Q_R,b)/E_b, and theta=E_P[Delta|X], with predeclared weights and bounded Q from finite task opportunities. Original independence is between block innovation roots; arms inside a block may be dependent. Scores missing because the host failed are not zero. No post-outcome filtering, weaker R, retrospective weights, or positive-result requirement is allowed.

## The concrete design bottleneck

The existing source model requires exact finite machine-instruction slices and a chrono clock that returns the current virtual row time, including queue/pause intervals. Existing QEMU evidence does not yet provide a closed token-bound outer-loop stop/clock interface. A proposed source-only nonpreemptive alternative instead runs a full body using virtual start plus measured real host elapsed, seals its result and new private state until an original later row at or after the measured duration, and revokes output on cancellation while draining an already started body. This leaves release/guard charging unresolved and makes virtual Q depend on temperature, caches, scheduler and previous arms/blocks. Merely randomizing arm order or cooling cannot prove the existing independence assumption. No measured runtime law or independent hardware campaign law is available.

The user permits changing the research route when explicit and justified. We are considering a declared successor execution law P_model in which ALL deployment algorithm/source/control work is charged by one deterministic simulated computing semantics. Actual wall/CPU/memory remain important separately reported costs but do not influence virtual Q or source branch choices. This preserves the scientific question of net task benefit under limited computation and real spatial error, while changing the compute-environment law. It does NOT claim theta under P_model equals theta under real-host P, or that the source is byte-for-byte native. The fixed roster, pairs, weights, E and primary comparisons remain. A model sensitivity analysis can be preregistered but cannot use outcomes to pick favorable rates.

## Necessary source and representation details

The R0 is a fixed C++ LaCAM entry with explicit shared repairs to ownership, empty targets, index domains and random comparison hazards. It keeps a persistent private source session and C PRNG state. Every complete normal call advances this private history even if its result is later stale/rejected. A source-local MT initialization explicitly performed by original code remains local. Host clock reads must be explicitly mapped; no native time/RNG/environment sneaks into the model. The H19 ECBS source has its own job state/callbacks and cannot inherit R0's state rules by name.

An abstract deterministic word/bit machine is permitted as a simulation model, or a concretely supportable portable VM instruction semantics could instantiate it later. Do not assume that Wasmtime fuel is host CPU time, charges every expensive native callback, or already supports every C++ dependency. No implementation is currently authorized. Give the mathematical/semantic contract sufficient to judge feasibility before a later build, with explicit obligations for the adapter. Source UB or unspecified comparator behavior cannot be cured by writing 'deterministic'. Hardware host OOM cannot be silently reclassified as a simulated algorithm failure.

Objects include exact finite algebraic expressions, finite containers, variable-length semantic IDs/epochs, immutable state roots and query-specific retention. A bounded reference may have constant abstract access cost within a declared representation domain; arbitrary big integers, scans, copying, comparisons, geometry, certified numeric refinement or cryptography cannot be one free or fixed-price macro. Paid CAPTURE fixes an opaque current root/time reference and pins its lifetime. Accessing decoded state and query-induced retention/GC are charged. Administrative model scheduling/charging can be an explicitly ideal mathematical facility whose actual host cost is separately reported, but it must have no hidden business reads or ability to select candidates.

Publication checks and ACTUATOR admission may depend on the current physical reference mode and exact local reference segment at the acceptance row. A command-version equality alone does not freeze continuous progress or autonomous END. An affordable root capture may precede a variable-length guard calculation that exhausts row supply. There must be a precise outcome for that case, no unauthorized motion, no reused stale dynamic guard, no new public row or retry-fault draw, and all spent work retained. Not every current guard admits a monotone decomposition into prior work plus one pointer comparison. Finite global O also means repeated failed checks do not imply an infinite execution.

## Requested construction

1. Choose one common deterministic compute semantics and define its units, representation, charge-before-step and pause/continuation behavior. Cover the source, host imports, all non-source servers, atomic capture and publication, including variable-size work. Explain why charging the meter does not recurse or hide free business work. State how operation types with size-dependent costs are decomposed or bounded without reading hidden values for free.

2. Resolve source chrono/CPU/RNG semantics and scheduling without real elapsed affecting Q. Preserve normal persistent source history and source/centre disposition barriers. Explain the strongest plausible route to later executable qualification and exactly what remains unproved; do not claim an unverified VM port has passed. Make finite resource exhaustion, source exception, evidence-host failure, and administrative shutdown distinct.

3. Give the current-row acceptance rule, including paid incomplete checks, physical/event ordering, attempt-watermark treatment and future retry. Establish safety-relevant invariants and a nonvacuous symbolic admissibility obligation: some supported inputs/capacities permit required guards to complete. Do not equate a total all-pending model with a useful study. No numeric parameter bindings or fresh event opportunities.

4. Derive the block-independence implication when the completed block trajectory is a deterministic function F_a(X_b,Z_b) and the Z_b are independent, stating all possible host leakage that would invalidate the implication. Distinguish abstract-model throughput and actual host scalability. State the exact scientific scope lost and retained when changing P to P_model, and whether STRICT prepaid versus NATURAL actual used supply remains meaningful under the new units.

End with a compact adoption checklist separating closed design clauses, required source/adapter evidence, later implementation qualification and actual hypotheses to test. This is a proposal to solve the bottleneck, not an authorization to implement or an experiment GO.
