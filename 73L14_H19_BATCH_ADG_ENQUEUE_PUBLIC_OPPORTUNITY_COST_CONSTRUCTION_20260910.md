# 73L14：H19 付费批量入队与公共服务生命周期

2026-09-10。NO-SKILL / CONSTRUCTIVE / NONBALLOT；不是正式盲逻辑或资格票。选择一套纸面修订：**一次 COORDINATE 启动一个有限捕获批控制器；控制器独占原 Srv_C 作业槽，在该槽内逐个封存、付费发布单记录单元，再继续下一个单元。** 不限制一次机会只能入队一个动作；不等待全批消息都准备完才发布首条；不拆 GROUP_ADMIT 的全组原子提交。没有实现或净收益结果。HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT。

## 1. 输入身份与原文支持范围

本目标写前实核不存在。本次只按指定窗口读取以下本地输入；SHA256/bytes/LF 覆盖文件全部字节，不能把身份核验说成全文科学阅读。

| 输入 | 实际阅读窗口 | SHA256 / bytes / LF |
|---|---|---|
| 73L11_ROOT_H19_NOMINAL_DURATION_PUBLIC_OPPORTUNITY_AND_CUT_SPLICE_CONSTRUCTION_20260910.md | §2–3，L25–46 | c845f894147dc2e3cdcf6def1df06fb8478298e82d4d670f9799296f00535046 / 18880 / 89 |
| 73C10R_ROOT_APPEND_DELIVERY_RECOVERY_AND_PAID_RETRANSMISSION_ANALYSIS_20260910.md | §2–3，L19–40 | 80e4bee047db7aa3ef21a83216e5f4d00f754e1fc073c995cca9dbdb94c5f374 / 10050 / 55 |
| 73P12_ROOT_CONTROL_EPOCH_ACTUATOR_COMMIT_AND_GROUP_RETIRE_ANALYSIS_20260910.md | 末 12 行，含 §5 与 H19 多动作入队问题 | 566fee834ae69292ab174d316df528a3cc3df715bd5505f7ea9229e36a0633f3 / 9077 / 55 |

另直接读取 [Hönig 等作者接受稿](https://whoenig.github.io/publications/2019_RA-L_Hoenig.pdf) IV-B，PDF 第 4–5 页交界，web 文字层 L362–506。亲见 Type1 前驱可已入队或已完成，而 Type2 前驱必须已完成；图例机器人 2 同时已有三条命令在队列。因此“一次公共协调机会只能入队一条”是 L11 新加的粒度，不能归给原论文。本文没有把自己的批量算法冒作原文已有算法。作者稿是 RA-L2019 论文的 December2018 接受稿，未在本题重新下载比对其字节。

原文还明确未知障碍时机器人自主停止并清命令队列。我们的 APPEND 是不可撤逻辑承诺、物理权限另行逐边取得，不能直接复用该清队列动作或继承原连续合并 MOVE 的平滑性/通信与活性保证。这里仅补强共同守卫适配的预取入队能力，不闭合失落 L7 的全部名义时长/cut 义务，也不评原法其它实现。

根在本题消息中另外明确提供当前 73 的生命周期约束：中心一个可暂停资源作业、计算完在后继发布点一次原子应用私有增量；每 writer 最多一个计算/待发布槽；未付完整发布即继续占槽、每 token 一角色。本报告将此作为根提供的确切输入，不冒称本题另读了全稿。以下对批控制器与提交单元的区分正是**须并入正文的显式语义修订**，不能仅靠“单 writer”四字说已经兼容。

## 2. 捕获域、最大集合和唯一选择次序

一次既有 COORDINATE 按原 FIFO 激活一个 ADG_ENQUEUE 批控制器。它只在实际生效的中心模式允许新逻辑承诺、会话/源图合法且没有中心 fail-stop 时工作；否则完成收费空结果或失败处置。捕获的是当时**已提交、验证合法且有限**的 ADG 与允许发布域：L11 所允许的当前后缀或已封存 cut 内动作、各 agent 原执行尾/下一个 staged 动作、generation/动作/执行身份。未提交新规划、未来 TASK、未生效 MODE 和评价器真值都不属于输入。

令 F0 为捕获时中心已通过合法证据提交的 finished 集，E0q 为当时 enqueued 集（E0q 仅是本节集合记号，与不购买臂 E0 无关）。完成消息刚在网络到达、物理已经 END、消息已安装或预计时长到了，都不能加入 F0。捕获后控制器仍独占原中心作业槽；其它 FINISH/CURSOR/MODE/新图提交及中心 GC 作业保持 FIFO 等待。控制器自身只写 enqueued，不写 finished，所以本批 F0 在中心语义上不变，已提交图的动作/依赖不变。外生 fail-stop 另按 §5 优先约束后续发布，不能被槽占用屏蔽。

这给出一个具体的最小快照办法：控制器保留合法已提交图的身份/引用、各 agent 捕获尾和批前沿；借原独占 writer 槽固定图及 finished 状态，不另要求每次深复制/重排序整张图。持有引用、读取头/尾、身份核对、所需私有字段副本和 GC 引用均收费并保持有效；不是赠送常数时间全图快照。实现若实际复制更多字节，该工作也须收费，不能改名公共初始化。不能让另一个中心 writer 或 GC 在批运行时并发改图后仍引用此固定性证明。

对每个 agent，考虑从捕获执行尾之后开始、按原 Type1 链排列的 staged 动作。只可取它们的一个连续前缀：每个成员均属于捕获允许域，所有 Type2 前驱都在 F0，首个 Type1 前驱存在时须匹配捕获尾的既有 enqueued/finished 身份，其余 Type1 前驱是此前缀中上一项。若原初始链头本来没有 Type1 前驱，只在它确为原图唯一该链头、原执行队列尚无前序动作且匹配已付 INIT 的起始身份/尾锚时准入；不造虚拟 finished 动作，也不因该不存在前驱拒绝合法初始入队。原动作端点、类型、槽序和 session 也须满足 L11。遇第一个不满足项即封闭该 agent 在本批的后续链；不能越过它挑后面动作。

令 B0 为各 agent 这样的最长前缀之并。B0 是**包含意义的唯一最大合格集合**，不是仅某个局部极大集合：任一合格集合都必须包含每个所选项的 Type1 前缀，且不能越过固定的 Type2/域障碍，因此包含于 B0；B0 本身满足全部条件。前提是原提交图确为已验证的每 agent 单链及合法外部依赖；图/链身份损坏不是“无候选”，须失败关闭。

本报告不做整张 ADG 的重新拓扑排序：合格新动作之间没有待完成的 Type2 边，因为所有 Type2 前驱均已在 F0，剩下只需各 agent 的原 Type1 链序。唯一跨 agent 次序取各当前合格头中按 L11 的（原计划时标、agent 规范 key、原动作 id）字典序最小者；某条成功发布后才将其合法后继纳入候选头。所有查找、比较、位长、删除/插入与空头检查收费。这个规范链合并是 B0 上的拓扑序，不需要凭未付费物理完成或私有“批内已选”将一个 Type2 障碍打开。

## 3. 与原单槽、严格后继发布规则相容的明确修订

**作业槽只有一个；批控制器不是第二个应用 writer。** 在同一占有槽中任一时刻至多有一个记录单元处于计算或待发布。批的来源描述、进度与持久引用属于该控制器的收费状态；不会同时私备多个已完成待发布单元，也不会把后继单元登记为新免费作业。COORDINATE 只是这一次有限批的公共触发，之后继续计算只消费原 PROCESS_SLICE，不新增 COORDINATE 行、服务器、优先级或容量。

首记录单元的计算包括捕获引用/每 agent 头尾、候选/依赖/身份核对、规范头选择、执行槽分配、不可变执行记录和 APPEND_ACTION 封装准备，以及完整提交增量和所需引用准备。首条不会在这些实际工作之前发送，不能用“流式”藏掉初始扫描。后续单元包括推进批前沿和同类收费核验/准备。这里的**单元完成**只表示该一条增量计算完毕，不表示批结束，也不等于 enqueued 已生效。

完成的单元封存后，必须等待计算完成行之后的严格后继合法发布行；沿原有限发布能力支付该单元完整发布，未能支付即仍占原待发布槽。此时不能计算下一条、先改 enqueued、半追加执行记录、预发 APPEND 或释放控制器。付完整发布后，才一次原子应用：追加唯一原动作及槽/前驱身份，标该动作 enqueued，建立独立不可变 APPEND 待发送意图，并更新本批已发布前沿及引用。这是**单记录原子提交**，不能把几条记录拼成不计位长的常数时间发布。

对发布容量不足，本题继承原“付不完整则占槽等待”的规则，不赠送跨行新额度、不扩大一行容量。一次尝试的检查/访问费照实保留，消息/记录仍不可见；若原发布规则没有提供任何能支付这条记录的后继行，该单元就不会发布，须报对应停滞/Stop/未完成状态。有限记录不等于必能在有限 O 内发布。本题也不将原发布原语未声明的分期结账能力暗中当作已存在。

只有前单元成功发布后，控制器才能在后继实际 PROCESS_SLICE 上继续下一单元的检查和计算；不在同一发布动作里免费扫描后继或启动一次 SOURCE_REQUEST。最后还须付费确认没有剩余合格头，并完成控制器终态记录、引用回收/转移与释放槽。**批完成**是这个末端处置完成，届时已发布集合才等于 B0。空批也要付捕获/检查/空结果及终态费用；没有免费空转触发器。

建议并入 73 §5/§9 的统一措辞是：普通作业有一个提交单元，仍在完成后的严格后继发布点一次原子应用；ADG_ENQUEUE 是唯一此处新增的有限批控制器，可在同一占有槽中先后运行多个上述单元，前单元发布之前不得开始后单元。每单元的计算、待发布、失败和原子性遵守相同规则，批控制器末端才释放原槽。GROUP_ADMIT 继续以全组为**一个不可拆的提交单元**；其它资源事务、原规划结果及其一次提交语义不因本修订自动改变。

## 4. 原 APPEND/执行/重传职责怎样接续

每条已发布 APPEND 都是原通道可读取的独立发送意图；后继传输/序列化/本地逐记录核验和安装仍消耗原 Srv_T/Srv_E 或其既定角色，批发布不会直接安装本地记录、更新可信 cursor、取物理授权或执行 MOVE。原传输在自身获得服务时可以与中心准备后续单元并行，仍服从同一公共 O 和原 FIFO/容量，不能因为“批”而一 token 送多条。

中心 L_C 可通过本批多次合法追加增长；本地 L_E 只随连续、前驱锚/身份正确的付费安装增长。乱序缺前驱不跳槽，重复记录不重建控制状态。逻辑 enqueued 与物理 GRANTED 不等价；每 agent 至多一个未闭请求、每 gate 至多一个活动 tid、逐边 END/READY/消费后再申请新 MOVE 等共同约束均保留。新图不能借批号重置 plan_epoch、原动作 id 或消费历史。

C10R 的 f_inst/f_cons/Kc 区分原封保留：FINISH_RECEIPT 可证对应动作曾安装/到 END，不能据此自动推进消费；WAIT finished 仍依合法 CURSOR 或后继 PRECHECK 严格蕴含的消费事实及收费 ADG 维护。批控制器没有自己的 finished 推断。这样 Type2 只使用 F0 的条件不是本地未安装的“纸面完成”。

SOURCE_REQUEST modulo 3 与 CONTROL_RETRY 不增机会。RESEND_CAP 的 cap/APPEND 两类持久交替及 APPEND 封存尾界/扫描游标全部沿 C10R：当前轮尾界不因本批 L_C 增长而延伸；一次实际发送意图提交只选一条消息。批内新追加不会免费获得重传或迫使已安装记录再发送。原初次 APPEND 与重传各自按实际网络费用，不把初次准备冒充已经投送。

## 5. 物理继续、MODE、失败与 GC 的总处理

批占用中心槽不停止真实物理世界、本地旧控制或其它服务站的合法工作。物理 END/任务服务可发生，通知可传输和排队，但其中心 finished/MODE/新图效果必须等所属付费中心作业真正提交。排在批之后的 MODE 尚未生效，控制器不得偷读其未来结果来截短批或改变查询；这不是无限延迟 MODE 的活性保证，正是长批的真实队列成本，须在比较中报告。

每次记录发布仍受实际已生效的 session/模式/源身份与外生故障守卫约束。DRAIN/fail-stop 状态下激活的批不新增逻辑入队；正常单 writer 期间 DRAIN 不会由旁路无费改写。外生中心 fail-stop 或全臂 Stop 依公共顺序及时阻止其后的任何中心计算/新发布，不能因为还有批尾就延后执行故障。守卫判断自身按既有费用/阶段进行，Stop 的世界终止约定不是新增控制器轮询真值。

若批在某单元准备、封存等待或终态处理中失败，**已成功发布前缀不可回滚**；其执行记录/enqueued/发送意图仍是承诺。尚未发布单元及剩余 staged 后缀不产生承诺，不推进 L_C、f_inst、消费或已发布批前沿。失败处置按原原因分类、全费用保留；“丢弃私有计算”只是使它不能应用，内存释放/终态清理仍收费，无机会则保留相应占用/未知状态，不赠送免费清理。以新批恢复只能在后来真实 COORDINATE、允许模式及来源仍合法时捕获当时状态，不能悄悄继续失效私有增量。

前缀已经发布而消息尚未安装的情形是合法状态，独立 APPEND 与 C10R 负责后续传输恢复。中心故障后，此前已经交给独立传输队列的消息按原分量故障规则继续，中心不生成新发送/重传；故障本身不撤销本地已安装动作或旧物理 cap。批完成亦不是消息送达、已消费或任务已服务的证明。

中央图、执行尾、批前沿和对应 APPEND 引用在使用期间必须保留。批运行时中央 GC 不并发修改其快照，结束后也只能释放本批已终结的引用；已发布记录仍受 cut/ADG、执行、pending/READY 和安装/消费事实各自约束。f_inst 允许退出重传候选，不一并抹掉执行记录；本地 GC 保留验证下一条追加的历史长度/尾锚。部分失败不能导致全部批记录被一并 GC，也不能因批结束就把未确认 APPEND 缓存当作无人引用。

## 6. 一个合法代价反例与条件证明

**唯一认可的候选代价反例：全批原子等待会损失可用的早期预取。** 给定已合法提交的一段同 agent 两动作链 a→b，无未完成 Type2 前驱，捕获模式允许，a/b 身份和尾连接均合法。捕获/选择/封装 a 的工作已完成，并有一条严格后继发布行能支付 a，之后原独立传输站也有服务机会；此时 b 的封装工作还未完成。全批原子候选必须把 a 一起扣住，本报告规则可在付费单元发布后使 a 进入原传输队列。该差不需要增加 O 行、偷用 finished 或让中心第二 writer 并发；物理授权仍可等待批结束后的中心作业。它证明提前入队/发送能力被全批屏障人为推迟，**不**证明 a 已 MOVE、增加了 Q 或固定 E 净收益正。这里是合法符号状态关系，不生成实例或事件载荷。

不能拿“把批内 enqueued 当 finished”作为反对根原候选的反例，因为根已明确 Type2 只用 F0。容量不够却先发布、回滚已发前缀、读取排队 MODE 等也只是本报告明确禁止的错误改写，不能冒充在这些规则下已经发生的合法反例。

安全性按已发布单元归纳：初始尾及 F0/E0q 已合法；新单元只追加该 agent 的连续下一项，存在的 Type1 前驱已在旧尾或此前成功发布前缀，无 Type1 的原初始链头满足上述 INIT 锚条件，Type2 前驱是 F0 的真实已提交 finished 事实。单元发布将记录、enqueued、独立意图与批前沿一并应用，且不增加物理 cap，故不会跳槽、重复承诺、把未来动作当完成或撤销旧责任。未发布失败保持归纳前缀；传输丢失/乱序不使本地跨过缺记录，物理守卫继续独立。

完整性仅是**本批捕获域的条件完成**：合法有限图、持续允许、无停止/故障、足够的实际 PROCESS_SLICE 与可付每个单元完整发布的后继行，并能完成所需终态清理时，有限规范链合并最终发布恰 B0。所有已发布集合都是 B0 在该规范序下的前缀；提前停止可只发布其中一部分。没有从“有限批”推出有限公共 O 一定足够，也没有从“已入队多个动作”推出投送、MOVE、无饥饿或 lifelong 服务完成。

## 7. 完整费用和强外部比较的边界

| 工作/状态 | 必须计入的实际代价与供给 |
|---|---|
| 捕获与首条选择 | 图/会话引用与头尾读取、私有状态、所有被检查的依赖/身份、规范比较及键位长；实际发生后首条才可 ready，不能声称首条立即可发。 |
| 每记录计算与发布 | 链前沿推进、槽号/原动作封装、分配复制/序列化、所有提交字段/索引/引用、发布检查、失败尝试与发布位长；占原 PROCESS_SLICE 和严格后继发布容量。 |
| 等待与网络 | 整批持有中心槽的排队效应、待发布/在途/未安装记录内存；原初传/重传/乱序拒绝/重复核验及本地安装，均按实际原通道工作。 |
| 终态与回收 | 无候选扫描、失败原因/终态、仅可释放引用的核验和实际清理；未确认缓存保留，不用常数摘要代替真实保留空间。 |

COORDINATE 仍是共同日历中原 GROUP_ADMIT 的类型映射，所有比较臂用相同原站点/容量/价格和 O。H19 的单次控制器可追加多条，内部组作业仍整体原子；“相同容量”不要求无用代码、相同记录数量或一样的完成时间。NATURAL 各自付实际使用，STRICT 沿原共同预付供给规则；H19 无组时不虚构 GROUP_RETIRE 工作，批末引用整理则不能假扮无组而免费省掉。

控制器可能长时间占中心，使 FINISH/MODE/规划触发排队；每记录后继发布屏障也可能使它慢于一次大事务。这些都是所选单槽适配的实际代价，应与提前投送收益一并报告，不声称本规则普遍支配全批原子或原作者实现。原图可预取多动作这一能力得到恢复，但原 H19 的清队列撤销、动作合并、原调度/实时环境等差异没有消失；外部原样/共同守卫两族仍须分名。首条早发的符号反例不能代替强外部全系统费用和固定总体真实服务比较。

## 8. 给根的整稿修改清单与实际终态

建议按本报告唯一规则修改 L11 §2 的“一作业一个动作”为“一机会一个有限捕获批控制器”；替换全图重排序/全批准备屏障为冻结 Type2-finished 下的付费规范链合并；在 L11 §3 补每记录原子追加及独立 APPEND；在 73 §5/§9 明确普通一次提交与该批控制器逐单元提交的生命周期区别、占槽/严格后继/发布不足和终态费用；C10R 的恢复与 GC 规则仅补批前缀引用，原轮询重传合同保持。上述改动必须一起出现，不能只删“一动作”四字而让批内工作免费或隐含并发。

本题未调用 C12/Claude 或任何 skill，没有 CLI 顾问句柄/意见可计；全部判断由本子代理直接构造。唯一新工件为本报告，编辑仅 apply_patch，shell 每段 rtk；未改 73、旧完成报告、行政页、Git 或桌面。未重查 QEMU/源码头/近邻，未实现、构建、研究测试、仿真、调参、保护参数赋值或生成实验载荷/结果；35–38、Q-CAL/Q-CONFIRM 和受限载荷未读。完成的是本有界纸面合同，保留精度、原源后端、外部整体适配及正式资格审查义务；不投资格票。
