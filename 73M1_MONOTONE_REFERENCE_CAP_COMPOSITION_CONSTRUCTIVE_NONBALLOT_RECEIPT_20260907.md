# 73M1：同一原 MOVE 的单调参考 cap 接入构造

日期：2026-09-07。性质：新的建设性数学推导；NO-SKILL / NONBALLOT；未固定的后继研究候选。

本报告由当前受托 agent 独立构造，不是 Claude 产物，不代替任何旧任务缺失的回执，也不确认任何未提供材料的结论。本文没有审稿、投票或旧稿补票含义。

## 1. 输入边界与本次交付

唯一当地科学输入为 `72_PIE_CENTRAL_SPATIAL_RETIREMENT_AND_PAID_OBSERVATION_PREEXPERIMENT_CANDIDATE_20260906.md` 的 §3，L46–60。只读取了这十五行；没有读取其他 73 草稿、skill、审稿或 Claude 材料。写入前确认本文件不存在。源文件未改动。

指定输入提供固定原边、纵向参考动力学、LAUNCH / APPROACH / BRAKE / HOLD 的实际公式、横向误差闭环和有限运动段结论。本文新增的是：在同一原 MOVE 内，把这些连续公式的剩余距离改以当前已安装参考上界计算，并给出 cap 增大时的混合模式接入证明。原文只直接证明零速 LAUNCH；下文的非零速 LAUNCH 接入是本次新推导，不冒称已包含在原文的恢复规则中。

本报告不证明外层预留、释放、授予或 tag 变更事务的正确性，不写源码、测试、仿真、实验参数或载荷。没有为 H、T_delay、B_CAL、B_max、B*、P_active 或 density N 赋值、设范围或设默认值。

## 2. 固定原 MOVE、cap 以及条件

原边始终为从 u 到终点 V 的同一条边，长度 ell=||V-u||>0，方向 e=(V-u)/ell。用 V 表示原文终点，以免和以下参考速度 v 混淆。参考位置为 p=u+s e，参考速度为 v；实体为 X=u+s e+z+F。

沿用原动力学与常数条件：

\[
\dot s=v\ge0,\qquad \dot v=a_{\rm cmd}+\eta,\quad |\eta|\le\delta,
\qquad 0<\delta<a_{\rm lo}<a_{\rm hi},\quad a_{\rm launch}>0.
\]

沿用 z 的同一世界坐标闭环、给定闭盒 Z、固定非空紧足迹 F：

\[
\dot z=-\kappa z+w,\quad \kappa>0,\quad
|w_j|\le\kappa\bar z_j,\quad |z_j(0)|\le\bar z_j.
\]

w 和 eta 取使原动力学有绝对连续解的有界可测扰动；没有冲量。执行器允许原文所用命令范围。HOLD 的参考零速保持、端点接触以及加速度允许有限跳变均沿用原文的理想混合公理。本文没有为真实硬件的通信、驱动滞后或控制故障补出保证。

当前**已安装** cap 记作 C；候选 cap 记作 C_new。C 只在安装事件改变，满足

\[
0\le C\le\ell,\qquad C_{\rm new}\ge C.
\]

重复安装同一 C 为无运动效果的幂等事件。尚未安装、被拒绝、迟到或永不安装的候选没有进入控制公式的效力；控制器继续使用旧 C。任何 C 更新都不改变原边、方向、原目标 ell、原 tid、参考 s/v、真实 x 或 z，也不在中点消费逻辑 c。

定义当前剩余距离与制动域

\[
\xi=C-s,\qquad
\mathcal D_C=\{(s,v):0\le s\le C,\ v\ge0,\ v^2\le2a_{\rm hi}(C-s)\}.
\]

若 xi>0，定义 r=v²/(2xi)。在 xi=0 时不计算 r；制动域已蕴含 v=0，必须走离散到达/保持规则。合法初态可为 HOLD 且 (s,v)∈D_C，或处在本文相应模式域中的活动状态。原 MOVE 从起点进入时 s=v=0；若 C=0，则只可 HOLD，不能发起 LAUNCH。

令 K=Z⊕F，定义给定可信前沿 q 的实体扫掠包络

\[
\mathcal E(q,C)=\{u+\rho e+k:\rho\in[q,C],\ k\in K\}.
\]

预留前提是：当前外层保留的集合包含有效 E(q,C)，且 q≤s；安装 C_new **之前**，外层已完整保留 E(q,C_new)，并维持本段安全所需的覆盖。q 必须来自可信参考事实，不能以普通 pose/y/COMPLETE 替代。若采用可信旧快照 q=s(t_q)，则后续 s 单调已足以给出 q≤s(t)；若外层更新 q，则每次仍须证明 q≤s。

这里把“已保留”作为安装的外部事实前提，而不是由 seq/cap/tag 字段自行证明。q 的选择、Mask、预留事务、资源并发和释放的正确性均不在本文定理内。本文证明给定这些事实后的连续包含关系。

## 3. 仅使用原连续反馈公式

所有模式均使用当前已安装 xi=C-s，不使用未安装候选：

| 模式 | 原命令与使用域 | 自动离开规则 |
|---|---|---|
| LAUNCH | a_cmd=a_launch+delta；xi>0、v≥0、r<a_lo | 首次 r=a_lo 后进入 APPROACH；外部合法 STOP 可进入 BRAKE |
| APPROACH | a_cmd=−r+delta(a_hi+a_lo−2r)/(a_hi−a_lo)；xi>0、r∈[a_lo,a_hi] | 有限到达 (s,v)=(C,0) 后 HOLD；外部合法 STOP 可进入 BRAKE |
| BRAKE | a_cmd=−a_hi−delta；(s,v)∈D_C、v>0 | 首次 v=0 后 HOLD；cap 更新不解除 BRAKE |
| HOLD | 原文的参考零速保持公理，s 不变、v=0 | 仅合法明确 START/RESUME 且 C>s 时从零速 LAUNCH；cap 更新本身不构成启动 |

在 LAUNCH 的零速初始瞬间，xi>0、v=0、r=0，适用表中公式。HOLD 不是简单地在 \dot v=a_cmd+eta 中令 a_cmd=0 并任由 eta 累积；它沿用输入明确给出的参考保持混合公理。若这一公理无法由底层实现，本文相应保证也未获实现认证。

cap 安装不重置连续状态，可以改变活动模式及随后的 a_cmd。对正在运动的旧 APPROACH，重新进入 LAUNCH 是本次构造的“运行中 cap 接入”，并非把原文“暂停后恢复只从合法 HOLD”改成允许非零速度恢复。真正的 START/RESUME 仍仅从合法 HOLD 发起。

## 4. 非零速度 LAUNCH 的有限重新相交

考虑安装后的状态 v0≥0、xi0=C−s0>0，且

\[
D=2a_{\rm lo}\xi_0-v_0^2>0.
\]

固定该 C，且本段没有再被 cap 更新或外部命令中断。令 L=a_launch、alpha=a_cmd+eta。LAUNCH 中 L≤alpha≤L+2delta，且 L>0，从而

\[
v(t)\ge v_0+Lt,\qquad
G(t)=v(t)^2-2a_{\rm lo}(C-s(t)),\qquad G(0)=-D.
\]

几乎处处有

\[
\dot G=2v(\alpha+a_{\rm lo})\ge2(v_0+Lt)(L+a_{\rm lo}),
\]

积分得到

\[
G(t)\ge-D+2v_0(L+a_{\rm lo})t+L(L+a_{\rm lo})t^2.
\]

右式的非负根为

\[
\bar T_L=
\frac{\sqrt{v_0^2+LD/(L+a_{\rm lo})}-v_0}{L}>0.
\]

因此第一次 G=0 的时刻 T_L 至迟为 Tbar_L；当 v0=0 时，该上界化为原文的 sqrt(2 a_lo xi0/[L(L+a_lo)])。从 v0>0 开始不破坏有限性，反而使积分下界含有额外正的一次项。

还需单独排除“先越过 cap 才相交”：若 LAUNCH 延拓到第一次 xi=0，则该时刻 v>0，故 G=v²>0；而 G(0)<0，连续性迫使更早出现 G=0。若在 Tbar_L 之前没有碰到 cap，则上述积分不等式直接迫使 G 在该时间内过零。这两个分支穷尽可能。因此相交必在 cap 之前发生，且

\[
v(T_L)>0,\qquad \xi(T_L)=v(T_L)^2/(2a_{\rm lo})>0.
\]

相交前 G<0，故 v²<2 a_lo xi<2 a_hi xi，始终处于 D_C。到交点进入原 APPROACH 闭带即可；不能在交点继续无条件 LAUNCH。G 对正时间严格增加，故本段首次交点唯一。

这一证明也覆盖旧 LAUNCH 中途增大 cap 后的重新起算。若安装后 r=a_lo，则直接 APPROACH，不再人为插入零时长 LAUNCH；若 r 已在整个闭带中，也直接继续 APPROACH。

## 5. APPROACH、BRAKE 和 cap 跳变的保持证明

### 5.1 APPROACH 的闭带与有限到达

在固定 C、xi>0 时，沿用原代数恒等式

\[
\dot r=\frac v\xi\left(r+a_{\rm cmd}+\eta\right).
\]

r=a_lo 时括号为 delta+eta≥0；r=a_hi 时括号为 −delta+eta≤0。因此带 [a_lo,a_hi] 在每个 xi>0 的连续段上正不变，D_C 随之保持。APPROACH 初始 v>0；在 xi>0 且 r≥a_lo 时 v²≥2a_lo xi>0，所以连续 v 在到达 cap 之前不能过零变负。带内

\[
\frac{d\sqrt\xi}{dt}=-\sqrt{r/2},
\]

所以本段不再受中断时，从 xi_A>0 到 cap 的时间满足

\[
\sqrt{2\xi_A/a_{\rm hi}}\le T_A\le\sqrt{2\xi_A/a_{\rm lo}}.
\]

到达时 xi→0、v²=2r xi→0，按同一理想零速保持能力把连续极限延拓为 (s,v)=(C,0)，转 HOLD。不在该点计算 0/0。这里新增的是 cap 到达的混合触发条件，未假设中点存在实体墙面或接触限位，也不改原几何目标。C<ell 时只是中点 HOLD；其有限到达不是原 MOVE 的 END。

### 5.2 BRAKE 有限停车，且 cap 增大不得取消

在固定 C 的 BRAKE 连续段，实际加速度满足

\[
\dot v=-a_{\rm hi}-\delta+\eta\le-a_{\rm hi}.
\]

令 b_C=2 a_hi(C−s)−v²，则

\[
\dot b_C=-2v(a_{\rm hi}+a_{\rm cmd}+\eta)=2v(\delta-\eta)\ge0.
\]

若从 v_B>0 进入合法 BRAKE，首次参考零速的时间不超过 v_B/a_hi，停车位移不超过 v_B²/(2 a_hi)，故不会越过进入时的 cap。首次 v=0 必须立即转 HOLD，不把恒负加速度继续积分成 v<0。

如果 BRAKE 中安装更大的 cap，命令保持 −a_hi−delta；旧进入时 cap 的停车位移保证仍成立。安装后的 r 可能降到 a_lo 以下，也不能先按 r 分类把 BRAKE 改成 LAUNCH。BRAKE 完成后即使新 cap 在前方，也保持 HOLD，直到另外收到合法明确恢复命令。

### 5.3 每次合法 cap 增大加强制动域

安装事件不改变 s、v、z。设 Delta=C_new−C≥0，则

\[
\xi^+=\xi^-+\Delta,\qquad
b_{C_{\rm new}}^+=b_C^-+2a_{\rm hi}\Delta\ge b_C^-.
\]

故 D_C⊆D_Cnew。对于 v>0 的旧 LAUNCH 或 APPROACH，xi^->0 且

\[
r^+=\frac{v^2}{2(\xi^-+\Delta)}\le r^-\le a_{\rm hi}.
\]

旧 APPROACH 的确可能出现 r^+<a_lo；这时第 4 节给出了原 LAUNCH 命令的合法非零速度重新相交证明。若 r^+∈[a_lo,a_hi]，继续原 APPROACH 即可。合法增大不可能把原合法状态变成 r^+>a_hi。

HOLD 状态 cap 增大后仍为 v=0、s 不变，且 s≤旧 C≤新 C，故保持 D_Cnew；这个状态可以有 xi^+>0，但不能仅据此推导自动启动授权。

## 6. 总接入规则及同刻事件顺序

以下规则仅声称在上述合法状态域内全覆盖；对域外可信状态不能假造恢复或停车证据。

1. **识别原终点及旧 cap 的物理到达事件。** 若可信状态已经 s=ell、v=0，则只可走原 MOVE 的 END；同一 tid 不能被后到 cap 包重新打开。若正在完成旧 APPROACH 的 (s,v)=(旧 C,0)，或正在完成 BRAKE 的首次 v=0，则先归入 HOLD。尤其是 C<ell 的到达必须先成为中点 HOLD，不能利用恰在同刻到来的较大 cap 把这次零速到达隐去并自动再发起运动。
2. **复核候选与安装前提。** 复核同一活动 tid、原边/方向/目标身份、候选 seq/cap/tag 的输入一致性、C≤C_new≤ell，以及此前已成立并持续有效的 E(q,C_new) 预留事实。失败、未送达或无法确认时，不安装并保持旧 C 的控制。完全重复的合法候选最多是幂等 no-op；较小或错误身份候选不能降 cap、换目标或复活已结束 tid。若系统未提供相应可信输入一致性机制，该接口事实为 UNKNOWN，不由本文补证。
3. **原 END 状态不再接入；其他合法状态原位安装。** 安装只把控制器使用的 C 原子更新为 C_new；s、v、z、边、tid、逻辑 c 不动。该原子语义是本构造的数学接口前提，不证明分布式原子提交实现。候选可提前被外层保留，但安装之前连续控制仍使用旧 C。
4. **BRAKE 具有模式优先级。** 旧模式为 BRAKE 且 v>0 时，安装后仍 BRAKE，直到首次 v=0 转 HOLD。新到的合法 STOP 请求在任一活动 LAUNCH/APPROACH 状态也优先转 BRAKE，因为更新前后均在相应制动域。STOP 与继续运动/恢复同刻发生时不准取消该制动段。v=0 的 STOP 直接保持 HOLD。
5. **HOLD 默认仍 HOLD。** 无 START、no-start、恢复命令迟到或永不出现、原 cap 已达但未安装扩展、C=s，以及只有 cap 更新而没有明确启动命令，均不发起 LAUNCH。只有在 HOLD 中收到另外的合法 START/RESUME、无更高优先级 STOP、C>s 且同一原 MOVE 的预留前提有效，才以 v=0 重入原 LAUNCH。中断后的恢复同样只从 HOLD 开始。若允许一个外部消息同时携带扩 cap 与 START，必须把 START 作为独立显式命令按本条审查；不能从 cap 字段推定 START。
6. **仅对仍在运行的 LAUNCH/APPROACH 分类。** 若 xi>0 且 r<a_lo，则进入/保持 LAUNCH；若 r∈[a_lo,a_hi]，则进入/保持 APPROACH。由第 5.3 节，在合法安装前态中这已穷尽所有运行分支。旧 LAUNCH 恰好首次 r=a_lo 时，无扩展则 APPROACH；若同时合法扩展使新 r<a_lo，则 LAUNCH。模式选取后使用新 C 对应的原反馈公式。
7. **边界及域外处理。** xi=0、v=0 只能为 HOLD 或原 END，不计算 r；C=0、s=v=0 只可 HOLD。xi<0、v<0，或 xi>0 且 v²>2 a_hi xi，均不属于已证域；不能因 seq/cap/tag 通过就宣称安全，更不能凭一次 BRAKE 命令把域外状态事后改称域内。输入规定的可信控制故障等仍按其原边界处理，本文不增加免责条件。

上述顺序允许 cap 在 BRAKE 中先完成安装，但不改变制动模式；也允许外层提前准备扩展资源。no-start 是禁止从 HOLD 发起运动的输入情形，不擅自把它改写成对运行中车辆的 STOP。若外层要求停止运行，必须通过原模型中的明确合法 STOP 事件进入 BRAKE。

这里没有规定全局消息协议、seq 的具体编码或 tag 事务布局。复核能排除错误输入和过期身份，却不能推出 r 的带不变性、b_C 的非负性、参考零速保持或 Z 的正不变性；这些各自依赖本报告展示的连续/混合证明和输入公理。

## 7. 安全不变性的组合定理

**条件定理。** 假定第 2 节和第 6 节全部前提成立，初态合法，并考虑有明确定义的混合执行。在任意迟到、拒绝、未安装扩 cap 或 no-start 情形下，每个连续流和每次合法离散更新都保持

\[
0\le s(t)\le C(t)\le\ell,\qquad v(t)\ge0,\qquad
v(t)^2\le2a_{\rm hi}(C(t)-s(t)),\qquad z(t)\in Z.
\]

证明按事件归纳：LAUNCH 由 G<0 与首次相交保持制动域；APPROACH 由 r 闭带保持；BRAKE 由 b_C 非减并在首次零速截断保持；HOLD 由参考保持公理保持。cap 增大使 b 增加且不重置状态；模式间仅改变原命令，不跳变 s/v/z；旧 cap 不变的等待或未安装候选没有新增状态变化。到达时端点混合规则把极限延拓为合法 HOLD。于是所列纵向不变式闭合。

z 的变分式独立于 cap 与纵向模式：

\[
z_j(t)=e^{-\kappa t}z_j(0)+\int_0^t e^{-\kappa(t-a)}w_j(a)\,da,
\]

因此 |z_j(t)|≤zbar_j。cap 事件和中点 HOLD 均不重置 z，不换世界坐标盒，也不旋转 F，所以这一结论逐段连续成立，无须把横向误差清零。

在外层给出的 q≤s 及有效预留前提下，进一步有

\[
X(t)=u+s(t)e+z(t)+F\subseteq\mathcal E(q(t),C(t)).
\]

安装前使用旧 C 的包含关系；新 E(q,C_new) 在安装前已保留，安装瞬间 s/v/z 不跳变；安装后使用新 C 的包含关系。只要这些外部覆盖事实确实持续成立，就没有由**此次连续 cap 接入**造成的实体覆盖空隙。这不认证外层是否正确完成了任何资源事务。

此处“制动/停车域”指参考状态的可证有限制动域；HOLD 只保证参考 v=0。真实实体仍可能因 zdot 回摆而移动，所以任何长时 HOLD 的实体覆盖仍需整个 Z⊕F，而不能缩成当前位置 F 或一个点。

对任意有限时间上局部有限的迟到消息、cap 安装和外部命令，上述归纳可直接逐段使用。本文不把未定义的无限瞬时跳变当作现成执行；若外部事件在有限时间聚积，其聚积点之后的执行存在性、调度与重启语义为 UNKNOWN。该限制不能用来丢弃已满足全部定理前提的界内碰撞反例。

## 8. 有限事件后的阶段有限性

设已安装 cap 的更新和影响运动的外部命令总数有限，实际发生的各事件时刻有限，且此后不存在新的中断。取最后一个相关事件之后的状态，已安装 cap 固定为 C_f：

- 若最终模式为 LAUNCH，则第 4 节给出有限 T_L；交点后第 5.1 节给出有限 T_A，因此本次活动阶段在 T_L+T_A 内到达 (C_f,0) 并 HOLD。
- 若最终模式为 APPROACH，则直接在其 T_A 上界内到达 (C_f,0) 并 HOLD。
- 若最终模式为 BRAKE，则在 v_B/a_hi 内进入 HOLD，停点可严格小于 C_f。不能将它写成必达 C_f。
- 若最终模式为 HOLD/no-start，则安全保持可以无限长；没有正在运行的阶段需要有限完成，也没有推出开始运动或到达 C_f 的依据。

因此，“有限 cap 更新/外部命令”只消除无穷次打断这一原因。要声称某次计划运动有限到 cap，还必须最终有一个合法启动并保持不中断的 LAUNCH/APPROACH 段；要声称有限到原终点 ell，还必须最终有效安装 C_f=ell 并使该段运行完成。未安装候选不能计入 C_f。任何一个等待从未结束，都不能从段内动力学推出其会结束。

原 tid 可跨越多个中点 HOLD、明确恢复和 cap 增大而保持同一身份；在有限个实际发生的阶段/命令且每个必要等待最终结束的条件下，可逐段拼接相应有限性。本文不提供全系统无死锁、无饥饿、任意丢包恢复、公平性或全局活性定理。

## 9. 中点 HOLD 与原 END 的严格区分

当 s=C<ell 且 v=0 时，原边目标仍是 V=u+ell e。此时只能记为同一 tid 的中点 reference HOLD；不关闭 tid，不因这一停止设置原 MOVE 的完成 READY，不消费原目标对应的逻辑 c，也不把下一个原 MOVE 的起点改到该中点。

只有可信参考真正达到 s=ell、v=0，才满足原 END 的几何门槛；tid 关闭、READY 与 c 消费还应服从原系统自身一次性端点协议。本文给出其必要触发位置，不证明该外层协议。由于 s≤C≤ell，此时必有 C=ell。cap 字段等于 ell 本身不能代替参考到达，普通 COMPLETE/pose 也不能代替该事实。

在中点 HOLD 后续接入更大 cap 时，p=u+s e 保持连续，z 不重置，后续仍沿同一原边。只有原终点到达后，才可在输入既有的 P5 前提下另谈下一原边的受限端点拼接；本文没有把中点当成新的 P5 端点，也没有认证非零速度换边或实体瞬间停车。

## 10. 最小反例与必要前提见证

下列例子按其逻辑对象区分。违反某个明确前提的例子是必要性见证；满足安全前提却推翻更强活性主张的例子是真正的反例。任何满足本文全部前提而仍失败的执行，才会反驳本文相应定理，不能事后删改前提逃避。

**A. 缩小 cap 可立即失去制动保证。** 取合法非零参考速度 v0，旧 cap 足以满足 v0²≤2 a_hi(C_old−s0)。若误装 C_new>s0 但 C_new−s0<v0²/(2 a_hi)，则即使用原 BRAKE、eta=delta，实际减速度恰为 a_hi，停车位移为 v0²/(2 a_hi)>C_new−s0，必越新 cap。这是“cap 只提高”必要性的见证；若更错误地装 C_new<s0，位置界已经瞬间失败。

**B. 新 cap 先启用、资源后来才保留，会失去实体包络覆盖。** 设只有旧 E(q,C_old) 得到保留，却提前启用 C_new>C_old 并使合法运动到达 C_new。令 h_K(e)=max_{k∈K}e·k；实体在 C_new 的最大 e 投影为 e·u+C_new+h_K(e)，严格大于旧包络的最大投影 e·u+C_old+h_K(e)。故此时 X 不包含于旧保留包络。该见证不依赖把实体缩成一个点，说明安装前完整保留是实质前提。

**C. 先按 r 分类会取消已经开始的 BRAKE。** 取一个合法 BRAKE 状态 v>0，并选择可行增大使 C_new−s>v²/(2 a_lo)，则新 r<a_lo。若无 BRAKE 优先级就套“r<a_lo→LAUNCH”，原负制动命令被替换为实际加速度至少 a_launch 的正加速命令，原“保持制动到 HOLD”结论当场失效。此例推翻这种错误模式接入及旧 cap 停车承诺；它本身不意味着按新 cap 正确预留后的总位置界必然失败。

**D. 把中点 HOLD 当 END 会制造错误端点。** 取 C<ell、s=C、v=0。此时 p=u+C e≠V，已经足以反驳“原 MOVE 完成”。若还错误地把参考起点跳到 V 而保持真实 x 不跳，则 z'=z−(ell−C)e。取 Z 中 e 投影最小的 z，则 e·z'<min_{y∈Z}e·y，从而 z'∉Z。这说明中点到达不能触发原 P5 的换边重置；不是满足原 P5 前提的反例。

**E. seq/cap/tag 全部匹配仍不能证明连续可停。** 让输入身份全部一致，但真实可信状态满足 v²>2 a_hi(C−s)>0。原 BRAKE 在 eta=delta 下停车越 cap，正如 A 中的距离计算。输入一致性没有修复状态不在 D_C 的事实。类似地，若错误地把 q 提到 s 前方尚未达到的位置，即 q>s，则由 K 的最小 e 投影可知，当前实体的一部分位于 E(q,C) 的最小投影后方；身份字段不能证明前沿安全。

**F. cap 增大不等于启动许可。** 从合法 HOLD、C>s、v=0 出发，始终 no-start。该执行满足全部安全前提并无限保持 s 不变，因此反驳“cap 已增大就必将有限到达 cap”的过强主张。将这样的执行强行变成 LAUNCH，是增加启动行为，不能称作原授权的自然推论。

**G. 有限 cap 更新不等于到达原目标。** 最后一次安装停在 C_f<ell，随后合法 LAUNCH/APPROACH 不再中断，有限到达 C_f 后长期 HOLD。所有安全与本阶段有限性均成立，但原 ell 永未到达。这是有限更新推出原 MOVE 完成或全局活性的直接反例，不是条件违例。

**H. 参考 HOLD 不等于实体停止。** 若给定 Z 允许非零 z，取合法非零误差并令 w=0，则 HOLD 时 zdot=−kappa z 可非零，实体仍移动。它仍留在 u+s e+K 中，故不反驳实体包络结论，却反驳把 v=0 解释成真实刚体绝对静止的表述。

旧 APPROACH 在 cap 扩大后可能落到 r<a_lo，单凭“原闭带证明不能直接套用”并不能宣称继续使用其带外公式必然不安全；本文没有制造这种无证明的反例。本文选择已有正 LAUNCH 命令，并提供有限重新相交与域内性的直接证明。

## 11. UNKNOWN 与结论边界

尚未由本报告证明的项目包括：外层完整预留事实的实现和并发正确性、退休 q 的实际认证机制、seq/cap/tag 的具体可信来源和事务语义、安装的硬件原子实现、驱动滞后与 jerk/转向/轮速等额外约束、真实 LIMO 误差界、有限时间事件聚积后的执行、任何等待最终结束、丢包后最终恢复，以及全局死锁/饥饿/活性。对这些内容维持 UNKNOWN，不从控制公式或输入一致性推定。

已构造的数学结论为：在明确前提下，单调提高的已安装 cap 对同一原 MOVE 的制动域是扩张操作；原 LAUNCH 在新 r<a_lo 时可从非零 v 有限重新相交；新 r 在闭带内可继续原 APPROACH；原 BRAKE 必须保持到 HOLD；cap 更新不能自动启动 HOLD，更不能在中点关闭 tid、生成原完成 READY 或消费逻辑 c。由模式连续流、cap 无状态跳变和 z 同一闭环，可组合证明 s 的 cap 界、参考可停域和实体扫掠包含关系；阶段有限到达仅按第 8 节条件成立。

本文件为新的建设性 NONBALLOT 回执，不代表后继 73 已被固定、接受或取代既有任务。
