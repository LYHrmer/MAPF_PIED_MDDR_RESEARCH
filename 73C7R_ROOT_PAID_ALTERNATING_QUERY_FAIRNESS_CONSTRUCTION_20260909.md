# 73C7R：根独立纠正与付费交替查询构造
2026-09-09，/root，NO-SKILL / CONSTRUCTIVE / NONBALLOT。根完整读取真实exec17370返回的Claude Opus意见与JSON；实际modelUsage为claude-opus-5。原文保持于73C7完整回执，不以本件覆盖。当前73仍是纯D，以下**PAID-ALT-D候选未采用**。HOLD。

## 1. 逐项纠正
支持“定向选择与固定robot身份轮询交替”的具体建设方向；固定身份避免tid代际重置游标，选择公平与交付公平须分开。除此之外不直接采用C7原文：
- 奇偶并非唯一无调参分割；任何事前写死的周期仍是设计选择。选择交替是简单明确的规则，不是数学唯一性或零设计自由度。
- 公共释放时机会分给某身份，不等于实际选择请求已付费提交。该身份的所有作业都因排队失败被丢弃，名义分配仍周期出现，却没有一次真正请求。原文“2|ID|次释放内必被选择”混淆两层。score为零后的私有fallback还可能改游标，不能同时说游标只由公共奇号释放推进。
- 按实际付费选择更新状态不自动构成免费择优；原73本来在成功提交选择后推进last，采样/网络失败不回滚。是否免费取决于读取和动作是否收费、是否事后选择结果，不能由计数入口名字推出。
- 含位长增长的序号、身份、索引等不无条件O(1)。相同能力与固定观察窗口下，各臂实际不同工作按实际记账；D′全窗费用未必总大于R。E0仍是必须的购买价值比较，不能因其不买证书而取消D′−E0；相同预算也不能取代原同O/容量/固定E合同。
- 固定全窗真实Q及全成本仍为研究主结果，公平间隔只能补充解释；不接受将主estimand缩为长期查询频率。闭合单组下交替可慢、也可同选择次序，不是必然严格更慢。
- 输入C7把背景tie简写为最久未查询，和当前73不一致。下面定向分支保持73真正的“贡献需求最早加入行，再tid key”，不按这处提示错误改主方法。

## 2. 根的一个明确修复：PAID-ALT-D
公共QUERY仍只释放一般SELECT描述符，排入共同Srv_C FIFO；不在释放时读取Active/score、替中心推进业务游标或免费选机器人。中心持有一个交替位phase与一个固定robot身份游标h，初态为身份轮询分支及规范序前哨。该定义是候选策略状态，不设置保护参数或新供给频率。
每个SELECT在中心单writer槽内付费CAPTURE。身份分支从h之后按固定robot循环序扫描，选择首个当前Active的robot及其唯一tid；一圈没有返回NO_QUERY。成功提交该决定时，非空选择把h推进到所选robot，空结果h保持；有无请求都将phase切到另一分支。扫描所有身份、解析/验证tid和记录均收费，不设未收费的扫描上限。
定向分支在同一允许知识上完整付费计算实际需求score，以73真实tie选正分者；无正分执行同一身份选择函数。正分选择不改变h，fallback身份选择则按上述规则更新h；该决定提交后同样切phase。所有非空选择更新原选择审计last，但它不反过来改变h。主73的last/tie定义和新h分域，避免把两种游标混为一个。
本作业在提交前因模式关闭/版本/容量等未完成，则phase和h都未提交、不推进；已消费CPU/排队/失败验证照计。既有作业按其规定完成/丢弃后，后继公共机会再尝试，不新增token。成功提交后采样失联、无进度或tid已关均不回滚h/phase，不按回执质量重选。重复同决定幂等。tid更换不重置固定身份游标，旧证书不能套新tid。
模式SHARED-OFF/DRAIN及失败控制仍按原权限处理；下述公平命题只在持续允许途中QUERY时成立。本件没有用策略状态为公开O追加行。

## 3. 有限选择前缀中的条件公平证明
设固定身份集大小为m。在某tid从一个选择事务边界起持续属于中心Active的区间内，若后继至少2m个SELECT决定实际完成合格付费提交、没有模式禁止，则它必被选到一次。这里数的是已完成决定，不是释放行数、墙钟时长或已获证书次数。
证明：最多每两个已完成决定就有一次身份分支。每次该分支按循环序选择h之后第一个Active；稳定目标在被选中前不可被跳过，所以从当前h到目标的有限身份弧严格缩短。其他tid进入/退出不能在固定弧中制造新robot身份，至多m次身份选择就遇到目标。定向正分不改h，定向零分fallback只增加相同前进步骤，不能跨过稳定目标而不选它。故2m是保守的决定次数界。
这不是在有限O里保证存在2m次后继提交。若所有相关QUEUE/PROCESS/发布容量已耗尽、选择一直不能完成或模式关闭，结论前提不成立；这些是应计的失败，不能事后增加供给。证明只保证每次稳定区间的机会，瞬时出现后马上END的tid不要求强行得到POSITION。
选择公平还不推出有效证书公平。对P8实际退休制度，只有达到初始cap后的真实CAPTURE、既定精度、完整交付验证和中心q_release更新，或物理END后完整中心FINISHED链，才清除旧占有。无限稳定区间加上反复选择及按身份不永久压制的合格采样/交付条件可以给后继证据；有限窗口须明列足够后继链。不能直接假设每份证书必推进q，把需证的进展塞进“成功”。

## 4. 比较、采用状态与剩余合同
若将来采用PAID-ALT-D，应明确它替换纯D的策略身份；比较仍在同控制/前缀/围栏/任务服务/O与供给下，对强R与E0分别报告固定全窗Q、失败及全部实际费用。R不应为本身不用的D索引虚付额外成本，E0保留全部cap/END/RUN能力。必要机制消融可保留纯D，但不能按结果择优改主臂。
本件把C7的启发修成一个有限付费选择合同，未证明净吞吐、新颖性或全组无死锁。若主73采用它，须统一§7/9中的初态、提交/空结果、h/last、模式、去重/tie及成本条款；R的现有tid轮询是否满足相应动态公平也须单独核，不能把新证明赠给另一算子。该变更当前不并入73，优先完成前缀与公平服务底座。
本件未实现/伪代码/测试/仿真/调参或生成实验载荷；未使用skill；保护参数原义未赋值。完整Opus意见本轮取得，但只有经根核验的具体命题才可进入设计。六门2 PASS /3 UNKNOWN /1 FAIL保持。
