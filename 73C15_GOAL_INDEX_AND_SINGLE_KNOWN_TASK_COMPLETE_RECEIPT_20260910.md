# 73C15：单已知任务与目标索引建设——实际完整回执

2026-09-10。CONSTRUCTIVE / NO-SKILL / NONBALLOT。此件只保全实际返回，不代表根采纳、来源资格或正式审稿通过。73 保持 DRAFT / NOT-FIXED / NOT-REVIEWED / NOT-ACCEPTED；HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT。

## 实际调用与完整性

提示文件：73C15_GOAL_INDEX_AND_SINGLE_KNOWN_TASK_CONSTRUCTION_PROMPT_20260910.md，SHA256 db55075b58b55ec15cc1550a713b0eeb2dac26381a647ca5420ce13691ca271d。输入73 SHA256 ab0283a5ef6fb9b3a408f0d453fb763340f9241b01a63928851ceb5ddef48939；仅所供来源窗口与具名接口事实，不是全候选审查。

单次原调用为 rtk proxy timeout 480s claude，-p / --safe-mode / --model opus / --effort high / --max-budget-usd 5 / --permission-mode dontAsk / 空tools / --disable-slash-commands / --strict-mcp-config及空mcpServers / --no-session-persistence / --output-format json。functions cell314已返回原exec34155，首chunk3fa09c；根仅续等原句柄，无重启。最终chunk7d8361 / exit0，完整stdout为同目录原JSON；无输出截断。此次调用未使用任何skill。

原JSON：type=result，subtype=success，is_error=false，terminal_reason=completed，stop_reason=end_turn；实际modelUsage为claude-opus-5 / provider firstParty，不以请求alias代替模型证据。CLI session_id=293f850f-94b0-43d1-8117-be0d0c158430，uuid=b4b526dd-f349-4afc-9a6c-0d855b54470e；duration_ms=303133，duration_api_ms=303093，total_cost_usd=0.5058725，output_tokens=17747，num_turns=1。result最后为END-C15，根已全文读取；下列正文按result原样保全。科学结论须另由根73E20独立核验，不以终态完整当结论正确。

## Claude 原始完整 result（含未经根采纳的断言）

# C15 建设方案：固定 R0 下 `reach_goal` 的 0/1 定义域、单任务缺口与最小来源修订

以下仅基于本提示给出的固定窗口（`planner.cpp` L19–92 / L323–390 / L393–471 / L474–569）与已列的亲核接口事实。窗口外的头文件声明、成员初始化、后处理阶段一律标为"窗口外前提"，不冒充已读。

---

## 1. 独立核：定义域、启发式含义、算法正确性三层分离

### 1.1 值域归纳（数组定义域层）

对固定窗口做结构归纳，命题是：**任一成功构造完成的 `HNode` 的 `reach_goal` 每个分量取值属于 {0,1}**。

- 根（L42–51）：显式逐分量写 0。不依赖任何窗口外性质。
- 非根、`parent->reach_goal[i] > 0`（L59–61）：只做拷贝，无自增。值域由归纳假设承载。
- 非根、`parent->reach_goal[i] == 0` 且到达（L74–79）：对 `reach_goal[i]` 自增一次。自增的被读值来自 L35 的 `resize(N)`，本构造分支中没有其它写入，故结果为一。
- 非根、`parent->reach_goal[i] == 0` 且未到达（L80–83）：**该分支对 `reach_goal[i]` 完全没有赋值**。它保持 `resize` 后的值。

因此归纳成立，但有一个必须显式登记的前提：**分支四与分支三都依赖"`reach_goal` 的元素类型在 `resize` 时被值初始化为零"**。这条来自头文件的声明，不在本题窗口内。根观察给出的"其余保持 0"与此一致，但作为证明前提它应当被单独标注为窗口外前提，而不是当作已核事实——因为下面第 2 节要用这条 0/1 性质去**取消下标表达式本身**，前提失效则整个修订的授权失效。

另一条与"值域"无关但常被混淆的事实：`reach_goal` 在构造后不再被写。LaCAM* 的 rewrite 会改 `parent` 指针与 `g`，但不会回溯改写已构造节点的 `reach_goal`。所以值域归纳对 rewrite 免疫——因为它只对"构造时刻传入的 parent"归纳，且被归纳量此后不可变。这正是"range 归纳与 parent 指针/深度语义是两回事"的准确含义：rewrite 破坏的是标记与"当前最优路径历史"的对应关系，不破坏值域。

### 1.2 原启发式含义层：它是闩锁，不是队列游标

L73 在"尚未置位"分支里**永远只读第一目标**（源内注释亦自陈只为第一目标规划）。因此该标记的唯一可能跃迁是"零→一"，此后沿构造链只被拷贝。三个直接后果：

- 它不可能表示"任意任务序列下标"。即便某 agent 的 `goal_locations` 行真有两项，标记也永不推进到第二项之后，算法从不真正规划第二个任务的完成。
- 它不是"当前处于目标顶点"的谓词，而是"沿本构造链某时刻曾到达过第一目标"的**单向闩锁**。被挤离目标顶点后不复位。L67 的距离判零正是用来在闩锁已置位的前提下区分"仍在目标上"与"已被挤离"。
- 从被注释掉的原句（L65、L412、L510、L521–527）可读出设计意图：置位态原本由 `dummy_goal` / `getDummyGoals()` 承担引导，即"已交付则导向一个停泊点"。现行代码把这个**布尔量放进了下标位置**。单任务缺口不是"队列偶然短了"，而是重构把标志当游标用之后留下的伪队列假设。

### 1.3 受影响读点（完整算法正确性层）

按窗口逐点列出，并区分"越界风险"与"语义风险"：

- **L64（`HNode` 构造）**：`goal_locations[i][reach_goal[i]]`，置位时索引取一。单已知目标行只有一项 → 越界读，`operator[]` 无检查，UB。这是缺口读点①。它同时驱动 L67–68 的优先级重置，故 UB 直接污染搜索序。
- **L346（`get_new_config`）**：把闩锁灌进 `a->goal_index`。本行及 L348、L352 只做与零比较，**不做下标**，故本身安全；但它把"闩锁"洗成一个名为"索引"的字段，是后续三处误用的传播源。（L339 的先置零是死存储。）
- **L411（`funcPIBT`）**：`goal_locations[i][ai->goal_index]`，置位时索引取一 → 缺口读点②。它决定 L419–420 的排序键，即决定该 agent 的实际提议。
- **L509（`is_swap_required`，pusher 侧）**：行取 pusher、索引取 pusher 的标记 → 缺口读点③，行内一致但索引仍可取一。
- **L511（puller 侧）**：**行取 pusher、索引取 puller 的标记** → 缺口读点④，且同时是行错误。见第 3 节。
- **L541**：`ins->goals[a->id]`。按亲核接口，`LACAMInstance` 只按每行 front 建 `goals`，长度必然可定义，无越界。但它意味着同一个函数内部，"无须移动"的判定用第一目标，而距离比较用标记索引出的目标——**函数内部已经自相矛盾**，与越界无关。

一个必须与本题分开登记、不在此修的窗口内观察：L352–355 对置位且深度达提交窗口的 agent 设 `v_next = v_now`，但**该处未见对 `occupied_next` 的写入**；同时 L348 使这类 agent 也不进 `occupied_now`。于是 L438 的顶点冲突检查与 L447 的交换冲突检查都看不见这个静止 agent。窗口内无修补，窗口外是否另有阶段补写不可断言。这条影响的是"冲突自由"而非"定义域"，独立登记，不并入本题修订，也不据此展开全源审计。

`num_agent_reached`（L62、L77）与根的 `curr_time`（L50 未设）之初值同属窗口外，标注不断言。

---

## 2. 最小来源修订（R1+R2 为最小自洽集）

### 2.1 R1：以一个全域**引导目标解析**取代四处"标志当下标"

不是加 bounds check，也不是 clamp。做法是在语义层承认标志是布尔量，从而**在所有读点消除下标表达式**，改为对一个统一定义的解析结果取值。解析对每个 agent 与每个标志态全域有定义：

- 标志未置位：解析结果为该 agent 已知合法分配队列的队首。
- 标志已置位、且**队列确实已知第二项**：解析结果为原第二项。此子域上与原读取逐顶点相同，原语义完整保留。
- 标志已置位、且只有一项已知：解析结果为**这同一个已交付第一任务的既有目标顶点**，仅作为已访问 agent 的终端引导。
- 队列为空（任一标志态）：不落入解析，落入 R0 已隔离的空 `H_goal` 首次读取前失败分流，给 NO_INPUT/HOLD，保留全 roster 与真实世界服务。绝不由当前位置或任何顶点补一个目标。

四处读点（L64、L411、L509、L511）必须**同一解析**。这不是风格要求：今天它们已经不一致（L541 用队首，其余用标记索引），而 L67–68 的优先级重置、L419–420 的排序键、L513–568 的交换判定若对"该 agent 的目标是哪个顶点"给出不同答案，就会出现"一处判它已在目标、一处判它尚远"的状态，这是超出内存安全的正确性缺陷。用一个总函数闭合定义域并强制四处一致，正是"最小"的含义：以一处定义替换四处偏读，而不是四个各自为政的边界判断。

对候选句的判定：它不新增、不复制 task 实例（队列只被读，从不追加）；不改 `reach_goal`（闩锁跃迁规则与只读第一目标的 L73 一字不动）；不推真实完成（`env->goal_locations` 不动，`runLACAM2` 本就不以真实 TASK 更新 env，真实完成仍只由 TASK_SERVICE 在真实环境每步至多消费一个队首）；不额外 append WAIT（停留行为由 L352–355 既有冻结产生，计划长度不因本修订而变）；第二项确实已知则保原第二项引导。它也不是"把当前位置当伪目标"：解析出的顶点是一个**已分配的**目标顶点，它在到达瞬间恰与位置重合，但 agent 被挤离后二者分离，这正是它不等于位置的判据。

### 2.2 R2：`is_swap_required` 的 puller 行更正——与 R1 耦合，但独立登记

必须前置说明一个用字段而非变量名得出的结论：**R1 单独施加会把交换层永久失活。** 因为若 puller 侧仍读 pusher 行，则在单已知目标域上，两个标志态解析都归到 pusher 的队首，`pusher_goal == puller_goal` 恒成立，L513 无条件 `return false`，`swap_possible_and_required` 的两条路径（L482 与 L494）都拿不到伙伴。而在**现状**下，同一现象已经部分存在：两 agent 标志相等时（含所有人皆未置位的常见态，例如根节点起的浅层）行同索引同 → 恒等 → 交换恒不需要；标志不等时则落入越界 UB。合起来说：**在单已知目标域上，交换机制今天没有任何良定义的启用路径**——要么惰性失活，要么未定义。

因此建议：与本题一并明示更正为**行取 puller、索引由 puller 的标记经同一解析给出**，并把它作为一条与 R1 分开标注的语义更正登记，各自附前后字段理由。理由取自后继字段用法：L524/L526 用 `puller_goal` 算 puller 自己两端距离，L567 首合取项 `puller_vpusher < puller_vpuller` 的唯一可读含义是"puller 严格偏好 pusher 的格子"，这是交换双侧条件中属于 puller 的那一侧；用 pusher 行填充它，会把该合取项退化成关于 pusher 距离的陈述，双侧条件塌成单侧。附带收益：R2 之后，单任务 agent 的解析结果与 L541 所用的 `ins->goals` 重合，函数内部的自相矛盾在该子域上消失；在真有第二项的子域上 L541 仍用队首，此为**残留不一致，登记不修**。

保原语义也是一个可接受选项，但代价必须写明：那就等于宣告本内部源运行的是"无交换的 PIBT 层"，而不是 LaCAM 的交换启用版本，论文不得以后者的走廊/死端处理性质为据。我推荐 R2；若选保原，须作为**另一个算法**声明，而非同一算法的实现细节。

### 2.3 对提议、搜索、随机消费、费用、身份的诚实结论

- **不能声称原样等价。** 在"标志已置位且队列有第二项"子域上，R1 与原读取逐顶点相同，是保守扩张。在"标志已置位且只有一项"子域上，原行为是 UB，不存在可供对齐的良定义基线；R1 在此是**给出定义**，不是复现观察行为。R2 在标志相等域上把"恒不交换"改成"按双侧条件判定"，是明确的行为改变。
- **提议与搜索改变。** L419–420 的排序键改变 → `funcPIBT` 主循环成功的分支改变 → 递归调用集合与最终 config 改变。L64/L67–68 良定义化后，置位 agent 停在交付点时走 L68 的重置；被挤离时落入 L59–70 中**未对 `priorities[i]` 赋值的路径**，其值为向量构造默认值，即最低档——这是又一处窗口内可见的缺失 else，它使"被挤离的已交付 agent"排到序末。这是搜索序效应，非可靠性效应，但它意味着修订后的优先级景观与"假想真有第二任务"的世界不同；由于单已知目标才是本研究的主域，这是可接受的，但必须写明而非默认。
- **随机消费改变。** L403–404 对每个候选无条件设置 tie-breaker，故解析点本身不新增抽取；但排序结果改变递归 `funcPIBT` 的数量与顺序，下游 MT 流因此错位。所有内部引用臂共同施加同一修订，故臂间可比性在内部源内保持；**内部源与其历史自身的比较不再成立**。
- **身份关系。** 修订后的源不是已修复 R0 `0b5b3364...`，而是新身份。触发态（某 agent 在非根节点到达其唯一已知目标）在生命周期设定下并非罕见路径，应预期广泛分歧，不可当作"罕见路径补丁"而沿用旧结果。与当前固定输入 `ab0283a5...` 的配对须重新声明。
- **费用全计。** 解析调用、队列长度检视、每个新增比较与分支、相应内存工作，在所有臂上全额计入实际费用；不设免计变体，不设仅调试路径。外层全 proposal 验证与唯一合法物理授权不动；PRE_COMMIT_SHAPE 仍在 plan 返回后、原 commit 清容器之前检查，源无输出形状即会话停止，不自动 clearAll 重试，正常软 STALE 保合格源历史。

### 2.4 被考虑并否决的替代（各自保留，本路线非唯一）

- **索引钳位到队列末项**：单任务域上行为与 R1 相同，但它以"标志是游标"为默辞，且会在假想较长队列上静默改变行为。语义更差，否决而非等价。
- **复活 dummy 引导（被注释的原设计）**：`prepareDummy` 写独立 `dummy_goals`，不改 `env->goal_locations`，`update_dummygoals` 已注释。停泊点因而不是已知合法分配输入的函数，采用它等于引入非派生于已知任务的目标，与"不制造未来任务"冲突，且需要自己的一整套定义域证明。作为替代登记，不作最小修订。
- **把标志做成真游标**：需要向 env 队列追加或伪造项，禁止。
- **不预选"所有 agent 必须两任务"作为成功域**：明确拒绝。主域是标准单已知目标 LMAPF，两项子域只是解析的一个特例分支；也不做运行后按任务数筛掉某臂的 block。外部 H19 用自己的 ECBS，弱对照不能替内部源的未定义背书，可比性只在单已知目标域上主张。

---

## 3. 唯一符号反例：定义域闭合 ≠ 吞吐/完备/真实服务

设两个 agent u 与 w，各只有一项已知合法任务。u 的目标顶点 g_u 位于一条走廊内部，而该走廊是地图其余部分通往 w 的目标 g_w 的唯一通路。u 先到达 g_u，标志置位。按 R1，u 的引导仍是 g_u；按 L352–355，深度达提交窗口后 u 被冻结在 g_u；按 L348 它不入 `occupied_now`，于是 `swap_possible_and_required` 在 u 所在格上取不到伙伴（L481 得空），L541 的"无须移动"排除也倾向把它当不可动者；其优先级又已被 L68 压到最低档。w 无法置换 u，含 u 于 g_u 的构型是搜索空间中的不动点，g_w 在该阶段不可达，w 的任务不被服务。TASK_SERVICE 在真实环境每步至多完成一个队首，PROJECT 不得制造未来任务，故没有新分配来解封 u；队列非空也不保证有两项，惰性等待不构成出路。结论：**该规则下吞吐可自此步起为零，搜索在被截断的提交窗口循环下不具备完备性，真实服务可无限饥饿。**

必须同时讲明归因：这不是 R1/R2 引入的缺陷，而是"单已知任务下的终端停泊语义"本来就有的后果；修订把它从**未定义**变成**已定义且可分析的坏情形**。这是本方案能主张的全部：定义域闭合与四读点一致，不蕴含吞吐、完备或真实服务保证。附带一句：即便在全体标志未置位、R2 已启用交换的域上，吞吐仍无保证——LaCAM 的完备性论断针对固定目标的一次性 MAPF 与完整搜索，提交窗口截断的生命周期循环不继承该定理。

### 失败与继续域合同

- **空队列**：任一解析点遇空 → R0 已隔离的空 `H_goal` 首次读取前分流 → NO_INPUT/HOLD，保全 roster 与真实世界服务，不补目标。
- **不变式违反**：若解析点遇到不属 {0,1} 的标志态，走 FAIL_STOP_SOURCE，**不钳位**。理由是取消下标表达式的授权正来自 0/1 证明；前提被违反则结论不成立，该次结果不可采信。同理，1.1 节标注的"元素类型 resize 值初始化"窗口外前提未经独立核实之前，R1 的授权是有条件的，须先补该前提的独立核对。
- **形状与恢复**：PRE_COMMIT_SHAPE 位置与语义不变；无输出形状即会话停止，不自动 clearAll 重试。
- **修订施加范围**：所有内部引用臂共同应用同一修订；实际分支、比较、输入读取、失败与内存工作全费。
- **继续域**：修订源仅可在单已知目标 LMAPF 域上以**新身份**继续，与固定输入重新配对；先前 R0 数据不迁移。第五堆 `>=` 改 `>`、隔离同步入口、空目标失败分流与本修订均仍为纸面源码差异，未实现。
- **门态**：六门仍为 HOLD，2 PASS / 3 UNKNOWN / 1 FAIL。本建设方案不清 FAIL、不转任何 UNKNOWN、不构成源码资格通过，不给 PASS/GO。第 2.4 节替代路线保持开放。

### 附：影子局部变量（指出，不递归修补）

L557–564 在 while 体内以新的局部声明重算四个距离，外层 L520–527 的量此后永不更新。故 L533 的循环条件在循环体内是**不变式**，L567 的最终判定也仍用初始顶点对的距离——函数并未真的"沿走廊模拟置换"，而是仅按初始对定型。由此还派生一个非终止风险：`n` 恰为一时循环继续，`u == v_pusher` 只排除即时回退，在环形走廊上推进可持续而条件恒真，且该循环内未见 `is_expired(deadline)` 检查。修掉遮蔽会同时消除条件不变性并恢复预期的进展/终止论证。**独立登记，不并入本题修订，不展开为无限修补计划。**

END-C15
