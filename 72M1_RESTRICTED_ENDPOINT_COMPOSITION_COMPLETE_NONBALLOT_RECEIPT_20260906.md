# 72M1：受限端点拼接完整建设性回执

日期：2026-09-06。真实任务路径：`/root/construct72_endpoint_composition`。

归档说明：以下正文是归档前已返回的完整终稿，原样保留其读取范围及当时操作回执。本次仅按根任务授权新增此归档工件；未重读或修改候选 72。正文依据下列旧快照，不构成对后续新字节的审查。

**NO-SKILL / NONBALLOT 建设性数学回执。**我仅完整读取指定候选 72 的第 1–10 节，没有读取任何 skill、历史审查或其他科研文件；没有网络访问、文件修改、实现、构建、测试、仿真或实验参数赋值。实际文件身份为：

- SHA256：`9e63b986fccc1933f4b63e53c59014a2dde12d862f473c3f5f930d81a4cf9da6`
- 38960 bytes，160 行。
- 文件状态仍为 DRAFT / NOT-FIXED / NOT-REVIEWED；本回执不授予资格门。

候选的现有条件足以支持一个**固定世界足迹、同一误差闭环、同点零参考速度的受限端点拼接引理**，但应把它明确写出。该引理不涉及真实速度归零，也不支持一般姿态或非零速度窗口交接。

令实体的平移基点为 \(x=p+z\)，足迹为 \(X=x+F\)，其中 \(p\) 是世界坐标参考点。旧边为 \((u,v)\)，新边为 \((v,b)\)，方向分别为 \(e,e'\)，长度分别为 \(\ell,\ell'>0\)。

**一、必须列明的混合公理。**

1. \(F\) 是整个拼接期间不旋转、不更换的世界坐标足迹；\(Z\)、\(\kappa\) 及扰动界保持不变。\(z=x-p\) 始终在同一个世界坐标系定义，不能随着边方向变化而暗中旋转或重新清零。
2. 连续流满足 \(\dot z=-\kappa z+w\)，\(w\) 为有界可测输入，不含冲量；拼接没有对 \(x\) 或 \(z\) 的跳变重置。因而 \(z\) 为绝对连续状态，已有的 \(Z\) 正不变性可跨模式延续。
3. 旧边到达时采用候选明确给出的混合端点延拓：\(s=\ell,\ v_{\rm ref}=0\)，然后 reference HOLD。HOLD 精确保持 \(p=v\)，并继续运行同一误差闭环。保持参考零速度是理想控制公理，不由普通 pose 或 COMPLETE 推断。
4. 新边进入时只重置边局部参考状态与控制模式：\((s',v'_{\rm ref})=(0,0)\)，模式进入合法 LAUNCH。不重置物理状态、误差状态或扰动过程。参考加速度允许有限跳变；没有额外的 jerk、转向、轮速、控制器记忆或执行器滞后约束。
5. 同一标量参考控制律在所有允许的四邻方向上成立，误差闭环也不随方向失效。这是理想平移模型的方向适用性公理，不能由“四邻格图”本身推出。
6. 新边完整 \(U'(0)\) 已通过静态障碍及工作域检查，且中心先完成新 grant 的完整资源保留，gate 才能在验证完成后的合法 ACTUATOR 机会进入。验证、通信和排队期间继续 HOLD，不发生其他物理跳变。

**二、同点拼接为何不改变实体状态。**

设旧边在 \(\tau\) 到达参考终点，新边直到 \(\sigma\ge\tau\) 才实际进入。中间有
\[
p(t)=v,\qquad X(t)=v+z(t)+F,\qquad z(t)\in Z
\quad(\tau\le t\le\sigma).
\]
应注意，通常 \(z(\sigma)\ne z(\tau)\)：等待期间误差仍然演化。

在新边进入的同一瞬间，
\[
p(\sigma^-)=u+\ell e=v,\qquad
p(\sigma^+)=v+0e'=v.
\]
因此没有改变 \(x\) 的局部坐标重置给出
\[
z(\sigma^+)=x(\sigma)-p(\sigma^+)
=x(\sigma)-p(\sigma^-)=z(\sigma^-).
\]
它既不需要机器人精确位于顶点 \(v\)，也不需要把误差清零。新旧参考速度均为零，所以
\[
\dot p(\sigma^-)=0=\dot p(\sigma^+),
\]
即使 \(e'\) 与 \(e\) 垂直或者相反，参考位置和参考速度仍然连续。边方向只是新参考表达式的参数。

真实平移速度则在几乎处处意义下为
\[
\dot x=\dot p-\kappa z+w.
\]
HOLD 和切换时可能仍有 \(-\kappa z+w\ne0\)。若 \(w\) 仅有界可测，甚至不能承诺真实速度处处存在或连续；所证明的是物理位置无跳变、参考速度无拼接跳变及误差包络持续成立。若另外假设 \(w\) 在切换时连续，则相同闭环与连续 \(z,\dot p\) 才进一步给出真实速度的连续性。

**三、新原边如何重新进入 P1。**

新边进入时
\[
\xi'=\ell'-s'=\ell'>0,\qquad v'_{\rm ref}=0,
\]
故
\[
(v'_{\rm ref})^2\le 2a_{\rm hi}\xi',
\]
且这是合法 LAUNCH 初态。已有 \(z(\sigma)\in Z\)，同一误差闭环继续保证不变性；不需要真实速度归零。

于是候选 §3 的 LAUNCH 有限时间到达切换面、APPROACH 闭带不变及有限参考到达论证，可以以 \(\sigma\) 为新的时间起点再次应用。需要制动时，仍只在已证明的 BRAKE 域进入。

这里“任意新四邻边”严格指：**PIE-D 输出的、端点确实从 \(v\) 出发、完整几何包络静态合法、执行器假设适用并已获得 grant 的那条原边**。不是每条格图边都必然能获准，也不是从可选邻边中另选一条来恢复运动。拼接没有新增 MOVE、连接轨迹、绕行或伪目标。

**四、跨退休、resident 和 grant 的连续覆盖。**

令 \(K=Z\oplus F\)，旧端点包络为 \(E=v+K\)。旧边任意合法后缀均包含 \(E\)。所以从参考到达，经过 END 采样、处理和网络等待，直至中心完成旧 tid 交接，仍有
\[
X(t)\subseteq E\subseteq U_{\rm old}(q).
\]
gate 的本地终点 latch 不足以提前释放旧资源。

中心完成终点提交时，把 \(\operatorname{Mask}(E)\) 从旧 tid 改为新 resident \(\gamma'\)，只删除其余资源。因此该原子提交不产生覆盖空隙。其后 READY 的排队、投送、验证以及 POLICY 等待期间，reference HOLD 仍给出 \(X(t)\subseteq E\)。

新原边具有
\[
U'(0)=\bigcup_{s'\in[0,\ell']}(v+s'e'+K),
\qquad E\subseteq U'(0),
\]
从而由 Mask 的单调性，
\[
\operatorname{Mask}(E)\subseteq\operatorname{Mask}(U'(0)).
\]
中心只有在全部新资源为 FREE 或当前 parent resident 所有时，才一次提交完整新保留并改 tag。因此提交前有 resident 覆盖，提交后有新 tid 的完整 \(U'(0)\) 覆盖。grant 发送、验证、排队以及 no-start 均不改变这一包含关系；尚未进入时 \(q=0\)，不得提前按新边进度退休。正式进入后再应用 P2。

任意长、甚至无限的合法 HOLD 均不破坏上述包络不变性，但可能永远不能取得下一次进展。有限条这样的合法拼接可用归纳组合；每次最终不中断的运动段有 P1 的有限参考运动时间。**整个串联的日历完成时间有限，还需要各等待和中断最终结束的额外条件。**

**五、最小纸面反例及其归类。**

- **端点不接。**令新边起点为 \(v+d\)，\(d\ne0\)。若沿用 \(z^+=z^-\)，则 \(x^+=x^-+d\)，发生物理跳变；若强制 \(x^+=x^-\)，则 \(z^+=z^--d\)，可能离开 \(Z\)。这是同点假设的必要性见证，也属于原路径锚点错误；不是满足上述引理全部前提的反例。
- **更换为较小误差集。**若当前 \(z_*\in Z_{\rm old}\setminus Z_{\rm new}\)，即使参考端点相同，切换后也不能断言实体属于新 \(v+Z_{\rm new}\oplus F\)。这是包络变更越界；若实现未经重新认证就据此退休，则同时是覆盖协议错误。
- **转向改变足迹。**取沿世界横轴的非正方形长矩形 \(F\)，其长、短半轴满足 \(a>b>0\)。旋转后的足迹含有超出原横向矩形纵向范围的点。继续使用原 \(F\) 建立的新旧包络不能覆盖该转动。这是固定世界朝向模型之外的情形；若声称原定理直接适用旋转机器人，该见证推翻扩张后的主张。
- **非零参考速度拼接。**若终点仍有 \(v_{\rm ref}>0\)，在 \(\xi=0\) 时已经违反 \(v_{\rm ref}^2\le2a_{\rm hi}\xi\)，不能调用候选制动保证。若带非零速度从 \(e\) 瞬间转入不同方向 \(e'\)，参考速度发生跳变，超出有限加速度流。它否定一般运动窗口拼接，并非这里的零参考速度引理。单凭此例也不能直接推断必然发生实体碰撞。
- **READY 前消费。**gate 已封存终点 \(\gamma'\)，中心仍记录旧 GRANTED tid，此时 POLICY 就增加 \(c\)。这一步本身已违反 §5–6 的消费合同；正确中心仍应拒绝以尚未完成交接的 parent 启动新请求。仅有提前消费不必立即导致碰撞；若进一步把本地 latch、NONE 或提前的 \(c\) 当成中心已释放的证明，则又破坏资源覆盖归纳。应准确记录为协议错误，不能伪造为“其余合同均成立”的碰撞反例。

这些反例明确指出必要前提，没有提供一个同时满足全部显式前提却违反拼接结论的轨迹。反过来，若实际发现此类轨迹，就必须承认当前定理被反驳，不能追加条件免责。

**六、尚未完成的广义可达集接入。**

若要处理实际动力学、姿态或控制器记忆，至少应把速度、姿态、执行器状态、控制器内部状态及相关延迟纳入状态 \(\chi\)；获得切换时可信可达集 \(R\)，并证明从采样至实际进入的所有等待演化仍被覆盖。新控制域必须容纳**所有当前可能状态**，而不只是与 \(R\) 有一个交点；还须证明重置与物理连续性相容、完整姿态足迹及后续扫掠包络受控，以及所有所需资源在运动之前已被保留。

这些条件当前尚未建立。若兼容性需要新增连接动作或换轨，则已经超出本候选“新 MOVE 仅来自 PIE-D”的动作域，不能借端点拼接之名加入。该受限引理也不提供全局无死锁、无饥饿、吞吐收益或真实 LIMO 动力学认证。
