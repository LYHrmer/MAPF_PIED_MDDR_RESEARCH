# 73C1：真实服务计数与固定观察期收益的数学构造

请作为科研数学顾问，只根据本题作独立建设性分析，不调用工具，不使用任何 skill，不假称读过论文、项目或实验。写中文，目标约2000字，末尾给 ADVICE-COMPLETE。此次是理论条件和反例构造，没有运行或实现请求。

背景：终身多机器人系统由共同规划器给原动作；两种执行协调方法使用相同机器人控制器、有界空间偏差、任务规则与计算费用。方法D可能因较早取得空间已清空的可信证据而较早启动某一原MOVE。方法R与低主动观测成本方法E0为对照。较早参考轨迹到达不自动等于较多真实任务完成。假设所有方法都有一个事先固定公共观察终点E，不能看到结果再挑E。真实服务仅发生于事先固定的离散服务时刻序列 t_1<t_2<...<E；每行对某机器人的有效、尚未完成队首任务检查真实实体是否在该任务服务区域，还须未停止，必要的停稳要求另属明确服务谓词。一个任务最多计一次。完成时任务队列可改变，未来任务分配也可能依赖完成历史。规划/查询/交接费用均影响以后运动。服务计数Q_a(E)是所有合法服务事件数，不以证书或参考到达计数。真实实体中心 p=reference+z，z属于固定已知集合Z；其形体F也固定。哪些服务谓词要求中心落入、哪些要求全形体落入应明确。

请交付：
1. 构造一条精确的局部截面严格收益命题：D的参考到达有上界u，R/E0参考到达有下界l且u<l，但还需要什么几何、速度、任务身份、停止、其他计数与固定服务行条件才能推出某个预定t_j的总Q_D>Q_R及Q_D>Q_E0？要防止对照虽然参考未到却实体已在宽服务区服务。给足够而非必要条件，允许只给定任务B的机制见证；同时明确从B多一次推总计数需额外前提。
2. 给一条可以推出整个固定E计数不劣的事件配对定理，逐项说清前提是只为理论受限族还是整个LLMAPF都可信；不能把结论藏进“每项到达都更早”的未经证明前提。能否用共同外生有序任务队列、相同单机器人后续服务时间和保持领先的归纳获得非循环的受限例子？新任务到达/服务时刻离散时须考虑匹配。
3. 给一个无需数值、用符号不等式说明的反例：D在早期多服务但在固定E被追平或反超。反超可由其额外收费查询导致后续更晚服务，或新任务分配分叉，但不要换世界规则和事后截尾。
4. 如何将局部机制假设与主配对端到端吞吐可证伪检验分开写，让审稿人看到真实可验证贡献又不保证正效应？约五句具体主张。
所有符号仅为数学量。不要给实验规模、窗口、预算或参数数值/范围，不请求采集或生成任何实验数据，不给伪代码、代码或执行命令。你不是正式投票审稿人；指出不成立处并给可修正条件。

