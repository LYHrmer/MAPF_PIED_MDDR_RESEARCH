# 73P15 名义参考、代数事件与付费进度证据的模型备选

状态：NO-SKILL / CONSTRUCTIVE / NONBALLOT；2026-09-10。唯一新建设报告，不是 C14 改写或重试，本题不调用 Claude。HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT 保持；不修改 73、行政页、旧件、Git 或桌面，不实现、构建、测试、仿真、调参或生成载荷/结果，不赋保护参数值、范围或默认。

## 1. 明确推荐与模型身份

建议根优先比较本件的**可信名义参考发生器**，作为缩小参考侧数值/判序义务的具体模型备选；尚不宣布根已采用，也不替换现工作稿。唯一所选控制为：用原符号 a_launch 恒加速，在原 a_hi 制动锥边界切入恒减速到 cap，显式 BRAKE 则保持不可取消的制动锁存。原 APPROACH 及其 FINITE_CAUSAL_REFERENCE_HISTORY 路线作为另一个具名备选保留。

这项修订**删除并替换原参考方程中的 eta 扰动层及其反馈 APPROACH**。新 s 是可信名义轨迹发生器的内部状态，满足 s_dot=v、v_dot 为当前名义段的恒定加速度；不是把原 eta 偷偷赋成零、不是测得/抵消实际 eta，也不是原任意有界可测参考驱动域的等价实现。delta 与 a_lo 在旧模型中的角色保留于旧备选记录，本模型不再依赖它们来生成参考段；不将其设零、改名、挪为别的参数或给它们新值。

本方案仍能研究“有真实空间偏差时，付费参考进度证据能否安全减少空间责任并带来净任务收益”，但不能继续声称处理参考加速度不确定性，也不能把购买 POSITION 说成测量/校正 z 或实体 pose。它把观测的信息来源重点移到**实际本地授权安装、控制尝试接纳和中断历史的不完全共享**。是否有研究价值取决于这些信息在合法制度下确实未被中心免费确定，而不取决于协议是否人为禁止中心作正确推断。

## 2. 固定输入及实际阅读

起始 73 文件 73_PIE_SPATIAL_EVIDENCE_TERMINAL_HANDOFF_AND_PAID_SERVICE_PREEXPERIMENT_DRAFT_20260908.md 的完整字节身份为 SHA256 `e72ccb8cd65bbbd037aa2ea005666bd46e307c4f3ae2da860f0cf9a9f2b9fbeb`，194152 bytes / 619 LF。本题科学读取 §3 L44–63、§6 L142–167，以及 §5 的精度、cap 安装和控制水位必要窗口 L98–122；初次合并输出的 §5 中间发生截断，所需窗口已另行完整读取，不称本题读审 73 新全文。

P13 全文身份为 `56156882067661aa7746db7a8138ef2869c153d2bc694d586eb5e16ba0d9cc2f`，23740 bytes / 135 LF；P14 全文为 73P14_ROOT_CERTIFIED_PROGRESS_CONSTRUCTION_AND_HYBRID_ORDER_ANALYSIS_20260910.md，`6b85834fb23146413389bb2375133eddbf5e4599e19d2b3effee72262a8e00a9`，10635 bytes / 73 LF。本题实际完整读取这两件，保留它们的历史判断与调用终态，不修改完成件。不重查数值库、模拟器或 R0；以下为从所读控制/证据合同独立推导的数学构造，没有新增后端运行证据。

## 3. 真实空间误差及跨边拼接完整保留

实体仍严格为 X(t)=u+s(t)e+z(t)+F，z_dot=−kappa z+w；原 F 非空紧足迹、Z 闭盒、kappa 及 w 的原有界可测允许域和初态包络不变。**非零空间扰动与非零偏差仍被允许**，不是只留一个形式上存在而实际永远为零的 z。其变分公式与 Z 正不变性仍用原证明；本模型不把 no-start、网络丢失、控制迟到或名义轨迹停顿重新命名为空间误差。

参考单调只约束 s；实体平移基点 u+s e+z 可因 z_dot 回摆。即使参考 HOLD，实体仍可在原 Z 中变化，因此 END/HOLD 不自动证明实体静止或任务已服务。原足迹/包络资源责任不因名义参考更易计算而缩小，POSITION 仍不给中央 z、w 或普通 pose 的可信读权限。

原边真正 END 到后继原边进入之间，世界参考 p 保持在原终点，z 按同一个世界闭环连续演化。换边只重设新边局部 s/v 为其原零速入口，不清零或旋转 z，不改变 F/Z/kappa/w，不以中点 cap 触发换边或消费。由于本构造到原终点时名义速度为零，原端点拼接与包含 endpoint 包络的覆盖推理可以继续逐项使用；仍只是固定朝向理想平移模型，没有补出 jerk、轮速、驱动滞后或实机跟踪保证。

## 4. 唯一局部发生器及有限到 cap 证明

保留原控制符号 a_launch>0 与 a_hi>0 的意义，未给二者任何值。给定本地实际已安装 cap C，合法参考域仍是 0≤s≤C、v≥0 和 B_safe=2a_hi(C−s)−v²≥0；B_safe 是证明量，不是保护预算。中心已授 b、实际 C 和 q_release 的关系不变，C≤b≤ell。新名义模式为 ACCEL、CAP_DECEL、BRAKE、HOLD；CAP_DECEL 是正常到 cap 的自动减速，BRAKE 是明确控制尝试触发且必须完成的制动，两者虽使用同一减速度，控制义务不同。

一个真正接纳的新 RUN 只在合法 HOLD、C>s 且 no-start 未阻止时启用名义运行。从 B_safe>0 的运行状态取加速度 a_launch，直至首次 B_safe=0；随后取加速度 −a_hi，直到 s=C、v=0，转 HOLD。若重入状态已经处于 B_safe=0 且 C>s，则直接进入 CAP_DECEL；C=s 且 v=0 为 HOLD，不构造零时长 ACCEL/CAP_DECEL 循环。运行中到达这些自治边界不消费新的控制尝试，也不产生额外消息。

证明从任一合法初态 s0、v0、C>s0 出发，记 B0=2a_hi(C−s0)−v0²。ACCEL 段有

**s(tau)=s0+v0 tau+a_launch tau²/2，v(tau)=v0+a_launch tau；  
B_safe(tau)=B0−2(a_hi+a_launch)v0 tau−(a_hi+a_launch)a_launch tau²。**

故非负首次切换时间唯一为

**tau_acc=[sqrt(v0²+a_launch B0/(a_hi+a_launch))−v0]/a_launch。**

B0>0 时它严格正；B0=0 时它为零。切换速度 v_star=sqrt(v0²+a_launch B0/(a_hi+a_launch))，且 v_star²=2a_hi(C−s_star)。随后 CAP_DECEL 持续 v_star/a_hi，恰好走完 v_star²/(2a_hi)，于 C 零速停止。因 C>s0，整个不中断运行段具有有限正总时长 tau_acc+v_star/a_hi；这些是由原符号和合法初态推导的表达式，不是给时间预算或 H 赋值。

ACCEL 中 B_safe 在首次根前非负、s 尚未越过 C；CAP_DECEL 中 B_safe 的导数为零，因此全段保持原制动锥。参考 v 在零速接点停止，不延伸到负速度。这个有限到 cap 证明不需要 eta 的实际历史、数值积分或 P13 中一般幂积分的到达判等；它也不承诺每次 RUN 都能被实际接纳、永久丢包时有进展，或有限 O 提供了足够服务。

## 5. BRAKE、cap 增量与同刻次序

明确 BRAKE 被 ACTUATOR 真正接纳后，从当时合法 s_b、v_b 起取 −a_hi，持续 v_b/a_hi 到首次零速；停止位置为 s_b+v_b²/(2a_hi)≤C，再转 HOLD。B_safe 在制动期间保持原值，故不越 cap。新 cap 或新 RUN 都不能提前撤销已开始的 BRAKE；BRAKE 期间已处理而无作用的 RUN 仍消费其原尝试，不成为零速后的自动启动钩子。零速后只有另一个尚未处理、身份合格的新 RUN 才可按原总序生效。

实际安装 C_new≥C_old 时 s/v/z 均连续，B_safe 增加 2a_hi(C_new−C_old)。ACCEL 中继续向新边界运行；CAP_DECEL 中若是严格增加 cap，则新 B_safe>0，可切回 ACCEL，再按新 cap 的边界减速。相同 cap 的幂等重放不改变现有运动。BRAKE 保持制动到零，HOLD 安装后仍 HOLD。这是对原“扩 cap 后按 r 选择 LAUNCH/APPROACH”的明确替换，不声称保留原加速度轨迹；加速度可在合法接点跳变，但 s/v/z 不跳。

旧 cap 到达与安装同刻时，先完成物理零速到达/HOLD，再处理 cap；安装后不自动恢复。若旧 cap 为 ell，则原 END/永久关闭优先，旧 tid 的安装和 RUN 不能重开。ACCEL 到制动边界恰与外部安装同刻时，也先完成原自治模式接点，再按实际安装后的 B_safe 选择运行段；这不生成第二条命令。已开始 BRAKE 的零速与新控制处理同刻亦服从物理接点优先，只有原队列中确实合法、尚未消费的后继尝试可能生效，不凭到零补造 RUN。

控制接纳仍完整绑定原 session/tid/slot/parent、control_epoch 和公共尝试水位。cap 与 RUN 权限独立；认证/入队不等于 ACTUATOR 已接纳，no-start 和丢包不被新轨迹发生器修复。中点 cap 的 HOLD 不发 END，不消费 slot，不改变 parent。只有原 ell 上满足原 MOVE_LIVE/未关闭等完整守卫的零速接点才建立原 END 记录；随后仍走付费 END_PROOF、中心 FINISHED、READY 和本地消费链。

固定 C 且最后一个实际 RUN 之后无新的 BRAKE/中断时，可用第 4 节有限到达。每次合法扩展重新确定后继片段；原有限 CAND 和有限公共命令前缀给有限次实际更新，正常运行片段内部至多经历加速/减速/停止的有限接点，没有无穷个零时长模式切换。未达到 ell 或缺新的 RUN 时允许真实停滞，不能借有限到 cap 命题补全系统活性。

## 6. 代数表示能闭到哪一层

所选表示域要求**进入名义参考计算的实际量**有有限精确实代数表示：本次原边/已安装 cap、名义系数、初态，以及真正 ACTUATOR 接纳和 CAPTURE 的时刻。可用整系数多项式与有理隔离区间/根身份表示实代数数；精确四则、平方根及代数数的相等/次序可归约到有限多项式与隔离区间操作。它们是数学上的有限计算，不是常数时间读取或已绑定库的性能承诺。

在上述输入域内，第 4–5 节每个新片段的系数、状态、自治边界和绝对到达时刻都由有限次代数运算得到。有限事件序列归纳后仍在这个域。因此“旧 cap 到达是否与这一次已给定的安装时刻相等”在该域内有有限判定，原先 HOLD 优先的并发边界不必用浮点容差或相等概率零处理。这是相对 P13/P14 一般 APPROACH 到达值的实质简化。

**收费本身不保证输入时刻为代数数。** 既有 O 尚未固定数值，本件不生成 O、不改变 H 或给时间字段默认；必须对实际允许的公共行时刻及命令接纳来源另给表示资格。若输入包含未有代数表示的 cap、外部时刻或初态，不准先近似到一个方便数，再称本节证明仍覆盖原输入。有限紧足迹/任意几何对象也不会自动提供代数 CAND，原几何来源资格仍需与该接口相容。

公共行和应用接纳时刻若满足该域，只能推出**参考子系统的这些入口及自治事件**可按代数比较，不推出所有世界事件为代数数。z 的原解析式仍含指数卷积；即便考虑允许域中的常值非零 w 单段，z(t)=z_eq+(z0−z_eq)exp(−kappa(t−t0))，真实服务位置阈值的交点表达式可含对数。这里不是给 w 选实验值，只说明原真实空间模型的计算义务并未随参考段二次化而消失。

因此 TASK 服务、实体相交/服务区进入、驻留或评分 Q 若需比较真实 z 或其首次事件，仍须独立认证其输入、包含性和判序；本件未读该层全部规则，不把它列入已闭项。把此类真实事件仅在后继代数 O 行通知算法，也不能将通知点冒作真实发生点。如果它们直接决定下一次参考输入的非代数时刻，会破坏本节入口前提；如果只经原有限行交付，它们的真实评分资格仍独立未闭。

## 7. 付费 CAPTURE 与观测精度

新 authority 不需要读取实际 eta。它只能在原付费 CAPTURE 点固定自己当前名义段/本地模式与控制版本的不可变根，含合法 tid、实际 C、段起点时刻、起点 s/v、当前段及已处理命令身份；不能让中心旁读这些字段或日志。因为这就是定义 s 的可信发生器状态，不再从普通 pose 反求参考，也不新增外部物理传感器的精度神谕。其与实际控制执行状态的一致性仍是需资格化的可信接口，不能只凭“名义”一词成立。

后续数值作业在同一捕获版本上，用相应二次表达式及已确定事件段计算 s(tau) 的外包区间。保留 P13/P14 的精确 dyadic 端点 [L,U] 和 q=max(0,L)；只有实际 U−L 满足事前 profile 的 epsilon 才签发合格精度。代数数可用有理隔离细化，在固定精度有可认证正余量时有限收窄；本件不给 epsilon 设值、换成零或改成按几何需求临时选。精确代数内部状态不等于给中心免费发送完全精确 s。

证书仍属于原 CAPTURE 时刻；之后世界继续推进，不刷新完成点。旧同 tid q 的安全单调性保留，但其误差上界不转成当前时刻精度，旧 tid 不给新 MOVE 作证。得到 q 不等于得到 C 安装、RUN 接纳、CURSOR、END 或真实任务服务证明。模型/表示不合法与普通观测没算完分别按原 P13/P14 分类，不将错误物理世界当成传感器失败。

## 8. 中心为什么仍可能需要证据，以及最不利条件

§5 已规定中心不接收 cap 安装、START 或中点到达 ACK，CUM_CAP 不含 s/q，控制队列和审计器私有状态不向策略开放。知道名义控制公式，不等于知道实际本地 C、哪个 RUN 已接纳、no-start 是否阻止、BRAKE 是否先发生，以及它们的真实处理次序。仅凭中心已提交 b 和已发送 RUN，不能确定当前 s。

一个合法信息反例是两条中心已交付知识相同的历史：都已提交同一 cap 并发同一 RUN，但其中一条因本地 no-start/未实际接纳而留在起点 HOLD，另一条已接纳并在原 END 前向前运行；没有额外 ACK，普通反馈可以同样缺失。后者的正进度不能作为前者的安全下界。若这种接纳差别确属本制度允许且对中心不可知，POSITION 可通过付费读取本地真实段状态区分它们。它购买的是控制进展事实，不是把通信时延叫成空间偏差，也不构成 D 比 R 更会购买的效果证明。

**最不利条件**是合法公共信息已经确定 cap/RUN 的全部实际接纳时刻及之后所有中断，且不存在仍未知的本地控制分支。此时中心可按同一名义公式重建 s；求值计算仍应收费，但信息获取上可能不必再买 POSITION。即使数据没有直接附上 s，也不能因此宣称它不可推断。不能为了让查询有用而隐藏原本公开的时间、禁止正确推断，或把“协议强制买证书”冒作信息价值证据。

在这个退化域，付费查询可能只增加成本；若仍需 authority 签名，它衡量的是接口认证开销/权限选择，而非缺失进度信息的价值，主张必须相应收窄。D/R/E0 与外部强基线的共同信息域及可合法推断能力应明确一致。本件不直接修改这些臂或增设预算/新臂，只要求根选择本备选时承认该反例，不能用更容易数值闭合或获得阳性结果作为采用理由。

真实空间误差 z 在两种信息域下都保留。即使中心能预测 s，它一般仍不能预测 w/z，但本研究若一直用同一最坏情形 Z 保护资源，未知 z 本身不会自动恢复 POSITION 的信息需求，因为 POSITION 并不测 z。这是此简化对研究解释最重要的代价。

## 9. 控制和证据费用不能由有限代数性豁免

名义段准备、代数根构造/比较、事件记录和状态更新属于实际控制工作；新 RUN/cap/BRAKE 的认证、队列、代际/水位、拒绝及无作用尝试仍按原能力和服务收费。自治接点不产生额外 O、服务容量或消息；其记录与原 END 所需槽位必须已有合法准备和真实供给保障。到 cap 的数学有限性不保证原有限服务能及时完成每个控制准备。

尤其不能把任意复杂代数运算塞进原 ACTUATOR 原子接纳，称其仍然只是一条固定免费赋值。原子接纳/自治记录可用的实际收费执行界、需事前准备的段/守卫对象，以及版本在准备与实际接纳之间的有效性，必须由后续具体接口证明；本件尚未提供实际后端。准备失败时不得假称控制已接纳，模式/守卫本身未认证时不得继续生成可信物理结果。不能先让宿主免费算完全部事件，再事后随意记一个成本。

常驻控制维护与 POSITION 专用版本保留、复制、细化、封装分别计实际工作，禁止借共同底座免费替查询预计算高精度证书。E0 不购买 POSITION，不承担虚构的查询专用开销；所有臂仍共同承担自己真实控制/物理费用。D/R/F_cap 的合格证据、严格后继发布、通道、接收验证与中心退休不变，复用只计已发生一次的匹配版本工作，失败与未完成不退款。

有限 O、丢包、长期 no-start、STOP/FAIL_STOP 或排队拥塞仍可阻止到 cap、观测或退休；没有为了新模型偷偷加后继 RUN、精度窗口或再采样。只在实际达到 c_i 后有匹配、足精度、已付交付并退休的证书时，原组进展中 q≥max(0,c_i−epsilon_i) 的推理才继续成立；仍不是无条件任务收益。

## 10. 差异表和下一最小证据义务

| 项目 | 与当前控制的关系 |
|---|---|
| 参考 eta 与原 APPROACH | 明确删除该层并替换控制律；不是 eta=0 代入或全域等价。 |
| 名义运行与到达 | 由加速、上制动锥边界、恒减速组成；在合法代数入口域内给出有限到达/判序。 |
| BRAKE | 使用名义 −a_hi；保留显式制动不可提前取消及后继新 RUN 义务。 |
| cap 增量 | s/v/z 连续、余量只增；自动到 cap 减速可因扩 cap 重新加速，手动 BRAKE/HOLD 不自动恢复。 |
| 同刻物理接点/END | 保留先物理 HOLD/关闭，再处理安装/控制尝试；用精确代数比较替代本域内的未决近似比较。 |
| X、z、w、F/Z 与跨边 | 原空间误差、足迹、不变包络、连续 z 和零速原终点拼接全部保留；真实实体不被理想化成无误差点。 |
| POSITION | 从名义本地段状态付费取证；删除实际 eta 历史能力，保留固定 epsilon、CAPTURE 冻结、通信及退休费用。 |
| 中心信息 | 不赠送实际接纳/模式；合法公开信息足以预测时须承认其信息退化，不能强行制造查询价值。 |
| 世界任务/评分与实际计量 | 未被代数参考证明覆盖；涉及 z 的服务事实及原收费执行后端仍需独立证据。 |

下一最小证据对象应是**参考事件入口与收费接纳的封闭表**：只针对本备选，逐一列明 C/边长/初态、公共 O 时刻、实际 cap/RUN/BRAKE 接纳时刻和 CAPTURE 点的来源、精确表示、控制准备/发布费用及同刻优先序；检查有没有真实 z/任务服务事件直接把未认证的非代数时刻送入发生器。该表的每一项都须由已有合法接口或具体后续来源证据支持，不能仅写“assume algebraic”。

在这一步闭合前，本件只能关闭**局部代数模型中的运行/制动/cap 兼容与判序数学**，不能宣布全世界可精确仿真或成本后端可用。真实 z 服务与 Q 的认证另保留具名义务。根可据此比较“保留原 eta/APPROACH 的更广驱动问题”与“明确名义参考、保留真实空间误差的问题”是否更符合最终研究目标；本代理推荐后者作为当前优先评估备选，保留原路线，不自行采用或放行。

本报告实际完成只结束 P15 这项有界建设工作。未读禁限载荷、未生成任何控制参数/O/实验配置或结果，未调用 Claude/skills，未更改旧文件。HOLD 与原六门状态保持。
