# 73C6：Claude Opus 规划计算服务模型的有界建设任务

2026-09-09，根准备；NEW BOUNDED CONSTRUCTION / NONBALLOT。这是新题：选择可实际成立的规划计算服务模型，非失落73C4异步环重派、非73C5泛路线重试、非正式审稿。只读本提示即可，不调用任何工具/skill，不编写代码或伪代码，不引用未取得文献。请用中文完成约1800–3000字的建设性答复，结尾独立写ADVICE-COMPLETE。完整返回后由根不用skill独立分析；不能自授PASS。

研究目标是大规模lifelong多智能体路径规划中，有界真实空间偏差、付费可信观察及计算/通信耗时下的实际任务吞吐。主新动作只来自固定PIE-D源，包装只准原MOVE/WAIT/HOLD/已证制动；可有外部系统基线自己的规划动作。当前HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT。所有保护参数保持原义未赋值，不能给取值/范围/默认值，不能以数字实例、实验或图像替证明。

已确定而不能偷换：
- 每次规划输入是中心已付费取得的不可变SharedEnv/任务/游标/计划版本，不含真正私有位置或未来世界输入。
- 规划源有chrono经过钟、CPU clock日志、C rand、MT及缓存；候选源状态只有输出通过后继中心PROJECT/版本CAS才成为下一基准，拒收不污染它。失败费用、公共时刻、首次预算资格不可回滚。
- 原源初始化/首次预算问题已有明确同步隔离入口修复方案；局部第五堆>=将纸面改为严格>。原native身份独立，没有实施。
- 物理与真实TASK_SERVICE继续，实际Q来自完整真实服务前缀；不能因规划忙冻结世界或重开固定E。可信END与READY在各站付费处理、持续资源覆盖；源求解本身不直接作用机器人。
- 各臂具有相同服务能力/费用/机会规则，但自身闭环作业数、成本和结果可以不同。源/方法无解和实际超时不删总体样本。

现合同A：固定有限公共O，在PROCESS_SLICE(Srv_P)行仅给有限工作容量，源每步骤先收费才能推进；暂停时源无工作，chrono now返回本次公共行物理时刻，CPU日志取实际收费指令服务。整体运行想要可精确重放。已核普通fork/COW只能分叉私有内存；墙钟暂停或perf采样/溢出通知没有给严格每步骤停止保证。QEMU icount属于全系统模拟，需要Linux/时钟/外部调用与状态保存接口，当前未得合格后端。其他authority/中心/网络也各有自己的FIFO和付费作业、合法CAPTURE及后继发布。

根构造备选B NP-PLANNER，仅更换Srv_P服务域：
1. 相同FIFO、一次一个非抢占隔离作业，t_start冻结输入I、源基准S和预算。完整作业内只读I与自身状态。
2. 源体实际elapsed=d_body，最早t_seal=t_start+d_body封存Y、S_prime。宿主可以先计算私有结果，但t_seal之前任何算法不能看Y或依其成功改变查询/公共O；之后接收/验证/CAS仍付费。给定本作业执行环境轨迹，局部结果不含未来世界输入，可望用事件归纳证明因果。
3. 以宿主CLOCK_MONOTONIC_RAW的h为时基，chrono映射t_start+(h_now-h_start)，原单位、局部起点、增量、比较不改；CPU clock另计实际CPU及基准偏移。承认这不等于A的同公共行读钟。
4. Srv_P供给变为共同连续单工人能力及固定可开始机会，不再称精确指令脉冲。创建/COW/复制/输出/状态保留/尾部退出成本分别报告，STRICT若采用则改为此能力预付而非凑相同工作。
5. 普通任务修订使后继CAS拒收，不中断源体。WORKER_FAIL、初始化截止和Stop需要真实监管终止，与虚拟时间一致；信号到退出尾部继续收费且占站。不能事后把完整作业耗时截成E-t_start、保留本应丢的状态或把kill成功当回收。
6. 时间测量含宿主环境噪声，单靠原seed不再保证逐结果相同；原block独立/配对推断必须给新的环境创新/获取或相应随机化推断合同，不能用随机臂顺序就宣称独立。
7. 已核Linux subreaper可在旧父基准退出后接续事务子代，管理器要证属性/祖先/重新接续与退出，非业务CAS改变父子关系。fd、线程/钩子、外部状态和完整时钟覆盖仍须证明，不假称fork解决一切。

请针对以下事项给出一个明确推荐、拒绝理由和最小可审查合同，而不是无限列待办：
a. A与B哪条更适合本研究目标，能否给比二者更小但不漏算/不偷真值的第三选择？若推荐B，要具体处理宿主先算完规划但虚拟期间发生WORKER_FAIL/Stop的因果边界。
b. B的elapsed包含OS调度噪声，对配对推断究竟需要怎样的可行证据制度？不要凭空说清空缓存可证独立，也不要将所有测量研究一概判不可能。
c. 保留“拒收不污染源状态”是否必要，能否明确改为源运行状态总继承而只拒绝外部计划？若这会改变PIE语义/反事实可比性，指出确切位置。不能静默替换。
d. 给最小失败见证：怎样的错误安排会免费推进/偷看未来/少算费用；分别给一种可行修复。区分你在此确证的命题与尚缺外部实现证据。

不要求你证明普遍吞吐提升、新颖性或所有源码定义域。不能给实验参数、运行命令、代码/伪代码或生成载荷。只作科学模型和接口的建设性判断；结尾ADVICE-COMPLETE。
