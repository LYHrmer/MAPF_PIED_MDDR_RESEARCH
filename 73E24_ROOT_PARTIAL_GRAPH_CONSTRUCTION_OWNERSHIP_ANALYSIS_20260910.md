# 73E24 根独立分析：图部分构造时的唯一顶点归属

2026-09-10，根 /root；NO-SKILL / CONSTRUCTIVE / NONBALLOT。直接补查 E23 明确留下的图部分构造义务，不使用 Claude 或 skill，不启动实现、构建、测试或仿真。起始 73：SHA256 dd7abfa7086ac284277d037d1b851ae7cd199dbfc8481b547546f141e4a2e288，196691 bytes /625 LF。上一批10文件已实际提交 7f062bd8eecd3b474cfc1eac9a4c85e9ac262428；本件为后继纸面分析，不冒充上批已同步内容。HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT 保持。

## 1. 直接核源与条件缺口

根本轮完整复读固定 graph.cpp 185 LF，实际结论只依赖批准的向量地图构造 L16–59 和析构 L9–14；也重新完整读取 SharedEnvironment 版 LACAMInstance 构造 L19–38。原仓库固定提交为 [0b5b33649fc367abcb1cd0a7ad5ac03511bf9357](https://github.com/YueZhang-studyuse/LMAPF-delay/tree/0b5b33649fc367abcb1cd0a7ad5ac03511bf9357)。graph.cpp blob fb8ab481d5b1e3a959a18b826fd5919b69772e7e、SHA256 7bdb71a22dbdf6df873370c20173f0ef9ab97c34fbb649f222c7bcb9fa2813a0、4405 bytes/185 LF；graph.hpp blob bdf87cf5a1b911c039ebcf10374ddf317086fbd4、SHA256 166b3ecbe68e7ddcd549504044a3c47cc3abb9ca98653b8639a654607480daee、1699/61。LACAMInstance cpp blob 50f18e9ce8295ba94e4a7d5caa293d5affca0688、SHA256 840d442a5f3a0804ae751e1c39797a3f669dd43dcadf48786b73b1fc0e2db1c4、4137/139。以上为已取得且核完整字节的源码对象，未运行、未修改。

原向量构造先建立 U，再逐自由格分配 Vertex、把裸指针 push 到 V、写 U，最后给各顶点 neighbor 添加借用指针。原 Graph 析构只有遍历 V 后 delete。若某顶点已分配但 V.push_back 扩容抛异常，这个尚未进入 V 的对象没有局部拥有者。即使全部已进 V，后续顶点或 neighbor 容器分配抛异常，外层 Graph 的构造尚未完成，也不能假定它的通常析构体会替这条路径删除全部 Vertex。

根核 [N4140 except.ctor 第1–3段](https://timsong-cpp.github.io/cppwp/n4140/except.ctor)：异常展开销毁已完整构造的对象/子对象；构造未完成不等于正常调用该最外层对象析构体。原 V/U 元素是裸指针，容器自身的销毁不负责 pointee。由已核源码与这一语言条文可推出上述条件漏清理风险；不是已观察到的 bad_alloc 或实际泄漏量。文件读取版构造不属于批准入口；看其源码不等于读取任何地图/场景载荷，也不在本件扩展它的资格。

## 2. 唯一所选纸面归属修订

在 E23 的唯一直接建立 Graph、禁止拥有图对象复制/迁移基础上，加入一个属于 Graph、在建图主体开始之前已完成自身初始化的顶点拥有者容器。该容器独占所有已经移交的 Vertex；V、U 与 Vertex.neighbor 始终只是借用索引。此拥有者在成员声明/寿命顺序上早于借用容器，使异常展开和正常销毁中借用容器先结束；成员自身清理不依赖外层 Graph 构造是否完成。

每个 Vertex 从成功分配到进入拥有者容器之间由当前局部唯一拥有责任覆盖；拥有者接管成功后，才向 V/U 以及后续 neighbor 发布借用。如果接管容器分配失败，局部拥有者清理尚未移交顶点；如果移交后 V/neighbor 分配失败，成员拥有者清理已移交顶点。不能先把局部责任清空、再执行可能抛异常的接管，也不能由两个拥有者同时负责同一顶点。没有重试产生第二个同 id 顶点的权限。

原 Graph 析构中对 V 的裸 delete 必须随归属迁移一起移除；正常清理及异常展开只能走同一个真实拥有者，不能保留原路径再“额外清一次”。Vertex 的 neighbor 只销毁指针容器，不递归删除或解引用邻接目标；因此清理已构成的部分邻接不要求对循环图做拓扑排序。对图外的 starts/goals/Solution 等借用，仍须 E23 的外层寿命约束及 E22 的先停止借用后清理，不给新逃逸接口。

只有 Graph 完整成功后才进入 LACAMInstance 构造体；若 starts/goals 扩容或其后合法日志操作抛异常，已经完整构造的 G 成员按同一拥有者清理。完整 LACAMInstance 尚未形成前不能向 planner 发布它；该条件与 E23 的直接命名构造一致。输入安装后的这类硬失败沿既定 FAIL_STOP_SOURCE，不当正常 AA 软拒收节点，不重建图、回滚随机或免费重试。

以上是归属与发布责任的文字合同，不是实现或伪代码，也没有声称未来采用哪种容器、分配器或源码 ABI 已被认证。真正源修订仍需单独身份。

## 3. 根独立归纳与未覆盖分支

正常构造的归纳量是：每个成功分配且尚未释放的顶点恰有一个已建立局部或成员拥有者；每个已发布 V/U/neighbor 指针都指向仍被成员拥有者持有的顶点；图外尚无借用。分配前成立，分配后局部接管保持唯一性，移交后成员接管保持唯一性，发布借用不增加拥有者。任何后继普通分配异常只结束局部或成员寿命，各自删除自己唯一对象，不依赖缺失的 V 条目；正常结束则把这一不变量交给 E23 的外层借用链。

该归纳只处理正规异常展开及有效对象。整数转换/循环索引/rows×cols/地图长度关系、U 索引合法性、内存被破坏、未定义行为、分配器/析构异常、强制终止或工作进程故障都不由它自动解决。若清理不能完成，仍只能记真实失败与保留成本，不能假称已释放、已回到可继续节点或提供免费收尾容量。原图拓扑/障碍语义以及全部输入/机器数值域仍需独立来源资格。

成功正常路径中，原自由格扫描顺序、Vertex id/index 及四邻接插入顺序保留，新增拥有者只记录对象责任；失败边界与对象/容器工作则确有改变。该修订不能声称与原裸所有权实现在所有墙钟结果等价。根没有借异常问题改变原任务、原 MOVE、目标引导或随机初态。

## 4. 公平费用与机械合同

输入向量复制、拥有者建立/增长、局部到成员移交、V/U/邻接容器的实际工作、失败对象和异常清理均记入各来源臂自己的真实求解成本。源 deadline 或公共 Stop 不豁免已发生费用；资源不足不能以审计期间的构造证明替运行中的实际容量。所有内部引用 R0-REPAIRED-LACAM-ADAPTER 的臂共同承担本修订，H19 自己的 ECBS 不虚收 R0 对象费用。

拟登记 W90 为 NOT_IMPLEMENTED / NOT_RUN：部分 Graph 构造的每个已分配顶点有唯一拥有者；移交前后失败均不遗失或重复释放；邻接只借用；外层 LACAMInstance 构造失败时完整 G 可清理；原裸 V delete 不与新拥有者重复。它与 W89 的直接构造/输出寿命不同，不以“已 forbid copy”代替异常构造责任。

本有界分析仅关闭一个明确纸面设计缺口，不升级六门，不给全源继续域或实际泄漏检查票。未赋任何保护参数或输入数值，未读取35–38/Q/受限载荷，未产生代码、运行或实验结果。
