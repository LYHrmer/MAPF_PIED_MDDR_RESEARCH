# PIE-D框架下有界跟踪误差的终身多智能体安全执行与进度查询调度

**实验前主稿，2026-09-14。** 本稿将已接受的参数化方案整理为论文正文；实验结果留空，具体总体、模型及实现绑定尚未全部完成。科学定义以[固定73](73_PIE_SPATIAL_EVIDENCE_TERMINAL_HANDOFF_AND_PAID_SERVICE_PREEXPERIMENT_DRAFT_20260908.md)为准，当前资格状态见[项目入口](.github/README.md)。本文不替换冻结规格，也不启动新一轮完整设计审查。

## 摘要

终身多智能体路径规划需要在持续执行任务的同时更新路径。有尺寸机器人存在有界跟踪误差时，名义路径上的离开不能直接证明实体已清空相应空间；持续保留整段占用又可能增加其他机器人的等待。本文以PIE-D为直接改进框架，研究有界跟踪误差下的执行授权与有成本的进度查询调度。方法以足迹和误差包络构造保留空间，在不改变原规划动作身份的条件下授予单调前缀权限，并依据可信的参考进度下界释放已经可证不再占用的空间。阻塞导向策略将查询机会分配给当前可释放阻塞的活动动作，其观测、计算和通信工作均进入共同有限供给下的执行过程。本文给出条件安全归纳与区间事件几何，并设计同能力轮询、不查询途中进度及已发表持续执行方法的比较，以固定窗口内真实任务完成率评价净收益。大规模仿真及性能结论尚待完成。

关键词：终身多智能体路径规划；PIE-D；有界跟踪误差；安全执行；资源受限观测；空间释放

## 1 引言与研究定位

仓储机器人需要不断接收任务。规划器即使给出离散无冲突路径，实际执行仍受规划耗时、动作延迟、实体尺寸和跟踪偏差影响。PIE将规划与执行并发组织，并把规划等待纳入评价；PIE-D进一步研究概率执行延迟造成的执行和未来起点失同步。[1,2] 本文沿这一框架研究另一个明确的问题：真实实体可能偏离名义中心线，而且调度器取得可信进度需要付出时间和计算通信成本时，如何安全释放空间，并使取得信息的收益体现在实际任务完成量上。

本文的“处理误差”指在声明的误差范围内维持安全占用和执行权限。研究对象是式（2）的逐分量有界跟踪误差，其不变盒同时含横向与纵向分量，其中横向分量无法通过时间重参数化消除；本文也区分普通反馈误报、迟到和随机no-start。模型内的空间偏差不能仅用名义动作延迟代替；本文也不以降低定位误差或在线估计扰动作为已完成的算法贡献。

本文扩展了PIE-D的两个模型要素：连续跟踪误差决定需要保留的空间，资源受限进度查询决定何时能够取得足以释放空间的信息。下文“付费”统一指查询消耗声明的计算和通信供给，等待由队列和事件时序产生；其计量单位是模型资源工作量。P_model与宿主实际耗时分别记录，当前没有建立模型工作量到实机耗时或金钱价格的标定关系。这两项扩展须通过共同执行域与明确对照检验，不能直接沿用原PIE-D的规模结论。

方法保留PIE-D作者代码中的路径提议及承诺／未来路径切分来源，在执行层增加三项相互衔接的机制：以实体足迹和误差不变集覆盖可能占用；在同一原MOVE内协调前缀授权与可信进度释放；根据已知阻塞选择付费观测对象。信息更早到达只有在引发有效释放、允许后续授权并最终增加真实服务时，才产生正的任务效应。因此，安全性、局部释放和全窗口净收益分别论证和评价。

本文需要检验两项相连的实用性主张：在相同观测能力下，阻塞导向选择是否优于稳定轮询；购买途中进度是否优于仅依赖共同终点交接的执行。二者均成立时，才支持“途中观测值得购买且值得定向分配”。纯安全结论、更多退休事件或某个局部提前放行均不能代替这两项比较。

### 1.1 与已发表方法的关系

PIE在执行已承诺动作期间改善未承诺路径，其原模型不包含执行延迟，并采用相应的通信假设。[2] PIE-D通过规划器、执行器和预测模拟器组织延迟条件下的并发过程，给出PIBT-D及重规划策略等实例化。[1] 本文的直接改进锚点是PIE-D。当前工程来源使用其固定作者代码中的LACAM-only入口，完整发表组合的复现须单独对应，不能仅凭复用组件名称宣称已复现全部PIBT-D＋A行为。

LaCAM2对应Okumura的IJCAI-23论文，其中LaCAM*获得初解后可继续改善路径。[3] 本文使用其来源的规划组件说明路径从何产生，不将该论文的搜索性质直接扩展为连续误差执行或lifelong吞吐保证。

Hönig等通过动作依赖图（ADG）和commit cut衔接持续规划与执行，其模型对空间偏轨和动作完成通知有明确假设。[4] 该方法为外部持续执行对照提供依据。本文所用ECBS组件的修复仅属于该对照的复现准备；ADG、计划接续及共同执行域的符合性须另行对应，ECBS本身不是本文拟议的误差处理方法。

占用的亚动作粒度表达本身不是本文的新内容。连续时间冲突搜索把智能体占用表示为连续时间区间并据此判定冲突[5]；连续时间安全区间的优先规划把同类表达用于二维路网上的多智能体规划[6]。这两条工作在规划期给出连续时间占用，本文与其区别在于执行期：保留区间的下界\(q_i\)在同一原MOVE内随已付费的可信进度证据单调上升，从而在不改变动作身份的前提下释放已可证不再占用的空间。因此本文的可检验新颖性限定为“执行期由付费可信进度下界驱动的在线释放”，不包括连续时间占用表达。

以动作依赖图衔接规划与执行的这一谱系把执行扰动建模为时序延迟。Berndt等在同一谱系上提出可切换动作依赖图，用滚动时域混合整数规划在线重排通过共享资源的次序，并保持无死锁[7]。该工作以其假设把单机器人空间避碰整体外置给车载导航，因此有界跟踪误差不在其模型之内；其扰动为智能体停止一段时间的时序量；其几何只在依赖图构造期用车体面积与闵可夫斯基和判定空间互斥，不含误差不变集，也没有执行期可收缩的保留区间。以上依据其作者预印本的假设条目、符号表与算法一，正式版的节号尚未逐项对应。本文因此不把它作为直接可比对照：其评价量是累计路径完成时间，与本文固定窗口真实服务率不同类。需要指出，两种机制在结果层都可能减少级联等待，本文未检验“仅重排次序”能否取得同等任务效应，该比较列为局限。

本文不以“前缀”“误差包络”或“付费观测”单个概念宣称首创。可检验的研究内容是上述在线释放机制在指定PIE-D来源、原动作限制和完整费用条件下的组合效果。相关执行、观测和连续几何工作的完整对照沿既有文献矩阵补入，当前七项来源只承担框架、组件、近邻排除及主要外部比较的出处说明。

## 2 问题定义

### 2.1 任务与物理模型

规划层采用离散四邻接图，机器人持续执行已分配任务。每个原MOVE具有不可混用的动作身份，包括机器人、计划代际、动作槽和起终点。执行层只调整同一MOVE的可执行前缀、等待和合法制动，不另加绕行边或伪目标。后续规划仍可能随本臂已经付费取得的信息和执行历史改变，因此不同方法的闭环路径无需相同。

设机器人 \(i\) 的当前原边为 \(u_i\rightarrow v_i\)，长度 \(\ell_i>0\)，单位方向 \(e_i=(v_i-u_i)/\ell_i\)，参考进度为 \(s_i(t)\)。固定朝向平移模型下，实体占据空间为

\[
X_i(t)=\{u_i+s_i(t)e_i+z_i(t)\}\oplus F,
\qquad
\dot z_i(t)=-\kappa z_i(t)+w_i(t).
\tag{1}
\]

其中 \(F\) 为非空紧足迹，\(\kappa>0\)，\(\oplus\) 为闵可夫斯基和。对每个空间分量 \(d\)，假设

\[
|z_{i,d}(0)|\le \bar z_d,\qquad
|w_{i,d}(t)|\le \kappa\bar z_d,\qquad
Z=\prod_d[-\bar z_d,\bar z_d].
\tag{2}
\]

由变分公式，

\[
|z_{i,d}(t)|
\le e^{-\kappa t}\bar z_d+
\int_0^t e^{-\kappa(t-a)}\kappa\bar z_d\,da
=\bar z_d.
\tag{3}
\]

因此 \(z_i(t)\in Z\)。参考进度单调仍允许实体因 \(z_i(t)\) 的变化发生横向运动或局部回摆。普通位置和完成反馈不提供式（2）的可信来源，也不直接授权释放资源。

### 2.2 执行权限与保留空间

对当前MOVE，中心已授上界为 \(b_i\)，本地已安装上界为 \(C_i\)，可释放进度下界为 \(q_i\)。执行维持

\[
0\le q_i\le s_i(t)\le C_i\le b_i\le\ell_i,
\qquad
\dot s_i(t)=\nu_i(t)\ge0.
\tag{4}
\]

当前保留空间定义为

\[
\mathcal U_i(q_i,b_i)
=\{u_i+\sigma e_i:\sigma\in[q_i,b_i]\}\oplus F\oplus Z.
\tag{5}
\]

所有臂使用相同的固定空间裕量 \(F\oplus Z\)。式（5）的收缩来自已经证实的参考进度，误差界本身不随策略需要缩小。执行域以有限闭瓦片覆盖，\(\operatorname{Mask}(\mathcal U)\) 包含全部与集合相交的瓦片，闭边界接触亦计入。完整原边的扫掠包络必须静态合法；离散中心路径无冲突并不自动满足该条件。

### 2.3 信息和真实服务

可信POSITION服务在付费捕获时刻 \(t_j\) 冻结指定原MOVE的状态，并给出参考进度下界 \(\underline s_{i,j}\le s_i(t_j)\)。在要求精度的组准入条件中，服务还须预先具有合法精度 \(\varepsilon_i\)，满足

\[
\max(0,s_i(t_j)-\varepsilon_i)
\le \underline s_{i,j}\le s_i(t_j).
\tag{6}
\]

\(\varepsilon_i\) 是观测服务的精度，与空间误差 \(Z\) 不同。采样、传输、接收核验和释放提交为不同事件；证书迟到不等于取得了交付时刻的新状态。算法无法读取世界真值或可信控制器内部状态来绕过付费接口。

真实服务由物理实体在公共服务机会满足已分配队首任务的完整服务区域条件决定。END、READY以及逻辑动作游标消费分别描述执行和协议事实，不能代替真实任务计数。设预定完整窗口为 \(E\)，任务真实完成时刻为 \(T^{\mathrm{service}}_{i,j,\pi}\)，则

\[
Q_\pi(E)=
\sum_{i,j}\mathbf 1\{T^{\mathrm{service}}_{i,j,\pi}\le E\}.
\tag{7}
\]

具体评分还包含预设停止边界、每行服务机会及未知处理规则，见第4节。研究检验固定窗口真实服务率的变化；费用通过执行时序影响该量。

## 3 方法

### 3.1 保持原MOVE的前缀控制

中心先为目标前缀预留资源，再提交累计上界 \(b_i\) 并发送授权；本地只接纳同一动作身份且不低于已安装值的累计授权。扩展上界不会改变 \(s_i,\nu_i,z_i\)，也不撤销已经发出的权限。参考控制在LAUNCH、APPROACH、BRAKE和HOLD模式间切换，并维持

\[
\nu_i^2\le 2a_{\mathrm{hi}}(C_i-s_i).
\tag{8}
\]

其中 \(a_{\mathrm{hi}}\) 是既定控制模型的符号条件。制动按已证制动域执行；开始制动后须先到零速，扩大cap或接到RUN均不提前取消该过程。中途到达 \(C_i<\ell_i\) 只形成同MOVE的HOLD；扩展cap本身不启动运动，恢复需要另一次明确RUN。

上述条件依赖可信底层控制、固定朝向平移及规定扰动域。当前可有限表示的参考子域还沿用 \(0<\delta<a_{\mathrm{lo}}<a_{\mathrm{hi}}\) 和 \(a_{\mathrm{hi}}-a_{\mathrm{lo}}=4\delta\)，驱动在既有公共机会之间分段常值；这些是参数化模型限制，尚不构成实车控制器或数值后端的验证结果。

### 3.2 可信进度释放与安全归纳

中心在时刻 \(R\) 完成同MOVE证书核验和付费事务后更新

\[
q_i^+=
\max\!\left(q_i,\max_{j:\,t_j\le R}\underline s_{i,j}\right).
\tag{9}
\]

**命题1（延迟证据的前向覆盖）。** 若式（2）、式（4）成立，所用证书具有相同当前动作身份且满足下界条件，则自提交时刻 \(R\) 起、至该MOVE原END之前，

\[
X_i(t)\subseteq \mathcal U_i(q_i^+,b_i(t)).
\tag{10}
\]

证明：每个 \(\underline s_{i,j}\le s_i(t_j)\le s_i(R)\)，旧 \(q_i\) 亦不超过 \(s_i(R)\)。由参考进度单调及 \(s_i\le C_i\le b_i\)，后续参考中心始终处于保留线段，加入 \(F\oplus Z\) 即得实体覆盖。该更新仅从实际提交时生效，不回溯释放旧时刻的资源，也不把旧MOVE证书用于后继MOVE。

中心按唯一资源所有者维护占有，并在一次提交中应用完整几何及所有权增量。新的授权只增加已认证无冲突的资源；退休只删除同原责任且已与式（5）不相交的资源。计算、版本或容量检查未完成时，原责任保留。

原END只在可信参考 \(s_i=\ell_i,\nu_i=0\) 时发生，并永久关闭该旧动作入口。终点包络

\[
\mathcal E_i=\{v_i\}\oplus F\oplus Z
\]

始终有人承担占有责任。中心核验历史END证据后，将该包络由活动动作责任原子转为同机器人驻留责任，持久保留可重传READY；本地验证并消费相应READY之后才允许后继动作。中途HOLD、超时或消息丢失均不能模拟END或撤回已发授权。

**命题2（条件碰撞安全）。** 若初始实体及驻留责任被覆盖，静态包络合法，可信控制及唯一中心／本地守卫不分叉，授权、释放和终点交接保持上述不变量，则模型内不同机器人的实体不相交。

证明：初始驻留、先预留后授权、单调cap、命题1及无空隙终点交接逐步保持实体覆盖。若两实体相交，其交点属于某一公共闭瓦片，两者责任都需持有该资源，违反唯一所有者条件。组围栏只限制新增准入，不替代物理所有者；退栏不删除终点责任。该安全结论不蕴含一般无死锁、无饥饿或正吞吐。

### 3.3 有限组准入与累计扩展

普通初始授权先尝试完整原边。为处理某些互相阻挡的起点占用，GROUP_PREFIX对当前合法未授原请求形成的阻塞图检查循环强连通组。候选组完整扫掠区域不得存在组外占有或外国围栏，全部成员须具有有效原起点和动作绑定。

对成员 \(i\) 选严格内部cap \(c_i\)，令 \(l_i=\max(0,c_i-\varepsilon_i)\)，定义

\[
A_i=\operatorname{Mask}(\mathcal U_i(0,c_i)),
\qquad
B_i=\operatorname{Mask}(\mathcal U_i(l_i,\ell_i)).
\tag{11}
\]

仅当各 \(A_i\) 两两不交、各 \(B_i\) 两两不交且其他准入条件成立时，中心一次提交组围栏和全部初始前缀授权。候选cap来自固定几何端点及中点集合，按既定有限顺序选取；不宣称搜索全部连续可行调度。

组成员无需同步安装或启动。已授MOVE可在资源允许时继续扩展，不必等待全部成员到cap；已有证据达到充分清除条件时，剩余完整扩展均可授。此条件进展仍依赖足够执行、观测和交付机会。成员完成原动作后，组围栏只在全部成员满足规定终点接续条件时经付费事务撤除；永久失联或长期占栏造成的等待须进入结果。

### 3.4 按阻塞关系分配付费查询

中心在已交付知识上维护合格需求集 \(\mathcal P\)，包括被外国物理所有者阻挡的原MOVE请求及尚未扩至终点的前缀需求。只有忽略外国物理所有者后确可获准的需求参与评分；外国组围栏或其他准入失败不会因一次空间退休消失，故不作为这类可释放阻塞计分。

对活动动作 \(\tau\)，设其原终点资源为 \(\operatorname{Mask}(\mathcal E_\tau)\)，需求资源集为 \(\mathcal D_p\)，定义

\[
\operatorname{score}(\tau)=
\sum_{p\in\mathcal P}
\mathbf 1\!\left\{
\exists r\in\mathcal D_p:
\operatorname{owner}(r)=\tau,\quad
\tau\text{为该需求的外国活动责任},\
r\notin\operatorname{Mask}(\mathcal E_\tau)
\right\}.
\tag{12}
\]

同一需求的多个资源只计一次。D选择最大正分动作，按贡献需求最早加入时刻和规范动作key确定平分顺序；无正分时回退到稳定agent轮询。R始终使用该轮询规则，并将选定agent绑定到当前唯一活动MOVE。每次成功发布选择才更新轮询位置，后续丢包不会回滚已完成选择。D和R使用同一可信来源、费用函数和服务容量。

式（12）是指定启发式，并未证明查询收益最优。正分查询可能没有新进度，证书可能迟到，释放后也可能仍有其他阻塞。D的正分选择不自动继承纯轮询的等待次数界；一般公平性及净收益均不能由该评分公式推出。

### 3.5 区间事件几何与费用

对闭瓦片 \(T_r\)，令

\[
I_{i,r}=
\{\sigma\in[0,\ell_i]:
(u_i+\sigma e_i+F\oplus Z)\cap T_r\ne\varnothing\}.
\tag{13}
\]

在给定有限凸分解及精确几何域内，\(I_{i,r}\) 可表示为有限闭区间并 \(\bigcup_h[a_{i,rh},d_{i,rh}]\)，从而

\[
r\in\operatorname{Mask}(\mathcal U_i(q_i,b_i))
\iff
\exists h:\ a_{i,rh}\le b_i,\ d_{i,rh}\ge q_i.
\tag{14}
\]

直接几何实现N-GEOM求当前集合的精确相交；G-GEOM按入口和出口事件维护

\[
n_{i,r}(q_i,b_i)=
\#\{h:a_{i,rh}\le b_i\}
-\#\{h:d_{i,rh}<q_i\}.
\tag{15}
\]

因 \(q_i\le b_i\)，被减区间均已计入，故 \(n_{i,r}>0\) 等价于式（14）。闭边界采用严格退出条件。对成功提交且不重算的单调更新路径，每个区间入口和出口各消费至多一次；废弃、重试、预处理、所有权维护及精确数值位成本另计，不据此声称整个算法常数开销。

有限计算模型 \(P_{\mathrm{model}}\) 将应用规划、查询、几何、采样、通信及维护的实际工作交给共同服务容量。世界在相应等待期间继续推进；宿主实际CPU、经过时间和内存单独记录。可信服务仅通过付费、冻结的因果参考视图构造证书，精确求值和封装不免费；这些来源和后端的具体实现符合性仍待完成。

## 4 实验设计

本文的实验围绕PIE-D框架下存在真实跟踪误差的终身多智能体路径规划展开。我们考察：在保留原规划提议及原MOVE身份、使用共同实体控制和安全条件的情况下，将有限的观测与处理资源分配给阻塞相关的途中进度查询，能否提高固定评价窗口内的真实任务完成率。大规模lifelong仿真是主要评价方式；退休次数、局部提前放行或规划器输出只用于解释机制，主要效应由物理世界中实际完成的任务决定。以下为实验前设计，具体输入与实现资格尚待绑定，所有结果栏留空。

### 4.1 来源总体与配对设计

实验总体由固定公开来源中的city、game、random和warehouse-s四类构成。按预先规定的路径顺序枚举全部候选配置，保留每类所有静态合法来源，并记录每项静态排除的身份、失败谓词和证据。合法性依据输入与共同世界条件判定，不以规划速度、是否找到解、初始化成功或预计收益筛选。某必需类别或profile没有合法成员时，总体尚未实例化，不能用其他成员补齐或重新分权。

来源身份包含类别、地图、agent、task、有效delay、任务分配策略和实际生效的语义选项。其中，有效delay按全部预定共享profile可能使用的公共执行机会定义；原始文件别名完整保留，但不因未使用部分的文件差异重复赋权。机会支持先由共同设计及有效非delay输入确定，再确定有效delay与来源身份，最后绑定实验块和随机创新。来源清单、别名合并、适用域与全部权重均须在结果产生前固定。

将类别、地图、来源、profile及重复索引组成的评价单位记为块\(b\)。主分析采用四类别等权、类内不同地图等权、图内不同合法来源等权、来源内预注册profile等权、profile内固定重复索引等权；这些权重的乘积为\(w_b\)。主权重仅定义在事先确定的模型内总体\(\mathrm{MAIN\_IN\_MODEL}\)上。各臂在同一块共享固定设计和外生创新规则，闭环中的实际派单、查询、采样和规划调用则由各臂自身状态产生。配对设计不要求各臂拥有相同的实际轨迹或请求序列。

### 4.2 内部比较与外部系统比较

唯一确认主比较是在NATURAL计费制度下的D-S与强轮询R-S。两者复用PIE-D作者的LACAM-only提议来源，采用相同的明示修订、GROUP_PREFIX平台、实体控制、空间几何、累计权限、终点交接、可信信息能力、服务容量和费用函数。D使用阻塞导向的查询选择，R使用稳定轮询；两者均可依据已付费且可信的进度证据退休原MOVE后方的空间。该比较估计相同平台上的查询策略效应，并包含策略引起的后续规划、排队、缓存历史和成本变化，不解释为固定规划路径上的直接查询效应。

E0是同总体下必须报告的内部实用性对照。它保持共同的控制、cap、组、任务、游标与终点恢复能力，仅不主动购买途中POSITION；无用途的退休索引和工作无需执行，费用按自身实际发生量记录。所有共同适配臂享有相同的付费真实END首推、缓存重放和公共重试能力，不为某一臂额外设置周期终点轮询。因此，D-S−R-S回答定向选择是否优于同能力轮询，D-S−E0回答该制度下购买途中观测的净实用性。只有两项均支持正效应，才声称“途中观测值得购买且值得定向分配”。

H19E0和H19R-S作为两条预先指定的外部系统臂，分别与D-S比较，并共同报告。它们采用自身的规划、依赖管理和整动作放行制度，在相同世界、供给与收费条件下接受评价；具体组件来源与适配规则置于复现附录。因这些系统同时改变多个执行环节，差异只能归于整个具名系统，不能全归于D的查询选择。内部R、E0和释放消融属于本文自定义对照，不替代已发表方法；PIE-D原样与规则复现的忠实性、共同适用域另行记录。某外部系统在既定域未取得资格时，保留不可估或不可推断状态，不在结果产生后缩小地图集合，也不以另一条外部系统代替它。

### 4.3 真实服务评分、故障与成本

每个块的设计暴露\(E_b>0\)来自预先固定的完整评价窗口。窗口从共同合法待命物理世界已给定时开始，应用软件可以尚未完成初始化；初始化、输入核验、预处理与在线工作均占用同一窗口及服务容量。慢初始化不重开窗口，初始位置上的合法驻留也可能完成真实任务。因此，软件初始化完成、首次MOVE启动和任务服务分别判定，不能由某一状态直接赋予任务得分。

令\(Q_{a,b}\)为臂\(a\)在块\(b\)中、截至预设停止边界的完整可证真实任务服务数。每个agent在每个公共TASK_SERVICE行至多完成一个合法已分配队首，同一任务实例及其重传不得重复计数。真实服务发生在停止前，即使证书稍后才送达也计入；普通完成消息、READY或逻辑游标消费不决定\(Q\)。各臂报告\(Q_{a,b}/E_b\)，实际已认证物理暴露单独报告，不以它、初始化完成时间或实际耗时替换\(E_b\)。

合法超时、无解、no-start、普通反馈错误、通信故障或中心停止服务按模型保留；在途机器人仍依既有责任推进，后续合法服务仍可计数。全臂评价停止由预设cutoff、物理或信任前提破坏、不可认证推进等规定事件决定，停止后不追加服务。故障状态、评分证据及安全认证分别报告：安全状态区分已违反、仅在可证前缀验证、未知与模型外。软件初始化未完成不能直接计零；主机未发起、日志丢失或任务事务未获完整认证，也不能以失败零分或虚构停止补齐。

完整roster同时报告初始化是否完成及其时间、首次MOVE是否发生、停滞与未完成、任务评分可用性及全部故障标志。状态未知保留为未知，比例以固定roster为分母并给出相应识别界。不筛选各臂成功初始化者，也不筛选事后共同成功样本来替代主分析。合法失败保留实际可证\(Q\)和完整分母；评分未知则使所需比较不可估。源语义未定义、适配未闭合或同能力比较不成立时，即使可描述任务计数，也不发布相应因果推断。

成本按两套用途分开报告。确定性计算模型\(P_{\mathrm{model}}\)中的实际工作通过公共容量、通信和排队影响闭环及\(Q\)，NATURAL按实际完成的工作收费；STRICT对相同机会与供给预付，含未使用供给，仅用于机制分析，并另报实际工作。工作账覆盖规划、查询选择、采样及封装、几何与索引、消息与验证、授权及维护、日志、重复和失败工作，以及首次、复用和失效成本。费用已通过闭环作用于主效应，不再从\(Q\)重复减去一个任意成本奖励项。

世界准备、精确任务求值及其他宿主CPU、经过时间和内存另逐臂报告，不能回写为虚拟服务时刻，也不能替应用查询、几何、规划或授权工作免除模型内费用。观测采样至交付时滞、中心排队、全队与局部等待、通信量、内存、组占用及外部等待用于解释任务效应；若世界求值或维护本身难以扩展，同样保留该结果。主效应的范围是共同合法待命世界中的模拟闭环，不直接声称真实机器人从上电开始的部署吞吐或实机加速。

本文的在线性由信息和动作的因果顺序定义：规划与策略仅使用当前已合法获得的信息，计算、查询及消息在模型内完成后才作用于后继执行。宿主批量仿真不要求与虚拟物理时间等速，但不能暂停虚拟时间给予算法未计费的计算，也不能提前揭示未来真实任务或扰动。P_model表达明确的确定性资源成本，尚不等同于经硬件标定的实机耗时；组件的精确计算正确性与整体扩展性分别由对应验证和规模实验支撑。

### 4.4 消融与压力分析

既定内部消融由D-S、R-S、D-F_cap和R-F_cap组成，比较查询选择、实际释放制度及其交互。F_cap保留相同前缀授权能力，但在原END前不退休，实际释放下界保持为零，并可省去无用途的退休工作。G-GEOM与N-GEOM的补充比较固定同一查询策略和S制度，分别考察相同集合函数的计算组织与NATURAL下的闭环费用；集合语义相同不要求费用驱动后的轨迹相同。STRICT、释放消融及几何分析均为次要分析，不以其中显著的比较替换主量。

空间profile须包含无法通过时间重参数化消除的横向偏差，并区分准确采样与普通误报、丢包条件。模型内主总体与破坏物理或可信前提的压力总体在结果前依生成机制和支持集分开定义：模型内出现意外越界仍保留原主行及标志，不事后转移到压力层；模型外压力层使用自己的固定权重，不并入主显著性分析。参数选择依据模型和预先登记来源，不为制造收益增大误差或延长处理，也不预设效应随误差、拥塞或价格单调。局部提前退休而全窗口任务收益为零或为负，属于应保留的结果。两台LIMO仅可作为可选的现实假设补充检验，不替代大规模lifelong评价或定义density N。

### 4.5 主估计与统计推断

条件于固定合法设计\(X\)，主配对差、目标和估计量定义为

\[
d_b=\frac{Q_{D\text{-}S,b}-Q_{R\text{-}S,b}}{E_b},\qquad
\theta_{DR}=\sum_b w_b\,\mathbb{E}_{P_{\mathrm{model}}}[d_b\mid X],\qquad
\widehat\theta_{DR}=\sum_b w_b d_b.
\]

该目标是指定来源组成及执行模型下的创新期望，不代表所有未来仓库。每个完整潜在分数须满足\(Q_{a,b}=F_{a,b}(X,Z_b)\)，其中映射确定且良定义，\(Z_b\)在给定\(X\)后跨块独立；块内各臂可相关。来源私有状态仅在本臂、本块内持续，不跨块共享可变缓存、随机状态、学习或按结果调整的排程。相同地图本身不建立独立性，也不通过重采样固定地图声称跨地图泛化。宿主时钟、未定义访问或未约束的平台行为进入该映射时，原推断条件尚未成立。

令\(Q^{\max}_b\)为公共机会结构允许服务的“行、agent”对数，\(U_b=Q^{\max}_b/E_b\)，则\(0\le Q_{a,b}\le Q^{\max}_b\)且\(|d_b|\le U_b\)。按既定95%约定取\(\alpha=0.05\)，Hoeffding半径为

\[
r(\alpha)=\sqrt{\frac{\log(2/\alpha)}{2}\sum_b(2w_bU_b)^2}.
\]

主区间为\([\widehat\theta_{DR}-r(\alpha),\widehat\theta_{DR}+r(\alpha)]\)与已知目标范围\([-\sum_b w_bU_b,\sum_b w_bU_b]\)之交。全部\(U_b=0\)时返回零点退化区间。该构造不要求正态、对称或可交换分布；这里的显著性水平沿用既定推断约定，不是实验保护参数的数值绑定。

该区间的可检出性由设计本身决定，须在任何评分产生之前报告。记相对效应\(\varepsilon=|\theta_{DR}|/\sum_b w_bU_b\)。在等权且各\(U_b\)相同的设计下\(r/\sum_b w_bU_b=\sqrt{2\log(2/\alpha)/B}\)，\(B\)为块数，故点估计能超出半径的必要条件是\(B\ge\lceil 2\log(2/\alpha)/\varepsilon^2\rceil\)；若要求“真实目标不小于\(2r\)则区间下端以至少\(1-\alpha/2\)的概率大于零”这一分布无关的检出保证，所需块数为其四倍。按\(\alpha=0.05\)，\(\varepsilon=0.05\)对应2952块，其保证检出对应11805块。上述块数按等权设计给出，而§4.1的主分析采用四层嵌套等权，各块\(w_b\)一般不相等。半径只通过\(\sum_b(w_bU_b)^2\)依赖设计，故任一设计等价于块数为\((\sum_b w_bU_b)^2/\sum_b(w_bU_b)^2\)的等权设计；由柯西—施瓦茨不等式该量不超过实际块数，等号仅在各\(w_bU_b\)相等时成立。因此本节嵌套加权设计所需的名义块数严格不少于上述等权数值，实际差额须在权重固定后按该式计算并与块数一并报告，不得以等权数值代替。因块数与\(\varepsilon^{-2}\)成正比而\(\varepsilon\)以\(\sum_b w_bU_b\)为分母，\(U_b\)的收紧对所需规模是二次作用：\(U_b\)减半则所需块数降为四分之一。区间过宽属于设计规模不足，按本节末段处理，不得事后改界或调整保护参数。

\(\varepsilon\)的分母是几乎必然上界，这一点决定了规模判断的量级。\(\sum_b w_bU_b\)对应“每个agent在每一个公共服务行都完成一次服务”的极限，而实际可达服务率通常只占其一小部分，因此一个相对实际吞吐并不算小的绝对效应，其\(\varepsilon\)可能低一个量级，对应的名义块数相应高两个量级。仅靠收紧\(U_b\)不足以把该块数降到可执行范围：收紧倍数\(k\)使块数降为\(k^{-2}\)倍，而由旅行上界得到的\(k\)受窗口内可完成MOVE数的限制，机器人相对评价窗口越快则可服务次数越多、\(k\)越接近一，收紧越少。规模可行性须由三项同时提供：按上段收紧\(U_b\)；选择阻塞效应显著因而绝对效应较大的工况，并相应收窄结论适用范围；必要时采用随观测方差自适应的区间。三项的作用相乘，任一项单独通常不足。

若按预注册权重与\(U_b\)算得的名义块数不可执行，推断方法的更换须在任何评分产生之前完成并固定，判据是预注册的块数而非已观测数据。可选方案为经验Bernstein区间或配对bootstrap；二者在本节嵌套加权设计下的有效性须各自另行推导，等权设计的结论不得直接套用。方法一经固定不得因结果调整。若三项措施合计仍不足，按本节末段报告规模不足，不缩小\(\alpha\)保护、不增删重复索引、不更换地图集合，也不改用可达显著的替代终点。

\(U_b=Q^{\max}_b/E_b\)只由公共机会结构给出，因而对物理旅行可行性宽松：它允许每个agent在每一个服务行都完成一次服务。旅行上界给出另一个几乎必然界——每个原MOVE自静止到静止耗时有正的下界，故窗口\(E_b\)内每个agent完成的MOVE数与服务数均有上界，其中领先的一次服务对应§4.3所述合法初始驻留即可服务的情形。两个界都几乎必然成立，取其较小者仍是几乎必然界，故可在不削弱集中不等式前提的条件下收紧\(U_b\)。所用MOVE耗时下界须由已登记参考控制条件单独证明并在评分产生前预注册；固定73的\(T_{\mathrm{nom}}\)是上界，不得用于此处。取值大于真实最小值会使\(U_b\)偏小并使区间失效，这比区间偏宽更严重。density \(N\)及公共机会支持尚未绑定，故本节的块数、\(Q^{\max}_b\)与实际可检出效应一并报告UNINSTANTIATED，不在此填写数值。

D-S−E0按相同设计、权重和界报告点估计及同水平区间。“值得购买且值得定向分配”的合取结论要求D-S、R-S、E0所需块评分全部完整，且两个预定区间下端均大于零。其错误率按合取逻辑控制，不表示两个区间同时95%覆盖，更不允许任选一项显著即宣布成功。未支持正效应不等于效应为零或统计等效；安全反例、停滞或E0负结果不能由D-S−R-S的正估计抵销。

H19两条系统级次级比较组成单独声明的推断族。各配对差使用同一\(X,w_b,E_b,Q^{\max}_b\)，每个区间将错误概率设为\(\alpha/2\)，从而用并合界控制该两区间的同时覆盖错误概率不超过\(\alpha\)，无需两对独立。这不提供与主比较或其他机制分析合并后的同时覆盖。两条外部比较所需评分须全部齐备；缺任一必需评分时整个该族不可估，比较域不合格时不可推断，不择可发布或显著的一条宣称整体胜出。

缺少设计绑定时报告UNINSTANTIATED；必需评分未知时相关整项比较报告UNESTIMABLE；映射、来源域、权重、暴露、界或独立性条件无效时报告INFERENCE_UNAVAILABLE。不删除相应块或重新归一化权重。在全部潜在分数有定义的条件下，保留的是“发布数值区间且未覆盖”的联合错误界；若可发布性依赖结果，不能进一步声称条件于已发布仍有95%覆盖。区间过宽时不事后改界、增减重复、换地图或调整保护参数来取得显著。


## 5 讨论与适用范围

本文把安全问题和净信息价值问题分别处理。固定误差包络及可信权限提供条件安全依据；通过付费进度缩短保留区间，才可能减少他者等待。查询、维护或队列开销较大时，局部释放仍可能对应零或负的全窗口任务效应。这类结果用于界定方法适用条件，不能删去或用辅助指标替换。

结论范围是已声明的固定朝向平移、有界扰动、可信控制与信息来源，以及给定服务设施下的仿真闭环。当前模型保留真实有界跟踪误差，含无法由时间重参数化消除的横向分量，但未包含任意转向、失控侧滑和任意可信接口破坏。一般无死锁、长期公平性、硬件部署速度和跨所有仓库的收益尚无支持。大规模仿真为主要评价；有限实物演示只能补充检验具体假设。

PIE-D作者来源的共享修订及H19所用ECBS修复属于可复现比较的工程条件，不作为本文算法创新。实验前已接受的是参数化科学设计；原法完整复现、共同域、后端和实际运行符合性须用各自证据报告，不由本文叙述替代。

## 6 结果与图表计划（结果留空）

尚未运行本设计的实验。本节不提供数值、趋势图或“优于基线”的文字结论。



| 工件 | 正文用途与预定内容 | 当前结果栏 |
|---|---|---|
| 表：实验来源与臂定义 | 四类固定来源及alias口径；D-S、R-S、E0、F_cap消融和两条H19系统的比较角色、共同能力及适用状态。具体来源数与资格仅据后续冻结清单填写。 | 来源数、配置、资格证据：〔待合法绑定〕 |
| 图：固定窗口真实任务效应 | 主D-S−R-S与必报D-S−E0的块级\(Q/E\)配对差、固定权重点估计及各自区间；附固定roster中的评分可用情况。图注注明两区间不声明同时覆盖。 | 点、线、估计、区间：〔留空〕 |
| 表：外部系统比较 | D-S−H19E0与D-S−H19R-S同表展示，注明系统级解释、两比较同时推断族及资格/评分状态。 | 两项估计、同时区间：〔留空〕 |
| 图：已登记机制分析 | D/R×S/F_cap的任务效应及交互；同策略下G/N工作量与等待。可用真实付费事件联系“证据—退休—准入—服务”，不以退休量代替任务量。 | 效应、工作量、事件证据：〔留空〕 |
| 表：成本、安全与失败 | 模型内工作和宿主/world资源分栏；INIT、首次MOVE、评分可用性、安全域、停滞及全部失败/未知，保留固定分母。压力层单列。 | 全部测量与状态频数：〔留空〕 |

图表计划不新增GROUP-off整矩阵、回滚后端交叉或未绑定的敏感性扫描。已登记的机制参照若进入后续完整预注册，其具体合同另行具名，不能扩成事后择优分析。


## 7 结论（实验前版本）

本文提出在PIE-D框架下，以误差包络、原MOVE前缀授权和可信进度释放连接连续执行安全与lifelong任务评价，并将阻塞导向观测的全部费用纳入比较。已有论证支持声明模型内的条件覆盖和协议安全。任务收益及扩展性须由第4节预定比较验证；当前不填写性能结论。

## 参考文献

[1] Zhang Y, Chen Z, Harabor D, Le Bodic P, Stuckey P J. Concurrent Planning and Execution in Lifelong Multi-Agent Path Finding with Delay Probabilities. Proceedings of the AAAI Conference on Artificial Intelligence, 2025, 39(22): 23387–23394. DOI: [10.1609/aaai.v39i22.34506](https://doi.org/10.1609/aaai.v39i22.34506). [正式正文](https://ojs.aaai.org/index.php/AAAI/article/view/34506/36661).

[2] Zhang Y, Chen Z, Harabor D, Le Bodic P, Stuckey P J. Planning and Execution in Multi-Agent Path Finding: Models and Algorithms. Proceedings of the International Conference on Automated Planning and Scheduling, 2024, 34(1): 707–715. DOI: [10.1609/icaps.v34i1.31534](https://doi.org/10.1609/icaps.v34i1.31534). [作者主文](https://pathfinding.ai/pdf/zchls-icaps24-pie.pdf).

[3] Okumura K. Improving LaCAM for Scalable Eventually Optimal Multi-Agent Pathfinding. Proceedings of the Thirty-Second International Joint Conference on Artificial Intelligence, 2023: 243–251. DOI: [10.24963/ijcai.2023/28](https://doi.org/10.24963/ijcai.2023/28). [正式正文](https://www.ijcai.org/proceedings/2023/0028.pdf); [作者LaCAM2仓库](https://github.com/Kei18/lacam2).

[4] Hönig W, Kiesel S, Tinka A, Durham J W, Ayanian N. Persistent and Robust Execution of MAPF Schedules in Warehouses. IEEE Robotics and Automation Letters, 2019. DOI: [10.1109/LRA.2019.2894217](https://doi.org/10.1109/LRA.2019.2894217). [作者接受稿](https://whoenig.github.io/publications/2019_RA-L_Hoenig.pdf).

[5] Andreychuk A, Yakovlev K, Surynek P, Atzmon D, Stern R. Multi-agent pathfinding with continuous time. Artificial Intelligence, 2022, 305: 103662. DOI: [10.1016/j.artint.2022.103662](https://doi.org/10.1016/j.artint.2022.103662).

[6] Kasaura K, Nishimura M, Yonetani R. Prioritized Safe Interval Path Planning for Multi-Agent Pathfinding With Continuous Time on 2D Roadmaps. IEEE Robotics and Automation Letters, 2022, 7(4): 10494–10501. DOI: [10.1109/LRA.2022.3187265](https://doi.org/10.1109/LRA.2022.3187265).

[7] Berndt A, van Duijkeren N, Palmieri L, Kleiner A, Keviczky T. Receding Horizon Re-Ordering of Multi-Agent Execution Schedules. IEEE Transactions on Robotics, 2024, 40: 1356–1372. DOI: [10.1109/TRO.2023.3344051](https://doi.org/10.1109/TRO.2023.3344051). [作者预印本](https://arxiv.org/abs/2312.04190).

## 编写及复现说明（不纳入投稿正文）

本稿由根完成问题、方法、安全归纳及整合；paper_experiment_section完成固定73实验合同的论文转写，paper_anchor_references完成以上四篇的正式题录和相关正文定点核验。两个任务均已完整终态。本次不冒称重新精读全部论文或完成近邻排除，未新调用Claude。固定73的§1、§3–7、§11–12为正文主要依据；数学证明沿用其条件，不新增审查门。

**2026-09-17修订及缘由。** 本次共五项改动，均为可加不可减的澄清与补齐，不改变科学设计。（一）§1.1新增连续时间占用的近邻排除，缘由是占用的亚动作粒度表达已见于[5]和[6]，原稿未排除该近邻，可检验新颖性因此收窄为“执行期由付费可信进度下界驱动的在线释放”。（二）§1.1新增动作依赖图谱系的说明，缘由是本文外部对照[4]在同一谱系已有2024年后继[7]，须说明为何不作为直接可比对照，并同时保留“仅重排次序能否取得同等任务效应未检验”这一局限。（三）§4.5新增两段可检出性条件，缘由是原稿固定了推断程序却未给出规模条件，无法判断设计能分辨多大效应；新增内容是既有Hoeffding构造的算术推论，未改\(\alpha\)、\(w_b\)、\(E_b\)、目标或界的定义，也未更换推断方法。（四）§4.5说明\(U_b\)可由旅行上界与机会上界取小而收紧，缘由是原\(U_b\)只用机会结构，对物理旅行可行性宽松，而所需块数与\(U_b^2\)成正比；同时明确所用MOVE耗时下界须单独证明并预注册，取值偏大会使区间失效。（五）摘要、§1正文与§5共三处仅以“横向”限定跟踪误差的表述（原作“横向跟踪误差”“真实横向偏差”“真实横向误差”）改为“有界跟踪误差”，缘由是式（2）为逐分量有界盒而非仅横向分量；§2.1末与§4.4的“横向”分别描述实体真实横向运动及不可由时间重参数化消除的偏差，属正确用法，保留不改。

上述修订的核验状态：[5][6][7]三条题录于2026-09-17经Crossref单DOI查询核验，字段取权威记录；[7]另核其作者预印本arXiv:2312.04190v1（17页，%PDF-签名与页数已核）的假设条目与符号表原文，§1.1相关表述以该预印本为依据，IEEE正式版的节号与页码尚未逐项对应。本次未运行算法、仿真或研究测试，未新增任何数值结果，第6节结果栏保持全空。主端点、主总体范围、标题与摘要落点、外部对照集合均未改动：这些取决于\(U_b\)的实际收紧倍数，而density \(N\)与MOVE耗时下界尚未绑定，在未知该倍数前不作不可逆的主张缩减。

同日复审另作两处补正。其一，§4.5原只给出等权设计的块数，未说明§4.1的嵌套加权会降低有效样本量、从而抬高名义块数需求，属于朝有利方向低估自身规模要求；现按\((\sum_b w_bU_b)^2/\sum_b(w_bU_b)^2\)与柯西—施瓦茨关系补齐，并明确不得以等权数值代替。其二，§1.1对[7]原用“正文未表达有界跟踪误差”这一全称否定，现改为与其假设条目绑定的表述，并在正文标明依据为作者预印本、正式版节号尚未逐项对应。2952与11805两个数值是既定Hoeffding半径式在\(\alpha=0.05\)、\(\varepsilon=0.05\)下的算术结果，可按本节公式独立复算；生成该数值的实验前工具已于2026-09-18纳入版本控制（见`pre_experiment_tools/`），但该工具本身尚未取得独立验证回执，正文以公式而非脚本为依据。

**2026-09-18补正。** §4.5再增两段，缘由是前一日的修订会造成“收紧\(U_b\)即可解决规模问题”的错误印象。实际按MOVE耗时下界计算的收紧倍数受窗口内可完成MOVE数限制，机器人相对窗口越快则收紧越少；而\(\varepsilon\)的分母是几乎必然上界，实际可达服务率只占其一小部分，故真实绝对效应对应的\(\varepsilon\)比\(0.05\)低约一个量级、名义块数高约两个量级。因此新增内容明确规模可行性须由收紧\(U_b\)、选择绝对效应较大的工况、必要时改用方差自适应区间三项相乘提供，并规定推断方法的更换必须在评分产生前依预注册块数完成固定、不得因结果调整。上述量级判断由实验前工具按示例参数计算得到，\(a_{\mathrm{launch}}\)、\(a_{\mathrm{hi}}\)、\(\delta\)与density \(N\)均仍为UNASSIGNED，故不写入任何具体倍数或块数。同日另将该MOVE耗时下界的推导实现为实验前工具并纳入版本控制：其表达式对各加速度参数单调递减，故须以各参数的上界与边长的下界代入，取加速度的保守下界会高估MOVE耗时、使\(U_b\)偏小并使区间失效。

固定R0来源为YueZhang-studyuse/LMAPF-delay，提交0b5b33649fc367abcb1cd0a7ad5ac03511bf9357。当前复用的是LACAM-only提议和承诺／未来切分，经明示共同修订；完整PIE-D候选另固定于improve_morereveal的74cfba3c81a0c165c2e7044dea6fd4dee8ddf415。复现材料应逐项区分原字节、论文规则和共同适配。

H19所用ECBS组件固定于whoenig/libMultiRobotPlanning的4c75fa20c435c440d8b6bd6dc81668ddc7296ba0。已完成的两处源码修复和静态模板编译见[本地组件说明](implementation/h19_ecbs/README.md)及[Q1 §4](73Q1_STATIC_CANDIDATE_BINDINGS_20260914.md)。这不等于H19整套系统已经复现或运行验证通过。ECBS保持外部对照角色，后续优先完成PIE-D误差执行主线。

待补内容沿用原任务：完整来源／别名／roster、有效delay及公共机会支持、WORLD与服务区域、模型profile及费用精度、各臂来源和后端符合性、外部共同域、固定预注册及真实运行证据。新增输入正文读取仍受此前有限授权约束，本稿未扩大读取，也未运行算法、仿真或研究测试。

\(H,T_{\mathrm{delay}},B_{\mathrm{CAL}},B_{\max},B^*,P_{\mathrm{active}}\)及density \(N\)保持UNASSIGNED，未新增数值、范围、默认值或实验配置。第4节的统计水平继承固定73；所有任务效应、样本量和测量数据均待合法完成后填写。当前稿是实际正文初稿，不是完整实验前目标完成的凭据。
