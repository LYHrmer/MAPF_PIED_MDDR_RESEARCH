# 73P13 可信参考进度精度、有限表示与付费观测构造

状态：NO-SKILL / CONSTRUCTIVE / NONBALLOT；2026-09-10。不是正式盲逻辑审查或资格票。HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT；六门仍为 2 PASS、3 UNKNOWN、1 FAIL。本件不改 73，不实现、构建、测试、仿真、调参或生成实验载荷/结果；所有保护参数保留原义且未赋数值、默认值或新范围。

## 1. 唯一建议与判定

建议只推进一条明确路线：**私有、因果的参考驱动历史 + 付费区间重算 + 精确有限下界证书**。它提供一个可核的局部数值构造，不把中央策略接到全知评价器，不把实体 pose、未来扰动或普通 COMPLETE 变为进度证据。当前原模型尚不能直接据此取得全局精度资格：实际已发生参考驱动的可信来源/有限表示、混合守卫同刻判序，以及实际付费后端仍未闭合。

本件闭合的是两项条件数学事实：有合格包含区间即可严格导出既定精度的 q；固定 cap、常值参考扰动且模式分段已正确确定时，APPROACH 的进度可用显式正积分作有限区间逼近。没有闭合“所有原有界可测扰动、全部合法命令历史、所有服务请求都可出合格证书”。不得以任意失败即不出证书的包装，把安全性改称可用的精度能力。

现在应在 73 保留 NO_CERTIFIED_GROUP 和资格 HOLD，同时记录这个具体数值接缝及其最小阻断。无需继续调查通用虚拟机、R0 或新增任意数值算法名单；若根采纳后续表示域，必须先明确它缩小了原允许的参考驱动域，不可冒称原模型已经由普通高精度浮点实现。

## 2. 输入身份、实际阅读与外部依据

起始 73 文件为 73_PIE_SPATIAL_EVIDENCE_TERMINAL_HANDOFF_AND_PAID_SERVICE_PREEXPERIMENT_DRAFT_20260908.md，完整字节 SHA256 `656cfdc496154a0d1a640f7ac7364a7fa854a0a5be120be33c29204dbe8151e2`，178035 bytes / 575 LF。本代理先精确确认 P13 和新 C14 路径不存在；科学阅读 §3 L44–63、§6 L142–155、§15 L553–575，以及 §5 L98–104 与 §9 L204–264 必要窗口。截断的检索输出不作全文阅读证据，所需窗口另行实际完整读取。本件没有声称读完 73 全部科学正文。

根后继修改来源接口后，本代理曾见新文件 `ab0283a5ef6fb9b3a408f0d453fb763340f9241b01a63928851ceb5ddef48939`，181390 bytes / 583 LF；不把它当作本题已审的新全文。随后以只读 Git show 取得科学提交 `5d9b37cd8411222a610e968228e5f605819d424a` 的原文件，独立核其完整 bytes/hash/LF 正好为上述 656c… 身份，并逐段比较 §3/6/15，三段均与当时新文件完全一致；原 L220–264 每个非空行也仍存在。没有 Git 写操作。下面行号均绑定起始 656c… 版本。

本次唯一新增外部依据为 [FLINT 官方 arb 实数球算术文档](https://flintlib.org/doc/arb.html)，读取时页题为 FLINT 3.7.0-dev。实际读取页面 L0–6、L94–100、L357–363、L524–543、L726–744、L794–800：球算术包含性、输入 double 的语义、外包端点导出、区间比较、平方根与幂。页面共 1535 个解析行，本件不称全页精读。没有取得该页面原始字节 SHA、固定源码提交、正式发布版、本机安装/ABI 或运行证据；它是可变开发文档，不能当作实际绑定版本。

该官方接口可支持基础包围算术：arb 运算包围输入区间上相应精确结果，arb_get_interval_arf 导出向外端点；arb_set_d 仅接收那个已表示的 double 值，不补上游模型误差；区间比较未证真时不能据此断言反面。增加运算位数不消除输入不确定性，NaN/无穷宽结果也不是精度证书。此依据不提供本项目混合控制求解器；后文变换、误差证明与失败规则是本代理独立推导。

## 3. 先区分信息缺口与数值误差

原参考方程为 s_dot=v、v_dot=a_cmd+eta，且 |eta|≤delta；实体为 X=u+s e+z+F，z 的跟踪扰动 w 与参考加速度扰动 eta 不同。POSITION 要证明采样时 max(0,s−epsilon_i)≤q≤s。此题不改变 z、w 的物理含义，不用随机等待冒充二者，也不把普通 pose 映射成可信 s。

设同一合法采样信息下，参考进度的可能集合为 S_tau。一个仅由该信息生成、对所有允许世界都合格的共同 q，必须同时满足 q≤inf S_tau 与 q≥sup S_tau−epsilon_i。因此必要条件是 diam(S_tau)≤epsilon_i；已知全部集合且能准确求下确界时，这也是数学上的充分条件。数值精度增加只能改善集合的计算包围，不能消除集合本身的直径。

具体的原模型内信息反例取同初态、同 cap、同命令且两条轨迹都尚在 LAUNCH 的一段。令两条参考扰动分别取原允许界的相反端，命令 a_launch+delta 相同；持续符号时间 tau 后，两条进度之差为 delta·tau²。若这个差大于已登记 epsilon_i，仅知道命令与扰动界的服务就不可能同时给两条世界满足精度的共同下界。这里没有给 tau、epsilon 或任何保护参数数值；反例条件必须与“二者仍在 LAUNCH”同时成立，不能据此断言任意精度 profile 都失败。过粗精度可能由平凡下界满足，却未必能支持 §15 所需的有限资源清空。

故一个真正可计算的路线须说明 authority 有什么额外可信信息。把“区间必须包含真值并足够窄”重新写成接口前置条件，不解决该来源问题；直接把评价器内部精确 s 免费读入算法也不解决费用与信息隔离。

## 4. 选定的有限来源模型及其身份代价

所选后续候选是纯仿真内部的可信参考子系统，私有保留原 tid 锚点、实际已安装 cap、已执行控制/模式更新、实际已发生参考驱动以及对应因果版本的不可变表达式历史。它与用于驱动物理参考的世界对象必须是同一条已认证因果来源；不是从独立 pose 估计 s，也不是事后挑一个与观测相容的 eta。模式记录本身不能是假定正确的神谕；第 7 节给出它尚缺的判序证明。

为使该路线有明确有限输入，可考虑 eta 在既有公共 O 相邻不同时间之间为分段常值，系数有有限精确表示；相同 t 的多个行不制造正时长段。原模型参数、时间、cap 和边长可用有限有理/代数表达式及已证定义域表示；输出区间另用 dyadic 端点。这里没有选任何系数、分布、参数值、分段数量或新采样窗口。此表示域是原“有界可测 eta”的一个明确子类，**不是所有原允许世界的等价实现**；公共 O 的取值与保护状态未改，但这种驱动表示及其来源资格仍须根决定和事前固定。

即便世界底座为了推进已生成后续常值段，authority 的可遍历根也只能包含采样点之前真正已执行的部分；不能包含未发生尾部、未来段、主 seed 或后续创新流。段的剩余未来不可借“同一日志对象”传入观察计算。输出给算法的仅是规定的证书及标识，原历史、eta、w、z、物理 pose 与世界随机状态始终不对策略开放。知道公开的表示模型不等于读取一次私有未来实现。

该选择对 authority 内部是明确新增的可信来源能力：它能付费访问已发生的实际 eta 历史，而原先只有命令及扰动界并不提供这一信息。策略外部仍只收到 q/证书，不使新增来源能力自动等同于已有原生传感器；它可能改变可达到的精度及工作量，必须对各可购买观察的臂同样注册和计费。没有宣称真实机器人可观测实际 eta。参考 HOLD 对扰动的理想抑制仍按原控制公理，不能借该日志修改 HOLD 动力学。实际 world/controller 来源一致、参数表达式解码与日志因果截断尚无实现证据，本件保留未知。

## 5. 有限下界证书及精度证明

合法付费 CAPTURE 固定 sample_point=tau 和同 tid 版本根后，数值作业必须构造真实 s(tau) 的包含区间 [L,U]。L、U 为有限精确 dyadic 数，以整数及二进制指数表示；完整 wire 编码保留数值和方向，不再转成丢失半径的裸 double 或无误差十进制。证书通过条件是实际外向端点满足 U−L≤既定 epsilon_i，而不是内部名义运算 precision 达到某个位数。

取 q=max(0,L)。由 L≤s 且 s≥0，立即有 q≤s。若 L≥0，则 s−q≤U−L；若 L<0，则 q=0 且 s≤U<U−L。因此 s−q≤epsilon_i，给出原式 max(0,s−epsilon_i)≤q≤s。q 无需另行舍入；q≤ell 随 s≤ell 得到。若要传整个区间，可按原合同保守裁剪，但不能通过裁剪掩盖不包含真值的上游结果。

epsilon_i 必须来自观察 profile，先于几何组选择，不能看到 c_i 后调精度以制造组通过。若 epsilon_i 自身不是精确有理数，比较须使用其可认证表示，不能把输入近似当真值；以已证下界确认宽度合格是保守检查，相关计算也收费。任意逼近能保证有限终止的情形需要该 profile 允许正的精度余量并能认证它；本件不给 epsilon 赋值或设新范围。若某 profile 要求数学上完全精确，本构造不承诺一般有限终止，不能偷设 epsilon=0 以回避误差合同。

证书精度只属于采样时。原同 tid 的 s 单调，故延迟交付的旧 q 仍是之后 s 的安全下界，多个已验证旧 q 取 max 也安全；它们不自动满足交付当下 s−q≤epsilon_i。旧 tid 下界不能转给新 MOVE，数值证书也不是 CURSOR、CAP_EXTEND 已安装、END 或中心 FINISHED 的证据。

## 6. 原 APPROACH 方程的可核局部计算

先限制到固定 C、常值 eta、不跨外部命令/模式更新的一段，且 xi=C−s>0。记 D_a=a_hi−a_lo，k=2delta/D_a，beta=delta(a_hi+a_lo)/D_a+eta，r_star=beta/k。由原已有符号条件，k>0，且 r_star=(a_hi+a_lo)/2+eta D_a/(2delta) 属于 [a_lo,a_hi]。这些均是原符号关系推导，不是实验参数赋值。

令 y=sqrt(C−s)，原 r=v²/(2xi)。由原方程逐步得到 y_dot=−sqrt(r/2)，r_dot=(sqrt(2r)/y)(beta−k r)，进而 dr/dy=2k(r−r_star)/y。因此，在初值 y0>0、r0∈[a_lo,a_hi] 下，

**r(y)=r_star+(r0−r_star)(y/y0)^(2k)，Phi(y)=∫[y,y0] sqrt(2/r(z)) dz。**

当 y 从 y0 走向 0，权重 (y/y0)^(2k) 处于 [0,1]，故 r(y) 仍在原带内；r(y) 单调或常值，积分核 g(z)=sqrt(2/r(z)) 也连续、正且单调，满足 sqrt(2/a_hi)≤g≤sqrt(2/a_lo)。y=0 处直接用连续极限 r_star，不需要计算 log(0) 或原方程中的 0/0。

这给出一个有限包围构造：在规范化积分变量 z/y0 的有限二进制分割上，用区间算术包围各端点 g，再取端点最小下界/最大上界形成上下 Riemann 和；从规范化积分换回 Phi 时，和及误差都须向外乘 y0。无须先有限判定 r0 与 r_star 恰好相等与否：单调性已经证明，端点区间的 min/max 可保守包围任一方向。规范化积分的纯分割误差不超过规范化最大子区间长度乘端点变化量；Phi 对应的误差还含上述 y0 因子。有限端点运算和 y0 包围误差另行向外累计，不能遗漏。原带界给出有限变化上界，细化分割和端点计算即可把这些误差缩到任意事前可认证的正目标内。

Phi 严格单调，其斜率绝对值有上述正下界，所以积分包围可以反演为 y 的包围。比较某试探点的 Phi(y) 与 elapsed 恰好相等时，不必强行判等：用积分残差包围及正斜率界包围 y 即可。再向外计算 s=C−y²，得到所需的有限 [L,U]。若输入端点由既往已认证计算给出，也必须同步收窄输入包围；只增运算 precision 而保留粗输入不保证终止。

T_cap=Phi(0) 有限。在仅有同一 cap 的 APPROACH 到 HOLD、没有外部更新的局部段，时间超过 T_cap 后的 s 延拓为 C，且到达处连续；用这个截断延拓可逼近给定时刻的 s，不要求先对 elapsed=T_cap 作精确判等。这解释了“数值进度可近似”和“离散事件顺序可判定”是两种不同义务。

LAUNCH 在常值 eta 段为多项式运动；原首次 r=a_lo 守卫可由其首次根包围，启动后的交叉方向由正加速度及原守卫关系确定。BRAKE 常值加速度严格负，首次零速后是 HOLD；HOLD 的参考进度为常量。这些局部计算在有限个**已经正确确定顺序**的段上可组合。它们不证明任意有界可测 eta 可由有限数据读取，也不证明所有跨段的同刻 guard 均可判定。

FLINT 所示球/幂/平方根/端点接口只是上述算术所需的一种官方 API 依据。多精度分配、整数长度、端点编码、分割、求和、输入细化、逆包围、证书检查与失败路径均不是常数时间神谕。没有运行该构造或取得它的实际运算成本，也没有取得 FLINT 固定版本绑定，故不能把本节数学可计算性写成后端已合格。

## 7. 最强剩余阻断：同刻混合守卫不能由窄区间替代

原 73 §3 明确规定：到旧 cap 的物理 HOLD 先于同刻 cap 安装；HOLD 上安装新 cap 后仍需新的 RUN。而若安装发生在旧 cap 到达之前、当时仍为 LAUNCH/APPROACH，则按新 cap 重算模式并可继续。因此精确判序会改变之后是否继续运动，不仅改变一小点位置舍入。

APPROACH 的到达时刻由上述积分给出，一般是可计算实数。任意逼近并不自动提供“它与给定公共时间恰好相等”的有限判定。对本模型这一受限函数族，本件没有证明所有相等判定都不可判定；准确结论是：所获区间 API 和局部收敛推导没有提供总的判序过程或可验证事件证书。不能据此扩大成普遍不可能定理，也不能把缺证改称已经可实现。

实际阻断形状是旧 cap 的到达 T_cap 与既有 cap 安装时间相同。原语义要求先到达/HOLD 后安装、等待 RUN；如果有限数值估计把它当作“尚未到达”并在 APPROACH 中先扩 cap，后继会继续走。另一个任意接近而确实早于到达的合法安装，则确应继续。单个浮点 equality、固定 tolerance 或把重叠区间任意选边，不能同时实现这两种原语义；后继差异可继续累积，不能由采样时一个很窄的 s 包围兜住。这里未给任何时间窗口、参数值或轨迹载荷。

在第 6 节纯局部 APPROACH→HOLD 中，连续延拓避免了为计算 s 而判等；加入这类外部安装后，模式历史本身不再由连续近似唯一决定。保存日志中的模式标签，只把这个问题上移到日志生产者。不能假定“世界引擎已正确处理”就当作 P13 的具体构造，不能用事件概率为零排除原允许的同刻情形。

最小未闭接口是：针对所选有限表示域，给出原优先规则下可信、有限可验证的实际事件序列/判序来源，包含 guard 与命令同刻情况，并明确其控制费用。若不给这个接口，就需根另行授权修改控制语义；本件不选择或偷偷实施那种修改。仅把未决判序变成每次观测失败，不能让用于驱动物理世界的错误模式继续执行并仍称原模型合格。

## 8. CAPTURE、计算、发布与总失败规则

一个新鲜 POSITION 作业先按原 §6 在 Srv_E(i) 支付完整 CAPTURE，匹配当前 tid 或保留的同 tid 终点记录；不匹配仅为 NO_MATCH。CAPTURE 固定采样点与不可变历史根，不在采样操作中免费遍历整段历史或求高精度解。保留根的元数据、位长与引用操作按原服务容量收费，后续遍历、历史复制、驱动/命令解码、数值证明、端点封装另在同一作业的既有 PROCESS_SLICE 中计算。

物理推进不因该作业占槽而暂停。所有重算只读捕获时前缀；AUTH_COMPLETE 不刷新到最新 s 或最新 cap。若现有区间还不够窄，继续占原单作业槽等待后续既有资源；没有额外采样、隐式重启、免费子作业、新 O 行或新增容量。因占槽拖延其它本地工作是实际代价，不能预先假设没有影响。证书准备完毕仍须按原严格后继行及完整发布容量提交，之后原网络、接收、验证与中心 q_release 提交逐项收费。

选定规则只把“已证包含且宽度合格”的结果标为精度合格 POSITION。尚未满足宽度不发布一个带合格标签的粗区间，也不把 q=0 当成该 profile 已满足。到 Stop 仍未完成、合法资源耗尽或观测模块终止时，记原作业已发生全部成本及不出证书的终态；不能退款、重采样补救或事后裁掉计算。若传输丢失，重传保留原 sample_point 和证书身份，按原通道收费；重新获取必须是原协议允许的新付费 CAPTURE。

必须分开两类失败。第一类是**物理参考和守卫历史已被认证，仅观察数值服务未完成**：它可成为真实的 NO_CERTIFIED_POSITION/未完成观测，策略没有新 q，固定 roster 和其真实结果保留；§15 的条件前提因此不成立。第二类是**物理参考或混合事件顺序本身尚不能认证**：这属于模型/后端资格未闭，不能把它洗成普通传感器未响应或算法零分。若无法证明评分世界仍符合原物理规则，就不能生成或使用本来不存在的可信 Q，必须保持相应 INFERENCE_UNAVAILABLE。

出现错误包含性、错误方向舍入、未认证输入、NaN/溢出或无法确定的 guard，不得输出正证书。局部观测失败与来源/物理认证失败由公开固定的失败分类区别，不能观察到臂的效果之后再挑分类。当前仍是实验前设计，以上是纸面总规则，没有声称这些失败已经发生在一次研究运行中。

## 9. 同能力与公平费用

D、R 和允许购买 POSITION 的对照共用相同来源 profile、精度、CAPTURE 规则、数值服务、失败分类和原服务容量；每臂只支付自己实际触发的工作。E0 不购买 POSITION，不应被强加查询专用高精度重算费；它仍承担其真实控制/物理公共底座费用。F_cap 即使购买，也只能按原规则更新 q_evidence，不能借本数值层获得中间退休权限。该构造没有给 D 新规划、执行或随机状态访问能力。

世界推进本来需要的参考控制表达式、已应用命令与 guard 处理，属于明列的共同物理/控制服务；观测专用旧版本保留、索引、重算、证书与传输属于观测触发的实际服务。不能为了查询而无条件替全部臂预先算好高精度 s，再把它记作“免费模拟器底座”。若历史存储由常驻控制能力确实产生，其实际维护费仍要报告；若因查询才需多保留版本，新增保留与最终回收费随查询计。

共享已付中间结果必须有匹配 tid、sample_point、版本与数值前提的证据，不能跨臂或跨世界共享私有进度，也不能重复收取同一实际运算却不记录真实工作。若世界控制认证与观测证书部分复用，按实际执行一次计费并明确承担者；这不授权一次查询免费获得将来生成的控制知识。物理/环境费用和应用费用都保留明列，不能因分账而漏算总资源。

本件没有给运算配额、内存上限、观察频率或保护参数赋值；原有限服务与发布限制继续生效。实际严格计量后端仍未知，与“数学上有限的区间计算”是独立义务。这里不再调查 QEMU 或替 SOURCE_REQUEST 改服务模型。

## 10. 对 §15 条件进展的精确影响

若合法组成员实际到达 c_i 之后，确实有一个匹配的后 cap CAPTURE，且本构造给出合格证书，则 q_sample≥s(tau)−epsilon_i≥c_i−epsilon_i；裁剪到原非负下界即能支持所登记 B_i 的退休证明。数值表示不改变组的几何条件，也不替代中心实际付费退休。该推导使用既有 epsilon，不把几何阈值反过来选精度。

旧样本延迟到达仍不能伪装成后 cap 采样；物理 END 也不能替代完整中心 FINISHED/端点转换。有限 O 不保证总有后 cap 查询、足够数值计算、发布或交付，更不保证 D 比 R 获得更早证据。E0/F_cap 仍缺中间退休能力，不能因此推定它们一定败或 D 一定胜。

若原 profile 精度或来源尚未资格化，仍为 NO_CERTIFIED_GROUP。本件不把“所有请求永远未完成”称为一套已关闭的精度能力，也不强加额外机会来使条件进展看似成立。后续必须同时展示一个真实可服务域与该域内正确证书的可达路径；本件只提供局部数值路径的数学部分，没有产生资格票或研究结果。

## 11. 闭合范围、停止调查点与改稿用途

| 义务 | 本件证据与结论 |
|---|---|
| 同信息下 q 的信息极限 | 已给 diam(S_tau)≤epsilon_i 的必要性及条件 LAUNCH 反例；提高位数不能补缺失驱动。 |
| 有限 wire 下界与误差 | 已证包含区间加实际宽度条件可导出 q；未增加隐式量化误差。 |
| 固定 cap / 常 eta 的局部 APPROACH | 已从原方程导出显式 r(y)、正积分、上下和及逆包围；数学构造有条件成立。 |
| 原所有有界可测驱动 | 未闭；有限分段表示是模型子类选择，实际来源、分布与输入资格未固定。 |
| 全混合命令/guard 顺序 | 未闭；现 API 不给总判等和同刻优先序；连续 s 逼近不能代证模式。 |
| 原付费协议接入 | 已给 CAPTURE 冻结、占槽、严格后继发布与失败总规则；实际计量实现资格未闭。 |
| §15 系统进展与效果 | 仅保留原有条件命题；未证明有限 O 下完成或 D 对 R 的效果。 |

根可直接将前三项作为建设性数学附件的结果，并把后三层来源/判序/后端义务保持显式 UNKNOWN；不应仅用一句“可信传感器提供 epsilon 精度”覆盖它们。当前最小下一决策是是否接受第 4 节的有限表示子域，并要求其事件顺序来源；在此决策和明确接口前停止扩大数值库/仿真器检索。没有必要为了当前不能闭的全混合域另造一套执行器。

## 12. C14 实际终态与独立性

本题使用一次全新 C14 Opus/high 窄建设调用，唯一提示先落盘；实际 exec 43881 经原句柄续查，最终 chunk 4c2ef1 / exit 1。完整 JSON 是 is_error=true、stop_reason=refusal、terminal_reason=api_error，实际模型 claude-opus-5；提供方提示被安全机制标记，无科学正文、无 END-C14。subtype=success 不能抵消实际失败。本题不重试、不换模型、不修改提示绕过拒绝；未重做已失败的 C12 或已完成的旧题。

提示 SHA256 `036354c1c963f8ebb6fd7377e25d33673dc469da0fb3dc6128e82541c00eef4a`，9184 bytes / 31 LF；原 JSON SHA256 `d22bf9d0fe5ff074dd206d2f6ba4f9cd6886ee7b4e0be7ae8ee96d0c7335a9b6`，1525 bytes / 1 LF。完整错误原文、实际模型用量、CLI 会话、耗时与终态见 73C14_CERTIFIED_REFERENCE_PROGRESS_COMPLETE_RECEIPT_20260910.md；不把计费 thinking token 当作已收到意见。

本报告数学、信息反例、付费规则及未闭判断均由本代理不用任何 skill 独立分析，没有 C14 科学背书。根仍须独立全文读取、核身份及判断是否纳入 73；本报告完成只完成这一有界建设题，不等于实验前研究闭合。旧文件保持字节，不修改行政页、Git 或桌面；没有读取禁限载荷或 35–38，没有保护参数赋值、实验实现或结果。
