# 73E25：源执行模型取舍与计时、读域、发布接口原证据

2026-09-11；/root/successor_source_execution。NO-SKILL / CONSTRUCTIVE / NONBALLOT。唯一新件；不改73、不运行源或后端。保持HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT。

## 1. 实质取舍

**不建议把P29原文直接纳入73；优先继续建设“源完整体非抢占实测elapsed＋全站明确离散成本语义＋独立宿主实耗账”的一个组合候选。** 这是待根取舍的下一合同，不是已采用模型，也不是已有后端资格。原严格机器指令片需要E17已指出的定制执行控制；本轮没有发现现成闭合接口，不继续泛查QEMU/Unicorn。

P29解决了源体抢占和硬取消冲突的一部分，却把处置验证移到原“完整发布检查容量”。若该容量仍指真实宿主指令/CPU硬限，普通stopwatch不能在越额之前保证停止；发布、处置、ACTUATOR和其它站继续有同一问题。因此它目前是**转移了执行边界，未关闭边界**。反过来，纯仿真可以事前定义有限primitive及成本，在任何应用读写发生前判足额才执行；这个数学语义不要求宿主每条机器指令都可抢占。两种费用语义必须明确选择，不能一边按抽象成本放行，一边声称是真实CPU配额。

上述组合的必要变化是：所有非源应用站、发布器及相应控制检查采用同一明确的离散成本层；源体保留实测elapsed决定逻辑占用。它不是只给Srv_P省开销。完整primitive清单、表示、暂停边界和成本函数目前尚缺，故不能立即替换主稿。原参数意义不变且未赋值，不能把原保护预算偷偷改成新单位。宿主elapsed引入的统计依赖仍交根另闭。

## 2. 实际读取与身份

完整读0911交接、P29、C22完整receipt、E17和C8SR；读73§8/9及§10源约束窗口，合并输出截断处另补读。主稿SHA256为9e06199cc6045ddd750b861b3091ec239c7c6ff2ae7eeaebbdd0e7fde61ec113。P29为94b881eac78b738b6f61b8171bc1f05724fa2c3ed4d02d3b8f1ef4f4cae8323e；C22 receipt为bc0c8e5cd4f4362571fed53219926eeba69a26f489a370e32a3db0e145dcaf21；E17为799ee4793c7aa20f74fed57878d1902b25f88b35f1bda5f05bd747275081c84d；C8SR为e2cd4cb2d0ccf9ff79533cc20bb72ae64a9240eed8de4bb43624472827f07873。本文不冒称亲自恢复旧Claude调用或独立核Git远端；未启动任何Claude咨询，不重投旧题。研究目录无AGENTS.md或.codegraph；适用当前注入指令。

本轮亲核本机包元数据：Linux 6.8.0-138-generic；libstdc++-11-dev:amd64 11.4.0-1ubuntu1~22.04.3；libc6-dev:amd64及已列libc6为2.35-0ubuntu3.15。这只给参考接口身份，不将本机直接登记为未来实验平台。读完整两种clock声明、alias及duration定义窗口和bits/time.h全文：

| 本地参考对象 | SHA256 | bytes |
|---|---|---:|
| /usr/include/c++/11/chrono | 2e37fa31f1c1f81f747782bf2d11bd04e256f7418a9ef8c29cd991d296bdc430 | 97212 |
| /usr/include/x86_64-linux-gnu/bits/time.h | e92f48b8008563fdc2c872e16754099160d83d81fc9adfe6414e06ffbf8288cc | 3296 |

chrono L1125–1187中system_clock/steady_clock均用纳秒duration，high_resolution_clock明确alias system_clock；不能因名称就推其单调。bits/time.h的CLOCKS_PER_SEC为1000000；这是既有ABI单位常数，不是实验参数。未核未来实际include、宏展开、链接库或生成任何构建。

## 3. 最小计时与完整服务体合同

建议只选一个源elapsed接口：CLOCK_BOOTTIME；它沿单调钟并包含宿主挂起。CPU另用CLOCK_PROCESS_CPUTIME_ID，覆盖进程全部线程；不把调度等待算CPU。原时间差保存seconds/nanoseconds的整数记录，实际分辨率和失败由clock_getres/返回状态界定，非由单位名称保证。文档明确相邻单调读数可相等，所以d=0合法且不加epsilon。[Linux clock_gettime](https://man7.org/linux/man-pages/man2/clock_gettime.2.html)

实际边界必须完整列出：开始点紧前于尚未收费的输入安装；结束点在核心、PRE_COMMIT_SHAPE、受保护commit、不可变输出/结束节点封存和只依本次输入的临时清理之后。两端同钟相减为d，源所有中途读钟映为a加该次经过量。转换至上述纳秒duration后再走原float秒、毫秒截断、比较及预算更新；非法表示或溢出不借浮点日志补值。保留AA的正常S'，后继启动须晚于原结果处置/槽释放，且不早于a+d，才有跨job源钟非降。此映射显式不同于旧“当前公共行t”，不能沿用native等价说法。

CPU采样须包住完整体，并登记两钟采样的先后和测量器费用；不能假称多次系统调用为同时原子读。结束后只写计量尾记录及观察者日志的工作归独立测量开销，不倒填d、也不混入新的源业务；源结果的实际复制/封存已经在完整体内，后续通信仍归原通道。以这种明确测量边界定义“完整”，无需无限计时自身递归。初始化、异常展开、结果拒绝、取消后尾部同样有费用归属，完整测量失败只留可信前缀。

getrusage可给进程user/system CPU、缺页和切换计数；ru_maxrss是累计峰值，不是某job内存增量或驻留积分；部分字段未维护，不能把其零读数当零费用。CPU与elapsed差也不是纯外生噪声。[Linux getrusage](https://man7.org/linux/man-pages/man2/getrusage.2.html) 当前接口足以支撑测量设计，不足以证明所有来源工作/子进程/内存已经计全。

## 4. 持续读域、撤权与退出

建议会话出生只接收已授权初始数据；每job仅交付当前冻结输入、前一正常私有状态、源角色随机状态和声明时钟。源进程不能继承评价器未来任务/O/真实状态的可读映射；完整fd、argv/env、文件与库入口、共享内存、子进程、回调和通信时序必须列成可达表。execve重新建立程序内存，却默认保留未设置close-on-exec的fd，并显式接收环境；所以“新进程”不等于读取域闭合。[Linux execve](https://man7.org/linux/man-pages/man2/execve.2.html)

seccomp只筛系统调用及参数，内核文档明确它不是完整sandbox；不能拦住已经可读的共享内存或所有用户态取时。持续会话也不能靠假设过滤器能任意撤销来切换读权限。应以实际可达调用及管理器只在合法交接时交付数据证明域，再以系统限制增强；目前该表未闭。进程隔离没有清除宿主缓存、温度或调度历史，亦不证明block独立。[Linux seccomp文档](https://docs.kernel.org/userspace-api/seccomp_filter.html)

采用非抢占必须明改源故障为P29的REVOKE_AND_DRAIN：原行撤输出/新体权限，已启动完整体及必要尾费保留，不能称已在某条源指令前停止。真正崩溃或未返回仍是实际失败/未知；不新增自动成功超时、析构保证或尾时上界。pidfd可绑进程身份并观察退出；wait4能返回指定子进程状态和用量。停止信号、pidfd可读、wait终态、引用可回收是不同事实；有子孙/额外线程时须覆盖全域，未确认不复用Srv_P。[pidfd_open](https://man7.org/linux/man-pages/man2/pidfd_open.2.html)、[wait4](https://man7.org/linux/man-pages/man2/wait4.2.html)、[wait终态](https://man7.org/linux/man-pages/man2/wait.2.html)

## 5. 发布成本的可采用修正文与公平界

可供根采用的条款方向是：**离散服务容量计事前定义的抽象工作，真实CPU/elapsed另列。每个primitive仅在其收费描述可由此前合法元数据确定且本行足额时开始；不足不读authority、不生效果。发布检查、复制、位长处理、原子提交和处置释放均有这种完整成本描述。隐藏业务值不得在费用预判中提前读取。** 普通局部计数不代表机器指令。任意路径扫描、可变精度认证或析构不能命名为一个恒价primitive；须有可审查的分解或事前可证有限完整成本，不能事后测完CPU才追认曾有容量。有限字段个数不限制其位长，数学可终止也不等于本行可支付。

该修订须覆盖Srv_C/E/A/N、源发布/处置及控制检查，明确哪些既有主张只对模拟成本成立。源体开始、a+d阈值、原严格后继发布、原通道、中心CAS、处置及后继启动仍按P29分阶段；不能以将处置归发布而省掉任何实际操作。撤销依赖的任意会话销毁在其发生之后另计行政尾费，不伪装成有限发布记录。若根不接受这一全站语义改变，P29仍不能替代现严格执行合同。

内部D/R/F_cap/E0用同一个R0修复/AA会话合同；H19E0/H19R-S使用自己的ECBS及已有job局部Environment，不硬塞R0私有状态、修复费或随机机制。两类来源共享完整体边界、单worker、合法启动/发布/撤权和同类成本规则；各自建图、搜索、返回检查/源适配及真实费用分别保留。ECBS源码/回调/数值和依赖覆盖仍UNKNOWN，本文未新增其完整源码资格证据。

NATURAL累计实际执行的抽象工作及真实竞争；STRICT在同O/离散容量下连空闲供给也预付，但仍另报实际工作向量。已取消的源指令配额不可继续虚收；源非抢占worker保留时间若要另收费，必须先完整定义计价函数，不从旧指令单价自动换算。两模式均报真实完整体、尾费和宿主成本；抽象预付不证明实际两臂用量相同。未定计价不是可填默认的空格。

## 6. 三层结论与停止扩查点

| 层次 | 本轮真正前进与边界 |
|---|---|
| 数学可定义 | 固定O下源封存/后继发布、明确撤权及离散primitive付费可以组合；具体全站原子操作成本与统计假设尚待闭，不能称整稿已定义完毕。 |
| 源码可适配 | 实际Linux接口和本地clock类型/单位已核；原now入口/依赖、完整服务末端、读域和继续域尚未全部绑定。普通计时只支持非抢占测量。 |
| 实际后端可用 | 系统文档与头文件存在不等于完整后端已合格；未实现未运行本身不单独构成FAIL，当前UNKNOWN来自尚缺具体组合合同和静态覆盖证据。 |

下一跳是根决定是否接受全站离散成本语义，再将同一有限primitive/位长/可暂停/发布合同覆盖所有writer；不是再找一个计时器或模拟器名称。当前有限接口证据已足以作这项取舍，停止扩查。无skill/Claude重启、代码/伪代码、构建/测试/仿真/调参、配置或结果；无35–38及受限载荷读取，保护参数未赋值。本文完成不更新六门，不固定73，不代表研究完成。
