# 72C7：Opus 有界一维参考运动的可变终点数学构造

这是一项独立控制数学子任务，用于比较潜在研究路线；不是审查或修改当前固定候选。只依据本提示推导，不调用工具、不读取文件/网页/skill、不写程序、不做测试或仿真，不生成实验参数、数据或结果。H、T_delay、B_CAL、B_max、B*、P_active、density N保持未赋值、不给范围/默认。请完整回答约2500中文字，以ADVICE-COMPLETE结束。只讨论理想参考运动和明确模型，不宣称真实硬件成功或算法整体有效。

背景：机器人沿既定原边前进，不能换路、不能倒退、只能在原始终点才消费逻辑MOVE。几何上有时允许先走到原边中间安全位置、无限保持，再在邻居已离开相关区域的证据到达后继续。原边长度ell，参考位置s∈[0,ell]，参考速度v≥0，运动模型 sdot=v、vdot=a+eta，|eta|≤delta；控制a可由理想执行器提供，a_lo>0，a_hi>a_lo+2delta。实体跟踪误差另由固定世界闭环zdot=−kappa z+w及不变包络Z保证，这里不能把参考v=0等同真实实体速度为零，也不重置z。

已有固定终点c的构造仅作为待核线索：
LAUNCH时a=a_lo+delta，从v=0启动；xi=c−s>0，达到v²=2a_lo xi后切APPROACH。
APPROACH时r=v²/(2xi)，a=−a_lo−delta，想保持a_lo≤r≤a_hi并有限到达c。
BRAKE时a=−a_hi−delta，从v²≤2a_hi xi的域内制动至v=0，停在c之前。
HOLD由理想混合端点/保持公理夹住参考v=0，允许无限等待。假设每条边内只发生有限次外部模式命令，避免Zeno。首次启动可能任意no-start，已收到未来命令不必立即执行；无消息时旧已接受的cap仍有效。

新问题：允许上限cap∈(s,ell]在同一原边内单调提高，cap变化在后继实际控制接纳时发生，cap不后退且未接纳新cap前必须守旧cap。参考进度s一直单调。外层如果没有先预留从已有可信下界q到新cap的完整实体扫掠空间就不会发新cap，但你不证明外层协议，只给执行层可行/失败条件。

任务：
1. 先重新计算上述r导数、LAUNCH交点与BRAKE域，指出该给定不等式是否足以建立不变带及有限参考到达。不把无扰动示例代替鲁棒证明。
2. 构造或反驳一个最小的cap提高接入规则。特别考虑旧APPROACH尚有非零v时cap增加，r因xi增加下降到a_lo以下；能否切回LAUNCH且始终不越新cap，不引入反向s？旧BRAKE途中、旧cap终点HOLD以及迟到cap分别如何处理？不能要求每次都从原边起点或把s清零。
3. 陈述一个足够的不变量与组合证明：0≤s≤cap≤ell、v≥0、可停止域、连续误差保持、过时cap幂等/较小cap无效等。cap序号正确只说明输入选择，不等于连续安全；不要给未证明的外层资源事务安全票。
4. 区分无新消息的永久安全保持、获得合适cap/服务条件后的有限阶段到达、全多机器人无死锁和实际净吞吐；后两者不能由标量控制推出。若必须增加有界jerk/执行器饱和等假设才现实可信，应说明当前理想域的局限，不凭空给硬件参数。

这是符号推导，不实例化任何地图/规模/任务/扰动序列。若给出新控制规律，明确仅为待后继整体审查的解析建议，不是实现或实验授权。
