# 73P8：根构造的有限阈值组准入与付费扩展需求

2026-09-09，/root 直接数学构造，NO-SKILL / CONSTRUCTIVE / NONBALLOT。输入73为529a67132eabf915401df097c0a78be91e2c1e47ce9f9205518acdef5ec7420c、137460 bytes /480 LF。P5现稿全文再次核读，P2/P4已于前轮完整读，本次重读相关定义。本件是根的新构造，不重启pending_init的P7，不冒作其完整回执；P7约定文件仍未取得。未实现、写伪代码、生成O/配置/随机轨迹或研究测试，所有保护参数保持原义未赋值。HOLD。

## 1. 选择：先证明一组前缀可继续，再允许通常的安全扩cap

P3说明任意最大cap会把可行次序锁死；P5只证明存在一组两阶段候选。本件给一个更具体的可付费选择模块。它在任何本组成员获得初始前缀以前，找到各自初始cap c_i与保守未来起点l_i，使初始包络两两分离、未来完成包络也两两分离；此后保留原同tid单调cap、逐资源owner及付费q退休。

阶段屏障是完成可行的充分证据，**不作为禁止提前合法扩展的必要门槛**。每个成员在共同公开维护机会仍可申请CAND中的所有更大候选，取当前可授最大者；E0同能力。这样既不用免费到cap ACK，也不因缺POSITION人为取消E0的扩展机会。当前主73仍为整边执行，此件待全稿组合与正式审查后才能采用。本次根完整读取P9独立报告（af234f0d8eef793240c28b06319dcc40410bae19bf03dd77d99e04bf8c2de970，22715 bytes /147 LF）并不用skill复算后修订；P9所审P8旧身份7663fdb4548f43ed71781c23f2d24761854ddf3141b57f5972eb54e0b6c778f3，15119 bytes /93 LF保留为输入记录。P9原件不改；修订后身份另核，不能让P9冒充已审新字节。

## 2. 依赖的几何、信息与绑定条件

沿用P2/P4同一原MOVE的完整alpha/parent/occurrence/tid/控制及几何身份，0≤q_i≤s_i≤C_i≤b_i≤ell_i，参考运动/制动/HOLD与不可撤销b、累计CUM_CAP、RUN水位、真实原END和一次READY消费保持。所有相交含闭边界，原整边静态合法仍必需；不沿新边绕行。

有限CAND_i由实际相交区间端点、0、ell_i及相邻中点组成。证书profile须事前有明确的信息量合同：合法POSITION捕获在本原tid的参考进度s时，输出下界q_sample满足s−epsilon_i≤q_sample≤s，epsilon_i来自该服务既定可认证精度，不能看过候选需要多小才临时选择，也不把它称为实体跟踪误差或给保护参数赋值。没有已成立精度入口时返回NO_CERTIFIED_GROUP，不假设精度为零。

对初始候选c_i，令l_i=max(0,c_i−epsilon_i)，A_i=Mask(U_i(0,c_i))，B_i=Mask(U_i(l_i,ell_i))。要求c_i为严格内点、l_i≤c_i，A_i两两不交且B_i两两不交。A_i包含初始resident；B_i包含真正原终点resident。它们是整个阶段包络，安全不依赖各机器人同时START或同步s。

## 3. 当前请求组：明确可算，但不许任意占住别人

组候选只取尚无已授tid、合法PRECHECK与parent/原MOVE绑定仍有效的中心请求；它们的实体当前按原准入保持在resident起点。中心在同一付费捕获中取得请求/owner/版本，以请求为顶点，若一个完整原边M0与另一请求所属初始resident的外国owner冲突，则有有向依赖边。所有扫描、去重与强连通分量计算收费。

只考虑含循环的强连通分量；每个外部占有者必须有对应成员，否则不能强收进组。更严格的准入检查是：F_G=并集_i Mask(U_i(0,ell_i))内的全部当前物理owner只属于本组所列初始resident，没有其他活动tid或外国resident；F_G也不与任何已有其他组的准入围栏相交。这是可明确判定的充分闭合条件，不称最宽可行分组。图/版本不完整或数值不可判定给明确UNKNOWN/BLOCKED，不作无冲突默认。

对所有候选组按最早请求加入行、再按成员规范key元组排序，取首个通过第4节有限几何检查的组；没有则NO_CERTIFIED_GROUP。这里明确登记独立公共GROUP_ADMIT角色，只由未来O的具名行释放通用描述符，入既有Srv_C FIFO；模式不允新occurrence、初始化/中心未就绪、记录不一致分别给MODE_BLOCKED/NOT_READY/INVALID，不能发现循环便免费追加token或服务器。同一GROUP_ADMIT作业只检查/提交一个规范首合格组或明确空/失败，不兼做其他角色。

沿用P2将各agent公开SOURCE_REQUEST行按公开序号modulo 3派为SOURCE_OR_EXTEND、RESEND_CAP、CONTROL_RETRY的候选文法，不实例化任何O。SOURCE_OR_EXTEND的未授分支在本件改为只尝试整边ell，明确取消P2旧任意最大初始部分cap；只有GROUP_ADMIT可授本件初始组前缀。已授分支仍可安全增cap，其余产生原请求/重传/RUN按P2总函数。GROUP_ADMIT与这些角色按公共行全序和同Srv_C FIFO竞争，无隐藏高优先级；一个token仅一个角色，无命中仍付费，不在结果发布时递归触发下一角色。不同角色是否有足够供给属于明确日历条件，不由规则名称保证。本件明确了模块入口和冲突顺序，尚未移入73全文及其矩阵。

## 4. 仅线性多个阈值向量的候选族

令Theta为全部成员CAND_i的归一化值x/ell_i的去重升序并集。对每个alpha∈Theta定义
c_i(alpha)=max{ x∈CAND_i : x≤alpha ell_i }。
0属于CAND_i，故该函数总有定义；剔除含0或ell_i的向量，逐个按第2、3节条件付费验证，选择规范最小通过alpha。若无一通过，保留全部resident，返回NO_CERTIFIED_GROUP，不先发很短的试探cap再等以后补证明。

向量数不超过Σ_i|CAND_i|，没有枚举笛卡尔积，也没有用连续二分代替精确闭边界。这个有限族不是所有可行向量：某个非等长/非对称问题可能有可行独立cap却没有共同阈值表示，失败必须保留，不能宣称完备。

对P5等长正方形/G+及共同epsilon_obs，记A=r_geo+epsilon_obs、B=ell−r_geo。P5已证明每边存在CAND_i∩(A,B)。令d_i为该开区间最小候选，d_max=max_i d_i；alpha=d_max/ell是某成员实际候选，因此属于Theta。此alpha所得c_i至少为d_i且不大于d_max，所以A<c_i<B，全部满足P5的(G)。由其闭瓦片分离引理，A_i及B_i分别两两不交。只要本组被上述闭合组定位、没有外国owner/围栏，有限族确实会找到一个合格向量；不要求不同方向CAND相同或共同晶格。

选择“最小通过alpha”可能不是这个证明指定alpha，但每个通过者都满足同样A/B分离与信息条件；下面完成论证不依赖它恰为P5中手选值。该发现命题仍以固定PIE已给这些原MOVE为前提，不从几何反推出源一定输出它们。

## 5. 准入围栏与初始原子提交

F_G是中心付费维护的准入围栏，不是第二层物理owner；每资源物理owner仍至多一个。围栏只拒绝非本组原tid对F_G的新增占有，不能证明空间清空、不能提高q、不能撤销已在F_G内的外国责任。因此必须在设栏前已确认没有外国owner，而不是先设栏再等待别人消失。围栏具有未来准入的排他能力，当前无物理owner的F_G资源也不能报告为对外完全FREE。围栏查验、登记、引用与时间/面积占用、外部阻塞代价单列并进入全成本。设栏前连已授未接收/未START的外国责任也由owner检查排除；设栏后普通整边、其他组准入及每条扩展新增占有路径都必须检查F_G，不只GROUP_ADMIT自己检查。

中心完整付费检查PRECHECK保持条件、当前请求/parent/源/几何/资源版本、A/B集合和全部所需记录容量后，在一次中心提交中设F_G，登记组身份/成员唯一tid、c_i/l_i/B_i、初始b_i=c_i，并把各A_i从其原resident移交到对应tid，形成可重放的各个累计授权。各A_i不交，所以这是单中心有限串行准备后的原子发布，不是跨机器人同时接纳。发布前工作/容量不足就没有任何部分准入；未通过或版本失效不留半组围栏。

各机器人仍独立收到、验证、安装授权并在各自公开ACTUATOR/RUN机会启动。消息迟到/no-start或某成员永不接纳只造成持续保留和失败/等待，不撤回已发组授权，不伪造全组已开始。所有初始member的PRECHECK必须受原73的迟到保持及实际gate接纳规则约束；本件不声称多份异步证书等于同时私有状态读取。

组内成员可以完成真实原END、付费终点交接/READY消费及真实任务服务；合法终点gamma_prime接续仍属本组登记的责任，不因它不是旧tid而被围栏拒绝。在组围栏退出以前，按固定agent组成员关系阻止这些成员的下一个原MOVE，不能因旧tid已FINISHED绕过限制。允许合法WAIT消费不改变驻留覆盖。组的终点owner永远保留，不为下一轮制造空地。

## 6. 后续扩展仍按实际资源可授，屏障只作充分证据

成员从b_i=c_i开始，在公共cap维护机会可枚举全部x∈CAND_i、x>b_i，按当前已付q_i检查Mask(U_i(q_i,x))的物理owner/本组围栏与完整绑定，取当前可授最大x。没有合法更大项给NO_EXTENSION；消息重发与RUN另按已有公共角色，CAP增大本身不启动HOLD/BRAKE中的机器人。不给D额外维护机会，也不因E0不买POSITION停掉维护。

关键保持：在任何后继扩展中b_i≥c_i≥l_i；因此新加入资源
Mask(U_i(q_i,x)) \ Mask(U_i(q_i,b_i))
包含于Mask(U_i(b_i,x))，后者包含于B_i。B_i彼此不交，所以新加入尾部不会制造其他成员未来B_j之间的冲突。旧A_i中与别人B_j交叠的部分可能继续持有，但它们本来就是需要合法q或原END解除的旧依赖。其他组/外部请求被F_G拒绝，不能插入新的外国占有破坏该保持。

若某时每个未完成成员已有中心q_i≥l_i，则其当前持有集合包含于B_i，所有完整后续需求也包含于B_i；已完成成员终点resident也包含于自己的B_i。因此各余下成员都能按通常规则扩至ell_i，无需同时提交。这个可计算的全组证书谓词只需付费扫描/维护；它不能读取真实s，也不作为拒绝尚未满足谓词但已实际可授的早扩展的理由。

没有强迫所有人停在c_i：早获合法扩展可继续原运动，已到旧cap的HOLD需新RUN，已在BRAKE须到零速；达到ell_i才产生原END。全过程仍是同一个原MOVE，不能中点消费或给新tid。

## 7. 扩展需求必须进入真实付费查询，不能假设证书自来

每个已初始准入且b_i<ell_i的成员有唯一中心CAP_EXTEND需求，期望同一原MOVE扩至ell_i；它不是新的PIE动作。原UNGRANTED请求在组提交时退出P，CAP_EXTEND在同一提交加入，不能并存双计；需求首次加入行固定为该组提交行，不随b/q更新刷新。其当前完整需求是D_i=Mask(U_i(q_release,i,ell_i))，随中心实际退休下界更新付费重算/失效。已经授部分x不删除这个最终扩展需求；b_i=ell_i、中心已合法获得END关闭事实或绑定失效才删除，不能读取尚未送达的物理END。

在查询参考函数中，以原UNGRANTED请求与这类CAP_EXTEND构成去重需求集；每个原occurrence最多计一个需求。只有结构/控制/围栏等非owner准入条件都满足，且当前确因外国物理owner无法完整扩展的需求才计分。任何未通过F_G准入条件的外部请求均标FENCE_BLOCKED，不用于引导组内POSITION；即使同时撞到物理owner也排除，因为仅退休某段owner不能解除该围栏。对Active中的tid t，按不同合格需求中存在r∈当前完整需求、owner(r)=t且r不属于t最终endpoint Mask的个数计score，tie保持73实际的贡献需求最早加入行、再tid规范key；CAP_EXTEND使用其固定首次加入行，last与无正值回RR仍按73；扫描/重算/索引维护全付费。不能仍只统计授予后已经删除的原P，再假定D会买阶段证书。

E0不购买POSITION，但同样有这些扩展请求、完整CAND尝试、已付END/READY/RUN能力；它可提前扩任何实际安全更大cap。强R用相同Active、cap和围栏，只在QUERY按共同RR选择。若F因子按定义不退休途中空间，则其计分证据和实际q_release分开，不能用不用来退休的证书释放资源。

## 8. 可以证明的条件进展与仍然不能证明的事

本命题只用于以q_release实际退休的S制度，不把F_cap的q_evidence代入。条件于有限合格组已原子准入、无外国占有插入、只有本组相关正分竞争、持续模式允许QUERY/扩展；初始授权/必要RUN/不中断控制使每个成员在某有限时刻参考进度达到至少c_i或真正END。令T为这些时刻的最大值；T是证明中的轨迹时刻，不是中心免费可读信号，也不是保护参数。

必须另有可核后继链：T后仍有相关正分阻塞时，后继公共QUERY/付费SELECT产生仍相关请求，其实际POSITION CAPTURE发生在T后，并获得完整收费交付、验证及q_release提交；若该holder已物理END，则由既有付费终点链在有限后继时刻提交中心FINISHED/endpoint-retag。失效尝试、早采样晚送达、NO_MATCH或重复无进度回执均不算完成这条链；必要失败重试必须仍有合法后继供给。资源可授后，cap维护、累计授权、有效RUN、控制和END到中心FINISHED也须有后继服务。有限O耗尽后不补行，不从“已购买采样最终返回”推出新采样必然存在。

关键引理：若j的完整扩展需求中资源r由外国成员i持有，则r不在j已经兑现的S_j内，故r∈D_j\\S_j⊆B_j。已q_release,i≥l_i的holder只持B_i子集；中心FINISHED的endpoint resident亦在B_i；任何最终endpoint Mask也属于B_i。B_i与B_j不交，故外国阻塞必来自未清旧前缀、中心仍Active的非endpoint资源，score的endpoint排除不会漏掉本组可解阻塞。物理END但未中心FINISHED仍可能持旧资源，不能以本地END代替这一引理中的完成。

令已清集合为q_release,i≥l_i或中心已FINISHED的成员。它单调增加，成员一旦进入就不再阻塞本组扩展。有正分时选中者尚未清；T后实际采样及既定精度给q_sample≥s_sample−epsilon_i≥c_i−epsilon_i，非负裁剪后中心q_release≥l_i，或上述终点链完成清除。若别份在途证据先清同一holder，计那次既成清除，后来回执不双计。至多每成员一次有效清除，不代表每成员只需一次采样或总费用有同样界。没有正分且所有非owner条件合格时，引理排除任何外国阻塞，后继维护可扩至ell并按明确控制/交接条件完成。任意合法早扩展均保持新资源属于B_i，故不破坏归纳。该结论不提供跨组D公平性，也不保证任意固定窗口有足够后继容量。

R在固定有限Active中轮询，并在上述交付/控制前提下也可取得必要证书，故不由此推出D胜R。对P5所述原环，E0没有途中清空证据仍适用首个整边授权不可能性：先有任一原END必须先有某b=ell，而这第一次完整授权仍被后继未释放起点阻塞。当前方法允许E0自由安全扩cap，不增加仅对E0的阶段门槛，所以这一失败来自无途中证据与独占资源制度。

这个证明包含组发现和具体选择的条件，而不再只说“存在手选cap”。它没有给任意固定E的统一完成界；不能将有限最终可服务写成已完成真实TASK，仍须P5/73的有效队首、服务区域、目标保持及预定TASK_SERVICE/Stop前提。更不推出主roster净收益、最优性、一般无死锁或强新颖性。

## 9. 围栏退出、失败和规模成本

F_G只在全部成员的原tid已按共同付费END收据在中心FINISHED、其终点resident交接成立、所有本组尚可发布的cap授权均受关闭水位约束后，付费核验并释放准入围栏。去围栏不删除任何endpoint物理owner；后续新MOVE按原规则重新竞争。组描述符/旧消息引用的GC另等所有有效引用释放，不能把围栏去除当可立即删除全部历史。

中心fail-stop、永久no-start、消息永久丢失或未闭原tid时不超时撤栏。已发生的组几何/设栏/重试/驻留成本和外部FENCE_BLOCKED等待都进入完整窗口；不能删掉这类组或只报成功阶段。失败可长期锁住较大F_G，这是真实代价，也是相较更局部调度的重要反例，不作无损同步保证。

设本次候选组人数为m、M=Σ_i|CAND_i|、R为其完整原边可涉及资源并集大小；它们是符号规模，不为density N或保护预算赋值。朴素确定性参考的每个候选可扫描每成员每资源形成A/B并用成对集合检查，给保守O(M m² R)工作界；全请求图的扫描/分量计算及每个候选组另付，不能把这个局部界说成全系统线性。Mask表达/区间算术有其按输入位长费用；非凸分解数量需另计，不能藏入常数。

可逐候选流式保留最佳/当前集合而不物化笛卡尔积，但源区间、围栏索引、组/扩展需求、在途消息和缓存仍占内存。运行中相同未变请求快照是否复用失败判定须显式版本键、维护/失效费用；本件不假设免费缓存。总成本分含：原边建表、分组/候选验证、围栏、初始/增量授权、QUERY/证书、RUN与重复通信、END/READY、中心等待和外部阻塞。所有策略同cap控制与机会，但按自己的真实工作计费，不凑等工作。

## 10. 当前根结论与最小剩余组合

本件提供一个完整有限几何选择族、单中心组预留条件、避免外部插入的付费准入围栏、普通早扩展能力、实际CAP_EXTEND查询需求和闭合组条件进展证明。它实质排除了“先任意贪心前缀，事后再说能够继续”的论证漏洞；不证明所有可行实例都有这样的分组。

P9独立查错已完整取得并由根全文复算；本次据其反例修订后cap实际采样、有限后继服务和中心FINISHED量词，补endpoint不漏阻塞引理及围栏全路径检查。GROUP_ADMIT、旧modulo 3维护角色和FIFO冲突顺序、CAP_EXTEND加入/tie/中心知识退出已明确，但尚须在全73统一收费阶段、P/Active索引维护、MODE/外部适配及全部控制交接；精度profile的一手可实现来源仍未闭，前缀完整移入全文亦未完成。当前暂不把P8替成已采用的主协议或G6通过票。pending_init的P7和失落Claude句柄状态保持实际未知；根未另启相同外部任务。HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT。
