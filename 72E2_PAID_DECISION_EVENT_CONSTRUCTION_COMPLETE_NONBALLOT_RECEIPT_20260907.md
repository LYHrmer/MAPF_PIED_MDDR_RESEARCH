# 三层付费事件建议完整原文归档

任务：/root/construct72_paid_decision_events

标记：NO-SKILL / CONSTRUCTIVE / NONBALLOT

来源说明：只根据根提供问题推理，并未读取候选72。

## 上次实际完整返回原文

建议采用以下三层合同。它保留 PIE-D 作为唯一产生新 MOVE 的路径，不冻结物理世界，也不预设实验参数。本合同只规定因果与计费语义；安全性、活性和费用上界仍须另行审查。

**一、机会只授予执行权，不提供算法答案。**

公共有限表 \(O\) 固定按 \((t,row)\) 排序。每行先将物理世界连续推进到 \(t\)，再执行该行事件。同一时刻的严格后继行仍是后继。机会只含预先规定的事件类型、服务器或物理服务资源及容量；不得携带根据 Active、阻塞数、任务、位置或私有状态生成的 tid、排序、就绪标志。

QUERY 机会只能提交“运行 QUERY”的通用作业，不能先选 tid。其他机会同理。作业描述符只能引用公共常量和已发布的不可变对象。固定 FIFO 的入队、出队和槽分配使用有界原子操作，并计入预付的控制开销；这些操作本身不再生成调度作业，因而不会出现“排队还要排队”的递归。

**二、读取、计算与发布分别发生。**

系统保留真实参考 \(q\)、真实私有状态 \(c\)、真实任务账本、中心已提交账本 Active，以及各自的版本标识；另保留作业队列、工作槽、完成待发布槽和已发布对象。中心账本只有一个 writer，所有提交串行排列。其他可变状态也必须明确唯一写入者。

每个作业经过“等待—付费读取—付费计算/传输—完成待发布—发布或丢弃”。PROCESS_SLICE 只向指定服务器提供有限工作容量；余额不足便暂停。完成结果没有发布前不可被其他作业使用，并继续占用原工作槽。gate、证书和网络各自消耗指定服务器的容量。

CAPTURE 是付费原子读取：在实际完成该读取的事件行，从指定真实来源取得带来源版本和读取行号的不可变 payload。不得在开始排队时免费取得数据，之后补记费用。读取集合时，费用须覆盖实际读取范围；可用不可变版本根固定视图，但后续访问、遍历和排序仍逐项付费，根句柄不附送计算结果。读取容量可以提前积累，数据只在原子读取发生时取得。

普通结果最早在其完成行的严格后继行发布。发布只允许预先定义的有界检查与状态转移，其成本必须预付或由该行容量支付。任何需要扫描、推导或重新规划的检查都必须成为新作业。

**三、各操作遵守同一因果链。**

QUERY 的最小操作序为：

1. QUERY 机会入队通用作业；取得槽后，付费 CAPTURE 中心已提交的 Active、所需阻塞字段、RR-last 及其版本。
2. 作业用服务器容量完成扫描、比较和必要排序，依据冻结快照选择 tid，并生成 POSITION 请求候选。选择前不能发送 POSITION。
3. 候选完成后等待后继发布行。中心 writer 执行已计费的版本检查；仅当选择所依赖的字段仍有效时，原子提交请求并令 RR-last 等于该 tid。RR-last 在这个成功提交时刻更新，不在机会到达、读取或计算完成时更新。
4. 冲突候选丢弃或重新排队，重新读取和选择均重新付费。重复发布同一候选不得再次更新 RR-last。请求提交后，即使后续 POSITION 失败，已经发生的轮转提交也不倒退。
5. POSITION 作业取得执行容量后，才付费 CAPTURE 当时实际参考 \(q\)。其结果经付费传输并发布后，才能成为后续算法输入。

POLICY 同样先取得所需快照，再执行付费计算。它对真实 \(c\) 的领取、槽占用和消费必须发生在明确的付费原子步骤，每次真实改变都更新版本；不得仅在影子账本上记一次“消费”。完成待发布期间，真实消费仍然存在；废弃结果后的释放也只能通过规定的付费状态转移发生。

CURSOR 抓取真实 \(c\)，TASK 抓取真实任务账本，POSITION 抓取实际参考 \(q\)。ENTRY、TASK_REQ 和 NETWORK 若需从候选集合选对象，也先付费读取和选择；它们的外生机会不能直接提供选择结果。NETWORK 只传输明确的不可变 payload，读取尚未冻结的数据和编码数据均计费。接收机会不附送发送方的新状态。

**四、等待发布期间世界继续运动。**

POLICY 结果必须标明所依赖的来源版本、适用时间和状态条件。过时结果不能借“发布”进行免费修补：允许无条件废弃，或重新提交付费重验作业。

gate 在自己的服务器上付费读取、验证并产生不可变证书。由于验证完成与 MOVE 接纳之间仍有时间，证书必须覆盖这段期间允许的世界演化以及实际激活时刻。可以依据持续执行的参考段、已有预约和有效时间窗口建立这种保持性；版本标识应区分“参考随时间正常演进”与“参考命令被改写”。

发布端只执行已计费的有限比较，例如版本、预约令牌和有效时间。它不能临时重算碰撞、可达性或任务可行性。若无法证明证书在等待期间保持适用，候选只能废弃或再次付费重验。版本相同本身不证明连续世界仍安全。

ACTUATOR 持续执行已接纳、已付费形成的命令。事件间的连续动力学推进属于物理演化；临时选路、补轨迹、计算 gate、搜索下一槽或改写 \(c\) 均属于算法工作，必须付费。需要停止、切换或超时回退时，使用此前已形成并计费的明确规则。gate 状态只能由证书接纳、显式失效或已规定的到期转移改变，不能由每行免费求值的复杂谓词维护。只有 PIE-D 的既定路径可以接纳新 MOVE。

公共 TASK_SERVICE 是固定资源上的真实服务机会。它只作用于此前已提交的请求，并执行规定、已计费的有界资格检查和真实任务账本转移；没有合格请求则空过。策略可以决定提交什么请求，不能增加、移动或重写 \(O\) 中的服务机会。

**五、评价器开销与算法工作分账。**

评价器推进物理、访问既定事件行、扣容量、搬运不透明句柄和维护固定队列所需的宿主运行时间，属于元评价开销。它不能替算法访问业务内容或形成选择。凡会影响算法决策的信息读取、集合操作、计算、通信及提交检查，都进入算法费用账。原子控制操作的边界和收费规则须预先固定，不能把隐藏扫描包装成“一次提交”。

两个禁止的错序例子是：

- QUERY 行免费遍历 Active，选最大阻塞 tid 并立即发送 POSITION，随后才启动排序计费作业。后续读取已受未付费选择影响，补记费用不能修复因果。
- POLICY 在旧快照上完成，等待期间参考命令或任务账本变化；发布行免费重算 gate 并立刻交给 ACTUATOR。即使最终动作安全，也隐藏了重新读取、验证和执行决策。

该合同允许保守失效导致重试或空过机会；它不据此承诺无饥饿或持续进展。正式审查还需给出原子操作边界、费用表、版本依赖和证书保持性的证明，才能判断具体实现是否满足合同。

## 归档声明

根将独立筛选，这不是正式审查、没有通过票。
