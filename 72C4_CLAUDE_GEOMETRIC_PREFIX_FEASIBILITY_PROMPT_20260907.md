# 72C4：Opus 有界几何前缀可行性推导

仅CONSTRUCTIVE / NONBALLOT。当前72已固定送科研导师审查，不得把本建议当修复72或正式逻辑审查；这里只比较一个潜在扩展的必要数学条件，三路收齐前不会写后继候选。你只根据本提示独立推理，不读任何文件、skill、网络，不使用工具或子代理；不实现/构建/测试/仿真/生成载荷或结果；不给H、T_delay、B_CAL、B_max、B*、P_active、density N数值/范围/默认。请完整给约2500中文字，以ADVICE-COMPLETE结束；发现错误优先给反例，勿迎合预设可行。

问题为四邻格正方形环的几条已有PIE-D原MOVE，规划器/原边/目标/逻辑消费顺序不改。边长ell，世界参考中心分别p1=(s1,0),p2=(ell,s2),p3=(ell−s3,ell),p4=(0,ell−s4)，各si∈[0,ell]且单调。真实实体相对参考的闭包络K=[−rho,rho]^2，rho>0来自固定F与正不变跟踪误差Z；不能将rho当调参结果。相邻包络碰撞条件似可写为ell−si≤2rho且s_(i+1)≤2rho（循环下标）。请你重新推导，尤其检查所有非相邻对，不能直接照用。

现72先独占完整原边的粗瓦片M0才允许动，导致纯环无首个grant。此前顾问提出只预留去终点部分、等下一成员READY后再交终点资源，但仍绕不开终点等待环。根也发现同尺度闭瓦片下，停在终点瓦片外需s+rho<ell/2，而释放原点瓦片需q−rho>ell/2且q≤s，互不相容。这个反例不证明连续几何不可解：若各si相同且4rho<ell，所有同刻闭包络可分离；但独立no-start/异步进度使共同进度不能假定。

待判断的不同几何想法：使用比原规划网格更精细、仍有限且有全局ID的闭资源分区，或显式连续集合认证；先独立授予每条原边从0到某个内部上限b的保留，要求其整个启动/运动/制动/无限保持可达集合都在已保留区内。b是符号位置，不是新图顶点或新逻辑MOVE。若所有agent最终各自在中段停住并付费证明si>2rho，那么前一agent能否在不需要邻居先到原终点/READY的情况下，把自己的同一tid授权上限扩到ell并沿原边完成？只在原终点READY后才消费逻辑槽c。阶段内no-start/网络任意迟到时别人必须可无限保持，不能靠撤销已发grant释放。

请给四部分：
1. 对上面方形几何，推导“所有si独立处于[0,b]仍安全”与“所有si独立处于[b,ell]仍安全”的严格充分条件；考虑旧阶段/新阶段交错和晚到证书，而非仅全体同步阶段切换。若两阶段条件不足给出具体符号反例。
2. 指出精细闭资源分区何时能分别表达这些分离集合，边界接触怎样处理；不要把连续不相交等同任意粗瓦片不相交，不给分辨率实际数值。指出需要付出的资源数/几何/索引/通信费用和不能承诺的规模界。
3. 只给一个待证授权不变量的清楚形式，例如实际允许参考上限cap单调、下一cap安装前已保留其可达集合，连续控制在未知消息时仍尊重旧cap。明确它相对72的全U(q)覆盖、原P1参考终点/制动/恢复和幂等tid/阶段版本改变哪些前提。不要凭几何可行就宣称控制器已构造或可运行，更不直接赋控制/延迟参数。
4. 最小失败/放弃条件和公平比较：mandatory中段证据若加入应为所有同能力查询臂共同服务；不得让E0故意缺必需能力来制造收益；阶段启动可能增加no-start/处理机会，不能免费藏掉。保留全roster动态失败，不按运行后可行交集筛结果。说明这只是假设扩大后的潜在执行层机制，不是已经证明查询收益或新颖性。

请尤其区分：几何可行、闭环控制可执行、单中心事务/版本正确、长期活性、实际净吞吐五种结论。没有不变量或缺前提的部分保持UNKNOWN。该结构是纸面推导，不是在实例化受保护规模或生成实验场景。
