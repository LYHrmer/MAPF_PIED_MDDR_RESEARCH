# 72C2：Opus 付费事件因果构造任务

只根据本提示推理，不使用任何skill、工具、文件、网络或子代理；不实现/构建/测试/仿真、不生成实验载荷或结果、不赋任何实验参数数值/范围。意见仅 CONSTRUCTIVE / NONBALLOT，不是正式审查。请给完整但有界的建议，约1500中文字，末尾写 ADVICE-COMPLETE。你没有读完整候选72，不能声称全文审查或投通过票。

研究是大规模lifelong MAPF下的空间跟踪偏差与付费观测，保留PIE-D为唯一新MOVE来源。实际实体X=u+s e+z+F，s沿单条原边单调，固定世界向误差z始终在已证明闭盒Z；匹配tid的可信样本q<=s允许只退休与完整未来后缀U(q)不交的资源。终点E始终包含在后缀中。单可信中心串行持久账本、每agent至多一个grant；已授权不因超时撤销，终点先gate不可逆关闭再中心将E转resident、清其余、发READY，后执行器付费消费逻辑槽c。零参考速度端点交接不重置实际z，不声称真实速度为零。物理在全部排队/计算/通信期间持续。

拟采用的因果模型：外生有限机会表O固定全序。每行先推进物理，再按有限已付费发布容量提交先前行完成的结果，最后只登记本行通用机会。QUERY只排通用SELECT，不能预先免费选tid；固定FIFO/不透明token的元控制操作预付有限成本，不递归生成调度作业。每个writer至多一个计算或待发布槽，结果未发布时占槽、后继writer作业不能先读旧版本。PROCESS_SLICE提供有限容量；CAPTURE在该行物理/先前发布之后实际付费读取，原子读取容量不足则不读取。可固定不可变版本根，但集合遍历/复制/排序/计算另计费。结果严格后继行付费有界检查和原子提交；若需重新几何/全局计算则新付费作业。中心为owner/Active/阻塞请求P/RR-last/mode唯一writer，executor为c/gate唯一writer；任务环境为真实A唯一writer。

SELECT从中心已提交快照付费扫描P、owner与端点mask，选最能解除阻塞的tid或RR后继；成功发布POSITION请求时才更新RR-last。中途废弃不推进；请求发出后采样/网络失败仍推进。POSITION的实际q要等自身authority付费CAPTURE。POLICY只读自家c和已交付知识，合法WAIT或READY-MOVE的c增加只在成功消费提交时发生，不能影子预消费/回滚。所有gate等待期间仍由旧HOLD/已提交后缀持续覆盖；version相同不能代替连续安全证明，命令/parent变更使旧证书失效。ACTUATOR只接纳已付费形成并发布的命令与有界幂等守卫；连续既定控制动力学费用共同计入。

真实任务TASK_SERVICE是固定外生服务机会，按已分配队首和实际到位/服务区判断，每agent最多一次，独立于是否主动查询或提交算法观察请求。环境服务检查/转移和宿主成本另计且各臂同规则；不可把算法全局扫描藏为评价器工作。比较含同几何/控制/证据能力的D-S与RR-S，以及从不主动途中POSITION、只用共同付费终点ENTRY的低成本E0；净效应计全部实际工作，固定总评价窗口，不因慢初始化重开。

请独立给：1.这三层模型最危险的两个剩余错序或费用漏洞，已经排除就明确说明；2.如何精确定义同一writer的物理自主切换、算法提交和只读CAPTURE，既不矛盾又不冻结世界；3.最小版本依赖与不变条件，哪些不能仅靠版本验证；4.一项可证伪的费用/容量反转条件。不要增加任意租约/立即停车/新路径规划器，也不要把本问题扩成一般分布式事务或重复此前中心CANCEL任务。
