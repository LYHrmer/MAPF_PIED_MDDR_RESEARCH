你是Claude Opus，执行一次具体构造C34，不是正式审查。仅基于以下已核论文规则与局部设计，构造可独立采用的拼接引理和最小数据合同，不泛审稿、不用工具/网络/skill、不写代码或实现伪代码、不运行实验、不赋参数数值。约1200–1800中文字符，必要公式可另列。
Hönig等RA-L2019《Persistent and Robust Execution of MAPF Schedules in Warehouses》Algorithm1规则：每agent原动作序列时间严格递增，动作a=(agent,index,t,s,g)；Type1连接同agent相邻动作。对每动作a与每个其他agent j，只取j序列中第一个满足s(a)=g(b)且t(a)≤t(b)的b，加入Type2 a→b，然后break；不是加全部匹配边。入队要求Type1前驱已经enqueued或finished，所有Type2前驱finished；finished只来自执行完成通知。Algorithm2取desired的全部前驱闭包，按agent最大index得cut，旧动作继续执行；以cut尾终态作新规划起点，统一平移新时间，使无new→old依赖，再按Algorithm1接新ADG。原论文允许清理已finished顶点。
当前项目明确是H19共同物理/计费适配，逐边停止且不可撤已发布动作，不声称作者原样性能。旧有限DAG G的每agent链唯一。一次付费cut先把desired与所有已入队/执行/授权/发送缓存可能生效/旧cut承诺的责任动作并入种子，再取Type1/2前驱闭包P；cut外未承诺staged后缀被撤出允许域，不会在失败后复活。P中旧动作/依赖/时标/id不改，物理继续执行、中心finished单调。新序列S每agent从P尾接续，所有新动作t严格大于所有保留旧动作t；新序列单独按Algorithm1生成的ADG无环；源动作端点/共同几何接续另行验证。新图接纳不能为既有enqueued旧动作增加未finished前驱。
根拟构造：在每agent串接P前缀与S，对Type2按完整首次匹配语义定义新图。对旧a,j，若旧首次匹配b保留则保持，不另给a加通向S_j的重复匹配边；若原b在cut外或不存在，则只找S_j中首个合法匹配。新a只可能匹配S中的b。旧Type1保留、每agent旧尾到新首连接。需要证明这与从头对P+S运行Algorithm1得到的活依赖相同；闭包保证没有保留动作依赖已丢弃未完成动作；新时间严格性排除new→old，结合两部分无环推全图无环，旧enqueued资格不被撤回。请独立检验，若某一步不成立给最小反例并修到具体成立的版本。
难点请建设性解决：原finished顶点允许GC，不能免费保留或全图复制；身份/端点索引/旧首次匹配记录可能已经删除。给出最小持久证据合同，使删除完成正文后仍能合法判断每条保留依赖已满足，以及首次匹配仅选更早的已完成动作时不用错误追加限制后继的新边。可以采用带原动作id的完成摘要或等价保留索引，但必须说明需要哪些字段/生命周期、何时能删除、所有维护收费；不能把消费/位置证书当finished或保留无限零费历史。不要求证明固定内存，不要求任意GC后仍可恢复。
唯一交付：1 精确条件与三分情况拼接定义；2 简短证明/必要反例；3 可落地的最小GC证据合同，区分图语义等价、逻辑入队与连续物理安全/付费服务进展，不以图无环宣称后者。末尾严格END_C34_H19_SPLICE。
