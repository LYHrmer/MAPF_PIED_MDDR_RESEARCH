# 73P2：同一原 MOVE 的前缀权限、公开重试与公平成本组合

2026-09-08；独立建设性 **NO-SKILL / NONBALLOT** 回执。本文不是 73 正式审稿、固定版本、通过票或实现许可。根代理委派的任务已完成为本文；没有启动 Claude，没有补冒任何失落任务，没有使用 skill，没有修改 73 或其他既有文件。结论保持 **HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT**。

本文给出一套明确可选协议，同时证明其安全所需条件，展示其调度不完整性。有限候选集、公开重试和安全组合均不证明主 lifelong 总体有正效益。整边方案继续是完整备选与机制对照，前缀方案不是预定胜者。

## 1. 实际读取与身份

先全文读取指定 2026-09-08 交接，再全文读取 73M1、73P1，最后全文读取 73 草稿。第一次合并显示发生截断，随后取得同次完整工具输出并保存在本会话内存，按字符区间补齐正文；没有用摘要代替全文。73M1 中实体 support 不能对任意当前 z 等于包络 support 的纠错按交接及 73P1 保留。目标文件在写入前精确检查不存在。

| 输入 | 开始实核 SHA256 | bytes / lines |
|---|---|---|
| 73M1_MONOTONE_REFERENCE_CAP_COMPOSITION_CONSTRUCTIVE_NONBALLOT_RECEIPT_20260907.md | 907b61f8ac9e20c3d11bc1e9f6080f252d38fab40a329a2dad82dbe0fd6fe6d8 | 23184 / 280 |
| 73P1_PREFIX_RESOURCE_COVERAGE_AND_GRANT_COMPOSITION_CONSTRUCTIVE_NONBALLOT_RECEIPT_20260907.md | c14b013ad1374ce955fef937371a40f41cdfa7c45c02ebbee836ef3ce73c0226 | 29998 / 330 |
| 73_PIE_SPATIAL_EVIDENCE_TERMINAL_HANDOFF_AND_PAID_SERVICE_PREEXPERIMENT_DRAFT_20260908.md | e0baef1ebf3b5c6a999b5324ac6023588e8da9affc82f1cba3ceb7ee972045dc | 107921 / 418 |

读取范围只包括上述三份科学文本及指定交接，没有读取 35–38、桌面、Q-CAL/Q-CONFIRM 或 map/scen/config/agent/task/delay 载荷正文，没有网络检索。本文的一手来源新颖性、官方源码和素材许可问题均未补证。报告结束前再次精确核同三文件身份；实际结果在完成回执中给出。

## 2. 首先排除的两种伪修复

**错误修复一：在“扩 cap ACK”中顺带给到达证据。** 安装了 C 并不意味着已经到 C。同一合法 grant 后，执行一可因 no-start 停在 s=0；执行二可到 s=C。安装 ACK 的内容即使相同，也不能在执行一推出 q=C。若把 ACK 改为只在 s=C 时签发且允许其退休资源，该 ACK 实质上是一种途中进度证书；收费不能改变其信息类型。让 E0 获得这种证书后仍称其“无途中进度信息”，会偷换比较。

**错误修复二：只有 POSITION 或 cap 到达报告才能触发下一扩展。** 该规则令 E0 因选择不观察而永久没有下一次扩展机会，即使前方资源后来完全空闲。它人为删掉执行能力，并非不观察的自然代价。本文将扩展机会、重传、控制尝试和终点查询全部放进共同公开日历；它们不以真实 s、C 到达、POSITION 是否返回或当前效应为生成条件。

正确界线是：E0 可反复申请、安装并运行同一有限候选 cap；它在 END 以前不取得可供退休的途中下界，因而保守持有更多旧资源。这可能造成实际相互阻塞，属于策略代价；不能再额外剥夺它尝试扩展的机会。

## 3. 状态、几何与不可拆开的不变量

固定一个 PIE 原 MOVE：u→V，ell=||V−u||>0，e=(V−u)/ell，原 occurrence=(session,plan_epoch,i,k)，tid=(session,i,request_e)。全过程不改此边、方向、原目标、slot 或 tid，不在中点建立新原边。参考 s、参考速度 v、世界误差 z 满足 X=u+s e+z+F，K=Z⊕F；F 非空紧且方向固定，Z 为原世界闭盒。

中心持有已兑现 cap b，本地控制器持有已安装 cap C，中心可用于退休的已提交下界 q。核心不变量为：

\[
0\le q\le s\le C\le b\le\ell,\qquad v\ge0,\qquad v^2\le2a_{\rm hi}(C-s),\qquad z\in Z.
\]

定义 U(q,b)=union_{rho∈[q,b]}(u+rho e+K)。固定有限闭凸矩形瓦片 T_r 覆盖所有可能实体所在工作域，Mask(A)={r:A∩T_r≠空}，包括边界接触。K 为紧凸多边形或有已给定有限凸分解的紧集。完整原 U(0,ell) 静态合法仍是候选准入条件，部分 cap 不能掩盖前方静态障碍。

每资源最多一个物理 owner。采用 73 的同 agent 原子改 tag：初始 resident gamma 只交给其唯一合法 tid；活动 tid 的需求为 S=Mask(U(q,b))；终点只交给同 agent 的 gamma_prime。不得任意吞并同 agent 的其他 tag。跨阶段不同时授权另一个原 MOVE；因此不需要假设任意多个并行 scope 都可自动相容。若后继改成多 scope，必须采用 73P1 的联合需求计数合同，不能只看 owner 字符串。

中心 b 是已作出且不能靠超时撤销的潜在实权，本地 C 可以严格较小。q 是独立的可信下界，既不是控制目标也不是安装确认。协议不要求中心知道 C 或 s。

## 4. 一套唯一、有限、无需新参数的 cap 选择

对每个资源求 I_r={rho∈[0,ell]:(u+rho e+K)∩T_r≠空}。在上述有限凸表示下，I_r 是有限个合并闭区间 [a_rh,b_rh] 的并；凸 K 时至多一个。令 D 为 0、ell 和所有非空区间端点的去重升序集合。在 D 的每对相邻不同端点之间加入唯一算术中点，得到有限去重升序集 CAND。0 和 ell 均在其中。端点精确比较；有理原边可用 lambda=s/ell 的有理表示，不能用未经认证浮点误差改变闭边界。

这只是从已有几何导出的数学集合，不给任何保护参数数值、范围或默认值。若 D 有 J 个点，则 CAND 至多 2J−1 个点；J 是输入几何的计数，不是另一个实验调参旋钮。中点用于表达阈值之间的严格空隙，不声称有限取样保存一切连续调度可行性。若几何只有未证近似，返回 GEOMETRY_UNCERTIFIED，不在本阶段偷偷选择安全 epsilon。

在一次收费的 SOURCE_OR_EXTEND 作业捕获当前串行账本后，唯一选择为：从 CAND 中所有 x>b 按从大到小的规范顺序检查，取第一个使 Mask(U(q,x)) 的全部新增需求均 FREE 或合法属于自身当前责任的 x。没有则 BLOCKED，b 不变。初始未授请求使用 b=0、q=0 和当前 parent resident 作为旧需求；只接受 x>0，不发没有正向权限的零 cap 来冒称开始了 MOVE。所有候选都必须满足原完整静态合法性及身份条件。

一个成功扩展只增 b；一个失败尝试没有部分预留。q 的证书处理另成先完成的串行退休事务，不与失败的扩展拼成未声明的半提交。中心 writer 在作业计算、等待发布期间占唯一槽，其他中心业务不能在它之后先读旧账本；实际发布再检查 authority/原 tid/tag/命令有效性，检查费用来自明列发布容量。私有增量失败即无外部作用，已经执行工作仍收费。

该最大可行规则是本文协议内的唯一调度选择，目的仅为让候选可判定、可复核。它不是“最优 cap”，也不保证找到任何存在的可行调度；第 12 节给出其不可撤回贪心的两机器人反例。根可据此改选另一完整路线，不能将其不完整性隐藏在“cap 可选”三个字后面。

## 5. 原连续控制及 cap 的实际安装

沿用受限平移域：s_dot=v，v_dot=a_cmd+eta，|eta|≤delta，0<delta<a_lo<a_hi，a_launch>0；z_dot=−kappa z+w，|w_j|≤kappa zbar_j。当前 xi=C−s；xi>0 才计算 r=v²/(2xi)。LAUNCH 用 a_cmd=a_launch+delta 至首次 r=a_lo；APPROACH 用 a_cmd=−r+delta(a_hi+a_lo−2r)/(a_hi−a_lo)，在 [a_lo,a_hi] 内至 (s,v)=(C,0)；BRAKE 用 −a_hi−delta 至首次 v=0；HOLD 沿用理想参考零速保持混合公理。没有重新定义真实实体静止、一般转向或 LIMO 控制能力。

初始 grant 接纳前为 RESIDENT_OPEN 且 HOLD 于原 u、c=k、active_tid/pending 均空、parent 与原记录完全相符；付费 PRECHECK 证据和实际接纳点均检查这些谓词。接纳 cumulative grant 可直接安装它的已授 cap，不要求先收到较早丢失的小 cap 包；转 GRANT_ARMED，尚不 START。同 tid 后继 cap 包只能在 GRANT_ARMED/MOVE_LIVE 接纳，必须 b_packet≥C 且完整身份、已授 lineage 和几何版本一致。低于 C 的旧包拒绝；相同 cap 幂等。消息不直接改变 s、v、z、c 或原目标。

中心已完整预留 Mask(U(q,b_new)) 并提交 b_new 后才可封存签发 cap grant。grant 绑定不可变原身份、授权 lineage、单调 cap 序号和 cap 值，不把以后 q 退休导致的普通账本 revision 当成撤销旧 grant 的条件。旧 grant 合法迟到时仍不超过中心单调 b；相同 cap 序号不可能签发不同值。较新的累计包足以修复更早包丢失，不需再次 PRECHECK 或创建新 tid。

安装前后制动余量满足 2a_hi(C_new−s)−v²=[2a_hi(C_old−s)−v²]+2a_hi(C_new−C_old)≥0。旧 LAUNCH/APPROACH 若 v>0 且新 r<a_lo，用同一 LAUNCH 重新进入，不能直接套旧闭带证明；令 G=v²−2a_lo(C_new−s)，则 G_dot=2v(a_launch+delta+eta+a_lo)，正速度阶段为正，原 73M1 的积分下界给首次相交有限且发生于 cap 之前。新 r 已在闭带则 APPROACH。BRAKE 具有优先级，任何 cap 增大均不取消其减速；首次零速后 HOLD。

到旧 cap 与接包同刻，先处理物理首次到达：若 s=C_old<ell、v=0，进入中点 HOLD，再安装更大 cap；不得把已发生的零速接点隐去并自动运动。HOLD 安装后仍 HOLD，须另一个明确、已验证 RUN 命令在实际 ACTUATOR 接纳点满足 C>s 才可从零速 START/RESUME。只有原 s=ell、v=0 才可能原 END；终点优先关闭旧入口，迟到 cap 永不重启已 END 的原 tid。

固定 cap 的 LAUNCH 有有限首次交点，APPROACH 从 xi_A>0 至 cap 的时间界为 [sqrt(2xi_A/a_hi),sqrt(2xi_A/a_lo)]，BRAKE 停车位移≤v_B²/(2a_hi)。Z 正不变性由 z 的变分式给出。有限 cap 增量只减少反复切换这一原因，不推出收到 grant、成功 START、等待结束或到达 ell；任何一项永久不发生都可以安全长期 HOLD。

## 6. 公开、有限的申请和重试日历

本构造给出一种不用新周期参数的完整日历派生规则，尚未实例化 O。保留 73 的有限公共 O、每行先物理推进再已有结果发布、最后登记本行机会的次序。对每 agent 的既有 POLICY(SOURCE_REQUEST,i) 行按公开出现次序从零编号 n_i，用 n_i modulo 3 唯一映射到 SOURCE_OR_EXTEND、RESEND_CAP、CONTROL_RETRY。这个编号由公共描述符决定，空结果、失败、丢失和私有状态均不改变相位。它是显式更改机会文法的候选；不能声称当前 73 正文已经采用。

| 公共子角色 | 其后收费作业的总动作 |
|---|---|
| SOURCE_OR_EXTEND | BOOTSTRAP/中心失效→明确无业务；有 GRANTED 原 tid→按第 4 节尝试扩展，b=ell 时 NO_EXTENSION；有 UNGRANTED 原请求→只重试同绑定且有效 PRECHECK 下的初始预留；无未闭请求→只用 Kc 与原 Π 生成一个原请求，后继 PRECHECK/预留另收费，不连做下一槽 |
| RESEND_CAP | 有 GRANTED tid→重发当前最大已提交 b 的同一累计 grant，不新采样、不改 b；无有效 grant→NO_GRANT；每次真实发送/复制/验证收费 |
| CONTROL_RETRY | 有 GRANTED tid→发送当前中心已声明 RUN_ALLOWED 或 STOP_LATCHED 控制代际的显式命令；无 tid→NO_TID；它不询问本地模式、C 或 cap 是否到达 |
| 原 ENTRY(i,PRECHECK) | 按原角色对未授原请求取得收费准入证据；失败不回正确 c/位置，不自动改原提案 |
| 原 ENTRY(i,END) | 有 GRANTED tid 时发收费 END 请求，无真实匹配 END 则仅给限定失败回执；已 FINISHED 且需交付时重发其缓存 READY；绝不因没有 POSITION 或中点报告取消终点机会 |
| 原 LOCAL_POLICY / ACTUATOR | 前者只按自己的 phase/c/pending 消费合法原槽；后者只接纳已付费验证的 cap/控制命令及必要当前守卫；二者不是中心读取私有进度的旁路 |

机会只登记不透明 token；选择原 tid、遍历 CAND、读取 owner、签发消息、查缓存与空结果均在其 writer 的收费作业内完成。公开有限 O 使所有尝试/重传行数有限，有限 CAND 使每次选择有限；重复消息还须遵守原有限 copy 规则。没有事件到达密度或网络送达的隐含保证。公开日历耗尽时保留 PENDING/HOLD/当前责任直至评分截止，不再延长窗口等到成功。

retry 是“同一原请求/同一已授 lineage 的下一次公开尝试”，不是不断分配新 tid。资源阻塞不更新关闭水位；新 request_e 只在中心可信确认上一未授请求 CLOSED 后可用。初始无效 PRECHECK 的失败可在下一公开 SOURCE_OR_EXTEND 依当前允许输入重新请求证据，不能从拒绝 payload 猜实际下一槽。全部未来工作都等严格后继行，单行不递归制造扩展、消息和 START 链。

## 7. 控制代际、无 ACK 设计与真实费用

cap grant 和 RUN/STOP 是两种独立权限。中心对每活动 tid 持单调 control_epoch 和 desired_mode；初始明确 RUN_ALLOWED，MODE BRAKE 的收费提交推进代际并置 STOP_LATCHED，MODE RESUME 的收费提交推进代际并置 RUN_ALLOWED。共同 CONTROL_RETRY 发当前代际，而不是看到车已停才新增机会。初始 RUN_ALLOWED 是原 MOVE 的显式启动意图，不是 cap 字段自动启动。

本地持最大已验证 control_epoch。旧代际命令不能覆盖新代际；同代际不同 mode 属无效身份。收到 STOP 时在实际 ACTUATOR 按合法 BRAKE/HOLD 处理；BRAKE 一旦开始必须到首次零速。较新 RUN 即使先到，也不能取消已开始 BRAKE，只能保存其较新允许状态并等待未来另一个明确 RUN 尝试。运行中 RUN 为幂等无作用；C=s 的 HOLD 下该次 RUN 无运动效果，不把它变成无限期自动启动钩子；之后 cap 增大仍需另一个公共 CONTROL_RETRY 的明确 RUN。

每个控制尝试绑定其生成的公共 CONTROL_RETRY 行及同 agent/session 身份，传输副本不改这个 key。本地按该公共全序持久保存已处理控制尝试水位；通过来源认证的新尝试在其实际控制接纳/拒绝点先按全序一次登记，成功、no-start、运行中无作用、C=s、缺已安装 grant 或其他 phase 拒绝均不允许同 key 在稍后重新生效。低于或等于水位的重放拒绝，未认证包不得推进水位；较新的尝试可使更旧迟到尝试失去机会，未来公开重试承担恢复。控制代际不得由旧包回退，水位只防重复，不代替 mode、tid、phase、C>s 和制动域守卫。这样，无作用 RUN 的迟到副本也不能在一次新 cap 安装后被重用为中点 RESUME。整数位长及持久写入收费，不使用无限拒绝位图或序号回绕。

RUN 在 cap 包之前乱序到达可被拒绝或本次无作用；下个公开周期重发当前累计 cap 与新的 RUN 尝试，不要求旧尝试保活。相同公共时刻按行序及原物理优先规则决定，STOP 与 RUN 若没有一致代际顺序则拒绝矛盾输入。no-start 只使本次合法 ACTUATOR 没有启动，不消费、不清资源、不阻止以后公共尝试；正常运行不会仅因重发 RUN 重新启动。

**本可选协议不向中心发 cap 安装/到达/START 结果 ACK。** 审计器可记录实际本地结果，中心应用不能读审计日志。中心不必知道 C，因为所有已授 b 都先兑现且持久；丢包修复靠累计重发。若以后为流控加入 ACK，必须单独限定为无 s、无到达谓词的传输/安装确认，并且绝不纳入 q；任何“到 cap 才发”的 ACK 必须另列为途中观察能力，重新定义 E0 和比较。

本地 cap/控制接收、验证、当前守卫和拒绝仍有费用。为避免把本地中点分支经完成时间直接编码成一条隐含 CAP 回执，可在给定公开 payload 位长上执行固定守卫序列、以各分支有限最大操作包络填充共同控制服务账；填充也占真实供给并单列。无需取保护参数数值。该约定只针对公共 cap/控制接口，不要求对有不同用途的 D/R/E0 全算法强制等工作。不向中心暴露其本地分支完成时间作为业务知识，中心日历与 q 更新也禁止使用 ACK 缺失、重传次数或处理时长推测进度。

这证明的是所列信息流及授权下界的类型约束，不宣称全系统严格信息论无侧信道。END/CURSOR 以及合法既有信息仍可能有统计关联；若要研究从延迟推断进度，须显式另建观察策略、保守置信/可证下界及费用，不能据相关性将 q 提高。

## 8. q 的前向退休、终点未消费与取消吸收性

D/R 的 POSITION 是独立付费请求。authority 在其实际付费 CAPTURE 捕获同 tid 的 q_j≤s(t_j)，其后封装/发送不刷新。中心验证完整身份后仅在收费退休提交 R 使用 q_new=max(q_old,q_j)，所有 t_j≤R。参考单调给 q_new≤s(t) 对 R≤t≤原 END 成立，不能回溯到乱序旧样本时刻。q<=b，普通 COMPLETE、CAP/RUN 接收、中心 b、没有 ACK 或一次 NO_MATCH 均不给 q。E0 的 q 在原 END 前恒为 0。

在中心 b 固定时，q 提高只将 S 改为 Mask(U(q_new,b))，精确删除不再相交且仍属原 tid 的资源。闭区间单分量条件为 q_new>b_r 才能退出；有限多分量按全部相交判断，不能照抄整边 lambda 最大值来判断前缀新增资源。q 退休不撤销 C：q_new≤当前 s，之后 s 不退回，因此旧已发 cap 仍安全。若非凸资源后面还有相交分量，扩大 b 可要求重新授予该资源，不能把一次退出当永久可用。

本地 s=C<ell、v=0 仍为 MOVE_LIVE 内的中点 HOLD，c=k、同 tid、原入口未 END；不生成 READY，不改 parent、不触发新原边 PRECHECK。b=ell 或 C=ell 本身也不是 END。仅 MOVE_LIVE 在 s=ell、v=0 以原不可逆守卫关闭入口、推进本地已执行关闭水位、建立 pending=(tid,occurrence,u,V,parent,gamma_prime,closed,ready_verified=false,consumed=false)，转 TERMINAL_UNCONSUMED，c 仍 k。

中心虽可能仍认为 GRANTED，合法同 tid END 捕获可读取这个持久 closed 记录。付费验证及中心提交后将终点 E=V+K 的 Mask 改 tag 为 gamma_prime、释放其余、登记 FINISHED/d_i 并封存 READY。END 后到来的 cap/RUN 全部无作用；READY 丢失只走缓存重传。READY 的本地付费验证只置 ready_verified；后继 LOCAL_POLICY 再核完整 pending/c/phase/endpoint/未消费，单次令 c=k+1 并转 RESIDENT_OPEN。Kc 仍只由已付费 CURSOR 更新，FINISHED/READY 不赠游标。下一原 MOVE 必须新的完整 PRECHECK 与先预留，原终点 resident 持续到合法交接。

未授请求 CANCEL 按原 73 吸收性规则：即使合法 request_e 尚未首次登记，也先持久 ABORTED/d_i 再回 CLOSED；迟到 REQUEST 无法产生 grant。首个正 cap grant 一旦中心提交，原 tid 已 GRANTED，此后 CANCEL 一律 DENIED_GRANT_LIVE，包括 grant 未收到、未 START、中点 HOLD 和扩展失败。单个未提交扩展作业可废弃其私有结果并保留原 b；已经提交的新 b 不可叫“取消扩展”而缩回。要恢复一个失败扩展，只能在未来公开机会从原 b 重新付费选择，不换 tid 或重置 s。

中心 fail-stop 前已提交的 cap 权限全部继续覆盖；尚未提交的私有结果永不发布，费用保留。本协议不重启中心、不回滚本地水位，不用超时回收当前或可能迟到使用的前方资源。SHARED-OFF/E0 仅停止新 POSITION；原 cap/控制/END 责任和重试仍继续。DRAIN 禁止新原 occurrence，可关闭未授请求，但必须继续处理已授原 tid 的合法扩展与交接，且不保证终能 DRAINED。

## 9. 覆盖及身份组合证明

初始 resident 覆盖 u+K；初始 b>0 的新 U(0,b) 包含它。中心先保留全部新增资源再同 owner 改 tag，gate 之后才安装并 START，无覆盖空隙。连续段由第 5 节保持 s≤C 和 Z 不变，结合 q≤s 与 C≤b 得 X⊆U(q,b)。原参考 HOLD 允许实体因 z_dot 继续移动，整个 K 必须持续保留。

扩展发布前旧 S 覆盖 X；发布后 q 不变、b 只增，新 U 包含旧 U。后续迟到安装只把 C 提到某个已授且不超过中心 b 的值，状态不跳，控制不变量保持。退休发布前后都以可信 q≤s 覆盖同一当前实体，其证明只对实际退休以后的时间成立。终点 b=C=s=ell 后 q 最多 ell，E 从不为空；END/FINISHED/READY/消费都保留 E，下一原边只在同点、零参考速、同世界 z 不重置下先授再拼接。

单中心原子串行 ledger 与 owner/tag 复核保持唯一物理 owner；旧申请由中心关闭水位拒绝，旧已执行授权由本地不可逆 END 及 phase 拒绝；中点不开新 occurrence。若两实体相交，其交点落入至少一公共闭瓦片，两者当前 scope 均需该资源，与唯一物理 owner 矛盾。这里的条件包括实际控制、可信捕获、持久身份、资源兑现和公共覆盖，不从“签名通过”或“未见碰撞”反推这些条件。

因此，所列模型内任意有限丢包、乱序、重复、no-start、合法制动和中心 fail-stop 均不因本协议合法事件破坏覆盖。事件不可定义、可信状态回滚、物理控制越界不是已获安全保证的输入；但若满足全部前提的碰撞出现，必须直接否定本条件证明，不能事后改前提。证明没有涉及通过率、全局无死锁、公平调度、最终到达或真实机器人认证。

## 10. D/R/E0 同能力和 S/F 语义变化

三臂具有同一物理模型、有限 CAND、最大可行 cap 规则、public 重试、累计 grant、本地接纳/START/制动、原 END/未消费、CURSOR/TASK/ENTRY 与服务拓扑。E0 不需通过 POSITION 取得扩展“资格”，也不需证明当前 C 已到才向中心申请 b_new；其保守 U(0,b) 仍是合法申请域。相同能力不要求不同知识历史产生相同申请成功率或实际轨迹。

前缀平台可定义 S 为沿可信 q 退休已获得资源，F_cap 为只增加前缀预留、直到原 END 才清除非终点资源：F_cap 的释放下界保持 0，需求为 Mask(U(0,b))。D-F_cap/R-F_cap 可以取得 POSITION 但不据此删除资源；其 q_evidence 若保存须与释放下界区分。E0 同样不途中退休，但省去无用的 POSITION 和证据处理。**F_cap 不等于当前 73 整边启动前就取得 M0 的 F。** 正式采用时必须重写并明确标注前缀平台的 2×2，不把旧 2×2 结果语义原封继承。

有限 cap 几何必须计 CAND 所需区间/端点/排序；E0 同样需要足以计算其 cap 规则的几何，但不要求它另建只有退休用途的阈值表或 D 分数索引。各臂可使用同语义精确参考函数与合法缓存，实际用途不同则按实际费用计；STRICT 预付供给另报，不能强加无用共同工作来抬高 E0 成本。

前缀 D 的阻塞需求也须改写：每 agent 至多一个 p，为未授初始请求的最小正候选，或 GRANTED 原 tid 在当前 b 后的最小下一候选。一次最大可行更新后若 b<ell，剩余所有更大候选都受阻；对最小下一候选登记一个 p 即可，不能把同原需求的每个 cap 候选分别计分。p 绑定 tid/cap阶段/几何与首次有效阻塞时刻，改变阶段后旧 p 退出；所有维护在中心收费，E0/R 无须额外支付 D 特有索引工作。

以 p 的该下一候选需求与当前 owner 作交，D 的 score(t) 是不同 p 中存在外国活动 holder t 的非原终点资源冲突者数；同 p 多资源只计一次，自己的 scope 不算外国阻塞。只取能由同原 occurrence 途中退休的资源作贡献，原终点资源仍排除；失效/获授/关闭需求按收费维护移除。最大正分、最早贡献 p 的首次有效阻塞时刻、tid key 的 tie，以及无正分的 RR 回退沿原规则。R 使用同 Active 的规范轮询；双方全部扫描/去重/排序/索引维护照各自实际工作付费。本文未证明该 D 分数具有最优查询价值。

整边平台继续完整保存：b 从初始 grant 即 ell，不需要 cap 枚举和累计扩展重传；q 退休和原 END 依原完整协议。主 D/R、必报 D/E0 不能在看结果后从两个平台择优；固定候选前必须唯一登记主路线及机制对照。如以相同公共供给比较整边/前缀，可以把不用的 cap机会显式空闲；整边自然经济对照还须允许省去无用途的 cap 算法费用并明示其本来机会合同，不能伪造较差整边基线。

## 11. 全费用与服务队列责任

NATURAL 使用相同有限供给、每站 FIFO、中心/本地各单 writer 及付费后严格后继发布；空轮询、失败、重复、过期、拒绝和不可用产物均按已经执行工作收费。STRICT 按同供给预付，实际工作向量及 padding 分列。共同价格不等于免费额外服务器，也不等于强制三臂业务工作量相同。

| 工作或事件 | 站与写域 | 必计费用及不能泄漏的内容 |
|---|---|---|
| 完整静态几何、I_r/CAND、排序、缓存及失效 | Srv_C；中心私有预处理 | 所有精确几何/位成本/排序/存储；E0 仅为自身 cap 规则做有用工作，不能免费领完整表 |
| SOURCE_OR_EXTEND 的捕获、所有候选检查、原子预留、阻塞 p | Srv_C；中心 ledger | 候选失败和所有 owner/集合读写、增量签发、发布检查/持久日志；最大可行不是免费 oracle |
| RESEND_CAP 与 CONTROL_RETRY | Srv_C→既有 Srv_N→Srv_E(i) | 查缓存/复制/编码/每次发送字节/接收/签名验证/幂等/控制代际；无新传感样本，禁止给 q |
| cap 本地安装及 START/RESUME/STOP | Srv_E(i) 后继发布；共同控制引擎实际 ACTUATOR | 当前守卫、序号位长、模式跳变/物理控制、必要 padding；审计记录不得作为中心业务反馈 |
| POSITION、QUERY 与 q 退休 | 选择在 Srv_C；捕获在 Srv_E(i)；网络后 Srv_C 发布 | SELECT 全扫描或合法索引、CAPTURE、封装、传输、验证、几何与退休；E0 不生成这些途中请求 |
| PRECHECK/END、FINISHED、READY、CURSOR、LOCAL_POLICY | 原 Srv_E/Srv_N/Srv_C | 失败查询、终点采样、关闭、重发、验证、一次消费、游标证据和保持等待；终点服务独立 QUERY |
| MODE/CANCEL、失败冻结、日志与 GC | 各原 writer/站 | 无免费后台线程；已授权限保留内存与重复拒绝成本；不把超时改成释放 |
| planner、TASK、真实服务、INIT 和共同物理宿主 | 沿 73 原角色拓扑 | 保持完整窗口内首次/复用、复制/验证/丢弃、任务真实服务与基础设施费用，不由前缀能力移出观察期 |

作业只在自己的槽完成并封存 envelope，随后释放该槽；不持中心槽等待本地 ACK、END 或 planner。通道付费处理后登记接收 token，不直接写中心 q 或本地 c。当前原已接纳物理控制、混合 END 和真实 TASK_SERVICE 不被应用计算槽冻结；新 cap 在提交前没有实际控制效力。任一未来服务容量/控制 padding 绑定必须先于实验，本文不给数值。

若区间总数为 M，单原 occurrence 的几何 membership 变化按 73P1 至多 2M 次；成功 cap 增大至多 |CAND|−1 次。但全部费用还包括每个公共重试的候选扫描、冲突、无变化证书、重传及序号位成本；公共机会数 J_try 不可从 M 上界替代。朴素选择每次逐候选逐区间检查的有限上界为 O(|CAND|M) 个抽象判定，此外仍有集合/owner/签发操作；全期以实际 J_try 累加。没有复杂度实测、收费标定或净收益结论。

## 12. 最小反例、可修替代与路线比较

**CE1：一机器人也能推翻“安装 ACK 即进度”。** 同 grant 安装 C>0 后 no-start，一直 s=0。若中心由 ACK 令 q=C，就会删除仍与当前实体相交的后方资源。存在性几何见证须选真实 z 为 Z 相应投影极值，并以 w=kappa z 保持，不能对任意 z 声称包络 support 等于实体 support。修复是第 7 节单向累计授权及独立 POSITION/END，而非把 ACK 换一个名字。

**CE2：单原 MOVE 的观察触发重试失能。** 初始 C<ell，某前方资源在稍后公开事件中释放；E0 无 POSITION。如果扩展只由 POSITION 返回/到 cap ACK 触发，它永不再申请，即使现在扩到 ell 完全合法。本文公开轮转修复这个人为死路；它不修复公共机会已经耗尽或永久丢包。

**CE3：两机器人足以证明最大 cap 贪心不完整。** 使用允许的固定、可重叠闭凸矩形资源，并选非零小 K。A 的水平原边沿途先进入 r 再进入 B 初始 resident 所持 f；r 的水平覆盖延伸越过 A 的 f 入口。B 的竖直原边从 f 内向上出发，进入 r 之后才可能退出 f，终点在 r/f 之外。可令 r 为沿 A 中段的横向薄长矩形，f 为沿 B 起始段的纵向窄长矩形，两者在两原线交点处重叠；A 起点在 r 左方，B 起点在 r 下方，初始 masks 不交，其余域用固定细矩形补齐。所有阈值有严格间隔，有限端点/中点规则提供相应正 cap。

等价的相交区间条件为 A 上 0<a_r^A<a_f^A<b_r^A<ell_A，B 上 I_f^B=[0,b_f^B]、I_r^B=[a_r^B,b_r^B] 且 0<a_r^B<b_f^B<ell_B、b_r^B<ell_B；其他初始外国资源不制造更早入口。上述横竖矩形可用严格间隔实现这些条件。最大可授 b_A 是 CAND 中严格小于 a_f^A 的最后一点，至少为 a_r^A 与 a_f^A 之间的中点，所以 a_r^A<b_A<a_f^A<b_r^A。B 的可授 b_B 必须小于 a_r^B<b_f^B；由 q_A≤b_A、q_B≤b_B，r/f 的退出严格条件均不可能成立。这给出的反例是确定的协议级不完整性，未依靠时间预算不足或任何未赋值参数。

若 A 的 SOURCE_OR_EXTEND 先发布，最大可用 cap 位于 r 内、f 之前，A 已不可撤回地占 r。B 不能通过 r；即使 B 可获得一个 r 之前的小前缀，也不能退出 f。A 的参考被 cap 停在 f 之前且尚未越过 r 的最后相交进度，任何可信 q≤s 都不能退休 r；B 同样不能退休 f。两者均满足控制/覆盖/付费重试，持续重复也无 grant 进展。

但有另一个安全次序：A 先取 CAND 中 r 入口前的较小 cap 并 HOLD，B 的全原边可获授且不碰 A 这一前缀，B 到原 END 后释放 r/f，A 再扩展完成。这个有限可行次序不被最大 cap 规则找到。反例使用本模型允许的重叠非均匀矩形，不冒充已经构造了规则细方格主实例，也不证明物理轨迹本身无解。它直接说明不可撤回预留的贪心可能提前占走他者唯一推进空间；不能由“有有限候选/存在可行排序”推出本策略完整。

可修替代包括：事先固定较保守的 next-candidate 选择、加入公共优先级/等待图条件的授予顺序，或整体保留整边方案。它们各自需要唯一规则、全费用和反例；只把最大改为最小也没有活性证明。联合授权/撤销尚未使用的前方 cap 更需要可信 fencing、当前安装/制动域和不可撤回消息处理，不能以超时当撤销证明。本文不将任何替代直接并入已证协议。

**CE4：同尺度闭瓦片仍可卡死。** 原 73 的单边起终点瓦片及半宽 rho>0 条件下，避免目标资源需 s+rho<ell/2，释放旧 origin 需 q−rho>ell/2，而 q≤s，不能同真。增加任意多个 cap 候选不改变此矛盾。细化资源会改变这一几何条件，但需计成本、预先固定且对三臂相同，不能运行后只为正例更换分辨率。

**CE5：前缀有潜在空间，但不是本贪心的普遍收益。** 若一组原边分别存在可先授且相互不交的前缀，运动到某合法中点后其可信 q 能在原 END 前释放他者下一扩展所需资源，则前缀能力允许整边全 M0 准入所禁止的某些分阶段次序。该条件还要求本协议真实选择到这些 cap、各必要付费工作和消息完成、合法 START/RESUME 发生；几何存在性本身不满足后几个条件。E0 可能因不途中退休继续等待，也可能靠某一 agent 终点释放就足够成功，不能预断。

**CE6：单机器人永久 no-start / 丢尽 grant。** 公共重试全部存在且收费、中心 b 甚至已 ell，本地仍可永不运动。条件安全无误，但“有限 caps + 公开重试 ⇒ 完成”错误。活性只能条件性陈述为所需资源以后可授、所需作业/发布容量及消息最终获得、最终有合法不中断的 RUN 段；有限观察期内是否满足必须保留结果，不能删样。

**CE7：CAP 有成本、没有服务净增量。** 如果整边最初就可完整授予，前缀版本可能额外消耗几何/重传/安装守卫却不提早任何真实服务；若前缀令参考在中点停车，更多 LAUNCH/APPROACH 和等待会进一步降低 Q。即使局部提前启动，原 73 的全窗口追平/反超反例仍适用。真实 Q 仍由原终点/resident、实际服务域与公共任务事件判断，中点 cap 不得当新服务目标，保护任务语义不改。

| 选择 | 明确能力与代价 | 必须保留的否定结论 |
|---|---|---|
| 整边预留 + S | 一次完整原 MOVE 权限，付费证据可退后方；无 cap 握手/候选选择费用 | 合法同步循环可能无第一个 grant；其安全与总体净收益均须分开 |
| 本文有限最大 cap + S | 同一原 MOVE 的渐进权限，可在得到可信进度后再释放/扩展；公开重试完整 | 贪心不完整、额外停止/费用、粗瓦片瓶颈；不能从空间存在性推出执行找到 |
| 本文同能力 E0 / F_cap | 仍可不断申请更大 cap，原 END 前保留已授前缀，不暗送位置 | 可能因保守持有停滞，也可能端点信息已经足够便宜而胜 D/R |
| 其他 cap 排序/联合执行 | 可能改变贪心死路 | 必须另有自给协议及近邻比较；不继承本文安全、活性或费用证明 |

## 13. 供根整合的未执行检查合同

以下全部 **NOT_IMPLEMENTED / NOT_RUN**，不是新增研究测试或实验载荷。未来覆盖须含：不同 q/C/b 的独立读写权限；初始累计 grant 先到而旧小包缺失；cap 新旧乱序；旧 cap 到达与扩展同刻；BRAKE 中接到大 cap/新 RUN；原 END 后所有控制重放；C=s 无作用 RUN 后必须新尝试；无 POSITION 的 E0 继续扩展；未授取消先于首次请求；已授扩展失败不得取消原 tid；中心未发布私有结果故障；END/FINISHED/READY 未消费与旧 Kc 重提；闭边界相交与非凸多分量再进入；最大 cap 贪心反例；cap 费用挤占终点/CURSOR/服务处理；完整窗口负效应。

未来同输入策略函数检查须把前缀 p 和全部候选 cap 区分，不能计分膨胀；费用检查须识别 E0 无用途工作、隐藏 CAP 到达回执和 writer 等远端锁。每个合同需真实费用全序、最小失败前缀、实际输入域和被检版本；纸面表项齐全不算通过。

## 14. 实际完成与尚未闭合

本有界交付完成了：一套有限且唯一的 cap 选择；同原 MOVE 的中心/本地权限与 q、连续控制、初始/中点/终点未消费协议；失落/重放/取消/失败及公开重试；D/R/E0 能力与费用分隔；S/F 语义变化；贪心不完整和整边/前缀费用反例。它没有认定前缀为最终路线，没有启动正式 73 审查，也不增加任何资格门 PASS。

根仍须处理：主平台/授予策略是否采用本候选以及贪心失败是否可接受；73 全文跨节一致性及原每 token 限制的明确替换；控制代际/物理自主事件/付费发布的最终合并；前缀 D 总函数与其维护的全稿定义；公平主预注册及旧整边平台的位置；已发表匹配 lifelong 基线与机制近邻排查；固定 R0 源码/依赖/许可；素材来源链；全部必要顺序审查及根独立验收。不存在以“有完整协议草案”替代这些事项的结论。

实际硬件可信传感/闭环/制动能力、数值谓词实现、收费/容量绑定、载荷与主 roster 均未认证或实例化。H、T_delay、B_CAL、B_max、B*、P_active、density N 保持原含义且未赋值，不给数值、范围、默认值。全程只做文本读取、身份检查和本新文档的 apply_patch 写入；没有实现、构建、创建或运行研究测试、仿真、调参、实验载荷/结果、Git 操作或机器人动作。
