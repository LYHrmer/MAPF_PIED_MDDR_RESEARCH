# 73L3：已发表 lifelong ADG 对照的适配合同与来源下一跳

性质：**NO-SKILL / CONSTRUCTIVE / NONBALLOT / NOT_IMPLEMENTED / NOT_RUN**。执行代理 `/root/source73_external_comparator`。原任务起于 2026-09-08，2026-09-09 根恢复同一任务后完成此回执；文件名沿用原约定，不把后续成文日期冒充首次任务已完成日期。

本报告不是73候选、正式审稿票或资格PASS。维持 **HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT**。只讨论实验前规格与一手来源，不报告性能结果、作者认可或复现成功。

## 1. 本次完成了什么，以及根需作的选择

Hönig等的2019年持久执行框架是实际包含lifelong机制的已发表外部候选。现有一手材料足以明确它的ADG和commit-cut身份，却尚不能绑定2019年完整执行器、仓库驱动及实验版本。不能将目前的作者规划库提交写成论文原实现。[作者论文条目](https://whoenig.github.io/)、[作者全文](https://whoenig.github.io/publications/2019_RA-L_Hoenig.pdf)。

可采用的下一步是明确选择**按论文重构的共同任务域适配**，在实验前冻结算法合同，在未来获准实现后再绑定本项目实现提交和逐规则验证。当前未取得完整作者实现不必成为永远等待的条件；这不证明作者从未公开代码，缺失的规则、接口和公平性也不能用“重构”一词消除。

建议将主外部比较命名为 `H19-PR-COMMON-SYSTEM`：保留可辨认的ADG动作依赖、动作完成状态、前驱闭合commit cut和旧承诺与新规划重叠；允许它使用自己明确登记的规划器产生自己的原动作。共同任务规则、实体模型、可信信息能力、费用及评分相同，轨迹、查询次数、规划输出及闭环派单结果可以不同。它与D-S的差异是**两个完整系统的差异**，不能解释成单一查询策略的因果效应。

若根另选相同PIE来源，则另命名 `H19-PR-COMMON-EXECUTION`，限定为共同来源上的ADG执行框架比较。它能解释执行差异，但不能承担原仓库整法及其任务分配贡献的原样复现。两者不可混名，不能在结果出现后选择更有利的一轨作为“主Hönig”。

本报告同时提出不依赖当前中心资源guard的 `H19-PR-TUBE-ADG` 备选：将共同空间包络的占用次序预编入动作依赖，运行时按付费清空证据解除依赖。它是需另证的保守空间扩展，不能冒称2019论文原法；有可能避免把所有外部方法强制套入当前候选的资源事务设计。

## 2. 一手事实胶囊与实际阅读层级

下列英文胶囊是本报告对作者PDF的事实摘要；后续各节是本项目提出的操作合同及独立推理，不是引述原论文已有的新空间协议。

> Paper facts, paraphrased: The framework accepts single-shot MAPF plans. Actions become ADG vertices; consecutive actions form Type-1 edges, while Algorithm 1 constructs inter-robot precedence edges. Dispatch permits enqueued or finished Type-1 predecessors but requires finished Type-2 predecessors. Successful robot notifications mark completion. Lifelong operation uses a desired commitment set, reverse-graph predecessor closure, and each robot's latest included action as its commit cut. New planning starts from predicted post-cut states with aligned schedule times. Old committed actions continue during planning. The warehouse example includes task assignment, heading and shelf stages. Robots may combine queued moves while still reporting individual completion. Assumptions include limited spatial deviation, eventual action completion and timely notifications; warehouse liveness uses well-known infrastructure. ADG cycles can arise from synchronous rotations. The desired lookahead is domain dependent. The evaluation uses ECBS-TA, Boost graph and separate simulation/robot infrastructure.

定位：作者PDF pp.2–3／提取L147–243为问题与假设，pp.3–4／L274–361为规划接口，p.4／L362–500为Algorithm 1和执行，pp.5–6／L566–668为Algorithm 2和拼接，p.6／L669–735为实现背景。[原文](https://whoenig.github.io/publications/2019_RA-L_Hoenig.pdf)。这段是改述，不是英文原文引用。

| 对象 | 本代理本次实际亲见范围 | 身份与局限 |
|---|---|---|
| 接续HANDOFF、73L1、73L2 | 三文件全文 | 73L1中的PDF/source hash是旧回执实取值；本次不冒称重算了远端PDF或头文件摘要 |
| 73草稿 | 标题/相关关键词定位；随后完整读取实际L175–274 | 覆盖付费作业、角色拓扑、外部、来源及主要预注册段；没有声称读完73全文 |
| 作者PDF | 提取文本L0–872的可见窗口并集，全七页 | 全文文本已读；未做逐图视觉核验；未保存PDF副本 |
| 作者主页 | 论文条目L178–184与库身份L49–56 | 条目列PDF/Video/DOI；库由作者维护；未证明完整执行器从未公开 |
| libMultiRobotPlanning根页 | 目录元信息及README功能/接口说明窗口 | 页面当前main不是2019实验版本；未重读73L1已读的两个头文件 |
| Amazon Science论文页 | 论文条目、下载和摘要窗口L280–332 | 页面标2018，保留与2019卷期的不同来源口径；没有取得新的完整执行器入口 |
| 官方Benchmark-Archive | 根README及2023/2024目录页内嵌README | 含公开结果摘要表的页面被工具一并展示；未用其中结果、规模或时间为本项目赋值，也未打开任何结果压缩包或载荷 |

PDF全文的累计可见窗口覆盖L0–455、L410–554、L506–602、L572–716、L588–733、L701–763、L757–866、L836–872。短窗口或重复打开不被重复计作新全文。工具提取行号不是出版社永久行号。

本次亲见的作者入口：[主页](https://whoenig.github.io/)、[规划库](https://github.com/whoenig/libMultiRobotPlanning)、[Amazon Science条目](https://www.amazon.science/publications/persistent-and-robust-execution-of-mapf-schedules-in-warehouses)。作者DOI链接与一次固定R0 README的网页打开返回Internal Error，未用于否定来源存在。没有重启整轮文献检索或69L1任务。

## 3. 三种身份必须分别登记

| 比较轨 | 保留对象及允许动作来源 | 允许的结论 | 不允许的结论 |
|---|---|---|---|
| `H19-PR-NATIVE` | 按论文重构仓库任务/状态、ADG、cut及原作用域；规划器与自主动作模型单独登记 | 在明确原域内核验所重构规则和原指标 | 作者原字节、原2019实验复现成功；未经证明跨入共同空间误差域 |
| `H19-PR-COMMON-SYSTEM` | ADG+cut骨架；自己的已登记求解器；共同任务接口、实体/信息/费用模型 | 主lifelong域的系统级外部比较 | 将差异全归因于D/R选择器；称保留了已被替换的货架任务分配和连续转向能力 |
| `H19-PR-COMMON-EXECUTION` | 同一原计划来源接口上的ADG+cut执行适配；每次计划仍由各臂闭环输入产生 | 控制来源差异后的执行框架比较 | 共用一个运行后获胜轨迹、抹掉规划成本；称这是原仓库整法原样 |

“共同来源”有两种不能混淆的含义：共同**算法/版本/输入规则**可用于主闭环；所有臂获得逐字相同的**固定计划输出**只适用于事前固定的有限计划补充族。主lifelong各臂的真实进度、已交付任务和cut不同，不可为保持计划逐字相同而偷读另一臂或冻结世界。

源码身份目前只可写为：73L1已核作者规划库commit `4c75fa20c435c440d8b6bd6dc81668ddc7296ba0` 的 `ecbs_ta.hpp` 和 `planresult.hpp`；它们为可候选固定的**求解器组件**，不是完整H19实现。本次没有再次取回这些文件，不冒充新源码审计。[固定ECBS-TA对象](https://raw.githubusercontent.com/whoenig/libMultiRobotPlanning/4c75fa20c435c440d8b6bd6dc81668ddc7296ba0/include/libMultiRobotPlanning/ecbs_ta.hpp)、[固定结果对象](https://raw.githubusercontent.com/whoenig/libMultiRobotPlanning/4c75fa20c435c440d8b6bd6dc81668ddc7296ba0/include/libMultiRobotPlanning/planresult.hpp)。

若采用重构，实验前绑定对象是“论文身份+本报告经根修改接受后的完整规则+求解器组件身份+全部适配差异表”；未来实现提交目前应为 `NOT_IMPLEMENTED`。设计验收不能强求先运行实验来证明版本身份，也不能把尚未写出的实现填成一个虚构commit。原生重构与适配重构需要分别留规则对应记录、失败分类和未来检查合同。

## 4. H19-PR-COMMON-SYSTEM：可提交根选择的完整接口合同

本节均为提议，尚未纳入73。以下逐项列明职责，不代表某具体求解器Environment已经证明或实现；其缺口集中列于§9。

### 4.1 任务、规划输入与动作域

真实任务环境继续使用主方案的唯一真实任务账本、同一分配规则与服务谓词。外部系统仅在自己付费取得的TASK快照中知道已分配任务；不从ADG的预测任务阶段直接改真实账本。任务身份、修订号、机器人归属及完成去重键沿用共同接口。原论文可选的自由任务分配在此被明确限制为共同已分配任务；若要保留自由分配，须另立任务分配系统比较轨，不能静默改变主source语义。

推荐首先评估作者固定ECBS-TA组件接入共同平移域：其Environment仅允许共同图上的合法相邻MOVE及原地WAIT，任务分配候选集固定为已由共同A分配给各机器人之任务；返回的动作可来自该外部求解器，**不受“仅PIE可产生新MOVE”的本方法内部限制**。该例是候选接口选择，尚未冻结Environment、搜索边界、clock/RNG及退出合同。任何其它求解器必须在外部轨名称和来源表中明确替换，不能结果出现后静默换算法。

共同平移域没有任意姿态/转向/Attach/Detach/Yield模型时，不把原仓库动作译成免费MOVE或零代价服务。外部适配使用共同平移动作语义，明确删除了货架和朝向状态；native另保留它们。若任务要求旋转或货架而公共世界未定义，对该native比较记 `DOMAIN_NOT_MATCHED`，不得修改主任务使之适用。共同系统轨的任务完成仍只由真实TASK_SERVICE决定；到达参考终点、ADG finished和完成任务是不同事件。

每次规划输入由中心付费捕获：图/动作定义的固定版本、已交付任务修订、机器人计划身份、中心已知完成下界、旧承诺cut及预测cut后状态。预测状态来自已承诺原动作的符号终态；它不冒充当前真实位置，也不消耗未来真值。每个输出动作记录外部source_job、输入版本、输出位置、plan_epoch/action_index及原起终点。中心只做合法性、冲突、cut拼接和授权核验；不能替外部求解器造一条更有利的新边。

没有新任务知识的机器人可保留已有合法承诺和原地HOLD；不通过未来任务真值补齐目标。已知任务在计算期间真实完成的情形，由共同任务修订接纳规则处理：旧动作若仍合法可继续，其任务关联不能再次服务；需要新目标的后缀另付费重算。不能以“任务必然将完成”的预测提前获得新的真实派单。

### 4.2 动作状态、队列与共同物理底座

动作至少保存 `staged / enqueued / finished` 的外部ADG状态，另外记录共同执行事务的授权、安装、在途、terminalpending与消费状态。这些状态不能压成同一个整数。ADG允许入队只给出逻辑次序许可；执行器实际运动还须具有当前动作/前缀完整资源覆盖、正确围栏身份及共同控制守卫。

中心保留原Type-1/Type-2使能差别。Type-1已入队可以允许后续动作加入逻辑队列；它不允许本地提前消费前一动作或越过尚未安装的共同授权。Type-2依赖的finished必须来自已交付并验证的可信完成证据；POSITION下界、普通COMPLETE、超时、重试耗尽、计划预计到达时间均不能替代。

实际MOVE由各自固定外部求解器提供，几何参考保持这同一条MOVE。共同guard可延迟、HOLD、制动、在已闭合前缀合同中单调扩cap，但不得改目标、重路由或把一个原MOVE伪造为多个已完成任务。若最终主方案采用同MOVE前缀cap，则所有共同适配臂获得同一物理cap能力、安装/恢复条件和可购买证据接口；ADG是否允许当前动作入队仍是该算法的决定。不给D-S独占更强的底层控制，也不为“能力相同”强迫外部购买无用途的中途POSITION。

原生连续多MOVE排队与共同逐边零速/terminalpending交接不同。共同系统轨应明确报告该约束及其额外成本；不能把它产生的停顿写成2019方法固有缺陷。若要允许跨边连续运动，必须先给所有共同臂一个包含后继整段覆盖、制动及取消的统一控制合同；当前文档不能以公平为名跳过这项证明。

### 4.3 END应保留事件推送选择，不能强迫基线等待轮询

提出共享的 `PAID_PUSH_END` 能力：每次自主END只产生绑定旧动作身份的本地不透明待处理token；本地server在实际获得容量后付费CAPTURE并封装不可变终点记录，随后走付费消息通道、中心接收/验证及原子发布。END发生到封装、投送、中心finished之间物理和队列继续推进；消息丢失可按共同有限重试合同重发保留的同一记录，不能重新免费采样全局状态。

该能力须向D/R/E0及共同外部臂同时开放，实际选择和费用按各臂自身发生的工作计。外部方法可只用结束事件推送而不主动查POSITION；把它强制改为先等待本项目的定向/轮询ENTRY机会，会人为引入原框架没有要求的等待。若最终共同服务只提供轮询，必须明确这是额外受限实验轨，不能将其作为未经解释的最强主外部适配。

可信END仅证明该动作满足共同终点/保持条件。它不能自动释放终点resident，更不能自动推进执行器私有游标；中心finished、READY发送、本地一次消费是不同付费转移。终点仍可能阻塞他者，必须由后续合法动作或共同清空证据处理。普通误报隔离是共同可信底座的职责，其收益不归主动查询策略独占。

### 4.4 有在途动作时的commit cut与拼接

中心在自己的单writer作业中付费捕获已提交ADG、已知动作状态及所有不可撤回承诺；cut的种子集合必须包含这些承诺，再加入公开、冻结的desired-set规则所选动作。对该集合取ADG前驱闭包，按每机器人原动作序取最末成员作为cut。不存在未完成动作的机器人使用一个**状态锚点**表示其已认证/被覆盖的驻留，不虚构一条可服务的新MOVE。

cut安装是中心原子提交：冻结本轮承诺边界和计划版本；此后旧计划只准继续到cut，不再向cut外授予或入队新动作。已发出的命令不能被新cut追溯撤回，因此cut不得落在这些命令之前。中心不持writer等待本地ACK或planner；已承诺prefix继续按原物理责任执行。切换期间终点、途中HOLD及丢包都不令责任消失。

规划worker从各cut符号终态求后缀，使用一致的计划时标接缝，并将新旧所有必要依赖联合检查。不能仅凭时标平移声称新的空间guard或扩展动作也满足原拼接结论。发布前须确认：旧承诺字节未变；新首状态等于对应cut终态；所有MOVE合法；已知任务关联可接纳；不存在新后缀指向仍须执行旧承诺的依赖；合图满足所选无环/循环处理合同；公共几何/控制条件成立。

新结果无效、超时、源异常或过期时只丢弃未提交后缀并保留全部已付费用。旧cut责任继续；到cut后没有合法新动作则持续HOLD并按共同停止/未完成口径计分。重试必须由后续有容量的机会触发，不在失败回调无限重算。新结果成功也不要求所有机器人同时物理到达cut；各机器人只能在自己的旧序列完成且新跨图依赖满足后进入新后缀。

desired-set和触发规则本身仍需根唯一选择并冻结，不能只写“适当前瞻”或借用保护参数改义。本报告不赋lookahead、规划预算、容量或任一保护参数的数值/范围；论文实验中的经验设定不自动成为本项目默认值。固定规则可以依赖已付费可见的剩余计划、已登记规划预算及过去已付费耗时，但不能使用本次未来求解耗时或别臂结果。选择规则未定时该比较仍是未固定候选。

### 4.5 取消、错误与总分类

在本次共同主域内，新增未知障碍若未被主世界/控制协议建模，就不能拿原论文障碍处理功能直接补进主profile；其native能力单列。普通反馈错误/延迟/丢包不改变实体空间扰动，二者分别遵守同一外生机制与证据接口。

未发、仅逻辑入队、已发送、已授权、已安装及已在途动作须区别处理。不能因为原native允许清空命令队列，就删除已获得运动权限的共同覆盖。任何撤回须有共同取消吸收性与围栏证据；不可撤回的旧动作继续到合法END/HOLD。中心故障只停止新发布，不回滚已提交状态；本地no-start不等于已终结；普通通知重放不能完成另一代动作。

所有条件有明确退出类：输入/源定义域错误、无可用计划、ADG环、共同资源停滞、任务修订失效、计算超时、通信不可达、可信/物理越界、不可核验日志分别保留原因。无环只说明该图没有结构环，不代表共同guard没有新增等待环；动作最终完成、反馈最终可达和server公平供给不是在含永久故障profile里免费成立的条件。

## 5. 全费用、共同机会与系统级评价

公平性是相同能力、同一计费/队列规则和完整总体，不是强迫各系统执行相同次数的操作。拟复用73的角色图：中心作业在Srv_C，本地捕获/验证在Srv_E(i)，原求解在Srv_P，任务复制在Srv_A，传输在Srv_N；原生整法若需要不同拓扑，另列实际结构和共同硬件供给，不凭相同单价声称算力相同。

| 外部工作 | 信息来源与应付费用 | 不允许的免费捷径 |
|---|---|---|
| 任务/状态输入 | 本臂已付费快照；复制、解析、身份和修订核验 | 直接看真实任务A、真实位置、执行器私有游标或另一臂输入 |
| ADG构建与维护 | 原计划结果；枚举动作、依赖检查、索引、增删、内存与GC | 把全图重建写为常数管理费；把预处理移出共同E |
| 使能与dispatch | 已交付finished/队列记录；扫描或增量维护、入队消息 | 全局免费扫描；给外部每轮免费读取全部机器人 |
| cut构造 | 本臂当前图与已提交承诺；desired-set、闭包、边界封存、输入复制 | 在planner外用不计时线程计算；先看真实完成再补费用 |
| 原求解/拼接 | 外部worker私有源状态及clock/RNG；搜索、验证、过期丢弃 | 引用外部原法名而只计内部D/R计算；免除失败搜索 |
| END与可选POSITION | 实际采样/冻结记录；触发管理、CAPTURE、封装、字节、各接收端验证 | 假定native回调即中心瞬时知晓；把签名当物理清空 |
| 共同安全服务 | 本臂实际几何、grant、cap、取消、保持、发布工作 | 只对新方法收费或只给新方法免费预建表 |
| 真实服务和留证 | 同一物理/任务环境规则；环境与宿主成本另完整记录 | 用ADG完成数替真实任务数；把算法选择工作塞进评价器 |

主外部估计应在与主D/R相同的固定roster、空间/信息profile、固定评价窗和真实Q/E上报告D-S与H19适配的配对差异，保留初始化未成、规划失败、停滞和不可核验。外部不成功不能从主分母排除。若原任务域不适用，应预先报不可匹配范围；不得把事后共同成功子集命名为主公平域。

D-S−R-S仍回答定向取证相对强轮询的问题；D-S−E0仍回答付费途中观察的实用性；D-S−外部系统回答完整方案是否具有竞争力。它们不是互相可替换的证据。若外部换自己的求解器或cut，性能差含计划质量、规划延迟、承诺策略、观测和执行保守性的共同作用。需要解释归因时另用预先登记的共同来源执行比较，而不能将系统级显著差称单因素消融。

大规模优势不能由短场景中较早释放一次空间推出。要同时保留“终点推送已经足够便宜”“外部任务/规划更强”“D查询开销压过节省”“ADG依赖过保守”“整边预留或cap新增环”等零/负效果。外部方案的旧原域原样失败或源码异常须独立分类，不算新方法的科学获胜。

## 6. 不依赖当前中心资源guard的候选：H19-PR-TUBE-ADG

该备选仍使用一个付费ADG执行监视器，但**不以当前owner账本/PRECHECK/grant/动态退休器作为运行时安全证明**。下列是独立保守扩展合同，不是原论文定理，也不是已经合成出的可运行算法。

1. 对每条外部原动作，按共同实体、横向误差及完整运动/制动/HOLD条件计算保守空间覆盖。必须包括起点resident和终点驻留；不能只列离散vertex/swap。计算和检查付费，几何不可决定时返回UNKNOWN。
2. 对每机器人、每资源，把连续占用的原动作及驻留阶段合并成一个访问段。其开始标记是首次可能进入资源的动作START；其清空标记是某个已完成动作的终态保持包络与该资源确实不交。**到达一个仍占资源的终点不能作为清空标记。**初始访问段在初态已经开始；末端驻留没有合法离开动作时保持未清空。
3. 对不同机器人的冲突访问段，按已登记原计划次序规定先后，将“前访问段可信清空”作为后访问段首次START的依赖。原Type-1/Type-2边保留；新增几何边显式标注。若无法为全部冲突访问段确定一致次序、存在环、或一个永久终点访问阻挡后继，则该候选计划不可发布，按公开失败/重规划合同处理；不能默认资源实际上不会碰上。
4. 运行时只有依赖已通过付费证据解除的START可执行；初态访问由共同不交HOLD条件认证。每个已开始访问的实际实体支持均在自己的覆盖内，在可信清空之前继任访问不能开始。因此对任一资源归纳：不存在两机器人同时具有活动访问段；若覆盖对连续实体占用完备，则得到碰撞排除的**条件性**结论。消息延迟只延迟许可；伪造可信清空或越出覆盖不在该结论内。
5. lifelong拼接保留已开始及已承诺的访问段和次序。只将新后缀接在相应cut终态之后；联合检查空间访问依赖以及不得出现新后缀要求旧承诺倒退的条件。原先未定何时离开的末端驻留，只有新计划中明确的合法离开动作完成后才能解除；添加未来离开动作不等于立即清空。新拼接不合法则继续原HOLD，不拆除旧责任。

这个构造提供一个可纸面检验的安全方向；它可能更保守、更难规划，不能据此声称已有持续进展或强性能。其访问段构造、冲突覆盖完备性、拼接总函数、选择次序及付费服务仍需独立完整规格与证明。它尤其不能用“所有机器人最终都结束”越过永久驻留、失联或循环。

公平接入时，它与其它系统共享同一实体控制、可信捕获/消息能力和服务器总供给，按自身ADG几何工作付费；不强制它额外运行当前候选owner账本。相应安全证明若成立，当前中心guard的存在就不是所有外部对照的先验条件。若最后仅选共同guard轨，也应明确评估对象是在该底座内的执行策略，而非穷尽全部安全协调架构。

## 7. 原实现缺失时的准确重构与未来核验合同

保留 `PAPER-RECONSTRUCTED` 不是降低标准。每条保留规则需对应原文位置，每条新增/替换规则需对应本项目规格与证明，混合路径需要显式表。未来获准后才执行以下未运行核验；本报告不创建测试代码、测试载荷或运行结果。

- 对动作状态和Type-1/Type-2使能，分别核验队列提前入队、跨机器人完成依赖、失联、重复及旧代通知，不仅检查最终碰撞数。
- 对cut，核验包含已承诺动作、前驱闭合、各机器人锚点、计算期间继续执行、边界冻结、无新到旧依赖及过期结果费用；不能只检查新plan首坐标。
- 对native，共同适配关闭后按原任务/动作域保留其队列能力；对common，则逐条核验改动而不冒称原样。
- 对整体实际实现，登记本项目commit、各外部组件commit、环境动作/任务合同、入口、依赖/clock/RNG及合法退出定义；未知字段不得靠源码默认替填。
- 任何未定义读取、空计划、构建/输入失败、合法规划超时、静态不适用、运行中停滞均有不同分类。零返回码和论文相同算法名不等于保真。

当前最小的可纸面关闭路径是：根选择轨名和求解器→补齐Environment/入口与desired-set总规则→完成空间/任务/费用适配证明→根不使用skill直接分析→依规定顺序正式审查。真正实现一致性属于后续阶段，不能在HOLD下为提前取得它而运行代码。

## 8. 73L2之后实际取得的素材来源下一跳

本次从公开检索和官方组织入口定位到 [MAPF-Competition/Benchmark-Archive](https://github.com/MAPF-Competition/Benchmark-Archive)。根README自述按年度发布竞赛实例、最佳已知解和问题生成脚本；2023/2024年度README分别指出随start-kit发布的Example Instances。该组织主页回链League官网。这是可引用的**官方归档关系**，不等于R0具体素材身份或授权范围。[组织入口](https://github.com/MAPF-Competition)、[根README](https://github.com/MAPF-Competition/Benchmark-Archive#readme)、[2023说明](https://github.com/MAPF-Competition/Benchmark-Archive/tree/main/2023%20Competition)、[2024说明](https://github.com/MAPF-Competition/Benchmark-Archive/tree/main/2024%20Competition)。

实际 `rtk git ls-remote` 退出0，得到 `refs/heads/main = 25ffd5b6a39b6fe30e5bc6cb5e22720a9531ea8a`。这仅是远端ref身份，不证明此前网页展示的README字节就是该commit下的对象，也不提供README blob/hash或素材许可。

原任务在读取固定tree元数据时，普通curl连接失败，随后升级工具调用返回 `aborted`。没有tree正文、退出成功或完整报告，不能把那次调用当完成、仍运行或通过。根后来仅恢复状态，本代理明确报告73L3不存在；此次获得继续授权才恢复写报告。

此次恢复后只增加下列**两个精确元数据下一跳**，随后停止取源：

| 对象 | 精确URL | 实际结果 |
|---|---|---|
| 固定根README | https://raw.githubusercontent.com/MAPF-Competition/Benchmark-Archive/25ffd5b6a39b6fe30e5bc6cb5e22720a9531ea8a/README.md | web返回Cache miss；正文未取回 |
| 固定根tree，非递归 | https://api.github.com/repos/MAPF-Competition/Benchmark-Archive/git/trees/25ffd5b6a39b6fe30e5bc6cb5e22720a9531ea8a | web返回non-retryable safe-open错误；tree对象未取回 |

没有第三次下一跳，没有重复原先recursive tree调用，没有从年度目录打开map/scen/config/agent/task/delay正文、生成器脚本、输出压缩包或图片地图。早期League instructions网页直接抓取仍是JavaScript壳；搜索结果可见部分竞赛说明，但未当作固定历史素材授权。

现在已知链条是：R0固定README在73L2已有League链接；官方组织与Benchmark-Archive有归档说明。**仍缺R0固定blob→某年上游固定blob→复制/转换/生成记录→适用许可的逐对象连接。**年度名称、相似文件名及竞赛结果类别均不能补这条连接。也未得到Benchmark-Archive针对地图、任务或生成材料的独立许可正文；根目录页面未显示LICENSE不证明全仓或上游无许可。

最短关闭动作是向未来来源核验提供公开的对象级NOTICE/出处表，或在获准仅取身份时核对两端固定Git blob元数据。若blob完全相同，它只能证明字节一致，仍需把适用许可和独立原素材权利声明连上；若不同，则需实际转换/生成出处，不能靠推测。本阶段不生成source_id、manifest或roster；也不联系维护者或将私有材料发给任何人。

## 9. 可接纳的新结论与明确未闭项

| 项目 | 当前结论 | 最小剩余工作 |
|---|---|---|
| H19确有lifelong ADG/cut骨架 | 有作者全文依据 | 不再以single-shot内部求解器否定其lifelong身份 |
| 完整2019作者执行器及实验版本 | UNKNOWN | 取得明确原入口/版本；或者根明确接受paper-reconstructed轨并补规则对照，不冒称作者实现 |
| 外部不只使用PIE动作 | 已给系统级合同 | 根选择具体外部求解器与动作Environment，完成来源/接口域核验 |
| 主共同任务与货架native任务映射 | 共同轨改动已显式化；native同域未证 | 冻结共同已分配任务接口、真实服务及输出动作合法性；不靠虚构货架补齐 |
| 完成信息成本 | 已提出共享付费END推送 | 根核其与现有73服务器、phase、terminalpending及各臂能力真正组合 |
| commit-cut运行协议 | 已给承诺、边界、输入、拼接、失败职责 | 固定desired-set/触发总规则与时间/任务接纳细节，做全文独立分析 |
| 共同guard适配 | 具明确保留/变化合同 | 与最终整边或cap方案逐项组合证明；不得由NONBALLOT相加判通过 |
| 独立于当前guard的空间ADG | 有条件构造方向 | 完整访问段/跨窗拼接规格与证明；未实现、未授活性保证 |
| 固定Benchmark-Archive ref | 已实取main完整hash | 固定README/tree读取失败；不能补造blob/许可 |
| R0四类及agent/task/delay出处/许可 | UNKNOWN | 固定对象级来源表、转换/生成关系与对应权利声明；不能用MIT或数据库许可自动覆盖 |
| 公平大规模竞争证据 | 未获得结果，且目前不应运行 | 先冻结设计/身份/总体与费用合同，完成规定审查；未来再在授权阶段执行 |

本报告可支持根把笼统“外部候选待适配”推进到明确轨名、能力与费用选择；它不能单独关闭G4/G5/G6，不能把当前guard路线指定为唯一最终答案。原72的最新有效门状态和73未固定未送审状态保持不变。

## 10. 实际动作与边界回执

原任务开始时、此次恢复写入前分别对精确73L3路径执行不存在检查，均退出0。仅用apply_patch创建本报告；没有改73草稿、73A1、任何冻结文件、Git索引或远端。没有读取35–38、Q-CAL/Q-CONFIRM或桌面；没有本地全目录扫描。

所有shell及管道段均以rtk起首，原始文本用rtk proxy。早期批量工具输出有截断：73L2随后单独重读全文，73相关节随后以L175–274完整窗口读取；没有拿截断摘要充全文。外部网页阅读与实际失败在§2/§8完整列明；本报告没有使用skill、Claude或新增子代理，不是正式导师/Nature/逻辑审查回执。

没有实现、构建、创建或运行研究测试、仿真、调参、产生实验载荷/seed/O/tape/manifest/结果或实车动作。H、T_delay、B_CAL、B_max、B*、P_active、density N保持原义未赋值，不给默认或范围。报告写入后仅对本文件做全文/身份静态核验，最终SHA256、bytes、lines随交付给根，不在正文写自引用摘要。
