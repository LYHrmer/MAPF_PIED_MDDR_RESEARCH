# C23 — constructive probability audit of a fixed paired target under host dependence

2026-09-11. New bounded NONBALLOT advisory, not a formal review. Use no skills, tools, files, network, MCP, shell, agents, or persistence. Answer only from this self-contained mathematical contract. Do not implement, give executable pseudocode, simulate, generate data/configuration/calendars/seeds, or assign any experimental parameter. Use at most about 700 English words and finish with exactly END-C23. Give mathematical conditions, not a pass vote.

The root-authorized source is draft 73, SHA256 9e06199cc6045ddd750b861b3091ec239c7c6ff2ae7eeaebbdd0e7fde61ec113, 216498 bytes/665 LF; its entire sections 11–12 and C8SR have been independently read. It has not been fixed/reviewed/accepted. HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT remains. This topic differs from completed C22 source lifecycle advice; do not reconstruct other consultations.

Fix the full preregistered block roster and design X, original nonnegative weights w_b summing to one, original exposures E_b>0, and original pair D/R in every block. Observed paired d_b=(Q_D,b−Q_R,b)/E_b lies in [−U_b,U_b], U_b=Qmax_b/E_b known from the future fixed public calendar. The sole target and estimator are theta=Σ_b w_b E[d_b|X] and Delta=Σ_b w_b d_b. Targets include all original failures under a total score; missing required scores make the entire comparison UNESTIMABLE, never success-selection or renormalization. Published intervals have an unconditional erroneous-publication bound, not coverage conditional on availability. The manuscript currently uses independent block innovation roots and Hoeffding radius sqrt(log(2/alpha)/2 × Σ_b(2w_b U_b)^2); within-block dependence is unrestricted. alpha is symbolic here. Original protected design parameters stay unassigned.

A proposed source service computes a sealed complete nonpreemptive body using actual host monotonic elapsed, publishes only at a legal original later calendar row, and retains its source private history within an arm. The body's elapsed and result can depend on previous arms and blocks through cache, temperature, OS scheduling, allocator state, and external load. Shared dependence is not removed by independent block RNGs or random D/R execution order. No source backend independence evidence currently exists. Do not silently replace theta by a random history-conditioned compensator, randomize only one arm, condition on an entire realized host trajectory, or use an uninformative whole-range interval as closure.

My independent hypotheses to challenge: (i) predeclared independent clusters C_g of the original blocks preserve Delta/theta without reweighting; arbitrary within-cluster dependence gives cluster span 2 A_g, A_g=Σ_{b∈C_g}w_b U_b, and radius sqrt(2 log(2/alpha) Σ_g A_g²). Clustering cannot manufacture independence, and a single common-host cluster can destroy informative inference. (ii) Revealing independent primitive innovations can instead form the Doob martingale M_j=E[Delta|X,Z_1,...,Z_j]. Then M_0=theta and final M=Delta; predictable conditional ranges c_j for each increment yield a bound around the original theta if Σ c_j² has a known deterministic upper bound. The c_j must include effects on ALL downstream blocks, not merely one block. This does not substitute a running conditional-mean target. (iii) A weaker direct martingale argument with original widths works if E[d_b|X,F_{b−1}]=E[d_b|X] almost surely; mere random arm order does not ensure this. (iv) Independent reset capsules for each entire paired block are a prospective execution-design change: keeping roster/weights/E/formula does not itself prove unchanged per-block marginal laws or numerical theta. A new host law must be declared honestly.

Please give: one smallest symbolic counterexample demonstrating why boundedness and randomized arm order alone cannot recover the original block-independent radius; identify any flaw in (i)–(iv) with exact sufficient conditions and a short derivation that retains theta; then recommend the narrowest genuinely qualifiable next design obligation, distinguishing a statistical regrouping from a changed execution law. If no presently evidenced option closes inference, say so and specify what additional evidence is necessary. No generic mixed-model/bootstrap invocation without its assumptions; no claims that a physical reboot or nonsignificant autocorrelation test proves independence. All numbers in a symbolic counterexample are mathematical proof objects, never experimental assignments.
