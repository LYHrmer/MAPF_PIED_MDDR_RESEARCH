# 73P1：前缀资源区间、授予与实体覆盖的组合构造

日期：2026-09-07。性质：独立建设性数学回执；NO-SKILL / NONBALLOT；供根任务选择的构造候选，不是正式审稿。

本次只使用任务提示、根任务对几何条件的明确补充，以及本人已完成的 73M1 数学构造。没有读取额外本地文件或网络，没有使用 skill，没有运行研究代码、测试或仿真。写入前确认本文件不存在；未修改 73M1 或其他已有报告。本报告不声称构造已经实现、具有新颖性、保证全局活性或已获固定采用。

## 1. 原命题的缺口与本报告采用的修订条件

原提示仅规定“固定有限闭瓦片覆盖工作域”。**仅闭性不足以使相交进度为单区间**：即使 K 是凸有理多边形，非凸闭瓦片也能使相交进度分离。因此原命题 1 不能原样作为后续事务证明的区间基础。

根任务随后明确允许以下修订，本报告将其作为新的显式构造条件，不声称旧提示已经给出：瓦片是固定有限个闭凸有理矩形；K 是紧的凸有理多边形，或有已给定有限凸多边形分解的紧集。后者逐片计算后合并成有限闭区间并集。更一般的闭凸瓦片也足以证明单个凸片的区间性；“有理矩形”是本构造选择的充分表示条件。

任意紧非凸 K、任意闭非凸瓦片都不能仅凭“有限个资源”推出有限个相交区间。若要把它们纳入有限事件计数，必须另有有限凸分解、有限多边形描述或其他能保证有限区间性的明确条件。

## 2. 几何、控制权限和资源记录

固定同一原边 u→V，ell=||V−u||>0，e=(V−u)/ell；以 V 记原终点，避免和参考速度混淆。原参考坐标为 s，实体为

\[
X=u+s e+z+F,\qquad z\in Z,\qquad K=Z\oplus F.
\]

实体通常是 u+s e+K 的真子集：当前 z 是一个实际误差状态，K 则覆盖所有允许误差。下文关于 K 的相交与 support 计算属于包络几何，不能自动当作任意当前实体的等式。

沿用 73M1 的原 LAUNCH / APPROACH / BRAKE / HOLD 动力学、固定原边、同一世界坐标 z 闭环、理想参考零速保持及其合法初态和非聚积执行条件。其连续结论是

\[
0\le s\le C\le\ell,\quad v\ge0,\quad
v^2\le 2a_{\rm hi}(C-s),\quad z\in Z,
\]

其中 C 为机器人当前已安装的本地 cap。cap 更新不改变 s、v、z、原边、原目标或原 MOVE 身份；BRAKE 优先保持到 HOLD；中点 HOLD 不完成原 MOVE。

中心对这一次原 MOVE 的当前已提交记录为 (alpha,q,b,S)，其中 alpha 绑定物理机器人、session、tid 和原 occurrence；b 为中心已经兑现资源保留的 cap，q 为中心已提交的可信前沿，S 为该 scope 的资源集合。必须保持

\[
0\le q\le s\le C\le b\le\ell.
\]

定义

\[
U(q,b)=\bigcup_{\rho\in[q,b]}(u+\rho e+K),\qquad
\operatorname{Mask}(A)=\{r:A\cap T_r\ne\varnothing\},\qquad
S=\operatorname{Mask}(U(q,b)).
\]

固定瓦片族 {T_r} 覆盖工作域 W，并要求所有实际实体都在该公共覆盖域内。仅覆盖参考中心、却让实体伸到覆盖域外，不足以支持本文的碰撞排除证明。瓦片允许在闭边界重叠；Mask 包含所有有非空交集的瓦片，包括只接触边界的交集。

中心账本对每个资源 r 最多允许一个物理机器人 owner。scope 身份 alpha 进一步确定哪些 grant 可以由哪个 session/原 occurrence 使用。若同一机器人的 resident、旧边终点和下一边 scope 暂时重叠，账本需要保留它们的联合需求；删除一个 scope 的需求不能误删另一个 scope 仍需的资源。可用同一物理 owner 下的 scope 集合表达；若 owner 标签直接含 occurrence，则需要等价的原子 retag 交接。这是组合前提，不是本报告提供的实现。

身份绑定必须使一个有效 owner/grant 对应唯一物理机器人；“两个物理机器人共享同一个有效 owner 身份”的情况不能由每资源唯一字符串 owner 排除碰撞。

## 3. 相交进度集合及闭边界

对资源 r，定义完整原边上的相交进度

\[
I_r=\{\rho\in[0,\ell]:(u+\rho e+K)\cap T_r\ne\varnothing\}.
\]

### 3.1 紧性与凸性的证明

考虑

\[
A_r=\{(\rho,k)\in[0,\ell]\times K:u+\rho e+k\in T_r\}.
\]

因为 [0,ell]×K 紧、T_r 闭且映射连续，A_r 是紧集；I_r 是其坐标投影，所以 I_r 紧，可为空。这里必须保留有限原边 [0,ell]；不能把无界直线上可能无界的相交进度也称为紧。

若 K 与 T_r 均凸，则取 rho_1,rho_2∈I_r，分别有 k_1,k_2∈K 使 u+rho_i e+k_i∈T_r。对任意 lambda∈[0,1]，令

\[
\rho_\lambda=(1-\lambda)\rho_1+\lambda\rho_2,
\qquad k_\lambda=(1-\lambda)k_1+\lambda k_2.
\]

凸性给出 k_lambda∈K 且 u+rho_lambda e+k_lambda∈T_r，所以 rho_lambda∈I_r。故非空 I_r 是闭区间 [a_r,b_r]，包括 a_r=b_r 的单点相交。

等价地，I_r 是线段 u+[0,ell]e 与 T_r⊕(−K) 的交集在进度坐标下的原像。需要的实质条件是这一沿边交集为凸；瓦片与 K 都凸是简洁的充分条件。

### 3.2 单区间的精确 Mask 公式

对 0≤q≤b≤ell，

\[
r\in\operatorname{Mask}(U(q,b))
\iff I_r\cap[q,b]\ne\varnothing.
\]

若 I_r=[a_r,b_r] 非空，则

\[
r\in S\iff q\le b_r\ \land\ b\ge a_r.
\]

若计算本地潜在包络 U(q,C)，相同公式把中心 b 换成本地 C；它不是中心 S 的定义。中心 scope 使用 b，因为已经发布但尚未接收的 cap 增量仍可能稍后成为本地实权。

进入资源的边界是 b=a_r，已经需要占有该资源；退出资源的边界必须是 q>b_r。q=b_r 时，[q,b] 仍与 I_r 接触，不能退休。把闭不等式改成严格相交、忽略单点区间或在 q=b_r 提前释放，都会遗漏合法的接触实体。

### 3.3 有限凸分解与非凸情形

若 K=union_{h=1}^{d} K_h，每个 K_h 为紧凸多边形，而瓦片凸，则

\[
I_r=\bigcup_{h=1}^{d}I_{r,h},
\]

每个 I_{r,h} 为空或闭区间。合并相交或接触的区间后，可写成

\[
I_r=\bigcup_{k=1}^{m_r}[a_{r,k},b_{r,k}],\qquad
b_{r,k}<a_{r,k+1},\qquad m_r\le d.
\]

于是

\[
r\in S\iff\exists k:\ q\le b_{r,k}\ \land\ b\ge a_{r,k}.
\]

不能把整个 I_r 强行取最小端点到最大端点的一个 interval 后仍声称是等价公式；那样填入了几何空隙。取凸包区间可作为更保守的过度预留，但必须明确其不再精确，且改变资源占用与费用问题。若瓦片也采用有限凸分解，则对 K 片与瓦片片逐对取区间，再合并；没有有限分解条件时，不能套用此有限事件算法。

有理 K 与有理矩形不单独保证以物理弧长 s 表示的端点是有理数，因为 e 的归一化可能含无理数。若另有 u、V 坐标有理，可以改用 lambda=s/ell、p=u+lambda(V−u)，在该参数下得到有理多面体投影端点；这一表示条件是可选的附加事实，本提示未给出时不能默认。数值精度和稳健谓词的实现仍未知。

## 4. 可信 q 的来源不能循环依赖碰撞排除

每个进度样本有同一 alpha 的可信事实 q_j≤s(t_j)，且样本时刻确实先于中心采用它的时刻。中心验证后取

\[
q'=\max(q,q_j)
\]

或对若干已验证样本取同样最大值。因为同一原 MOVE 内参考 s 单调，验证和提交时已有 q'≤s(t)，并且以后仍 q'≤s(t')。结合本地与中心 cap 不等式，还得到提交之前 q'≤s≤C≤b。

样本迟到仅使 q 可能更保守；重复样本不增大最大值；错误 session、tid 或原 occurrence 的样本不能参与该最大值。机器人到达新原边时 s 可以重新记为零，因此旧 occurrence 的进度结论不能跨边沿用。

本证明直接使用可信样本下界与 73M1 的参考单调性，不从“目前未碰撞”“资源账本无冲突”或普通 pose/COMPLETE 反推 q 的可信性。普通反馈若不具备这一保证，即使 seq/tag 合法，也不能成为退休事实。

## 5. cap 扩展和 q 退休的一个串行原子更新

### 5.1 已提交状态与候选集合

设当前已提交 scope 为 (alpha,q,b,S)，且全局 owner 账本一致。候选更新采用经过独立验证的 q' 和拟扩展 b'，满足

\[
q'\ge q,\quad q'\le s\le C\le b,\quad b'\ge b,\quad b'\le\ell.
\]

从同一个当前串行快照计算

\[
S'=\operatorname{Mask}(U(q',b')),\quad A=S'\setminus S,\quad D=S\setminus S'.
\]

q 只前进时 S'⊆S；b 只增大时 S⊆S'；二者一起改变时两个方向都可能发生，因此不能把组合更新简单当作集合只增或只减。由于可信 q'≤旧 b，且 U(q,b')=U(q,b)∪U(b,b')，还有 A=Mask(U(q,b'))\S：凡是不在旧 scope 而由扩 cap 新增的资源，都来自 (b,b'] 的进度，仍被 U(q',b') 包含。因此组合退休没有偷减“先保留旧 q 至 b' 扫掠中的全部新增资源”这一要求。

### 5.2 原子事务的语义

一个成功提交必须原子完成以下整体：确认候选仍绑定同一有效 alpha、当前记录仍为所据快照；确认 A 中所有资源均 free 或由同一物理 owner 以兼容 scope 持有；把新增需求 A 计入 owner 账本；把本 scope 的集合替换为 S' 并提交 q'、b'；删除 D 中本 scope 的需求，但只在该 owner 的其他有效 scope 也不再需要它时才释放该资源。任何新增资源有冲突，整个组合更新都不提交。

中心可以随后另行提交一个只退休 q 的事务来减少保守占有，但那是重新验证后的独立原子更新；不能把“整体失败”表述成暗中只执行了一半成功。此处不要求某种实现框架，只要求对所有观察者存在一个满足上述语义的串行线性化点。

认证 cap 增量 b' 只能在成功提交之后发布，绑定 alpha 及一致的版本/seq。这里“先预留后发布”指全部 S' 需求已经得到账本兑现，不能只把 b' 写入计划字段就把它当 grant。中心 q 的新发布状态也以本次提交为准；对机器人而言，知道 q 并不是继续运动的必要条件。

### 5.3 写入前、线性化点和写入后均覆盖实际实体

提交前有

\[
q\le s\le C\le b,\qquad X\subseteq U(q,b).
\]

同一时刻，经过验证的 q'≤s 且 C≤b≤b'，所以还已有

\[
X\subseteq U(q',b').
\]

因此当前实际实体位于两个包络的交集中。提交前旧 S 全部保留；提交后新 S' 全部保留；线性化时连续 s/v/z 不跳变。对任何要释放的 r∈D，由 r∉Mask(U(q',b')) 可知当前 X∩T_r=∅。在中心 cap 保持 b' 的后续时段，本地接受任何不超过 b' 的已授更新也仍有 q'≤s≤C≤b'，因此不会重新需要 r。

未来若中心继续扩大 cap，r 可能因另一个分离的相交进度区间而重新出现于 A，必须重新授予；不能把一次退出标成永不再出现。只有 q 已越过 I_r 的所有区间，才能说同一原 occurrence 以后再也不需要该资源。单区间时，q>b_r 就给出这种永久越过的充分条件。

成功发布以后，本地可任意迟到接收、重放或永不接收。未接收时仍 C≤旧 b≤新 b；接收合法 b' 时，73M1 的单调 cap 接入保持控制不变式，并且 C_new=b'≤当前中心 b。较旧认证 b' 若仍不小于当前 C，也不超过中心后来单调增长的 b；较小者被拒绝，错误 alpha 不能进入控制。

这种覆盖不要求中心等待本地 ACK 才能退休**可信 q 之后已不再需要的后方资源**。但它也不允许因为没有 ACK，就把已经授予的前方 cap 收回或重分；本地可能稍后收到已经发布的旧 grant。

### 5.4 两个易混淆的提前使用

“借未来 cap”是指把未完成预留的 b' 当作本地 C 或已兑现中心 scope；此时可能有 s≤C 但 C>实际已保留的 b，实体覆盖归纳断在 s≤已保留上界这一步。仅仅在中心内部计算候选 b' 没有问题，问题是未提交就把它产生的运动权限或资源保证对外生效。

“先读未发布 q”必须区分可信性与提交状态。若 q' 已独立验证且 q'≤s，在同一个原子事务内部计算 S' 正是本方案允许的行为，不能因尚未发布而称其几何不安全。若读到的只是未验证候选，则 q'≤s 可能不存在，提前释放可以直接丢失实体覆盖。若几何事实已可信，却跨事务读入未提交状态并提前改账本，则至少打破了“已提交 scope 与 owner 集合一致”的归纳接口；不能继续套本方案证明。某个这样的具体提前退休也可能仍几何安全，但需要另建其提交、崩溃和快照证明，本文不把接口不一致自动夸大为必然碰撞。

### 5.5 并发、失效与冻结

两个机器人同时申请同一新增资源时，串行账本只能先提交其中一个；后一个看到 owner 已变，必须失败或等待，不能基于旧快照写入第二个 owner。一个机器人退休 r、另一机器人新增 r 也按同一顺序线性化：先退后授时，前者已证明实体不再需要 r；先授时若前者仍 owner 则授予失败。若同一 owner 的其他 scope 仍需 r，不能释放给另一机器人。

中心失效只冻结分配并保留已授 owner 事实。提交前失效且未发布 grant，维持旧状态；提交后未发布，只多保留未被使用的潜在 scope；提交后已发布，即使消息延迟或本地没有 ACK，已授 scope 仍不能被超时释放。该分支依赖提示的原子提交与冻结语义；中心重启时如何重建、验证和恢复账本是 UNKNOWN。

## 6. resident、原 MOVE 与端点的覆盖交接

### 6.1 原点 resident 到初始前缀

在原起点，可信 s=0、v=0，但实际 z 未必为零。旧 resident 必须覆盖 u+K 的完整允许实体包络。拟安装初始 cap 为 C_0，中心先兑现 b_0=C_0，本地只在收到合法更新后安装；0≤C_0≤ell，且

\[
u+K\subseteq U(0,C_0).
\]

先把 Mask(U(0,C_0)) 的新增资源保留，再在同一 owner 下把 resident 需求与新原 MOVE scope 原子交接；交接前后 u+K 的资源均保持占有。本地收到合法 grant 且获得明确 START 之前仍原地 reference HOLD。C_0=0 时新 scope 仍是完整 u+K 的 Mask，不能把零参考长度误当成空资源集合。

初始 q>0、只保留参考点 u、清零真实 z，或先删 resident 再申请新集合，都不能由这一包含关系支持。若原起点实际上不是 s=0 的可信状态，应按其真实合法状态另建初始化前提，不能冒充 resident 原点交接。

### 6.2 中点 cap、BRAKE 与实际占有

当 C<ell 且 s=C、v=0 时，只是同一原 MOVE 的中点 HOLD，不完成原目标、不增加逻辑 c、不关闭 tid，也不因这个中点到达生成原 MOVE 的完成 READY。cap 包本身没有这些逻辑完成权限。

若中途请求 STOP，73M1 的 BRAKE 在首次零速前还会产生最多 v_B²/(2a_hi) 的参考前进，且该距离不超过 C−s_B。因此完整制动段一直留在 U(q,b) 中。不能在 STOP 命令发出、收到“停止请求 ACK”或普通速度报告时，把未来 BRAKE 扫掠提前释放。可信 q 可以随着真实制动进度安全前移，但绝不能超越当前参考 s。

BRAKE 完成后参考保持不等于实体静止，z 仍在原世界坐标闭环中演化；resident/HOLD 仍须有 u+s e+K 的覆盖。no-start、长时间 HOLD、消息丢失或中心冻结允许这种资源占有无限持续，安全证明没有给出回收时限。

### 6.3 原终点资源何时进入 scope、何时不能退出

前缀 b<ell 本身**不保证原终点 V+K 已获预留**；不能一面只采用 Mask(U(q,b))，一面声称尚未覆盖的终点早已保留。如果另有原目标 resident 或独立终点预留，那是额外 scope，必须在联合需求中显式记账。

当中心实际把 b 扩展到 ell 时，q≤ell 保证

\[
V+K\subseteq U(q,\ell).
\]

所以原终点的全部包络资源在发布终点 cap 之前已经保留。之后 q 的合法更新最多到 ell，正常前沿退休不会删除终点资源；到 q=ell 时 scope 正是 Mask(V+K)，仍非空。单点闭边界在这里直接阻止错误退休。

只有可信 s=ell、v=0 才满足原 END 的几何门槛，逻辑 c 的一次性完成还需原系统端点协议。本地 cap=ell、中心 b=ell 或普通 COMPLETE 都不能替代真实到达。即使已经合法 END/c 完成，实体仍可能停留在 V+K，故终点资源不能仅因 tid 关闭就放给别人：必须转为同一机器人的端点 resident，或先授予下一合法原边的完整起点包络，再连续交接。

若新原边从 V 开始且采用同一 K、同一世界 z 闭环，则其初始前缀包含 V+K，能用与原点相同的原子需求交接。新原边的 local occurrence/坐标重置只在真实原终点完成后进行，不把中点假装成该端点。

## 7. 中心 scope 与机器人实权的精确比较

中心记录的 b 是已经保留并可经合法消息兑现的上界；本地 C 是机器人目前能实际使用的上界。中心 q 是可信后方退休边界，不是强迫机器人向前跳转的控制命令。实际参考是 s，三者必须分别保留。

\[
q\le s\le C\le b,\qquad
X\subseteq U(q,C)\subseteq U(q,b).
\]

所以中心 scope 可以严格大于机器人此刻的实权包络。未接收 grant 时，机器人可以按旧 C 运动或停住，但中心仍需保留 b 对应的潜在未来权限。认证的旧增量在任意迟到后可能被接受，除非它低于当前 C 或不属于当前 alpha；中心不能凭“暂时没用到”“没启动”“ACK 超时”推定这些权限已失效。

q 的退休之所以无需撤回 C，是因为它只删除可信参考已经越过、以后不会倒退进入的相交部分；前方 cap 收缩则改变本地仍可能行使的权限，需要不同的撤销/静止确认协议。本文没有构造这种协议，也不靠超时替代它。

cap/seq/tag 复核只保证输入一致性和授权来源；s≤C、制动域、z∈Z 来自 73M1 的连续与混合证明；q≤s 来自可信样本和同一 occurrence 的单调性；资源兑现来自本报告的账本原子条件。不能用其中一项代替另外三项。

## 8. 覆盖归纳与条件碰撞排除

初态要求每个实体均有对应 resident 或已授 scope 的覆盖。假定每个机器人均满足 73M1 的连续控制前提，所有 actual X 位于公共瓦片覆盖域 W，且所有中心事务、样本和本地 grant 接入满足本文前提。

连续流期间，73M1 保持 s/v/z 的不变量；可信 q 不超过当前或以后 s；中心 b 不减。因此每个实际 X 均包含于该机器人的有效 scope 包络中。离散时刻分别应用第 5 节事务交集证明、第 6 节 resident/终点交接证明，以及 73M1 的单调 cap 状态无跳变证明。串行事务保持每个资源的唯一物理 owner。这给出逐流、逐事件的覆盖归纳。

现在反设两个不同物理机器人 i、j 在某个已定义时刻碰撞，即存在 x∈X_i∩X_j。公共覆盖提供某个 r 使 x∈T_r。由于 x 同时属于两者实体，r 必须同时属于两者当前有效 scope 的 Mask，故同时要求 owner(r)=i 和 owner(r)=j，与唯一物理 owner 矛盾。

该证明包括闭边界接触，因为 Mask 以非空交集定义。它不需要从碰撞排除反证 q 可信，也不需要实体真实位置沿 e 单调；真实误差可能回摆，K 已覆盖这种回摆。它排除的是满足所有控制、覆盖与账本前提的实体交集，不认证这些前提的硬件实现。

若同一时刻发生资源退休、其他机器人授予和 cap 接收，可按事务线性化顺序排列；实体 s/z 无离散跳变，且每个成功交接点均具有前后覆盖，所以不存在由这几个合法瞬时操作自身产生的未覆盖物理瞬间。有限时间内外部事件聚积后的未定义执行不在此归纳内，维持 73M1 的 UNKNOWN 边界。

## 9. 有限区间事件的计数与复杂度边界

计数限定于**同一原 occurrence、固定原边、固定 K 和固定瓦片族**，且中心 q、b 均只增不减。不同 occurrence 重新开始，其事件不能混在同一个“一生只变若干次”的计数中。

### 9.1 单区间不是“一次变化”，而是至多一次占有段

若 I_r=[a_r,b_r]，令

\[
E_r(t)=\mathbf1\{b(t)\ge a_r\},\qquad
L_r(t)=\mathbf1\{q(t)\le b_r\},\qquad
B_r(t)=E_r(t)L_r(t).
\]

E_r 因 b 单调，至多从假变真一次；L_r 因 q 单调，至多从真变假一次。B_r 的真值时段是一个后缀与一个前缀的交集，故至多形成一个连续占有段。于是一个最初未占有且最终越过的资源可经历“空→占→空”，即至多两次 membership 翻转，而不是至多一次。若初始已占有、最终仍未越过或 I_r 为空，则变化更少。

q=b_r 仍在占有段内；只有 q>b_r 才能退出。若 b_r=ell，则在合法 q≤ell 下不能经前沿退休退出该末端相交区间，因此终点相关资源可以一直保留。

### 9.2 有限并集的计数

若 I_r 有 m_r 个合并后的区间，每个分量对应的布尔项至多有一个真值时段，资源总 membership 为这些项的逻辑或。因此每资源至多有 m_r 个占有时段、至多 2m_r 次翻转。不同分量的时间占有段可能重叠，使实际翻转更少；不能强断言非凸资源一定翻转两次，也不能因 q/b 双单调就忽略再进入。

这个上界可在几何与阶段控制允许时达到：相交区间均严格位于原边内部且之间有空隙，cap 分阶段推进并在各空隙合法 HOLD；可信 q 在上一相交区间之后发布，下一段 cap 尚未跨入下一区间。于是可依次退出、重新授予，再退出。若一次 cap 预留已跨过多个区间，则可能一直占有，中间不释放。

设资源数为 R，合并后的区间总数 M=sum_r m_r。凸 K 和凸瓦片时 M≤R；K 有 d 个凸片、瓦片凸时 M≤Rd。scope membership 总翻转数不超过 2M。若同一物理 owner 的其他 scope 仍占有某资源，该 scope 的退出不一定产生一次全局 owner 释放；不同机器人的不同 occurrence 总数没有被本提示限制，故全系统总账本费用不能由单个 M 统一封顶。

### 9.3 这不是费用标定或消息复杂度保证

在显式有限区间已经求得的抽象比较模型中，有至多 2M 个进入/退出阈值；排序需要 O(M log M) 次比较，存储需要 O(M) 项。若支持按单调前沿推进的事件索引，并按原子提交维护每资源分量计数，则同一 occurrence 的成功阈值跨越总数为 O(M)。给定 J 次成功 q/b 提交，其元数据处理和成功事件处理可写成 O(J+M) 个抽象步骤，另加几何预处理、身份验证、owner 查询和原子存储成本。

该界依赖上述数据结构与计算模型；它不是已经完成的实现复杂度测试。朴素每次重算所有区间可用 O(JM) 次区间判定作为另一直接可达的有限上界，不能混淆两种算法。失败申请、冲突重试、重复或无效消息、签名验证、广播、存储耐久性与恢复均未由成功事件数计入；这些输入可任意多，因此总实际时间、字节、资源价格或费用仍为 UNKNOWN。

任意多个 q/b 数值更新可以落在同一组区间阈值之间而不改变任何 membership；所以“最多 2M 次资源变化”不等于“最多 2M 条消息或提交”。所有区间构造费用、误差包络标定费用和并发争用费用都未赋值，不给预算常数的数值、范围或默认值。

## 10. 精确反例与失败前提

以下例子分别指出原命题缺口、必要前提或过强结论；不把违反明确前提的例子包装成对条件定理的反驳。真正满足全部前提而仍发生的界内失败必须反驳本文对应结论，不能事后改条件规避。

**A. 闭而非凸瓦片破坏单区间。** 在以 u 为原点、e 沿水平轴的坐标中，取 K=[−epsilon,epsilon]²。瓦片为三个闭矩形的并：[a,a+w]×[−epsilon,d_y]、[d,d+w]×[−epsilon,d_y] 以及顶部桥 [a,d+w]×[d_y,d_y+w]。取 epsilon、w 为正，d_y>epsilon，d−(a+w)>2epsilon，并令以下整个相交进度位于 (0,ell)；这些几何构造量可取有理。该瓦片连通且闭，但 I_r 恰为 [a−epsilon,a+w+epsilon] 与 [d−epsilon,d+w+epsilon] 两个分离区间。桥在参考水平带之上，不增加中间进度相交。有限个其他闭瓦片仍可补齐工作域覆盖。因此“有限闭瓦片”原前提不够；修订为闭凸矩形后排除了此例。

**B. cap 乱序被错误接受为降 cap。** 机器人已经接受 C_2>C_1 的更新并合法到达某个 s>C_1，旧认证 C_1 随后重放；若只检查签名而把本地 cap 覆盖为 C_1，则 s≤C 当场失败。即使当时 s<C_1，也可有 v²>2a_hi(C_1−s)，使原 BRAKE 的最弱合法减速度无法在缩小 cap 前停车。正确规则拒绝小于当前 cap 的消息；seq 不能替代 cap 单调检查与 occurrence 绑定。

**C. 不可信 q 使实体后部失去覆盖。** 假报 s<q'≤b。选择一个合法常误差 z_*∈argmin_{z∈Z} e·z，并取合法常扰动 w=kappa z_*，则 z(t)=z_*。当前实体最小 e 投影为 e·u+s+min_{z∈Z}e·z+min_{f∈F}e·f，而新包络 U(q',b) 的最小投影严格更大 q'−s。因此当前实体确有点落在新包络后方。可选一个允许的有限闭矩形覆盖，使该点位于一块与新包络不交的瓦片内部；按假 q' 退休就会释放仍与实体相交的资源。这里明确选择了实际 z，未把包络最小 support 冒充任意当前实体的 support。

**D. 借未来 cap、先发 grant 后预留。** 设 A=Mask(U(q,b'))\Mask(U(q,b)) 非空，而新增资源尚未授予。取 r∈A，必有某个 rho_*∈(b,b'] 及 z_*∈Z、f_*∈F，使 u+rho_*e+z_*+f_*∈T_r。令实际误差固定为 z_*、w=kappa z_*，提前接受并不中断运行到 b' 的原控制段会经过 rho_*，实体于是实际接触未保留的 r。r 可能仍属另一机器人。此例反驳未兑现资源就发布实权；若特定粗瓦片使 A 为空，不能据此反称所有提前发布都必然产生缺口。

**E. 超时释放仍在 HOLD 的机器人。** 机器人合法 HOLD 在 s_*，取任意允许常 z_* 并令 w=kappa z_*，实体保持占据 u+s_*e+z_*+F。若中心因 ACK 超时、no-start 或自身恢复计时，把当前实体所需资源释放并允许其他机器人获得相交 scope，唯一 owner 字段可以只显示后来机器人，却已不再覆盖先前实体。选取后来轨迹进入同一实体点即可产生碰撞。该例违反“失效冻结且不释放已授”及 resident 覆盖，不是延迟本身导致失效。

**F. 中点假 END/c 完成。** 取 C<ell、s=C、v=0，普通 COMPLETE 或 cap 到达被误解释为原 END，中心随即关闭 tid、增加 c 并删除全部 scope，却没有建立中点 resident。实体仍在中点，从删除瞬间起就没有必要资源覆盖。若又按原终点 V 开启下一边，参考起点和实体连续性也不再满足原端点拼接前提。正确构造只在 s=ell 的可信原 END 后处理原完成，并保持实体所需资源连续交接。

**G. 闭边界被提前退休。** 取某资源 I_r=[a_r,b_r]，让真实 s=q=b_r 且 C≥s。在 I_r 端点存在 k_*=z_*+f_* 使 u+b_r e+k_*∈T_r；固定实际 z=z_*、w=kappa z_*，当前实体就接触 r。错误地采用“q≥b_r 即退休”会在接触尚存时释放；正确条件是 q>b_r。单点 I_r={b_r} 同样需要保留，不能因区间长度为零忽略。

**H. 非凸 K 被强压成一个精确 interval。** 让 K 是两个相隔的紧凸多边形之并、瓦片是一个闭凸矩形，使两个凸片沿边分别给出分离的相交进度。取 q、b 都在两个区间之间的空隙，则真实 Mask 不含该资源，而用两端点构造的凸包 interval 会错误地判为包含。这是“等价”及费用精确性的反例；这种多占资源仍可能安全，不能把它说成必然碰撞。

**I. 退休/新增采用非串行旧快照。** 一个空闲资源 r 同时被两个独立写入者读取为 free；若二者都发布 grant 再先后覆盖 owner 字段，最终账本只保留一个 owner，但两个机器人已经各持有效运动权限。唯一字段形式不等于唯一已授实权，公共覆盖的碰撞反证失去“另一 grant 不存在”这一前提。原子串行预留并在提交后发布才能排除此执行。

**J. 有限资源事件不能推出活性。** 一个机器人保持 no-start 并永久占有其合法前缀，另一个机器人的必要新增资源与之冲突。双方一直满足连续安全和唯一 owner；前者不被超时收回，后者可以永远无法获得 grant。该执行直接反驳“有限区间、有限翻转上界或串行账本足以保证无饥饿/全局活性”，但不反驳本报告的安全结论。

## 11. UNKNOWN、构造地位及保护边界

本报告证明的是修订几何条件下的精确区间描述、给定可信输入和串行原子语义的覆盖事务、与 73M1 连续控制不变量的条件组合，以及同一 occurrence 的有限资源事件上界。

尚未证明或标定：真实可信进度认证装置、签名/session/occurrence 生命周期实现、原子存储与恢复实现、所有几何谓词的数值稳健性、K 的硬件有效性、额外驱动滞后或转向约束、端点 c 的真实一次性协议、每项实际费用、无限消息负荷下的服务能力、等待最终结束和全局活性。有限几何事件不能替代这些项目。

H、T_delay、B_CAL、B_max、B*、P_active、density N 均保持未赋值；本报告不给它们数值、范围、默认值，也不生成载荷。本文件仅为根任务提供可选择、可反驳的新数学构造，不确认后继方案已经固定或已实现。
