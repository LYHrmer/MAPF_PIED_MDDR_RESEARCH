# 73P23：付费捕获当前参考段根并复用的来源合同

2026-09-11，`/root/source73_r0_remaining_contract`。NO-SKILL / CONSTRUCTIVE / NONBALLOT；未调用Claude、skill或新代理，没有外部检索、实现、伪代码、科学运行或载荷。仅就POSITION来源提出纸面修订，根尚未采用本报告。不投资格票，保持 HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT。

## 1. 唯一建议与不能跳过的缺口

建议在现有 FINITE_CAUSAL_REFERENCE_HISTORY 来源内，明确提供 **CURRENT_SEGMENT_ROOT：可信本地当前参考段的只读投影视图**。POSITION先在原Srv_E付费CAPTURE固定该视图的不可变版本和采样点，随后在同作业付费求该点的参考进度外包并编码；不强制重复遍历、积分整个因果历史。该视图承接实际已经生效的控制和eta，不改为只知命令及扰动界，也不授予中心读取历史、实际eta或实体pose的权限。

数学上，当前合法段的精确初态、模式、已生效cap/eta和采样时间足以求值；全史只通过当前精确状态与实际事件分支影响它。当前73已经要求付费固定不可变authority根，也要求物理/控制真实状态一致。但“逻辑当前状态存在”不等于“已有后端提供可独立保留、传递不可变、可由Srv_E合法读取的精确对象”。本报告选择补明这个有限接口；实际对象、权限隔离、计量、原子保留及容量仍未资格化。

因此允许复用已经合法付出的工作，也拒绝两种捷径：不能从“仿真器知道”推免费查询；不能为了制造高观测代价而要求无意义重算。该选择不改变epsilon来源、不取消eta、不改变O或各站容量，不从P22的TASK真值宿主例外扩出POSITION例外。

## 2. 输入身份及实际阅读范围

写前精确确认目标不存在且非符号链接。当前73身份与根任务吻合；主稿仅按本题科学窗口读取，未作整稿审查。

| 输入 | SHA256 | bytes / LF | 本题阅读 |
|---|---|---|---|
| 73_PIE_SPATIAL_EVIDENCE_TERMINAL_HANDOFF_AND_PAID_SERVICE_PREEXPERIMENT_DRAFT_20260908.md | 60e128eed81a7448e6e8bfcdd51cbc930e437716651ffb0b76259c004c71f661 | 208378 / 649 | §3 L50–71、§6 L150–175；§9按CAPTURE/本地authority/费用/发布关键词读取，补全L258–296；另定点读§5 END/EndRecord/GC相关窗口。§9首次输出中间截断已补齐，不把截断当全文。 |
| 73P20_ROOT_NOISY_ALGEBRAIC_REFERENCE_PROFILE_AND_C18_ANALYSIS_20260911.md | f897b9514fe25c97ef0b8c76f04b86fbc0b89828a9cf6f7cc648afc424b9e865 | 15346 / 88 | 全文 |
| 73P22_ROOT_TASK_WORLD_COST_AND_ATOMIC_LEDGER_ANALYSIS_20260911.md | d3424e9822286c1d0d47e94dc02496de76be323531d74398960f546fa7307a59 | 10398 / 64 | 全文 |

必要原义：73 L164明确允许可信来源获得已发生eta因果前缀，策略仅收证书；L172已允许固定版本根及已付结果复用。L250–258区分本地逻辑authority、物理自主变化、应用writer和实际接纳点；L156/244规定物理推进、先前完成发布、CAPTURE的顺序。P20 §6要求局部段从实际接纳点重建并收费；P22 §6明确合法世界对象可以经付费接口复用，同时禁止把知识取得搬入世界费。

## 3. 当前段表示为何够用

本建议只依当前已选noisy代数profile：有限实代数输入、原O允许的换值行、相邻不同时刻间eta常值，以及原LAUNCH/APPROACH/BRAKE/HOLD/END语义。不是原任意有界可测eta全域算法。

可信参考推进本来就必须确定当前活动原tid、实际C、模式、已发生切换及下一合法演化。将当前段表示为固定起点t_a、精确s_a/v_a、当前实际C、模式及本段已经生效的eta，再附原边与模型身份；其含义是从该起点至下一实际边界的原参考解。根不是当前值的裸浮点近似，也不是回调全知评价器的可执行闭包。外部控制的待处理队列、未来eta尾部、主seed与实体z/w不是此点求s所需输入。

设CAPTURE点的物理时间为tau，Delta=tau−t_a。合法段未跨越未处理的切换时，LAUNCH的s=s_a+v_a Delta+(a_launch+delta+eta)Delta²/2；BRAKE的s=s_a+v_a Delta−(a_hi+delta−eta)Delta²/2，且只延续到已规定首次零速；HOLD保持s_a。APPROACH使用P20已核局部式：xi_a=C−s_a，r_a=v_a²/(2xi_a)，r_star=(a_hi+a_lo)/2+2eta，A=r_a−r_star，N=xi_a−v_a Delta/2+A Delta²/8，s=C−N²/xi_a。其正xi定义域、最早到cap分支及A为零的情形沿现稿，不在本题改控制器。

这些公式说明：若精确段根已由可信控制合法建立，则求当前点不必重新积分更早历史。控制改变、eta换值、自主切换时，旧段已被正确推进到接点，后继段承接其精确状态；归纳即可接续。历史依赖可能仍以代数表达式DAG存在，局部公式短不等于字段小、求值恒时或DAG不需遍历。不得以“有限充分状态”掩盖实际表示长度增长。

在端点，先按原混合规则形成HOLD/EndRecord，再捕获，不能把xi为零代入APPROACH。当前段根也可以只精确表示函数和起点，未物化s(tau)；这种情况下，**采样点求值仍由POSITION付费完成**。物理语义要求可确定状态，并不要求为了每个潜在QUERY提前生成高精度数值答案。

## 4. 明确的来源、字段与能力合同

唯一可信本地authority维护当前参考状态。CURRENT_SEGMENT_ROOT是它的受限只读视图，至少绑定：session/agent、原tid或其合法历史终点身份、原边/几何和控制模型版本、段起点、实际生效的参考段字段，以及与phase/关闭事实一致的内部版本。现有EndRecord分支保持原义：仅匹配当前活动tid或已合法保留的同tid终点记录可给POSITION，其他情形依冻结视图返回原NO_MATCH，不扩到任意旧tid或新边。

内部版本必须覆盖构成段语义的实际更新；普通command_revision不能代替它。物理时间连续前进而段未换时，不必每个时间点建立新的完整根，但采样点必须与被冻结段一起固定，后续计算只能求该点。内部版本标识只供authority绑定和审计；不新增向中心暴露根修订计数、实际C、模式、v、eta或关闭队列的旁路。原证书已允许的tid、sample_point、区间及模型/几何绑定保持，不借新的元字段泄露私有事件史。

Srv_E只有在原请求到队首且付清CAPTURE操作后才取得该视图的只读引用。它不能作为planner/中心可解引用的指针，也不跨反事实臂共享。禁止authority查询作业追踪当前全局时钟、可变eta槽、可变controller对象或后续EndRecord，以此刷新已经固定的输入。传递不可变要求覆盖全部被引用的代数系数、DAG节点、几何身份与绑定记录；顶层指针没变不够。

完整因果记录仍是与world/controller一致性的原证据责任。该视图只是合法已发生前缀的派生充分状态，不新增驱动观测通道；若实际控制后端本来只持浮点近似，或者没有可审计地取得实际eta，那么不能把其地址叫做精确根。须先满足既定来源能力，不能以本题数学式补造数据。也不能擅删原强制保留记录或声称历史审计已不需要。

## 5. 同刻CAPTURE、控制更新与生命周期

严格沿原全序：先推进物理到该行时间并处理全部已发生自主切换；再处理前行已完成单元的合法发布；最后在该PROCESS_SLICE作业首个语义操作付费CAPTURE。故“先物理后CAPTURE”还包含二者之间已合法发布的CUM_CAP等实际更新。CAPTURE必须固定它实际所见的本地最新已生效根，不能只取行前或物理阶段末的旧副本。

旧cap同刻到达先HOLD；若旧cap为原ell先END，旧入口永久关闭。后续合法扩cap可能建立同点新段，但HOLD不自动RUN。更早行的ACTUATOR或同物理时间而更早row_id上的有效控制已计入，更晚行及采样点之后的更新不计入。只相同数值时间不意味着同一语义版本。

CAPTURE可先固定本地authority的当前根及历史终点索引根，随后付费按请求tid查找与核验；不能在原子捕获之前免费搜索历史或读取进度以决定要保留什么。CAPTURE所需引用、身份、位长和保留登记须在原允许原子操作容量内完成；不够则未采样，保留原队列等待后续既有机会，不生成O、不扣除世界时间。

捕获成功后，物理与后继控制可以继续建立新当前根；旧引用保持自己的时点及内容。END发生在CAPTURE之后不把该样本改成ell，也不取消已有历史进度的安全性；下一原MOVE不能让旧tid样本变为新边进度。后继发布只核该证书原绑定和完整性，不能为了“新鲜”要求旧根仍是当前根，亦不能拿后来的phase回写被冻结的服务分支。各原生命周期的拒收/关闭分支继续适用。

普通作业槽不能锁住连续物理或END。保留旧根须有独立生命周期责任，直到该作业、封装、在途或审计的有效引用结束才回收；若证书完成后已自足而不再引用根，根的释放仍按实际持有者和既有GC容量收费。根被查询保留而导致的旧节点寿命延长、复制或额外写入，不能算成免费物理副产物。实际内存不足/保留失败的处置必须区分查询失败与可信控制失败，不假设无限内存。

## 6. epsilon、有限编码及延迟的证明责任

冻结根与合法采样点给出一个精确实代数值s_tau。由该值的有效精确表示，可用认证有理外包细化产生二进制有理端点L、U，直到包含s_tau且U−L不超过**原事前观测profile的epsilon**。于是q=max(0,L)满足q≤s_tau，且s_tau−q≤epsilon；当L为负时由s_tau≤U≤L+epsilon可得同一结论。此处依原s_tau非负域，不为获取更漂亮阈值重定epsilon。

这是有限表示的数学构造条件：有效代数表示、已合法定义的分支和可认证的正精度要求。实际输入还须提供对既定epsilon的有效比较／正性证据，或从它可计算地取得一个经认证不超过它的正有理精度见证。该见证只是满足原精度的计算证明，不是新的保护参数、可调精度或数值赋值；若原profile连这种有效表示都未绑定，仍留下具体输入资格缺口。不能只用“epsilon在实数意义为正”宣布实际封装已能完成。

本题不选择新的传感创新分布或替换已登记封装规律。原profile规定的封装和创新继续只用该request/sample语义key，并必须保留区间包含及宽度证明；不能把较早已缓存区间直接改标为新样本，或因区间工作失败重抽创新。认证外包可复用已合法计算的包含信息，但端点读取、有效性核验和本次实际封装仍按需收费。

同tid内s单调，所以后继交付时q仍为该原MOVE进度下界；不能推出交付时误差仍不超过epsilon。旧tid关闭并开始新MOVE后，该证书只属于旧动作，不给新c、新任务或新边进度。F_cap只更新原q_evidence而不退休，E0仍不请求POSITION；本接口不改变这些策略约束。

## 7. 按实际用途划分完整费用

| 工作 | 费用与容量归属 | 禁止混淆 |
|---|---|---|
| 为实际已接纳参考运动确定正确当前段、自主切换、控制接纳及END所必需的精确计算和状态更新 | 原共同world/control合同，逐臂实际记录；应用控制步骤仍走原容量 | 不借P22 TASK宿主例外取消控制计量；不要求为了未发生查询预编码区间 |
| 查询请求、authority排队、CAPTURE身份与引用固定、保留登记 | 原Srv_C/通道/Srv_E各实际角色，沿既有机会和容量 | 不由元调度器提前读根、匹配业务或制作答案 |
| 仅为本次查询独立保留旧版本、复制/持久化、增加引用及延长内存寿命 | 实际购买查询的一臂；计算由原所属角色计，内存/存储按实际保留计 | 不把查询诱发的保留／复制称所有臂必需的世界工作 |
| 冻结段读取、DAG遍历、解码、采样点局部求值、精确比较／开方／认证细化、端点编码与签发 | 原Srv_E同作业，全部位长、分配、失败照计 | 局部式或已存在对象不等于免费，未发生的全史重算也不虚收 |
| 发布、缓存、网络每字节和每接收端、中心验证/退休、日志与GC | 各原站完整容量与费用 | 不在authority完成时直接更新中心知识或owner |

采用复用并不意味着世界总成本与查询成本有一条免费缝隙。若某后端为支持冻结新增了查询专用快照、读屏障或额外副本，这些实际工作由查询接口承担，且不能要求控制器在需要进展时无偿替它完成。根的普通当前表示若本来就是可信控制必要的精确数据，已付建立成本只记一次；读取和保留所产生的新工作仍照计。是否属于必要物理表示，应按没有这次查询时该操作是否仍为原控制语义所需判断，不按函数名、进程名或“缓存”标签判断。

同一臂内部合法复用已付精度计算时，只省实际未发生工作；不能因给主PIE、查询或外部比较不同使用者而重复收同一计算，也不能把一臂缓存交给另一反事实臂。D/R享同一来源能力与成本规则，实际查询对象／次数可能不同；E0不虚收查询专用状态和区间生成，仍承担自己的真实共同控制费用。NATURAL按实际工作，STRICT仍按原共同供给规则，实际工作向量均另报。复用降低POSITION费用可能使效应扩大、缩小或为零，不能预写收益。

## 8. 两个可构造的合同失败及总失败规则

第一类：捕获的所谓根只绑定command_revision和一条读取“当前时钟”的引用。在同一LAUNCH段内命令未改但物理继续，后继编码若用完成时刻代替tau，就返回后来位置而非样本位置；当其差足够大时甚至不含s_tau。这违反采样包含性，不需要假定发生碰撞。修复是同时冻结采样点及仅由冻结字段求值的段，不要求重新积分全史。

第二类：只固定顶层指针，后继cap安装或eta更新原地覆盖它指向的系数／状态节点。编码得到跨版本组合，既不一定满足冻结时的旧模式定义域，也可能包含采样之后才生效的输入。即使最终数字碰巧落在某个合法区间，也不能证明它来自所声明因果版本。修复必须使全部依赖不可变并保留所有权，不能仅加一个序号或在完成时重新采样。

失败总规则：CAPTURE容量／保留不够而尚未采样，等待原机会并计实际已做工作；捕获后区间编码、分配、通信或验证失败，保留原样本身份和完整费用而不给新证书，不自动NO_MATCH、重采样或冻结物理。冻结对象失效、源值与可信控制不一致或物理顺序未认证属于来源／协议／世界证据缺口，不能降格成普通传感丢包；沿现稿UNKNOWN/UNVERIFIABLE等分类保留固定roster和评分边界。若只是已认证物理历史上的查询作业未完成，世界可继续，算法保持旧知识。数学最终可编码不保证有限O或任一行容量内实际成功。

## 9. 可直接改稿的范围与验收边界

建议只细化73 §6 L164/172及§9 authority表：FINITE_CAUSAL_REFERENCE_HISTORY允许付费固定其与实际本地控制一致的CURRENT_SEGMENT_ROOT投影视图；输入为冻结段及采样点，后续只用冻结依赖局部求值；已依法付费建立的精确状态可以复用，禁止未登记权限和免费派生证书；历史遍历/重算仅在实际发生且合法需要时收费。原历史来源、全费用、信息边界、epsilon出处与同刻控制规则均保留。

可以关闭的纸面问题是“POSITION是否必须重复完整历史积分”：**不必须**，本profile有充分局部状态和明确付费复用途径。不能因此关闭的实际义务是：具体可信源是否真的维护该有效精确表示、是否允许Srv_E按最小权限捕获、冻结依赖/版本/生命周期是否可靠、精度输入是否有效、原子捕获及逐步处理是否受原容量约束、失败是否忠实留证。这些都有明确拒绝条件，不能凭逻辑authority或simulator内部变量代签。

本题无新来源或数值库调查，无任何Claude/skill/正式审查调用，无实现、伪代码、配置、测试、仿真、调参、保护参数赋值或实验结果；未读取35–38、Q或任何受限载荷。唯一新文件在研究目录以apply_patch建立，不改73/P20/P22、行政/Git/桌面或其它完成件。最终只读核验SHA256、bytes、LF后交付并结束本有界任务。
