# C21 — H19 observation enablement under unchanged ADG dependencies

This is one bounded constructive mathematical consultation, not a formal review or qualification vote. Use no skills, tools, MCP, network, files, code, pseudocode, numerical examples, parameter assignments, or invented experimental traces. Reason only from the evidence below. Reply in at most about 700 English words. Finish with a standalone END-C21.

Input identity: draft73 SHA256 7928cfd0ffa630e73d1cdc7ab912a47d4ead2d53854bbfa43e3b85d5e9f2b9fc; 214119 bytes / 659 LF. The requesting agent personally read the relevant geometry, grant, source, H19 enqueue and cost passages. No implementation, map, task payload, or result has been obtained. HOLD remains. H19-PR-COMMON-SYSTEM is an explicitly reconstructed common adapter using ECBS-FA and the 2019 ADG/cut mechanism, not a claim of native implementation equivalence.

Evidence and fixed contract:

1. Draft §10 L398: “逻辑enqueued可有多条，不等于物理GRANTED：每agent仍至多一个未闭请求、每gate至多一个活动tid”; H19 uses full-action b=ell and no GROUP_PREFIX/fences. An action gets an immutable execution slot only after paid ADG_ENQUEUE commits it; APPEND then installs it locally. SOURCE requests only the current MOVE from the committed execution ledger, with paid known cursor and the original PRECHECK/END/READY/consumption gates.
2. §10 L400–402: a batch captures the paid central finished set F0. A selectable action must lie in the allowed source domain; all Type-2 predecessors are in F0; its Type-1 predecessor is enqueued/finished or the preceding selected item in its same-agent chain. New enqueued items do not count as Type-2 finished. Finished is monotone historical knowledge. L394 states later splice does not add predecessors to old enqueued actions. A physical END or in-flight receipt is not yet paid central/ADG finished.
3. §4 L74–86 and §5 L106: prospective MOVE p requires all resources in Dp=Mask(U_p(0,ell_p)) FREE or its own legal parent resident, plus every other source/mode/binding/geometric/gate condition. A foreign active tid t owns Mask(U_t(q_t,ell_t)), with U_t(q,ell)=union over s in [q,ell] of (u+s e+K), K=Z+F. Tiles are closed. For any resource r in the full action tube, lambda(t,r) is its last intersecting progress: it remains in the suffix mask iff q_t<=lambda(t,r). Final endpoint resources have lambda=ell. Nonconvex intersections may have multiple intervals, but b=ell here makes the last-exit characterization valid.
4. §4 L76: a correctly bound paid sample gives q<=s at capture; paid central verification/retirement may monotonically raise q and remove exactly certified suffix-excluded resources. q never marks ADG finished, consumes a slot, changes the original action, removes endpoint ownership, or supplies task truth. H19E0 does not buy POSITION; H19R-S uses stable-agent RR and paid retirement, while retaining all Type-2 finished requirements.
5. All selection, capture, encoding, communication, verification, retirement, grant, control, enqueue, planning, failure and retained memory costs are charged at the original finite service stations. A center job occupies its single slot through publication, while physics and fixed TASK_SERVICE rows continue. Task service requires the actual full entity at a legal task endpoint at a scheduled row; visiting earlier does not guarantee service. Extra querying can alter queueing, control attempts, replanning inputs/cut and later task timing. Both external arms and all prespecified failures/missing outcomes remain registered; no results exist.

One question: what is the exact boundary of a q-only local admission gain, and what can it say about whole-run ordering?

Independently examine this proposed sufficient no-gain statement: in a fixed center state, if every foreign nonendpoint resource blocker of prospective action p corresponds, by its actual tid-to-original-action identity, to an unfinished mandatory ADG predecessor whose finished status is necessary to allow p, raising q alone cannot make p admissible. Handle the empty-blocker case and distinguish Type-1 enqueue precedence from a genuine mandatory-finished dependency. Crucially determine whether “already allowed/requested p with an unfinished Type-2 predecessor” is an empty legal domain, since all Type-2 predecessors had to be finished before enqueue and cannot be added later; do not turn such a state into a useful negative example. Split not-yet-enqueued candidates from actual requests.

Then give precise necessary mechanism conditions, and conditional sufficiency if possible, for an already-enqueued request to benefit from retirement of an active original action not so dominated. Bind each blocker to the current action, not merely its agent: a predecessor agent may now execute a different later action. Do not assume discrete ADG necessarily covers expanded tubes or coarse resource tiles. Explain why local redundancy alone establishes no universal Q or total-cost ordering once paid jobs alter the closed loop. Recommend at most two exact manuscript sentences retaining both external arms; identify any unproved reachability/source obligations. No invented map or claimed performance.
