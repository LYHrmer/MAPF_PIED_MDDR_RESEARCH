# 73P3：最大前缀的四邻接域反例与两种授予替代

2026-09-09。独立有界建设任务；**NO-SKILL / NONBALLOT**。这是对 73P2 CE3 动作域缺口的新分析，不修改 P2，不重做其全协议，不重复失落 Claude73C4 的异步环几何任务。本文只构造两条平行反向原边上的资源反例及两种明确替代规则，不认领新颖性、主总体效益或正式审查通过。

保持 **HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT**。没有实现、构建、创建或运行研究测试、仿真、调参、实验载荷/结果、Git 操作或机器人动作。所有文件编辑仅 apply_patch，新文件只写研究目录。

## 1. 实际输入、身份与 CE3 的域纠正

本次先精确检查目标文件不存在，核原 P2 与 73 当前身份，重新读取 P2 的完整 CE3 及 73 关于原四邻接动作域的文字。此前本 agent 已完整读取 73、73M1、73P1；本次没有把旧全文读取冒充新一次全文读取，也没有读取载荷、桌面或冻结禁读文件。

| 输入 | 本次开始 SHA256 | bytes / lines |
|---|---|---|
| 73P2_PREFIX_END_TO_END_PROTOCOL_AND_FAIR_COST_CONSTRUCTION_20260908.md | 5dfaa2adcbe6fcd11ecb16b9d093519f1733dc8fe947d7f64c7316a0db5ba5dd | 36848 / 202 |
| 73_PIE_SPATIAL_EVIDENCE_TERMINAL_HANDOFF_AND_PAID_SERVICE_PREEXPERIMENT_DRAFT_20260908.md | e0baef1ebf3b5c6a999b5324ac6023588e8da9affc82f1cba3ceb7ee972045dc | 107921 / 418 |

**根指出的域问题成立：P2 CE3 不能直接充当标准均匀四邻接单步原边的反例。** 在网格间距 ell 的标准图上，水平单步边从 (m ell,n ell) 到 ((m+1)ell,n ell)，竖直单步边的横坐标是 p ell。若二者在相对内部交叉，就需整数 m<p<m+1，不可能。P2 描述的“B 从交点下方出发、经过交点、终点又在 r/f 之外”没有给出符合这种原单步边身份的嵌入。

把交叉改成公共端点也不能自动修复：B 若到交点即结束，交点的终点包络仍可能占 r/f，原“END 后都释放”依据失效；B 若从交点出发，其初始 r 占有又要重新检查。不能靠延长原边、把多个原 MOVE 合并成一个 tid 或将中点当目标来救原例。P2 CE3 在较宽的连续直边域可以有几何含义，但其主四邻接域地位此前未证。P2 原字节保留，本报告承担这条纠正。

下面重新构造的原边在同一标准图中均为单步四邻接边，端点互不交换、互不重合。它仍是 **给定合法原提案的条件性协议反例**：没有运行固定 PIE-D 核心来取得实际提案，不冒称已找到官方主 roster 输出或源保真回执。

## 2. 两条合法原边、非零误差与固定工作域

令原边为 A:(0,0)→(ell,0)，B:(ell,ell)→(0,ell)，ell>0。二者分别是同一方格下行向右和上行向左的原单步边，不需要内部垂直交叉、额外邻边、重路由或虚拟目标。参考进度为 s_A,s_B∈[0,ell]，参考点分别为 p_A=(s_A,0)、p_B=(ell−s_B,ell)。

反例及替代次序比较的是这两个当前原 occurrence 的闭合子问题：完成前没有其他新授权竞争所列资源，已经完成者继续真实 resident HOLD。这个条件可以定义一个合法有限执行前缀，不等于在主 lifelong 评价里删除未来任务或不利请求；加入后继原 MOVE 后须重新分析，不能从本局部修复推出主系统保证。

取非空实体足迹 F=[−beta,beta]^2 与非零世界误差盒 Z=[−zeta,zeta]^2，beta,zeta>0，rho=beta+zeta，因此 K=Z⊕F=[−rho,rho]^2。只采用符号条件 ell>2rho，不绑定任何实验参数。各实际误差 z_i 按原同世界坐标闭环及有界扰动保持在 Z；没有令真实误差在端点清零，也没有把扰动改为延迟。实际实体 X_i=p_i+z_i+F 始终属于 p_i+K。

所有实体可能位置处在固定闭矩形 W=[−rho,ell+rho]×[−rho,ell+rho]，该构造中无静态障碍。选一个符号分界 h 满足 rho<h<ell−rho，设两个底座资源：

\[
T_L=[-\rho,\ell+\rho]\times[-\rho,h],\qquad
T_U=[-\rho,\ell+\rho]\times[h,\ell+\rho].
\]

二者覆盖 W；其闭边界在 y=h 重合，但 A 的完整包络最高 y=rho<h，B 的完整包络最低 y=ell−rho>h，所以 A 永远只碰底座 L，B 永远只碰底座 U。这里不把中间空隙做成一块同时接触上下实体边界的第三资源，避免偷偷破坏初始 Mask 不交。

对任意合法 s_A、s_B 和误差，A 的全部实体位于 y∈[−rho,rho]，B 位于 y∈[ell−rho,ell+rho]，两区间严格分离。因此本两边的实体运动本来可以任意异步而不相交。后面的停滞来自保守粗资源争用，不是物理轨迹不可行。

## 3. 两块跨行粗矩形及精确相交区间

选择符号 a,d,b 满足

\[
0<a<d<b<\ell,\qquad b-d>2\rho.
\]

这些是存在性构造条件；a,d,b、rho、h 都不是保护参数的改名或赋值。可在条件成立的有理表示域内选取，从而符合有限有理多边形/矩形的几何合同；本文不给任何具体数值或载荷。

定义另外两块固定闭凸矩形：

\[
T_r=[a+\rho,b-\rho]\times[-\rho,\ell+\rho],\qquad
T_f=[d+\rho,\ell+\rho]\times[-\rho,\ell+\rho].
\]

r/f 跨越两行且允许重叠，b−d>2rho 使其原始横向区间也严格重叠。它们与 L/U 一起构成固定有限公共瓦片族；没有在看到策略结果后增删瓦片。这里 a,d,b 特意定义为 **A 包络的相交进度阈值**，并非直接拿原始矩形横坐标当阈值；rho 的膨胀已显式计入。

对 A，在进度 s 时横向包络区间为 [s−rho,s+rho]。与 r 相交恰需 s+rho≥a+rho 及 s−rho≤b−rho，即 a≤s≤b；与 f 相交恰需 s≥d，且 s≤ell。对 B 将 s_A 替为参考横坐标 ell−s_B，得到：

| 资源 | A 的 I_r={s:包络相交} | B 的 I_r={s:包络相交} |
|---|---|---|
| L | [0,ell] | 空 |
| U | 空 | [0,ell] |
| r | [a,b] | [ell−b,ell−a] |
| f | [d,ell] | [0,ell−d] |

全部区间闭合，端点接触也需 owner。任何 S_i=Mask(U_i(q_i,b_i)) 都按 I_{i,r}∩[q_i,b_i]≠空计算，q_i 等于区间右端时不能退休。该表是实际包络和矩形交集的等价式，不借当前实际 z 的 support 冒充 K 的 support。

初始与真正原 END 的 resident Mask 为：

| 时刻与原参考点 | A | B |
|---|---|---|
| 原起点 | {(L)} | {(U),(f)} |
| 原终点 | {(L),(f)} | {(U)} |

括号只用于分隔资源名，没有新 tag 含义。由于 a>0、d>0、b<ell，上表在非零 K 下仍严格成立。初始 A/B 资源不交；B 初始确实持 f 而不持 r；B 完成以后只保留 U，其终点实体不再需要 r/f。A 最后必须保留 L/f，不能谎称两车完成后所有资源都 FREE。

## 4. 有限 CAND 下的最大 cap 是确定的

采用 P2 的有限规则：对本原边收集 0、ell 和所有非空相交区间端点，去重排序，再加入每对相邻不同端点的唯一算术中点。由上表，A 的端点集恰为 {0,a,d,b,ell}，因此

\[
\mathrm{CAND}_A=\{0,a/2,a,(a+d)/2,d,(d+b)/2,b,(b+\ell)/2,\ell\}.
\]

B 的端点集恰为 {0,ell−b,ell−d,ell−a,ell}；其最小正候选是 (ell−b)/2，紧接着才是 r 的闭入口 ell−b。这里没有连续极限、任意 epsilon、运行后分辨率调整或额外 cap 参数。

初态 owner(L)=A、owner(U)=B、owner(f)=B、owner(r)=FREE。考虑一种完全合法的公开作业次序：A 的首次有效预留作业先提交；此前 B 尚未获得跨 r 的授权。A 的 q_A=0；所有候选 x≥d 都需要 B 持有的 f，失败；(a+d)/2<d，且其需求仅 L/r，所以成功。故最大可授 cap 唯一为

\[
b_A=(a+d)/2,\qquad S_A=\{L,r\}.
\]

这一步是中心先预留、后发布的合法不可撤回 grant，不需要 A 已运动到 r。以后 B 申请时，所有候选 x≥ell−b 都需 A 持有的 r，失败；候选 (ell−b)/2 只需 B 原有 U/f，故其最大 cap 唯一为

\[
b_B=(\ell-b)/2,\qquad S_B=\{U,f\}.
\]

接纳点、C≤b、明确 START/RESUME 与公开重试均仍按 P2。可条件于两车各收到授权并完成一个不中断的合法控制段，最终分别到 s_A=C_A=b_A、s_B=C_B=b_B 并中点 HOLD；不能在这时原 END、消费 c 或改新目标。不需要设定成功时长或赋实验日历值来证明下面的吸收性。

## 5. 任意可信查询及公开重试都不能解除这次互等

无论 D/R 怎样挑 POSITION，即使每次都得到精确真实参考进度，也总有

\[
q_A\le s_A\le C_A\le b_A<d<b,
\qquad
q_B\le s_B\le C_B\le b_B<\ell-b<\ell-d.
\]

A 要退休 r 必须 q_A>b，因为其相交区间为 [a,b] 且 A 当前 cap 已进入 r；上式排除。B 要退休 f 必须 q_B>ell−d；上式也排除。因此 owner(r)=A 和 owner(f)=B 持续不变。

A 的任何下一个候选都至少 d，而 f 仍是外国资源，所以不能增加 b_A。B 的任何下一个候选都至少 ell−b，而 r 仍是外国资源，所以不能增加 b_B。证书乱序/重复只给更保守或同一 max 下界，不能突破这个界；STOP/BRAKE 只会更早停住，RESUME 不能越过现 cap，普通反馈不授退休权。已授 CANCEL 按 P2 拒绝，不能回滚已签发权限；中心 fail-stop 也不能清空资源。

上述蕴含逐个合法中心/本地事件归纳保持。因此从该状态开始，任何有限追加的模型内收费重试序列都不能增加两者 cap 或到达原 END。这是协议的资源停滞，不依赖日志缺失、预算耗尽、永久丢包或 no-start。若考虑没有这些阻碍的无限公平延伸，它仍不会结束；实际有限 O 中则保留未完成/HOLD及全部费用，不人为加长观察期。

本反例没有碰撞，也不反驳 P2 条件安全证明。它反驳“同图合法原边 + 有限 CAND + 最大可用 cap + 可信退休 + 公平公开重试足以找到可行完成次序”。没有对其他源提案、规则细方瓦片或实际主数据作全称结论。

## 6. 同一几何、同一 CAND 的另一合法完成次序

A 改为先取得 CAND_A 中的 a/2，而不是 (a+d)/2。它的全部已授包络只需 L，因 a/2<a 严格不触 r；可以收到显式 RUN 后到该中点 HOLD，也可以先保持，均没有原 END 或消费。关键是调度先不再授给 A 新资源 r，直到 B 获得所需权限；“仅第一次取小 cap”而不约束后继抢占，不能独自证明修复。

此时 B 的完整 cap ell 所需 {U,r,f} 全部 FREE 或属于 B 的合法当前 parent，且不含 A 的 L。故 B 可先完整预留再接纳原边、明确 START，并在无后续中断且消息/服务最终完成的条件下有限到真正原终点 (0,ell)。任意横向误差仍由 K 包络覆盖，两车上下管严格分离。

B 的不可逆 END 建立终点未消费记录；中心必须通过共同付费 END 证据，将 U 改 tag 为 B 的新 resident、释放 r/f、登记 FINISHED 后再发 READY。B 即使 READY 未消费仍占 U；这不阻塞 A。不能在仅报告 cap=ell、MOVE 到 cap 的错误中点或普通 COMPLETE 时提前做这一步。

在 B 真正终点资源交接后，A 的完整 cap ell 所需 {L,r,f} 全部可授。A 从旧同 tid 接入更大 cap，随后新的明确 RUN 才从中点零参考速恢复；到原终点 (ell,0) 后保留 L/f。B 继续合法占 U。两者各自的 c 只在相应 READY 验证后的本地一次消费推进，Kc 仍走原付费 CURSOR。

这个完成次序对 E0 同样可用：途中 q 全为 0，B 只在原 END 交接时释放 r/f，足以放行 A。所需费用包括 cap 选择、预留/签发、传输、接纳/控制、公开重试、END/READY/消费及等待；没有免费途中进度、借用未授前方资源或撤销已授 cap。它证明存在安全次序，不证明既定最大策略执行了该次序，也不证明固定完整观察期内一定及时完成或产生净任务增量。

## 7. 替代一：能全长则全长，否则只扩大已持覆盖

这是一个替换 P2 最大 cap 总函数的确定规则，保留它的身份、q/C/b、原 END、公开机会与消息协议。在 SOURCE_OR_EXTEND 的同一付费快照上：

1. 若完整 ell 的需求可从当前账本合法授予，则选择 ell。
2. 否则，从有限 CAND 的 x>b 中选择最大的、满足新需求完全包含于本 tid 当前 S 的 x；初次准入以当前合法 parent resident 的需求代替 S。没有则 BLOCKED。

第二分支不占任何此前未持有的资源，即使某新增资源当前 FREE，也不在这个分支抢占。这里的“已有”只指本原责任或合法 parent，不是同一字符串 owner 的任意其他 tag。所有几何/owner 判定、失败扫描和发布检查收费，q 仍只来自该臂合法已付证据；E0 用 q=0，规则同样成立。

在第 2–6 节新初态，无论 A/B 谁先处理，A 在 f 未由 B 释放前不能全长；第二分支最大的 cap 恰 a/2，其后也不能跨入新资源 r。B 的完整 {U,r,f} 不被 A 占据，故 B 一旦获得自己的合格机会，就可获 ell。随后按真正 END 释放，再 A 完成。因此在必要控制/服务/消息最终完成的条件下，该规则从这组初态避免所构造的资源互等；不要求免费知道 B 何时达到某中点。

安全由 P2 原证明继承：只选了原可行候选的子集，没有缩 C/b、提前释放或另造 MOVE。若在旧贪心已经把 A 放进 r 的状态才切换，本规则不能收回 r，不承诺事后修复。

代价是明显保守：需要多个 agent 先各取得一些新空闲资源、再通过途中退休互相放行的系统，可能没有任何单边完整可授；此规则只在旧已持包络内前进，保留整边准入的许多循环停滞。它不是普遍活性算法，也不是证明比整边/D/R/E0 更好的路线。它无需扫描所有其他请求，参考判定费用是本边候选/资源规模的有限函数；实际处理成本及减少的运动连续性仍需全窗口记录。

## 8. 替代二：有限完成次序检查并保留授予顺序

第二个候选允许占新的 FREE 资源，但在接受每个候选前增加一个明确的保守可兑现检查。它只使用中心已经合法获得的原请求、已提交 q/b/owner、完整几何与原终点包络，不读私有 s/C/c，不免费预测任务或 B 的下一条未登记边。没有合法可见原请求的 resident 视为固定占有，不能在检查中想象它将自动消失。

**有限判定。** 令当前已知有限集合 J 包含所有 GRANTED 原 MOVE 和完整结构/准入证据有效的当前 UNGRANTED 原请求；每 agent 至多一个。对目标候选 x 先在私有副本形成其正常授予后的账本。按原请求规范 key 的字典序枚举 J 的所有排列；对每个排列逐项尝试以下纯几何/账本步骤：

- 对排到的 j，检查当前假想账本能否授予其完整剩余 Mask(U_j(q_j,ell_j))，保留尚未排到者与非参与 resident 的全部已有责任。
- 若可授，假想它在同一原边内合法运行到真正原 END，并做原终点账本交接：**只释放非终点资源，终点 Mask(E_j) 永久保留为该 agent resident**；不执行下一条边，不重置世界，不假设任务已经得分。
- 全部参与原 MOVE 都可按此完成才称这个排列可兑现。第一个可兑现排列是唯一见证；没有则本候选拒绝。

逐候选仍按 CAND 从大到小扫描，选第一个有见证的可行 x；无候选则保持旧责任。此检查是必要服务最终完成条件下的一种充分证据，不是对全部可行物理/前缀调度的完备决定器。它会拒绝那些必须交错部分推进、没有任何逐边完整完成顺序的可行系统。

**不能只存一张可行排列而任由后续贪心改写。** 候选提交时一并保存该有限队列作为本次授予顺序。队列尚未清空时，只有其最前面的未 FINISHED 原请求可以再取得新的资源；它在自己的后续公开 SOURCE_OR_EXTEND 机会优先申请完整 ell。其他队列成员只能执行既有授权、合法 q 退休或不增加已有需求的 cap 更新；队列外的新原 occurrence 暂不授予。请求、规划/任务业务仍按原合同收费并可等待，不能靠这条规则造新动作或免费转授。

队首只有在中心凭付费 END 证据完成真实 FINISHED 后才从队列移除，保留其终点 resident。其他成员若已凭既有权限先结束，可在其真实 FINISHED 后移除；这只是实际需求缩小，不是免费预测。已完成 agent 的下一个原 occurrence 不能在本队列清空前悄悄占用后面成员所需资源。

若尚未授予的队列成员合法 CANCEL、source/parent 失效或其他绑定破坏使原见证不能引用，停止基于此见证的新增授予，付费重新检查当前真实账本；已经 GRANTED 的权限和物理责任原样保留。不能把撤销一个未授请求当成已经腾空它的 resident。中心故障/队首长期 no-start/消息缺失可以使队列长期保持，且占用/等待/保留内存均计费。这个调度队列本身不创建第二 authority 或超时回收权限。

**条件保证。** 固定一个有效队列，假定无上述失效/取消，没有队列外新占有，且所需公开机会、服务/消息和合法不中断运行最终发生。见证中队首完整需求在初始账本可授；期间其他活动成员的 q 退休或 END 仅缩小现有占有，不能使该需求新增外国 owner。因此队首在实际收费重验时仍可完整授予并到原 END。真实交接后，其 terminal resident 不超过见证保留的集合，归纳可处理后继队首。故这种 **闭合、有效的已知队列** 没有由本次资源授予造成的循环等待，条件下可逐项完成。

这不是全系统 lifelong 无死锁/无饥饿定理：未知后续请求、永久停止/失联、队列取消、或原状态根本无这种可兑现顺序都会使保证不适用。反复重新组成队列的公平性也未证明。它更不是“有限集合检查成功便不用等 END”的实时结论。

**对本反例的判断。** 若 A 候选 x≥a 且 x<d，假想账本中 A 持 r、B 持 f。检查 A 先完成时缺 f，检查 B 先完成时缺 r，两种排列都失败；x≥d 本来就不可授。候选 a/2 不持 r，排列 B→A 可兑现：B 全长/END 后保留 U，A 才取得 L/r/f 并最终保留 L/f。因此最大经检查可选 cap 正好 a/2，队列 B→A 又阻止 A 在 B 获授前抢回 r。若 B 的合法原请求尚未进入可见集合，检查不能替它造一个，A 可先等待；B 请求实际可见后再付费判定。

检查的保守假想全边运行不是真实执行或实验结果。设候选数为 C_num、已知参与请求数为 J_num、资源数为 R_num，直接枚举参考过程至多有 O(C_num·J_num!·J_num·R_num) 个有限集合/owner 判定，另加几何、账本复制、位长、验证和见证保存费用。这里三个计数都是输入对象数量，不是 density N 的赋值或另定义。终点会永久占资源，不能未经证明把枚举简化为“随便挑一个能完成者即可”的贪心检查。如此昂贵的保守规则只是一种可审查替代，不推荐无证据地把它当大规模主实现。

## 9. 两种替代的证据边界及公平比较

| 命题 | 替代一：全长或已有覆盖 | 替代二：可兑现次序并保留顺序 |
|---|---|---|
| 保留同一原 MOVE 与 q/C/b 不变量 | 是；只限制原可授 cap 的选择 | 是；标准预留不变，附加收费判定/调度约束 |
| 无免费途中进度 | 是；仅允许中心已有 q/几何/owner | 是；未登记原边不允许假想，实际推进仍只凭原权限 |
| 避免本两行反例的新初态 | 已给条件证明，不依赖 A/B 谁先处理 | B 原请求真实可见并有必要服务时已给条件证明 |
| 能修复已形成的 r/f 互持状态 | 不能靠选择规则撤回旧授权 | 该状态无可兑现排列；不能靠检查清空资源 |
| 一般活性 | 未证，保留许多整边循环停滞 | 仅证有效闭合队列的条件性完成；一般 lifelong 未证 |
| 大规模净效益与新颖性 | 无结果、无结论 | 无结果、无结论；枚举成本可能很大 |

两规则如果进入后继候选，须在 D/R/E0 上采用同一 cap 能力与调度规则、同服务容量和费用函数。E0 仍不购买 POSITION；不能为它免费提供到 cap 通知，也不能因它不查询而禁止它获得公开扩展/END机会。规则自身所需全部几何、扫描、队列和签发工作有用途且按实际发生计费；D 的查询索引不能伪装成所有臂共同必需成本。

不能只把“下一最小 cap”列成第三个已解决办法：A 虽先到 a/2，若其后机会在 B 完整获授之前继续提高到 a，闭入口 a 已要求 r，仍可复现互等。第二次机会的调度是实质问题，不能省略。本文因此仅给上述两种完整限制，并保留整边/原最大 cap 作为诚实路线对照；由根决定是否采用，不先修改 73 的主假设或 2×2 标签。

## 10. 未执行合同与具体剩余点

后继文档审计应逐项核：原边确为同图单步；K 含非零 F/Z；四块资源全部固定且初态/终点 masks 正确；闭端点 a、d、b 的进入/退出；CAND 的端点和中点；贪心第一次不可撤回预留顺序；任意 q≤s 的互等吸收性；小 cap 次序中 B 真正 END 与仍保留的 U；READY 未消费不妨碍合法资源交接；两规则中所有失败/扫描/等候费用；已授后不得取消；有限 O 内未完成全行保留。它们是纸面检查合同，**NOT_IMPLEMENTED / NOT_RUN**。

仍未闭合：固定 PIE-D 实际源提案身份；该固定非均匀跨行粗资源构造与最终主几何 profile 的关系；哪一种 cap/调度最符合主大规模任务及完整公开文法；规则二队列生命周期与所有 73 故障分支的最终全文整合；实际全成本和主效应；已发表基线/近邻及正式顺序审查。规则细方瓦片的另一个最大 cap 反例尚未在本文给出，不能将本粗矩形结果扩大到该子域。没有重复开展异步正方形环见证。

本次有界任务完成的是域纠正、一个同图平行反向边的严格符号反例、同一几何/CAND 的合法替代次序，以及两种明确收费替代规则及其限度。最终报告写完后会再次核 P2/73 身份，完成回执给出本新文件 hash/bytes/lines。H、T_delay、B_CAL、B_max、B*、P_active、density N 始终保持原义且未赋值。未编辑 P2、73、任何账本或其他既有工件。
