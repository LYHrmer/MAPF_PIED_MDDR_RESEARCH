Constructive subproblem C30, following concrete corrections of C29. You are a co-designer, not a generic reviewer. Do NOT claim you inspected source or ran code. Return a finite dependency and feasibility construction, not implementation/pseudocode or an actual experiment calendar. No numeric assignment/range/default to H,T_delay,B_CAL,B_max,B*,P_active,density N; do not rename them. Abstract lengths/counts/proof inequalities are permitted and must be marked non-experimental. English <=1000 words; finish END_C30_CONSTRUCTION.

Single task: build the CORRECT paid dependency graph for first nonzero MOVE through genuine TASK_SERVICE and a constructive embedding lemma into the existing finite public-calendar grammar. C29 conflated append with grant, put PRECHECK into ACTUATOR, and claimed a whole job costs one unit. Correct all three. Source's finite normal two-state output is a separate explicit premise being proved by root+source agent; do not purport to prove the C++ chain. Give exact per-station/cross-channel stages, same-time different-row handling, bounded full atomic guards/publications, FIFO occupancy, and explain whether finitely many bounds can jointly be met without treating a macro-job as a primitive. If genuinely missing any channel/token contract, identify that exact seam and propose the smallest shared definition.

Model:
- Finite word deterministic P_model: word operations paid before effects; big numbers, geometry, hashing, authentication, container traversal/copy, logging/GC expanded to actual basic steps. Imports have restricted authority. A job can take many steps.
- Same public O for all arms, ordered(t,row_id), t nondecreasing, may have multiple distinct rows at same t. Each row first evolves physics/autonomous switches, then publishes earlier-completed units within that row's finite publication allowance, then executes its type. New jobs/messages/results visible only on strict later rows. No recursive use of leftover supply for newborn work, no adaptive private-completion row injection. No cross-station borrowing.
- Stations: Srv_C single center writer/slot, Srv_E(i) single local writer, Srv_P isolated planner, Srv_A task snapshot, Srv_N(u,v) each declared directed channel. Per-station FIFO; a normal job holds its slot through its one future atomic publication; then sender releases slot while destination delivery proceeds. A source job waits for center result disposition on a reserved in-job control receipt slot; receives/validates/retires that slot in later Srv_P slices, never queues disposition behind itself. Planner history retained on normal soft stale, no rollback.
- Only existing public types: INIT; POLICY(SOURCE_REQUEST,i), POLICY(LOCAL_POLICY,i); CURSOR; TASK_REQ; PLAN_CHECK; ENTRY(i,PRECHECK); ACTUATOR(i); PROCESS_SLICE(server); NETWORK(channel); QUERY; COORDINATE; GROUP_RETIRE; END_LOCAL_RETRY; READY_CENTER_RETRY; TASK_SERVICE; MODE; FAULT; CUTOFF; STOP. CAPTURE is a paid job step, not a new public row type. Source/core normal-return and real END are not public processing supply.
- Every SOURCE_REQUEST opportunity counts (even empty) n_i from zero: n_i mod3 selects SOURCE_OR_EXTEND, RESEND_CAP, CONTROL_RETRY. No extra policy call hidden in receipt handling, no advancing/skipping phase based on private progress.
- QUERY/coordination/irrelevant public roles can have finite paid no-op work; cannot pretend they are free. An existence lemma over a symbolic calendar class is allowed; an actual O or selected parameter values is not.
- Full dynamic grant/cap publication and ACTUATOR guard capture CURRENT root in their original row, then complete necessary tests+commit within that row's supplied budget. No virtual time or other writer interleaving inside it. If incomplete: retain fees, no effect/watermark, later recapture; do not amortize an atomic guard across rows. Prior static work may be legally reused if paid/retained.

Exact protocol to respect:
A. World already in legal BOOTSTRAP HOLD, nonzero lateral-error tube Z, assigned task heads different from starts. Paid software INIT transfers bootstrap responsibility, creates resident indices and initial cursor evidence. Initial common Π length L=S=0, local c=0; known Kc only from legitimate paid evidence, never source-input guess. K_task only from paid TASK_REQ at center -> channel -> TASK CAPTURE at Srv_A -> copy/encode/sign -> channel -> center paid validation/install. A is a global atomic world ledger; no free per-agent head snapshot. CURSOR evidence similarly request/transport/Srv_E capture/reply/center validation; generic CURSOR uses its own stable agent rotation so repeated needed captures cannot freely select arbitrary agent.
B. Center's paid PLAN_CHECK checks all Kc>=S, PROJECT nonempty, no pending source job; freezes legal anchored input and requests Srv_P. Source computes given normal nonempty paths and PRE_COMMIT_SHAPE precedes its actual core planner_commit; all source/import/output/cleanup work billed. Center validates latest PROJECT/versions/anchor and CAS-appends Π; this creates original MOVE slots but NO resource grant, no RUN, no motion.
C. SOURCE_OR_EXTEND, from paid known Kc and committed original MOVE, allocates unique request tid/slot/parent, then the paid PRECHECK chain. PRECHECK CAPTURE at Srv_E proves local RESIDENT_OPEN, c=k, resident at original u, active/pending NONE, HOLD, original slot unconsumed/unended and exact binding. It is not an ACTUATOR test. Its certified reply travels to center. Center receives/validates, checks ordinary full-edge M0 resources FREE or own parent and no foreign fence, then atomically grants b=ell, retags owner, records GRANTED and initial control_epoch/RUN_ALLOWED intent, makes CUM_CAP message. The center commits grant only after this full paid work; initial grant never auto-emits RUN.
D. CUM_CAP transported to Srv_E; local verifies binding and reserves END record/handshake/event-descriptor slots. At its later paid current-state publication it installs C=ell, phase GRANT_ARMED, active_tid; no s/v/z/c change and no automatic START.
E. A later original CONTROL_RETRY policy phase at center creates fresh RUN tied to that public opportunity -> channel -> Srv_E authentication -> later publication of immutable ACTUATOR candidate. Authentication alone has no control effect. ACTUATOR consumes one candidate by published FIFO order, fully pays current identity/phase/epoch/attempt/motion guard and actual current delay-bit read/commit. Valid START needs installed positive C and reference HOLD. This physical/control engine has the agent's original control capacity; Srv_E and ACTUATOR do not overwrite each other's state.
F. One fixed external delay matrix, per-agent input order and rank of ALL public ACTUATOR opportunities including empty ones. Only current bit can be read at actual eligible START/RESUME, billed; no second draw or future tape API. Symbolic witness may require the realized attempt's bit permits START and finite message deliveries; cannot filter main roster using this condition.
G. Given uninterrupted valid RUN at full cap, already-proved ideal reference control reaches ell in finite T_ref. Actual z follows invariant nonzero box; real END creates durable EndRecord/pending/TERMINAL_UNCONSUMED and leaves c unchanged. It emits only an opaque future local END offer, not a free center certificate/READY. Fixed TASK_SERVICE row may count this agent when original still-effective head is at v and entire X(t) subset ServiceRegion(j), before Stop, even before any END proof or READY reaches center; intermediate cap HOLD cannot count. Take symbolic sufficient condition v+F+Z subset ServiceRegion(j); all swept envelopes/closed tile owners separated to allow ordinary grants. No initial-Q substitution, no resetting z.
H. Full E/weights/Q/E target and D/R/E0/H19 arms unchanged. This is only useful pipeline non-vacuity, not D superiority, lifelong global deadlock freedom, source full-history qualification, or all public instances legal. Do not claim the four-agent rotation proves superiority over cycle-free-input TPG methods.

Return (1) compact but complete dependency graph using these types/roles, including required phase/no-op delays; (2) a precise finite-word-work/per-row/publication bound scheme and topological same-t embedding argument, explicitly separating known definitions from unproved source work bounds; (3) smallest manuscript-ready statement and any genuine unresolved seam. Never say "each node costs 1" or "O finite therefore succeeds."
