你是研究设计建设性协作者，NONBALLOT，不是正式审查。只根据本提示工作，禁止工具、skill、文件读取、外部访问、代码、构建、测试、仿真、参数赋值和实验载荷；直接返回完整中文推导，约 2000 字以内，最后一行 ADVICE-COMPLETE。不声称执行或测得效果。保护 H、T_delay、B_CAL、B_max、B*、P_active、density N 不得出现任何取值、范围、默认或先验。不要给整篇泛泛建议。

任务：为下述 MAPF 实际误差组合设计建立一个严格的条件性正收益见证、对应零/负收益域，以及真实任务计数和认证知识分离的公平合同。不要重复全领域文献检索或源码审计。
PIE 是唯一新 MOVE 源；策略只控制现有原 MOVE 能否授权及付费查询对象，不换邻边/任务目标。机器人沿原边 u→v，参考 s 单调、参考速度可暂停/有限到达；真实实体 X=u+s e+z+F，z 是固定世界坐标偏差盒 Z，真实投影可局部回摆。占用后缀 U(q)=segment(u+q e,v)⊕Z⊕F，q 是独立可信低层参考 s 的持久下界，普通 noisy pose/false COMPLETE 不得改 q。闭瓦片边长 zeta，所有与 U 相交瓦片都持有，只有持久可信 q 后可以退休永不再进的后方瓦片；真实实体永远包含于后缀。事务 START 前须完整原边后缀资源和持久COMMIT；资源owner单资源线性化，不超时释放。
主 2×2 因子：查询 BLOCKER（针对阻塞最多不同待发原提议的活跃tid，ties firstseen/identity）或 RR（活跃tid循环）；退休 SUFFIX 或 TERMINAL-ONLY。所有臂相同查询能力/低层/故障创新/费用计数。主效应 FULL=BLOCKER+SUFFIX 对 RR+SUFFIX；共同底座效果另外比，不能计成查询创新。查询/证书传输/验证/owner retirement/prepare/处理全部占真实时间且计费，世界运动不断时。STRICT 总费用匹配为主，NATURAL 为副；不预设正效应。
可考虑三机器人符号见证：A 从 u=(0,0) 沿+x到 v=(ell,0)，B 从 w=(0,−ell) 到 u，C 在远处无冲突原边。Z⊕F 被世界坐标半宽 rho 的盒包含。A 在 s 已充分前进而未终结时，用可信q清空B原边掩码所需的u附近瓦片；BLOCKER选A，RR按预定游标选C。不得仅据 q 过点就说清空，须处理 tile边界、完整后缀/足迹、误差有界查询及B驻留盒。若几何见证不成立请修正而不改PIE原边。
真正服务计数Q由独立 scorer 在预先固定公共 TASK_SERVICE 机会检查真实纵向终点模式和目标服务区域，初始合法resident同理；每agent每机会最多一次队首任务，重复证据不多计。真实Q不以高层 ENTRY_READY收据到达为门槛，避免查询只加速认证计数造成假收益。算法任务队列/完成知识另由可信TASK_RECEIPT到达更新，普通COMPLETE无权。低层真实terminal会持久关闭旧tid，但证书形成/传输/资源交接可能较晚。下一MOVE仍须完整resident_ready及原提议。现有正收益见证最好比较物理到达时间，并说明在不事后挑cutoff的固定服务机会下，何时才出现真实Q增量。
交付：1) 具体足够几何不交关系及正确性；2) 含所有费用的事件时间不等式，说明这是条件性解析预测非结果；3) 无收益/负收益/不可比条件；4) Q与认证知识分离的必要可验证字段、因果/去重/Stop先后规则及一个反例；5) 明确哪些强主张无法由此证明。
