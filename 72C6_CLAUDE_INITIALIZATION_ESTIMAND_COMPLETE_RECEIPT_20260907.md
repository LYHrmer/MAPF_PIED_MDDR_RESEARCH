# 72C6：初始化比较量完整Opus非投票回执

真实第二次exec68174，exit=0、is_error=false、terminal_reason=completed，duration173226ms，正文至ADVICE-COMPLETE。首次exec40388网络失败另存，不回填成成功。客户端报告claude-opus-5/firstParty，input_tokens=1、cache_creation_input_tokens=4530、output_tokens=2357；这是返回的元数据，不是根独立核后端身份。仅自给提示，未读取固定72、skill或外部材料。完整原JSON如下，之后根逐条不用skill检验，不把建议当正式审查或自动纳入新候选。

```json
{"is_error":false,"duration_api_ms":173183,"num_turns":1,"stop_reason":"end_turn","session_id":"4f2889bc-48a2-4cdc-abe1-a88bb98844a5","total_cost_usd":0.0872425,"usage":{"input_tokens":1,"cache_creation_input_tokens":4530,"cache_read_input_tokens":0,"output_tokens":2357,"output_tokens_details":{"thinking_tokens":0},"server_tool_use":{"web_search_requests":0,"web_fetch_requests":0},"service_tier":"standard","cache_creation":{"ephemeral_1h_input_tokens":0,"ephemeral_5m_input_tokens":4530},"inference_geo":"","iterations":[],"speed":"standard"},"modelUsage":{"claude-opus-5":{"inputTokens":1,"outputTokens":2357,"cacheReadInputTokens":0,"cacheCreationInputTokens":4530,"webSearchRequests":0,"costUSD":0.0872425,"contextWindow":200000,"maxOutputTokens":64000,"canonicalModel":"claude-opus-5","provider":"firstParty"}},"permission_denials":[],"terminal_reason":"completed","fast_mode_state":"off","fast_mode_disabled_reason":"sdk_opt_in_required","subtype":"success","api_error_status":null,"result":"# 72C6 建设性数学意见（非投票、不触及固定72）\n\n## 一、两个目标必须分层：合法世界条件下的运行表现 vs. 含初始化的部署表现\n\n设每个 block b、每臂 a。定义两个互不可替换的对象：\n\n**目标 A（条件运行表现）**：θ^A = 在\"存在合法 INIT、且由环境规则从合法初态出发的真实物理轨迹与总 STOP 规则可计算 Q\"这一事件上定义的表现。Q 的定义域就是合法轨迹集合；Q/E ∈ [0,U]，U 只来自固定公共服务机会。**Q=0 是目标 A 内的合法取值**，但它需要\"完整有效运行\"证书：全 HOLD 或合法策略失败都属此列。\n\n**目标 B（部署表现）**：θ^B = 从\"只有输入描述\"这一起点、在同一不重开的公共窗口 E 内、与观测/规划/协议/初始化成本竞争同一预定有限服务容量之后的净结果。\n\n关键的不可逾越处：NOT_LAUNCHED、INIT_INCOMPLETE、日志未知、源不规范行为这四类，**都不在 Q 的定义域内**。它们不是 Q=0，也不是\"低分\"；把它们记为 0 等于虚构一条不存在的合法物理轨迹，即在未定义无效物理轨迹的前提下偷偷定义了它。因此：目标 A 在这些格子上无值；目标 B 在这些格子上**只能记录状态，不能记录 Q 值差**。可在纸面上诚实记录的是合法性、状态、时点、成本；不能记录的是\"若当时运行会得到的 Q\"，除非潜在合法世界本身已被独立定义。\n\n**最小可行联合结果结构**（每 (b,a) 一行，固定 roster，禁止运行后按成败删样）：\n\n- `L`：源/行为规范性 ∈ {规范, 不规范, 未知}\n- `S`：INIT 状态 ∈ {NOT_LAUNCHED, INIT_INCOMPLETE, INIT_COMPLETE, UNKNOWN}，四类互斥且穷尽\n- `τ`：INIT 完成时点，记在 E 自己的时钟上（E 不因初始化慢而重开、不缩放）\n- `C`：成本向量（观测/规划/协议/初始化），以消耗的公共服务容量单位记账，**不折算成 Q**\n- `A_Q`：Q 可用性 ∈ {可用（合法轨迹+总 STOP 已算）, 不适用（无合法世界）, 未知（日志缺）}\n- `Q`：仅当 `A_Q`=可用时填写，且 Q/E ∈ [0,U]\n- `cert0`：若 Q=0，须附完整有效运行证书\n\n报告形式为该联合结构的交叉表，**不合成任何单一分数**，不引入用户未认可的权重，不改变 E。\"物理未知\"单列，不与\"算法绩效差\"同格。\n\n## 二、有界部分识别能否替代整次 UNESTIMABLE\n\n可以，但只在一个严格受限的子域上，且需要区分两种缺失。\n\n**情形 1（可界）**：该臂有合法 INIT、潜在完整轨迹在 E 上有定义，仅 Q 未被观测。此时潜在 Q 是同一概率空间上的真实随机量，逻辑上必落在 [0,UE]。于是对 d_b=(Q_D−Q_R)/E 得 Manski 型界\n\nL_b = (Q_D^lo − Q_R^hi)/E ≤ d_b ≤ (Q_D^hi − Q_R^lo)/E = U_b，\n\n单臂缺失时宽度 ≤ U，双臂缺失时 ≤ 2U，且 d_b ∈ [−U,U] 恒成立。\n\n**情形 2（不可界）**：连潜在合法世界都未定义（NOT_LAUNCHED、INIT_INCOMPLETE、源不规范）。此时 Q_R 不是任何物理轨迹的函数，[0,UE] 这个\"范围\"不是逻辑约束而是新造的假设，d_b 无所指，任何区间的覆盖率命题都不可证伪。**不能靠设范围把未定义修好**；这些格子仍按现行规则留在 UNESTIMABLE/单列，不进入区间。\n\n**跨 block 的陈述方式**：权重 w_b 实验前固定、与结果无关、Σw_b=1，block 数记作 m。目标 Θ=Σ w_b θ_b，θ_b=E[d_b]。由 L_b ≤ d_b ≤ U_b 逐点成立得 Σw_b E[L_b] ≤ Θ ≤ Σw_b E[U_b]。因成对差 d_b 已把同 block 各臂相关性吸收进单个单位，block 间独立即可用 Hoeffding：对固定权重、取值范围 2U 的独立量，\n\nP(|Σw_b(X_b−E X_b)| ≥ t) ≤ 2exp(−t²/(2U²Σw_b²))，\n\n取 ε = U√(2Σw_b² ln(4/α))（上下端点各分 α/2）得\n\nCI = [Σw_b L_b − ε, Σw_b U_b + ε]，覆盖 Θ 的概率 ≥ 1−α。\n\n**最坏缺失机制下无条件成立的**：上式。原因是 L_b、U_b 只由已观测量构成，且不等式对每个实现逐点成立，故不需要 MAR/MCAR，缺失机制可与结果任意相关。前提仅四条：block 独立、权重先定、每个纳入 block 的 d_b 有定义（即无情形 2）、U 由公共服务机会外生固定。\n\n**条件于\"发布/完整个案\"则失效**。反例：两类 block 各占 1/2。第一类 R 初始化便宜、完成，d_b=0。第二类恰在困难实例上 R 因费用未完成 INIT，其潜在 d_b=−U/2。完整个案 CI 收敛到 0，而 Θ=−U/4，偏差固定、宽度以 1/√m 收缩，覆盖率 →0。同一数据的最坏界给出含 −U/4 的区间。这正是\"D 已完成、R 因费用未完\"时不能给全窗口部署差值的形式化理由：那不是缺一个数，而是缺一个世界。\n\n## 三、最保守的实验前建议\n\n保留 72 现行主目标不动：成对 d、必报 D−E0、任何必需评分未知即整次 UNESTIMABLE、不输出只对成功臂的 CI。仅新增**明确标注的二级结果**（非主、不参与结论）：(i) 上述联合结构交叉表；(ii) 仅在潜在 Q 有定义的子域上的识别集 CI，并附\"最坏缺失稳健、但不可条件于完整个案\"的说明；(iii) INIT 在 E 内完成率，作为率报告，不挂任何 Q。\n\n**纸面即可关闭的歧义**：合法 INIT 的可验证互不相交定义；总 STOP 规则；\"完整有效运行\"与 Q=0 证书；成本记入公共容量的记账口径；四类状态互斥穷尽；E 不重开、不缩放；U 的唯一来源；固定 roster 与禁止事后删样；权重先定；`A_Q` 必须在读取任何 Q 之前判定，以阻断结果依赖的定义漂移。\n\n**必须未来实验才知**：各臂在真实费用下的初始化成功率、τ 分布、区间是否有信息量（宽度）、部署净效果的符号与量级。\n\n最后两点平衡：完整记录只证明缺失被诚实分类，**不构成效果证明**；反之，\"未获授权实验\"与\"须先出示实际正结果\"若同时成立即为不可满足的循环。出路是现在就在纸面固定何为证据，使未来一次获授权的运行能够直接判定，而不是把正结果当作获得许可的前置条件。\n\nADVICE-COMPLETE","ttft_ms":125619,"ttft_stream_ms":125419,"time_to_request_ms":65,"type":"result","duration_ms":173226,"uuid":"470efc96-67c3-44c6-a4e8-c4758fe4737a"}
```

## 根直接数学分析与更正

1. 接受将全roster合法性、INIT状态/完成时点、各费用、Q可用性及可估性联合报告，不引入任意权重合成部署单分数。不等于条件于运行成功重选总体；“合法世界条件下”必须指事先定义的数学域，不能含糊指实际成功个案。
2. 顾问开头把“日志未知”一律说成不在Q定义域，与其后可界情形不一致。根纠正：缺日志可能只是已有合法潜在Q未见，也可能连源/物理定义是否成立都未知，须分判；前者可作未知观测，后者无资格任意补范围。
3. 最严重错误是完整个案反例：先说R的INIT_INCOMPLETE没有潜在Q，后又为该类直接设潜在d并计算总体均值。该反例不成立，不能用未定义量证明选择偏差。合法替代是全block均有定义的d，但日志可用性依d而变：两类同概率，d分别为0与−U，只有前类日志可用；完整个案均值为0、原固定总体均值为−U/2。这里U是符号服务率上界，不是保护参数数值，也不生成实验。
4. 部分识别必须有全固定总体潜在d的定义证明，才可用每臂[0,Qmax_b]及观测精确值构造逐点边界lo_b≤d_b≤hi_b。不得运行后删掉“无定义”block再重权，亦不能把该子集标原主总体。没有这种全域前提仍按UNESTIMABLE保留，联合状态表不受影响。
5. 顾问对上下端点分别用Hoeffding时还需要端点跨block独立；任意全局缺失机制可以使端点相关，不能只从潜在d独立推出端点独立。根给更稳健推导：若潜在d_b本身跨block独立、d_b∈[−U_b,U_b]，固定非负w_b和为1，则r=sqrt(2 log(2/alpha) sum_b w_b² U_b²)满足Pr(|sum_b w_b d_b−sum_b w_b E d_b|>r)≤alpha。逐点sum w lo≤sum w d≤sum w hi，于是[sum w lo−r, sum w hi+r]包含该同一集中事件的区间；即使缺失掩码跨block相关，也有对原Θ的无条件覆盖。前提是所有边界对每个实现都真、潜在Q已有定义，未知合法性不被这一步变真。alpha只作为符号使用，当前不新增绑定。
6. 不要求逻辑上必须先判A_Q再看到任何Q；必须预先固定判据，按有定义的安全/日志/源证据求值，不能因Q高低任意改变可用性或删样。原本某些状态必须到STOP/全日志时才可判断，这不自动破坏预注册。
7. Q=0不一律要求跑满窗口：若合法总STOP允许完整认证零长度/早停前缀，固定E下该规则可给合法零；完整认证的含义不能偷换为完整暴露。NOT_LAUNCHED/INIT未完成仍不能无前提当零。
8. “INIT完成率”也要保留未知状态而非丢未知分母；可报告固定总数上的已确认完成/未完成/未知，是否计算率区间需明确其真值存在与抽样域，不靠标签齐全自动证明统计量可估。
9. 这是可参考的预实验结果结构/数学分支，不是改变固定72的许可；当前72依旧缺任一必需评分即主UNESTIMABLE。若后继选择部分识别，必须在三路收齐后新编号完整定义并接受顺序审查。完整记录不是吞吐正、初始化成功或外部优越性的证据。
