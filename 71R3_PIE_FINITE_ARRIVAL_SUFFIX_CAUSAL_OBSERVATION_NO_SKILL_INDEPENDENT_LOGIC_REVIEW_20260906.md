# 71R3：候选 71 的无 skill 独立逻辑审查

日期：2026-09-06。身份：固定候选 71 的正式第三路独立逻辑审查。报告状态：COMPLETE。

结论：候选的有限参考到达、后缀包含和有界变量统计推导，在其明确假设内未发现数学反例；实验前协议仍有四项承重定义缺口，不能据本报告验收候选。独立六门为 **3 PASS / 2 UNKNOWN / 1 FAIL**。保持 **HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT**。

## 审查身份与真实读取记录

唯一科学输入为 [固定候选 71](/home/lyh/MAPF_PIED_MDDR_RESEARCH/71_PIE_FINITE_ARRIVAL_SPATIAL_SUFFIX_AND_CAUSAL_OBSERVATION_PREEXPERIMENT_CANDIDATE_20260906.md)。下文 L 指该文件的实际行号。

| 身份项 | 开始核验 | 结束核验 |
|---|---|---|
| SHA256 | `3a3da493613d36dfe64280d468dd34c90498cdfe854ba939128b1e84bb7b4d51` | `3a3da493613d36dfe64280d468dd34c90498cdfe854ba939128b1e84bb7b4d51` |
| bytes | 72567 | 72567 |
| 换行数 | 352 | 352 |
| 逻辑行数 | 352 | 352 |

开始时一次完整读取候选字节，完成散列与 UTF-8 解码，保存到工具会话内存缓存。随后仅从同一缓存按六个连续窗口展示并实际阅读：L1–60、L61–120、L121–180、L181–240、L241–300、L301–352。全部窗口均完整展示，无缺段，无需重新读文件补文。覆盖为连续 L1–352，不是关键词检索或摘要代读。审查结束只流式重核候选散列、字节与行数，没有重新通读候选。

本路没有打开、读取、调用或使用任何 skill，也没有使用 skill 报告模板。未读取其他候选、审查报告、账本、Claude 意见、旧 35–38、Q-CAL/Q-CONFIRM 科学载荷或结果、桌面材料；未递归扫描研究目录。候选自身的历史转述是已读文本的一部分，但没有将其视为旧审计亲见证据、他路投票或本路裁决依据。未向其他审查者索取意见。

所有 shell 调用均原样以 `rtk proxy` 起首。唯一文件写入是使用 apply_patch 新建本约定报告；创建前核验目标不存在。没有修改候选或其他文件，没有实现、构建、创建或执行测试、仿真、生成运行载荷或结果、部署。H、T_delay、B_CAL、B_max、B*、P_active、density N 均保持未赋值；没有给数值、范围、先验或调参建议。

## 本路一手来源核验边界

本路只定点访问候选已明确引用的一手来源，未开展扩展文献搜索。以下“部分”均不等于全文，网页工具返回总页数或总行数也不等于实际完整阅读。

| 来源 | 本路实际亲见范围 | 本报告使用范围 |
|---|---|---|
| [C3 PIE-D 官方论文](https://ojs.aaai.org/index.php/AAAI/article/view/34506/36661) | 8 页 PDF 的提取文本部分，实际展示 L0–344；未读余文，未审计图像或排版公式 | 核对 lifelong、并行规划执行、承诺窗口和原 no-start 延迟边界 |
| [C4 TIP 官方论文](https://cdn.aaai.org/ojs/17347/17347-13-20841-1-2-20210518.pdf) | 9 页 PDF 的部分文本：连续展示 L333–639，另亲见针对 following/atomic 的检索片段，包含模型和通信边界；未读全文 | 核对 no-following 适用域、原子激活抽象和通信延迟未由原文处理的边界 |
| [J2 官方期刊页面](https://link.springer.com/article/10.1007/s10514-025-10225-4) | 实际展示页面 L0–92，含题录、摘要及引言前段；未读全文 | 核对主动获取观测及其费用确有直接已发表近邻，不授予本候选的可信释放定理 |
| C4 固定 GitHub tree、一个推测的原文件 raw 路径、候选列出的外部 mcp.cpp 精确 blob API | 均访问失败：网页工具返回 Internal Error 或不可打开；没有获得代码正文 | 不据失败推断源码内容或修复状态 |
| 候选列出的 R0 SharedEnv.h 精确 blob API | 访问失败，没有获得代码正文 | 不冒称本路独立重核了 R0 字段或 blob 字节 |

J2 的官方页面支持候选对付费观测近邻的保守定位。C3 与 C4 的亲见部分支持把原提议核心、执行适配及动作域差异分别说明。本文不据这些部分阅读宣称已完成候选 L348 要求的四份期刊全文终审，也不把候选列出的源码散列当作本路已经复算的散列。J6/J7 正文及历史来源回执的完成情况，本路没有独立新增证据。

## 承重意见 B1：ABORT 后的逐 owner 清理缺少吸收性的迟到请求规则

**定位：** L111–123，尤其 L113 的再尝试前提、L115–117 的 owner 状态与 PREPARE 规则；L132–134、L150–152 的 DRAIN 与恢复；L217 的网络乱序/复制。

**具体缺口：** 文本要求 D=ABORT、旧 gate 永久关闭、所有旧 PREPARE 清理可证后才能重新尝试，这是正确的目标条件。但是 owner 的列举状态和 PREPARE 转移没有说明：清理时该 owner 尚未见过旧 PREPARE，是否也必须持久记录拒绝该 tid 的终态；以后第一次到达的旧 PREPARE 如何拒绝。L117 仍允许 FREE 资源持久预留，重复同 tid 则返回原 PREPARE 收据。ABORT 决策的不可逆性本身只能阻止旧 tid 合法 COMMIT，不能直接推出所有 owner 不会再增加旧预留。

**静态分歧：** 取旧 tid 在某个非父 resident 的参与资源 r 上尚未送达的 PREPARE。D 先变 ABORT，entry 已关闭；随后 r 收到清理/核查请求，此时其当前 holder 是 FREE。再让较早发送的旧 PREPARE 迟到。若清理只证明“此刻没有旧 PREPARED”，L117 的 FREE 分支仍可创建旧预留；若清理安装吸收性的 abort fence，则同一迟到请求被拒绝。这两个局部解释对旧责任清零、下一尝试准入及后继阻塞产生不同结果。

本意见没有把一次 FREE 查询擅自当作文本所要求的“清理可证”。问题恰是候选未给出能在消息仍可迟到时证明该条件的 owner 收据语义。若采取始终 UNKNOWN 的最保守解释，覆盖可保留，但再尝试/清理合同也尚未获得一个可核的完成分支。

**适用范围与影响：** 涉及未启动 ABORT、丢失/乱序/复制、恢复及新 attempt_generation；不依赖界外物理或 Byzantine 故障。这不是已经找到的碰撞反例，也不是要求失联时最终完成。承重之处在于 DRAIN 和“允许再试”的证明对象尚未闭合，以及旧 tid 是否可以在清理后重新获得预留这一方法行为没有唯一答案。

**客观关闭条件：** 在后继新编号中定义逐 owner 的持久 ABORT/关闭 fence，覆盖从未 PREPARE 的参与者；明确迟到首次请求、重复 PREPARE、重复清理及迁移后的返回和不变状态；明确清理收据证明的终态及完整参与者屏障。纸面分别证明“PREPARE 先于关闭”和“关闭先于 PREPARE”两种局部线性化顺序，都不能在关闭收据后重建旧 claim。未知权威仍可 PENDING，不要求跨资源原子动作、实现或实测。

## 承重意见 B2：RR 尚不是具有唯一初态和选择域的总函数

**定位：** L144–146；L159 的 INIT；L209 的 INIT 字段；L230 的按需创新键；L283 的 RR 下一项 C 见证。

**具体缺口：** RR 规则依赖“上次实际选择 key”，但 INIT 没有定义尚未实际选择过时的 cursor。文本又使用“相同 active tid 集”，没有给出该集合在 D/entry 各状态上的成员谓词；BLOCKER 则另外列出了持有阻塞资源、尚非可信终结及可退休性的筛选条件。L283 让不阻塞 B 的 C 成为 RR 下一项，说明不能简单把 RR 集合直接认作 BLOCKER 最终候选集合。

**静态分歧：** 在首次查询前，A 与 C 均持有合法未终结责任，只有 A 阻塞 B。按规范 key 从首项开始与从另一个环位置开始，均未违反已写出的“按上次实际选择 key 循环”条件，因为上次选择尚不存在；选择结果可以分别是 A 或 C。另取 C 处于 ISSUED/no-start、A 处于 RUNNING：把 active 定义为已 COMMIT 未终结，或仅定义为已物理启动未终结，会再次改变 RR 的查询对象。BLOCKER 仍可选 A。

**适用范围与影响：** 这直接改变主对照 RR+SUFFIX 的费用工作、创新消费、资源退休与 Q，进而改变 d_b 和 theta 所对应的方法。相同预付容量无法消除选择规则的差异；这不是无关的排序风格。候选对重复失败也消耗 cursor/ordinal 的规则本身是清楚的，本意见不否认该部分。

**客观关闭条件：** 给出不依赖结果的 RR 初始 cursor/首选规则；给出全部 entry/decision/终结待交接状态的查询资格表，并区分两策略共享的基础集合与 BLOCKER 的过滤集合；说明集合只能由哪些已收费、已发布的本臂记录更新。然后用首次查询、ISSUED 未启动、TERMINAL_CLOSED 未 READY、集合删除/加入及空集合的符号行证明唯一返回。只需纸面定义，不需要绑定任何保护参数或执行查询。

## 承重意见 B3：可信采样时点没有接入唯一的付费事件线性化规则

**定位：** L80、L86；L213、L217–218、L224–228、L232–236；L283 的包含费用上界 f。

**具体缺口：** 候选明确规定了请求、处理完成、后继行发布、网络传输和费用，也区分 sample_time 与 delivery_time。但 QUERY_OPPORTUNITY 的一次展开只写“发可信参考 QUERY job”，没有指定何时把真实参考 s 捕获为不可变输入：发起该 job、远端收到请求、生成工作开始、某个工作前缀结束，还是证书生成完成。sample_time 是证书字段，记录它不自动决定抽样发生的规则。TASK_RECEIPT 的原子快照也需要同类捕获点，不能由事后最新可见真值补填。

**静态分歧：** 保持同一公共 O、同一请求、同一 PROCESS_SLICE 工作量和同一交付行。让机器人在排队/生成期间从某瓦片仍与 U(q) 相交的阶段，合法前进到可以证明该瓦片不再相交的阶段。实现甲在请求进入时捕获参考 s，实现乙在生成结束时捕获参考 s。两者都可提供含真的区间、正确 sample_time、完整收费和延后可见性，且不冻结物理演化；前者可能不退休该瓦片，后者可能退休并使后继赶上同一公共 ACTUATOR/TASK_SERVICE 机会。候选现有规则没有在二者之间作出选择。

**适用范围与影响：** 涉及模型内合法运动及非零排队/生成等待，不需要伪造可信证据。P2 对两种证书都可能安全；缺口落在因果干预和费用时间的唯一性，因而会改变主方法、收益见证中的 f/r，以及固定 X 下的潜在 Q。给工作函数加费用而不指定取样点，不能关闭这个差异。

**客观关闭条件：** 指定取样对应的已有事件/结果发布点或已定义 job 内的精确有限工作前缀；捕获时固定 sample_time、参考值、身份/epoch 和区间创新，后续生成/验证只处理该不可变样本；规定故障/重试是重传原样本还是创建新样本及其 ordinal。对任务快照指定全局修订的捕获线性化点。给出捕获、完成、发布、发送、交付、使用的顺序和费用归属即可，不要求增加任意事件类型、实现低层或做实测。

## 承重意见 B4：外部守卫基线的可信游标能力尚未映射到现有事件和证书合同

**定位：** L159、L194–198；L86 的 QUERY 证书；L152、L213–216、L224–226 的收费服务。

**具体缺口：** 守卫适配写明 getT 使用认证游标低界、不免费读取他者真实 cursor/head/tail，并称依赖信息经同类型收费服务。但是现有参考 QUERY 证书不含可信消费游标；ENTRY_SERVICE 按终点→latch→转换→READY→退休→drain 推进；TASK_RECEIPT 更新的是任务账本。尚无明确的请求主体/触发规则、捕获字段、认证范围和结果消费者，说明外部依赖游标从哪个已列服务获得。不能仅靠把一个 job 称作 OWNER/ENTRY/QUERY，就确定它可以返回任意新增可信字段。

**静态分歧：** 一个 agent 在同一合法 resident 上消费一次 COMMITTED_WAIT 前后，真实位置、旧 tid 的终点状态、参考 s 和旧终点证书均可相同，而 c 已改变。因而已有物理证书不能一般地区分这个逻辑推进。实现甲只使用已列物理证书，对未获认证的 getT 门槛持续保守 WAIT；实现乙新增一个可收费返回逻辑 c 的查询，使门槛在后续机会解除。两者都可以被描述为“不免费读真值”，却具有不同的信息能力、请求次数、延迟和外部比较结果。

这里不声称已经亲见固定 mcp.cpp 中某个具体条件表达式，也不要求外部原论文拥有新的 lifelong 算法。指出的是候选自己采用的“认证游标低界门控”还没有唯一的收费适配合同。即使采用永久保守低界是安全的，它也不能与另一个能更新低界的实现当作同一个既定比较器。

**适用范围与影响：** 直接影响 G4 的公平信息能力和固定计划补充比较，不直接否定主四臂的 P1/P2。no-following 检查、末端索引守卫、来源失败不记作收益、全不适用时 UNESTIMABLE 等已有规定均应保留。

**客观关闭条件：** 将该依赖服务明确映射到现有机会种类；指定请求选择/排队、可返回的 cursor 下界与 head/tail 所依据的持久版本、捕获/认证/消息费用、重放/乱序规则以及 getT 的知识更新函数。对 WAIT 消费、MOVE no-start、READY 尚未消费、最终末索引给出符号对应。若仅从现有证书推导保守低界，直接写出该推导和不能更新时的唯一行为，并如实标明比较器能力即可。关闭要求是纸面适配和证据边界，不是先构建原法。

## 数学、控制与安全部分的独立判断

**P1 在其新准入域内成立。** L78 的逐分量变分常数界足以证明 Z 不变。LAUNCH 的首次交点论证可直接核得：设启动余长为 xi_0，因 v_ref 至少按 a_launch 增加，有 F_L(t) 至少为 a_launch(a_launch+a_lo)t²−2a_lo xi_0，因此候选给出的上界方向正确。交点在余长为正时发生，LAUNCH 不会先越过 APPROACH 的参考速率上界。

APPROACH 内 r_dot=(v_ref/xi)(r+a_cmd+eta) 正确；在下边界其括号项为 delta+eta，在上边界为 −delta+eta，方向分别朝内。有限正余长紧子域上的局部唯一性与 sqrt(xi) 的下降界支持有限末时连续延拓。末端不计算 0/0，驻停约束被明确列为混合物理公理；本审查不把它误作普通反馈已经抵消未知扰动的证明。

BRAKE 中 b_dot=−2v_ref(a_hi+v_ref_dot) 非负，停车距离与时间上界方向正确。仅旧较弱刹车锥不足以保证不越端点，候选已排除该入口。连续扰动造成真实实体仍可移动，候选也已正确区分 v_ref=0 与真实静止。P1 没有保证无限中断或永久 no-start 下完成，不能据此另立活性缺陷。

**P2 的核心包含关系成立。** 同一旧 tid 的参考 s 单调，含真的可信采样下界给 q≤s，且 z 始终在 Z，因此当前及此后旧边阶段在 U(q) 中。退休检验使用整段后缀和 Z⊕F，能够覆盖真实回摆；新边重入必须重新取资源。闭瓦片接触保守计入足以支持资源覆盖的碰撞反证。

**P3 的物理覆盖归纳未发现独立的碰撞反例。** 完整 PREPARED 已提供 START 前覆盖；父 resident 的同 owner 规范化避免自重叠保护缺口；中段先持久 q 再逐 owner 退休；端点标签转换保持同实体覆盖；READY 的完整转换收据屏障不需要跨 owner 同时快照。旧 RELEASE 对新 holder 的 expected_holder 检查和旧 START 的持久 closed fence 是必要且已写出的保护。B1 针对 ABORT 清理证据与责任吸收性，不能夸大为已经推翻这一条件性碰撞证明。

**效果见证仅支持条件性存在主张。** L281 的瓦片 x 上界与后缀 x 下界若按该见证的附加几何条件成立，严格分离足以排除闭边界接触。不能把未另定义尺寸界的任意闭覆盖直接当作自动满足这一关系；这不否定允许选择满足关系的见证。L283–285 使用完整处理上界 f、另一臂最早释放下界 r，以及预先固定的服务机会跨越，逻辑方向正确。到达更早但没有服务机会跨越时 Q 可为零，候选已说明。该见证不推出主总体 theta 必正。

## PIE、事件、来源与统计的其余判断

L159–188 已把物理进度、可信消费 c、预测锚点、冻结 SharedEnv、真实任务 A、可信 K_task 和 PROJECT 分开。每机会最多消费一槽，PREPARE/COMMIT/ISSUED 不消费；旧路径不因新任务预测失效而撤回；作业结果接纳检查目标和版本；丢弃作业的内部状态不进入接纳流。这些规则可以在纸面表达原提议来源约束，未发现必须通过补邻边或伪目标才能解释的分支。

PROJECT 明确只是所有权受限的预测，空目标造成 HOLD/未完成是已披露的 shared 适配代价。本审查不要求它精确预测异步 greedy 未来分配，也不把有限任务停滞本身当作未宣称的活性定理被反驳。原生轨道与 shared 适配的区分适当；本路没有独立重算源码表中的全部对象，故不追加“原字节已复现”结论。

有限 O、严格后继行处理、单行有限复制数、PROCESS_SLICE 与 PRELUDE 的分工，已经消除了靠同一回调无限递归或工作未完成先见结果的直接解释。普通错误包不更新 q/任务知识，可信界外另列 MODEL_BREACH，也没有将过早 COMPLETE 当作真实服务。除 B2–B4 指出的采样与信息/选择定义外，收费和因果性有明确纸面骨架。

STRICT 比较相同预付容量，NATURAL 比较同价实际消耗，这两个估计对象的区别已明确；实际工作不等于预付费用，候选没有强称 realized work 相等。对失败、空闲、丢包、重试、丢弃作业仍收费，方向公平。需要关闭的是具体策略和采样服务的唯一映射，不能用“所有臂共用一个未定实现”替代它。

来源选择器保留静态排除、别名、跨地图 task incidence、空必需类别和动态失败，固定分母/权重不按结果重分配。未实例化 manifest、未绑定参数、机械检查 NOT_IMPLEMENTED/NOT_RUN 是本阶段的诚实状态，不单独构成实验前协议失败。B2/B3 尚会影响 manifest 中所谓已固定“适配/服务 profile”到底定义了哪个输出函数，因此闭合数据协议目前仍 UNKNOWN。

Classify/Score 已区分未合法发起、可验证立即崩溃、完整停止前缀、评分未知和模型外事件；完整前缀后停止约定不再新增 Q，与固定设计暴露 E 相容。重复同 task 服务去重，真实完成不等待查询证据，丢失必需分数不填零或删臂。这些规则没有发现代数上的自相矛盾；未来仍须在日志中实际证明其使用的前缀。

统计区间的推导正确：d_b∈[−U_b,U_b]，非负权重下中心化项的支撑跨度为 2w_bU_b，独立块的矩母函数相乘和双尾界给出候选的半径。无需正态、符号交换或把固定地图重新抽样。若潜在合法评分总函数有定义，发布事件与未覆盖事件的交集概率由完整区间的未覆盖概率控制；候选 L276 已明确这不是条件于成功发布仍有既定覆盖率。B2/B3 未关闭时，不能把这个正确的统计定理当作已经唯一确定研究中的潜在 d_b；定理本身无需因此改写。

## 六门独立裁决

本表严格按候选 L346 的六门对象裁决实验前纸面协议，不继承 L346 转述的旧票，也不把 PASS 解释为授权实现、复现成功、性能证实或完成 L348 的全部终审。

| 门 | 本路结论 | 理由与边界 |
|---|---|---|
| G1：可解释的实际误差问题 | PASS | L11–17、L76–80 明确区分 no-start、实体偏差和错误普通反馈；可信纯仿真假设及不适用域明示，问题与真实服务目标一致。 |
| G2：已发表直接锚点及诚实路线比较 | PASS | 直接近邻可由本路定点一手来源部分阅读确认；候选对控制域差异、摘要/全文边界和强新颖性 OPEN 的表述保守。此票不授予排他新颖性，也不补造四期刊全文或历史来源回执。 |
| G3：固定官方 PIE-D 与可核适配/原法合同 | PASS | 固定对象标识、唯一原提议映射、原生/共享分离、冻结环境与失败分支、未来 ORIGINAL-CHECK 合同具备纸面可核结构；未以必须先运行替代本门。此票基于候选定义及 C3 的亲见部分，不冒称本路已复算全部源码对象或依赖。 |
| G4：已发表外部固定基线与公平适用域 | UNKNOWN | 原域/no-following、末索引守卫及不适用登记是实质进展；B4 的游标知识与收费请求映射尚不能唯一决定比较器行为。固定代码访问失败也不能补成亲见实现证据。 |
| G5：公共场景/来源和闭合数据协议 | UNKNOWN | 静态来源枚举、权重、失败与未知分数规则可读；B2/B3 使适配/服务 profile 到观察、请求和评分日志的映射仍有多个合法解释。这里的 UNKNOWN 不是因为尚未生成 roster 或赋参数。 |
| G6：物理—算法—事务—费用—评价公平纯仿真链 | FAIL | P1/P2 与条件性覆盖证明可接受，但 B1 的 ABORT 清理证据未闭合，B2/B3 直接改变主策略和因果收费输出，尚不构成唯一可核的完整链。FAIL 指协议验收失败，不是声称已运行碰撞或统计公式错误。 |

PASS 三项、UNKNOWN 两项、FAIL 一项。四项承重意见均有纸面关闭条件；它们不要求在控制门未开时通过实现、构建、测试、仿真或硬件证据来完成修订。篇幅、文风或候选序号不参与科学裁决。

## 结束核验与回执

全部科学阅读和最小公开来源核验结束后，候选仅作身份重核，SHA256、72567 bytes、352 换行与 352 行均与开始及任务指定值一致。目标报告在新建前不存在，本报告为唯一新增工件。报告落盘后的 SHA256、bytes、换行数和逻辑行数由最终工具回执另报，不在报告内部自引散列。

第三路独立逻辑审查已形成完整实际报告。候选验收仍未通过；保持 HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT。没有实施或实验授权。

COMPLETE
