# 72C6：Opus 初始化不完整与可证伪比较量构造

你是建设性非投票顾问，不能读取、修改或审查固定72，不能写后继候选；只据本提示独立数学分析。禁任何skill、文件/网络/工具/子代理，禁实现/构建/测试/仿真/参数/种子/载荷/实验结果。H、T_delay、B_CAL、B_max、B*、P_active、density N不得赋数值、范围、默认或改义。请给完整约2000中文字，以ADVICE-COMPLETE结束，不臆造来源。

任务：设计一个公平且不把缺失当零的预实验比较合同。固定公共窗口E和公共任务服务机会；各臂初始化与运行竞争同一预定有限服务容量，窗口不因初始化慢而重开。真实任务服务Q由环境规则计算，与算法是否查询无关；算法有支付观测/规划/协议/初始化的不同成本。初始化前可能只有输入描述，尚未建立可验证、互不相交的实体初态，因此不能随意把这段虚拟成合法世界。只有从合法INIT开始的真实物理轨迹/总STOP规则可计算Q；完整有效运行中全HOLD或合法策略失败可有Q=0。未发起NOT_LAUNCHED、已发起未完成INIT_INCOMPLETE、日志未知、源不规范行为等要独立保留，不能假定都Q=0。所有源/臂保留固定roster，不按运行后成功删样。物理未知并非普通算法绩效失败。

当前主比较形式是成对d=(Q_D−Q_R)/E，必报D−E0，Q/E∈[0,U]且U来自固定公共服务机会。跨block独立但同block各臂相关。若任何必需评分未知，则当前规则整次UNESTIMABLE，不输出只对成功臂的CI。此规则诚实，但若D初始化完成、R因费用未完，不能给全窗口部署差值。

请给三部分：
1. 严格区分两个目标：合法世界/INIT条件下运行表现，含初始化成功与费用的部署表现。明确哪些可以在不定义无效物理轨迹时记录，哪些不能。给最小可行的联合结果结构（合法性、INIT状态/完成时点、成本、Q可用性等）；不要把两个目的偷合成用户未认可权重的单分数，不改变E。
2. 评估用有界部分识别代替直接UNESTIMABLE的可行性：缺Q但能证明该臂完整潜在Q∈[0,UE]时可给差值区间；若连潜在合法世界未定义则不能给该界。跨固定权重block的估计集合和Hoeffding类置信保证应如何陈述，最坏缺失机制下哪些无条件保证成立、哪些条件于发布/完整个案的覆盖无效？给推导或反例，不能把未定义的潜在Q靠设范围修好。
3. 提一个最保守、实验前可固定的建议：保留当前主目标或明确二级结果，不强行改变72；标出能够通过纸面定义关闭的设计歧义与必须未来实验才知的初始化成功率/净效果。说明完整记录不是效果证明，也不应让研究永远因禁止实验而循环要求实际正结果。
