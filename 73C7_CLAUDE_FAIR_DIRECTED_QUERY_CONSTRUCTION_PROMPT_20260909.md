# 73C7：Opus 有界建设题——无调参的定向查询公平化
2026-09-09，根新委派，NO-SKILL / CONSTRUCTIVE / NONBALLOT。此题不是旧 C4 环几何、C5 总路线、C6 服务模型选择的重试。只用本文材料，不用任何 skill、工具、文件、网络，不生成实现、伪代码、配置、仿真、实验参数或结果。以中文给完整短答，最多约1200汉字；结尾单独写 END-C7。没有正式通过票。

背景：MAPF 的同一固定原 MOVE 可以有累计 cap，已付 POSITION 证书给单调参考进度下界 q，用于安全退休其后方资源。实体跟踪误差与观测精度是不同对象；随机延迟不能改名误差。中心维护有限 Active，每机器人最多一个原 tid，tid 会结束并由该机器人后继 tid 替代。robot 身份集是固定有限集合，规模只是符号，不赋 density N。QUERY 公共机会及队列服务与处理成本三臂同制度。E0 不买 POSITION 但保留同 cap / RUN / END 能力。R 在 Active 上轮询。D 当前选 score 最大者，tie 用最久未查询、再规范 key，无正分回 RR；score 仅依已知 owner 和未满足原请求 / CAP_EXTEND，计能由该 holder 非最终 endpoint 资源阻塞的不同需求数。扫描、游标、计数、索引更新、通信、捕获与丢弃工作都收费，不许免费 oracle。

已知组构造仅证明：初始 A_i 两两不交；未来 B_i 两两不交；初始 cap 后成员最终可到 cap，合法采样能给 q>=l_i；清到 l_i 的 holder 不再阻塞本组。封闭单组时 D 可逐个清；多组或不断新增正分需求时 D 可能永久忽略另一个合法 Active。已有独立任务在查几何，此题不重新证明几何。

任务：提出唯一一个不引入可调阈值/频率参数的 D 替代，保留真正定向选择的一部分机会并消除稳定 Active 被长期忽略的机制。可考虑公共 QUERY 序号上的固定交替以及按固定 robot 身份而非 tid 的循环游标，但请独立选择和修正。必须明确：
1. 公共序号按释放还是实际选择计算；空队列、排队失败、未命中 Active、丢包、tid 替换如何消费或保留游标，避免根据成功反馈免费择优。
2. 在哪些有限人口/公共供给/服务/交付条件下保证“选择公平”，哪些额外条件才保证“有效证书公平”；永久丢包下不许从前者推出后者。提供短证明或一个破坏它的反例。
3. 相比 R 和 E0 的同能力费用及新 estimand 应怎样限定；不能宣称该改动一定提高净吞吐，也不能无声替换原纯 D。
4. 明确其最小未闭合同。全部保护参数 H,T_delay,B_CAL,B_max,B*,P_active,density N 未赋值、范围或默认。你只给纸面构造，不声称实现资格。
