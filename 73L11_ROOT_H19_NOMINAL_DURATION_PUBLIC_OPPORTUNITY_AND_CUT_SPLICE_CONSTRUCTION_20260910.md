# 73L11：根H19名义时长、公共机会与cut拼接构造

2026-09-10，根 /root。NO-SKILL / CONSTRUCTIVE / NONBALLOT / PAPER CONTRACT / NOT_IMPLEMENTED / NOT_RUN。本文选择一个待整入73的外部完整工作合同，补L4/L5留下的具体接缝；不是失落L7的回执，不重启其代理。**HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT**。73正在P10按既定字节检查，本次不修改该输入，不据新构造关闭G4或其它资格门。

## 输入与亲核阅读

当前73实际SHA256 0beccea730aced26203406f8ffa69f5ffa93247f80cec64f00ec9de265065965，155562 bytes /531 LF，主GROUP_PREFIX。根当前重读§3、§5–10相关完整窗口；此前持续任务已完整读取该稿。L4全文本次已读，首次合并显示截断后补读§7–8，实际ace2f5365a73ade6650a3a1b02637c226d2229b0821b786f2e37d90f6e9e52d8，27882 bytes /174 LF；L5本次完整读，832354368806762b1c7c33a85ad379156ef059b0a7d4d1f0f921ad4dd10041db，10274 bytes /70 LF。L4的专属TASK屏障已由L5撤销，本件不恢复它。

根本次直接提取并读取授权桌面01_Hoenig_2019_Persistent_Robust_Execution.pdf的p.4–6文字。重点是IV-B动作状态/Type-1/Type-2、IV-C前驱闭包cut、共同规划时标和不新增new→old依赖；没有逐图视觉审查或重新全文审2019论文。原文件仍是已核SHA256 3b82372d6c6c67aaac6b67eaf91d1bc86529c2671f50dbabbf80e560a9fdb849；一手[作者PDF](https://whoenig.github.io/publications/2019_RA-L_Hoenig.pdf)。原法允许独立分配加单次求解器，且ComputeDesiredSet是领域接缝；本文明确替换该启发式及共同执行接口，不冒称其经验设置或作者整套实现原样。

已发表框架及组件仍名H19-PR-COMMON-SYSTEM / ECBS-FA，组件固定whoenig/libMultiRobotPlanning commit 4c75fa20c435c440d8b6bd6dc81668ddc7296ba0。L4四头文件亲核来自原代理；根本次不把重读L4说成又读取四原始源码。浮点/堆/计数和传递依赖仍有未知。

## 1. 唯一名义时长函数：由既定控制上界得到，不预测真实完成

对合法相邻原MOVE长度ell>0，用当前共同控制条件a_launch>0、a_lo>0定义
T_nom(MOVE)=sqrt(2 a_lo ell/[a_launch(a_launch+a_lo)])+sqrt(2 ell/a_lo)。
第一项来自§3零速LAUNCH在最小合法加速度下的首次APPROACH交点上界；第二项用剩余距离不大于ell及r≥a_lo给出到端上界。二者之和有限且为正。这里选择的是完整原边、已装满cap、从零速开始且随后不中断控制段的保守解析估计。并非实际运动时间，尤其不包括no-start、BRAKE、通信、排队、源计算或任务服务；永久停顿仍可令真实完成无界。用这个上界作前瞻可能反而推迟规划触发，不能宣传为最准确预测。

T_nom(WAIT)=0，因为共同WAIT是一次合法LOCAL_POLICY游标消费，不要求额外物理驻留时长。实际机会等待、消费及游标证据仍全部收费；零名义值不令WAIT自动finished，也不让多个WAIT在一行连续消费。它与原论文仓库动作/单位层耗时的差异必须在适配表保留。驻留锚点和空序列的名义量也为零，不制造完成动作。

名义函数只读不可变原动作、长度及已经登记的控制profile，不读真实s/C、普通pose、未来扰动或别臂结果。不依任务收益调倍数；没有给H、T_delay、任何保护预算或density N赋值/改义。它是明确函数定义，原先NOMINAL_DURATION_UNBOUND不再由任选一条经验时长填补。

定义域/比较合同同时固定：原动作/控制字段须有可核的有限数表示，四邻接长度及正控制常数按实际编码的精确有理值解释，平方根及有限和按实代数表达式比较；公共已发生时刻使用公共时钟编码的精确值。函数、有限和、历史时长比较均须付实际计算/位长/内存费。缺profile、非法长度、非有限编码、无法核验该数值域分别返回具名BINDING/INPUT/NUMERIC_UNAVAILABLE，不调用主机随机估计或近似比较后假称精确。这里没有实现代数数算术、绑定库、设置参数或生成O；实际数值后端仍须资格证据。

## 2. 明确共同公共规划机会及协调角色映射

根选择在下一次73整合中给所有共享比较臂统一登记公共PLAN_CHECK机会，入原Srv_C FIFO、用原PROCESS_SLICE及发布容量，无新增server/优先级/额外容量。PIE四臂在该作业中检查§8的all Kc≥S、无在途/待发布规划、模式允许和PROJECT输入条件，至多提出一次冻结追加作业；不再留一个未指明触发机会的隐式规划循环。H19在同一机会运行本节历史余量条件。各自谓词、冻结和失败成本都计费，工作量可以不同。机会本身不按新任务、实际位置或收益临时新增。

为保留H19原队列协调并公平复用原供给，将现公共GROUP_ADMIT行在共同日历文法命名为COORDINATE；内部映射为原GROUP_ADMIT付费作业，H19映射为ADG_ENQUEUE付费作业。主体/类型映射按算法身份事前固定，不由该行私有进度选择。原GROUP_RETIRE行在无组的H19中为不适用供给，NATURAL不强制虚构组扫描，STRICT仍按共同未用供给规则预付。这个映射不强迫H19运行本项目组策略，也不给它新增协调时间。计划及协调机会的实际日历仍未实例化；本件没有新增周期或采样参数。

H19的ADG_ENQUEUE先付费读取已提交图/逻辑队列/知识，按原计划时标、agent规范key、原动作身份排序，选择首个Type-1前驱已enqueued或finished且全部Type-2前驱已finished的staged动作；无则收费空结果。只能选择当前允许发布的后缀或已封存cut内动作，DRAIN/fail-stop不新增队列承诺。一次作业只将一个动作逻辑入队并准备其不可变APPEND_ACTION，后继传输/本地安装另收费，不在同一token再申请物理MOVE授权。全部候选和依赖扫描、拒绝及空结果计费，不能靠“队列操作”省略依赖成本。

原POLICY(SOURCE_REQUEST,i)的modulo 3不变：SOURCE_OR_EXTEND对H19从已付Kc及其执行记录发当前原MOVE请求/重试，普通初授仅b=ell；RESEND_CAP有当前GRANTED则重传cap，否则重传规范最早尚无消费证据的APPEND_ACTION，无对象则空；CONTROL_RETRY保持原控制尝试。重发只选一个对象，不同一token串做两种业务。存在GRANTED意味着相应原动作安装/PRECHECK已经合法，不必靠APPEND的另一个ACK才能运动。后继动作的安装可能因这次选择延后，属于公开规则的真实成本，不保证最小等待。

H19的MOVE finished仅由付费FINISH_RECEIPT及后继ADG维护提交确认；WAIT finished仅由付费CURSOR证明越过其执行槽，或完整后继PRECHECK严格蕴含其已消费，再付相应ADG维护成本。PRECHECK只作该生命周期用途，不免费刷新通用Kc。不能在WAIT入队、预计时间到达、本地未投送消费或TASK完成时写finished。共同END/READY、CURSOR、TASK与控制角色保持，额外ADG维护在其合法接收作业中完成或续占同站服务，不能由元调度器免费运行。

## 3. 逻辑队列与原动作身份：不用改写已下发执行历史

PIE内部仍使用§8共同槽长L及原核心段；H19外部允许每agent自己的执行长度L_i。对H19设同episode固定plan_epoch及只追加的执行序列Π_i[0..L_i]，c_i/Kc_i仍为相同意义的实际/已知消费下界。外部规划候选图的generation与动作身份另存，不能将它当作每次都重置c的plan_epoch。每个外部动作id=(源job身份,agent,源动作下标)唯一，映射到执行槽后永不复用或改变两端点/动作类型。

staged图可以包含尚未入队的未来动作；只有ADG_ENQUEUE成功中心提交才把所选动作追加到该agent执行尾并记enqueued。Type-1前驱条件及该agent原动作顺序确保它接在现有尾顶点，不跳过一个未入队前驱。该动作只追加一次；重复消息重发同一APPEND_ACTION，外部不同agent不靠填造WAIT凑公共L。LOCAL_POLICY/PRECHECK对外部使用自己的L_i，其余slot、resident、pending、alpha和可信消费条件不变。

APPEND_ACTION绑定中心提交、原动作及槽、前一执行记录身份、source/generation和几何/控制版本。Srv_E收费验证与安装；尚缺前驱记录的乱序包不能跳槽，冲突身份拒绝，不以新代际覆盖旧字节。没有安装记录时本地不消费/执行那个槽；中心可基于自己的已提交序列提出请求，但PRECHECK会限定拒绝，不泄露正确私有cursor。传输可与先前动作执行重叠，不必全队等待新图安装。

“逻辑enqueued”不等于物理GRANTED。中心仍至多一个未闭请求，每gate仍至多一个活动tid，必须完成原END/READY/消费后才可新MOVE。未来队列允许多个不可撤回逻辑承诺，物理授权仍逐原边并先预留后安装。该区别保留原ADG排队职责，也公开共同底座的逐边零速/完成证据代价；不继承原法平滑性、最小通信或活性保证。

## 4. PLAN_CHECK、历史量与cut捕获/封存

H19至多一个冻结planner job及一个待处理结果。记录最近一次**已经中心终态提交**的attempt，从该job实际付费输入CAPTURE到中心ACCEPT/STALE/合法失败处置提交的公共经过时长h。在途attempt不形成h；排队/失败/拒收全部包含，服务不足无法终态仍在途。没有首次历史时只允许按合法初始锚点产生初始job；不拿论文经验值或其他臂耗时补齐。非初始历史丢失返回PLANNER_HISTORY_UNVERIFIABLE。

PLAN_CHECK捕获后检查模式和在途状态。未就绪/禁止规划/已有job返回相应空或等待；否则有“缺初始计划”“出现上次冻结尚未处理的已交付TASK语义修订”“某参与agent从已知首个未finished原动作起的完整名义余量≤h”之一则可尝试新cut。参与agent指有已知未完成访问义务或有未finished原动作的agent；两者都无的空闲agent不以零余量反复触发全局规划。参与agent已耗尽旧计划时余量为零。无已知访问义务且无需要更新的队列/后缀时IDLE优先，避免空fleet驻留反复规划。同一行失败不递归重试，后继公共PLAN_CHECK才可重试。handled_TASK是冻结输入的已处理标记，不是假装实际服务；任务身份/目标变化与仅可靠去重的新回执分开。

desired对每agent从中心首个未finished动作起按原顺序累加T_nom，选首个累计达到h的动作，末段不足取末动作；h=0但有未finished动作时取首动作。全WAIT剩余可为零，不凭零时长跳过它们。无未finished则取该agent现有合法驻留锚点。并入全部已经逻辑enqueued、执行序列已追加、请求/授权/缓存发送可能生效的原动作，以及任何既有cut已经承诺但尚未finished的staged动作，再取旧ADG的Type-1/Type-2前驱闭包与各agent最大下标。不能因前一cut的某动作尚未入队，便在连续重规划时撤回它。

中心捕获只读自己的已发布图、队列、请求/cap承诺、已付finished/Kc/TASK及消息记录；**不得读取本地实际安装C、真实c/运动或未交付END**。L4中含“已安装/在途”的捕获语句在此收窄为中心已签发责任及其未知交付的保守包含，不能用ACK缺失排除。所有APPEND只有在中心已入队/追加后才可发，因此封存时必可识别；执行器即使已经超前消费，仍不可能越过全部已入队动作的末端。

cut封存只在完整前驱闭包、原动作连接、互异合法预测锚点、目标绑定及版本验证全部付费完成后一次提交：固定cut_id、已承诺前缀、可替换staged后缀边界及冻结源输入。捕获/计算期间中心槽串行而物理继续；提交核语义版本，失败无半cut。此后旧cut之外的未承诺后缀从权威可调度/desired候选图中移除，仅可作为无发布资格的私有历史保留；禁止其入队/请求/授权，也不能在规划失败后从该历史自行复活。cut内尚未入队的staged动作仍可按原ADG条件付费入队。旧已发cap/APPEND/RUN不可撤回，旧消息继续遵守真实END及控制水位。cut不是让机器人瞬移或增加真实服务的命令。

## 5. 新旧图衔接：严格时标分离和实际合图检查

新ECBS输入仅为每agent旧cut最后原动作的符号终态，空前缀用已认证锚点；任务只用冻结K_task首个未完成实例。预测访问visited不改A/Q，不补造未知下一任务。有限J、终端驻留和同层环分支沿L5限域，不能推及一般同步MAPF或真实TASK时间。

令旧保留cut动作的最大离散完成层为cut_layer；新源计划的相对层从零开始，写入图时统一平移到cut_layer，因此每个新动作起始层严格大于每个旧保留动作的起始层。没有旧动作时以原初始层锚点为接缝。不同agent较早的cut终态只做计划层驻留延拓验证，不额外追加可消费WAIT，不创建新的物理时间等待或TASK机会。cut_layer是已有离散层的推导索引，不是物理误差参数kappa或新增lookahead预算。

保留cut内旧动作身份和旧依赖原字节。对新计划及旧/新合序列按原Algorithm 1计算候选依赖，仅安装至少一个端点为新动作的新增边；旧原边不被新结果覆盖/删除。严格时标关系使新增Type-2不可能new→old；Type-1跨代仅为本agent旧尾→新首，新内部已核无环。所以任何包含旧、新两部的环都需要一条new→old边而不存在；完整付费合图检查仍实际执行，不能只写这个证明便省掉代码对象/输入核验。

还必须逐项验证原端点连续、源动作合法、TASK语义绑定、原依赖的首次匹配/传递链、短路径末端永久驻留及共同连续几何接入。不是只看离散vertex/swap；任何欠项返回具名INVALID_SPLICE/GEOMETRY/ADG失败。新cap只在以后独立原动作PRECHECK/资源准入后产生。各agent可在自己的旧cut尾完成并满足原Type-2及共同终点消费后进入新后缀，不增加“全队已到cut”或外部专属TASK确认屏障。

由于保留部分前驱闭合，旧cut内任一尚未finished动作的未完成前驱也在旧保留部分；删掉cut外未承诺后缀不会抽掉旧已入队动作的必要前驱。新图不向旧图加前驱，所以不把已入队动作重新变成不合法。此为原ADG依赖层的保持性，不推出连续空间guard无死锁。双方大足迹全边资源守卫仍可能保守阻塞，必须保留失败和停滞。

## 6. 结果、后续任务、模式及成本

结果只有在完整验证、cut身份未被替换、旧记录/执行槽仍一致、源正常终结且数值/库合同合格时才接纳。中心Kc/finished单调前进和旧任务被可信确认完成可以合法发生，不要求物理或一般账本revision冻结；新任务另标下一次PLAN_CHECK待处理。任务被撤销/目标改写/归属改变且使冻结义务不相容时拒新后缀，不改旧cut。接纳只替未入队且未承诺的旧后缀，保留每agent执行账本、c/Kc、tid关闭及resident责任；后继入队再追加新动作。

输入错误、ECBS穷尽false、公共服务停止、内存/表示不足、源异常、非法输出、合图失败、语义STALE和ACCEPTED分别登记。完整合法source调用结束与中心接受分离，保留实际计算和随机消费；出现无法证明继续的源状态则停止来源会话，不重启/播种或用新方法替它生成后缀。没有后继时到cut后HOLD及未完成任务照计。此前未终结job只轮询原身份，不以观测超时重新发同一题。

DRAIN停止新PLAN_CHECK、逻辑enqueue和新原MOVE请求；已签发物理责任和终点/缓存恢复继续。未消费逻辑后缀可保留而不新授，物理DRAINED不宣称全部未来规划槽已执行；恢复须原模式合同允许，不能回滚承诺或重新消费。中心fail-stop不再发布，未收到的真实END不被推断已完成。GC保留所有cut/依赖、消息、证书、执行索引及审计引用，收费回收；删图节点不等于释放物理owner。

费用清单增加明确的PLAN_CHECK及历史钟读、名义值/精确比较、desired/前驱闭包、cut冻结、ADG_ENQUEUE、APPEND_ACTION传输/重试/安装、WAIT完成维护、新旧合图和语义接纳。它们占既有中心/本地/网络/源服务与发布供给，失败和重复不退费。H19不产生本项目GROUP_PREFIX围栏，不付无用组计算；内部则不得免费获得H19的原ADG协调。共同NATURAL与STRICT按各自定义报告，不能用同容量虚称同费用或以总费用相等压平反馈时序。

## 7. 强外部比较与剩余闭合工作

主外部工作族明确预注册两条同源、同ADG/cut/整动作b=ell的H19系统臂：E0保留原完成通知与共同付费终点能力，不买途中POSITION；R-S共享原规范轮询及可信空间退休能力并付全部观察成本。R-S不修改原Type-2必须finished的依赖，也不借部分清空把动作宣布finished。保留E0可避免强迫外部购买未证明有益的观察；保留R-S可检验共同空间底座接入后的强外部系统。两条均报告，不能看结果后只选较弱者。它们是明确适配比较，原生2019身份/适用域检查仍另列。

内部主D-S对R-S及D-S对E0的原固定目标不改；上述外部两臂比较归预先声明的系统级次级对照及同时推断族，不能在执行后把最优外部选择回写为新主量。它们与内部不同规划机制/排队能力，任何差异按系统效果解释，不全归因于查询分数；未有任何实际增益数据。

本件关闭的是可审查的函数、机会和衔接选择，不关闭实际源/后端资格。下一次73整合须同步§5/6的外部L_i/APPEND、§7/9公共PLAN_CHECK及COORDINATE映射、§10全部外部合同、§11两个预登记外部臂及推断族和对应纸面witness，再独立检查。当前P10仍读0bec...，不能说它已检查本件或上述新字节。来源数值/堆、服务计费后端、近邻全文、素材许可链以及全部最终顺序审查仍未闭；因此不宣称G4 PASS。

唯一新件为本文，创建前精确确认不存在。全部编辑apply_patch，所有shell/链段rtk；仅元数据/正文/合法论文文字读取，无35–38及map/scen/config/agent/task/delay/results载荷，无实现、伪代码、构建、研究测试、仿真、调参或实验结果。H、T_delay、B_CAL、B_max、B*、P_active、density N原义未赋值。一次只读身份命令括号笔误exit1后修正取得实际输出，不记作研究测试。研究目标继续active，HOLD保持。
