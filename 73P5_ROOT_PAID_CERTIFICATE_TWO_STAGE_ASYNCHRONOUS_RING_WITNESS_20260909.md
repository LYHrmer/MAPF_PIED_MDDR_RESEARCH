# 73P5：根直接推导的付费证书两阶段异步环见证

2026-09-09，根/root直接数学分析，**NO-SKILL / CONSTRUCTIVE / NONBALLOT**。这是根继续完成已授权的几何与协议推导，不是重新启动失落Claude73C4任务，也不冒充其回执。当前73为decef3a8908b21796c3ff86eaf2a8361289c76d13cbd09b1f45d66c1b138b38f；73M1/P1/P2提供条件控制、覆盖与消息合同，P3已说明最大cap可能锁死可行次序。本件不改变它们的身份，不证明当前贪心必然找到此见证。

只有纸面几何、因果序及有限候选论证；没有源码实现、实验载荷、公共O实例、仿真、测试、参数数值或性能结果。保护参数保持原义未赋值。**HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT**。

## 1. 要证明的有限命题

对四条已给定的顺时针正方形原单步MOVE，在适当细的固定方瓦片和有信息量的可信下界条件下，存在一个只沿这些原MOVE、没有中点END/消费、不要求同时START或共同参考进度的两阶段合法执行顺序。阶段间只使用付费发布的同tid下界。

同一资源制度下，E0保留相同cap、控制和终点服务能力，却不购买任何途中清空证据，因此在这些原MOVE的第一个原END之前无法授出任何整条边的权限。该不可能性适用于任意合法cap扩展次序，不靠故意关闭E0的控制能力。

这不证明：给定PIE核实际必产生所需原提案；具体最大cap/查询器能找到顺序；一般lifelong无死锁；任意有限评价窗内完成；D胜R；净吞吐或新颖性已过。它严格强于仅有同步几何动画，弱于上述算法与实验结论。

## 2. 原边、包络和公共资源

正方形边长ell>0，原顶点依次为u_0=(0,0)、u_1=(ell,0)、u_2=(ell,ell)、u_3=(0,ell)，原边为u_i到u_{i+1 mod 4}。它们是同一四邻接格的四条单步原边。各自单调参考进度s_i在[0,ell]内，实体及全部允许误差的包络满足

\[
X_i(t)\subseteq p_i(s_i(t))+K,\qquad K=[-\rho,\rho]^2,\quad \rho>0.
\]

K包含非零实体与空间偏差；同世界误差可以回摆，不能在阶段边界重置。这里沿用既定参考控制可在cap内制动/HOLD、扩大cap后经新的明确RUN合法恢复的条件，不从四邻接拓扑推出硬件运动假设。

所有共同资源是预先固定的、世界坐标轴对齐、边长h>0的闭方瓦片，覆盖所有原边的完整K包络。边界接触计相交。工作域可取足够覆盖这些包络的有限瓦片集合；没有其他agent在这段条件见证中占用所需资源，没有静态障碍侵入完整原边。最后这一点限定一个闭合局部子问题，不允许未来按结果从主roster删去其他agent。

定义只用于本数学推导的几何间隔记号r_geo=2rho+h。它不是任何保护预算、规模或延迟参数的改名或绑定。我们要求存在epsilon_obs≥0以及各边有限候选c_i，使

\[
r_{geo}+\epsilon_{obs}<c_{min}:=\min_i c_i
\le c_{max}:=\max_i c_i<\ell-r_{geo}.
\tag{G}
\]

epsilon_obs是本见证的证书下界精度条件，既不赋实际配置数值，也不宣称所有只保证区间包含真值的证书自动具有此精度。

## 3. 闭瓦片分离引理与有限候选存在性

若两个包络在某一坐标轴上的投影间有严格大于h的间隙，则不可能同时与一个边长h的闭方瓦片相交。因为任取同一瓦片中的两个点，它们在每个轴上的坐标差都不大于h；严格不等式处理了闭边界。引理只针对共同资源，不是通过观察当前实体位置来降低包络。

在每agent的参考进度独立属于[L,U]时，考虑底边到右边这一相邻对。底边整个阶段包络的x投影为[L−rho,U+rho]，右边的x投影为[ell−rho,ell+rho]；它们的x间隙是ell−U−2rho。右边的y投影为[L−rho,U+rho]，底边的y投影为[−rho,rho]；y间隙是L−2rho。因此

\[
\ell-U>r_{geo}\quad\text{或}\quad L>r_{geo}
\tag{S}
\]

任一成立，就使这一相邻对的**整个阶段扫掠包络**不共享资源。旋转后的三个相邻对同理；正方瓦片与盒K在该旋转下保留同一性质。两个相对边的固定横向间距为ell，条件ell>r_geo足够排除共享瓦片。故(S)与ell>r_geo使四个全阶段包络两两资源分离，允许任意独立s_i而无需共同alpha(t)。

有限候选采用P2：各资源相交进度区间的全部端点、0、ell及相邻不同端点中点组成CAND_i。条件(G)可直接以有限精确集合判定，并非选择连续极限。一个更强而易核的几何充分条件是

\[
\ell>4\rho+3h+\epsilon_{obs}.
\tag{G+}
\]

理由：记A=r_geo+epsilon_obs、B=ell−r_geo；(G+)给B−A>h，因此可取A+h<s<B。对任一原边起点u和有向单位轴e，完整包络覆盖保证有一个已登记瓦片包含前沿点u+(s+rho)e。设该瓦片沿e的投影为[m,m+h]，则其首次包络相交阈值a=m−e·u−rho满足s−h≤a≤s，故a∈(A,B)。前沿点的横向坐标也在瓦片内，轴对齐盒K在此横向可相交，所以a确为该瓦片非空I_r的实际左端点；0<a<ell排除端点截断。a属于CAND_i，四个有向轴分别应用此证明即各得满足(G)的候选。此推导只用已登记有限覆盖、各瓦片轴向宽h，不要求共同晶格、相邻阈值等差、旋转对称或规划格整除对齐。取各边开区间内规范最小候选即可。

这里只给足够条件，不说(G+)必要；实际可用有限CAND检查得到更宽的(G)域。特别地，先前4rho<ell的同步几何条件本身不足以包含瓦片开销和证书信息量。

## 4. 第一阶段：独立启动、独立到cap

对每条已给定、具有合法PRECHECK/parent的原MOVE，中心按正常付费机会先预留Mask(U_i(0,c_i))，再发布初始cap授权。此时初始resident资源包含于该集合，按同agent同parent移交，不清空实体。所有第一阶段需求都包含于该边的完整阶段包络U_i(0,c_max)。由(G)有ell−c_max>r_geo，应用(S)取L=0、U=c_max，四个需求两两资源分离。因此无论预留提交的先后，均不会因这一组其他agent的已授前缀而冲突。

cap消息可以分别迟到、重复或丢失；累计安装、请求重传和围栏仍按P2。每个实际START/RUN是单独公开机会，原同tid控制始终保持s_i≤C_i≤c_i。有人仍未启动、有人在制动、有人已停在cap，都包含于各自完整U_i(0,c_max)，所以上述分离证明持续成立。这里没有“下一公共行所有人同时开始”的隐含动作。

较晚取证的充分路径是：条件于每个必要服务/传输/安装最终完成，并且每agent最终获得一次足以到其cap的合法不中断控制机会，它们各自有限到达c_i并零参考速HOLD。参考HOLD不是原END，因为c_i<ell；不消费旧occurrence、不派发新的原边、不向中心免费报告到cap。若§5的已付证书屏障较早建立，则允许尚在运动时合法扩cap，不再同时声称这些agent确曾停在旧c_i；两条分支分别使用各自控制前提。

若存在永久no-start、永久制动、永久失联或中心fail-stop，则这项条件推进结论不适用；安全覆盖仍按原证明，完整有限评价中保留停滞与已付费用，不删除该行。

## 5. 第二阶段的屏障仅由付费下界建立

在agent已停在第一阶段cap之后，要求它最终有一次实际付费POSITION捕获、封装、传输、中心验证及发布，其合法下界满足

\[
s_i(\text{sample})-\epsilon_{obs}\le q_{sample,i}
\le s_i(\text{sample})=c_i.
\tag{I}
\]

由(G)，发布后q_i≥q_sample,i>r_geo。采样和发布之间任意有限滞后不破坏该推理：s单调且仍不超过已装cap；max只能融合已经采样并实际交付的同tid下界。迟到旧证书不会提高另一原MOVE的q；未采样、未送达、认证失败或只有包含真值却下界仍零的区间均不能越过屏障。

查询可从公开QUERY机会持续进行，不等待免费“已到第一cap”信号；(I)是在确已到cap仍未越过屏障时确保最终得到有用证据的充分条件。较早的合法证书若已经给出足够q，同样可以使用。中心只读取自己的已付费、已提交q_i；当每个原tid都满足q_i>r_geo，经过一次收费的屏障判定/发布后允许第二阶段各原请求扩至ell。这是已知证据上的策略计算，不是免费读取真实s，也不是新全局同步原语。其他agent的POSITION是否有用只能从已授权字段判断；阶段判定及索引/扫描成本均计入。

q_i发布与相应Mask(U_i(q_i,c_i))退休须在同一中心原子提交保持覆盖。已授未装cap不被撤回，旧权限仍在其原集合内；在第一阶段这些cap不超过c_i，故前向退休由q_i≤s_i保证，不以超时清空间。

现在所有第二阶段完整需求Mask(U_i(q_i,ell))都包含于U_i(q_min,ell)，其中q_min=min_i q_i>r_geo。应用(S)取L=q_min、U=ell，四个第二阶段需求再次两两资源分离。其他agent此前的实际持有集合Mask(U_j(q_j,c_j))是其第二阶段需求的子集，因此任何扩展的串行提交先后均不新增本组外国冲突。

各cap扩展仍是先付费预留增量、后发累计授权；收到授权本身不启动HOLD中的机器人。已经在旧中点HOLD者需要后继独立的明确RUN机会恢复；若足够证据较早到达而某agent还在原合法运动模式，提高cap只按M1/P2继续同tid控制，既不伪造一次新的START，也不跳过BRAKE完成条件。每个cap至多发生这里两阶段的有限增长，在所需不中断控制机会最终存在的条件下可分别到真正ell/END。有人尚在旧cap而别人已经到终点时，整个第二阶段包络仍被上述分离引理覆盖。由此没有靠“差不多同时走”补安全。

到真正END后，终点包络是自己第二阶段集合的子集；付费终点交接将其保留为新resident，释放其它资源，不从世界删除已完成者。迟到READY只延迟本地一次消费，不能使已驻留实体失去覆盖。终点反而重新占用下一原边起点附近资源，所以下一批lifelong原MOVE需要新的协议分析，不能在这里免费重复任意轮数。

## 6. E0为何仍无第一个整边权限

考虑完全相同的四条原MOVE、初始resident、资源、控制和服务能力。E0不买任何途中POSITION或其它可证明途中清空的观测；其q_release在本原tid首次END之前保持0。它可以使用任意公开扩cap/RUN重试、全部必要终点能力以及正确的本地控制；以下论证不限定它必须使用本两阶段策略。

在没有任何原END之前，每agent的已授资源始终包含Mask(u_i+K)。这是q=0及不可撤回cap的直接后果：即使实体物理上已前进，未获清空证据的起点资源仍由同agent的parent或tid持有。

假设存在本组第一次把某条原边的b提升到ell的中心提交。在它之前不可能已有本组原END，因为END需要该agent曾得到cap ell并实际到ell；所有原边长度正。该次完整需求包含Mask(u_{i+1}+K)，而后继agent仍拥有这一非空起点Mask，于是违反外国owner互斥，不能提交，矛盾。中心发布有全序，此论证也排除了“几条边在同一物理时刻同时END”的逃逸：它们仍先需要一个不可能的首个完整权限提交。

因此E0可以部分前进，但不可能完成本组第一个原MOVE。cap已安装的确认若没有经过允许的空间采样，只证明授权送达；它不能提高q。把该确认做成到cap通知并用于退休，会增加途中证据能力，需要按新的观测策略和费用命名，不能继续称未购买途中证据的E0。

相同不可能性也适用于明确选择全程保留初始前缀资源的保守F_cap制度，即使它另算一个不用来退休的q_evidence。它不否定其他可证联合占用/相对进度协调架构，因此不能外推“任何无POSITION方法都不能通过环”。

## 7. 信息、费用与真实效果的证据层级

(I)是实质假设。当前73只要求可信区间包含采样真值，没有自动给每个profile的有效下界精度；永久返回[0,ell]是合法但无信息的失败见证。若希望将本结论用于实际主profile，必须先有该可信服务的明确精度/封装来源和费用，而不能在看到需要多少进度后临时缩窄区间。误差K保持固定，POSITION改善的是进度知识，不是物理跟踪误差本身。

两阶段顺序额外付出的包括候选/包络发现、阶段检查、初始/扩展授权、消息与接纳、RUN重试、POSITION采样和处理、退休维护、END/READY以及等待。全部为正或按共同实际工作函数记账，不能只计四次名义查询。有限完成条件不能给任意预定评价窗一个统一上界；永久故障和长排队仍可能让该窗内无任务增量。

若原任务确实要求相应原终点，服务区域包含该终点的完整K、初态没有已可服务队首；agent保持该终点直到共同Stop前适用于它的TASK_SERVICE行，且该行时同一task_instance仍是合法已分配、未服务的队首，则执行完成可转为73 §8 SERVICE_TRUE下的真实任务服务。闭合局部见证可以选择保持终点满足这些条件，后继lifelong运行不自动满足。没有这些前提，完成MOVE不等于完成任务；更不能只凭一个局部环成功推出整个roster的Q差或大规模吞吐提高。

本件区分四个层级：同步路径几何可行；本条件下付费异步执行顺序存在；具体算法必然找到；主总体净效益。这里只证明第二层及E0在所述独占/无途中证据制度中的限制，没有跨级。D与R都可能买齐所需证据，此见证没有证明D相对R的选择优势；若后续优势主要来自前缀授权与空间退休，应据此调整贡献归因。

## 8. 不可省略的失败条件与下一步

- 若瓦片太粗使(G)没有候选，安全第一阶段cap可能不足以清空起点；原同尺度中点矛盾仍有效。
- 若只有真值包含而没有(I)型信息量，机器人停到cap也可能永远无法发布足够q。
- 若任何必要服务/证书/控制机会永久缺失，不能宣称有限完成；不得据此删去失败样本。
- 若使用未来未发布的q、跨tid融合或到cap免费ACK，屏障依据不合法。
- 若贪心已在本见证外抢占了别的资源，或局部组之外有冲突占有，所证提交次序未必还能执行。P3的粗资源反例仍约束通用策略。
- 若在未来任务或READY消费之前偷偷重置终点resident，便破坏本证明的真实责任；每个下一轮次需重新认证。

下一步是根把这些条件与候选生成、付费阶段/重试、主几何profile和最强外部比较组合；是否采用阶段组策略仍待决定。本件不要求新实验载荷来先证这个数学命题，也不以未运行实验为失败理由。正式固定和按序审查之前，保持HOLD及六门原裁决。

2026-09-09根修订记录：P6完整查错回执核读的是本件修订前SHA256 187823da00cf20bf604686aed33b42b1aeb47cfd13f38a4b6581eadf2503b2c2；根全文读取后不用skill独立核对上述覆盖点论证及两分支、服务时刻量词，修订§3/4/7。原等差论证确有未给统一晶格的缺口；该缺口不反驳G+，新论证覆盖任意所列轴对齐方瓦片有限覆盖。P6字节保留，其意见不是对本次新字节的正式审查；本件仍为根可修订建设稿，不授资格。
