# 73C11：论文论证架构建设调用完整回执

2026-09-10。NO-SKILL / CONSTRUCTIVE / NONBALLOT；不是正式审查或资格票。C11 是新有界任务，旧 C9/C10/C8L 均未重跑。本回执保留顾问完整原文，其意见不自动成为根采纳结论。HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT。

## 1. 输入、真实句柄和终态

输入：73C11_PAPER_ARCHITECTURE_COUNTERFACTUAL_CONSTRUCTION_PROMPT_20260910.md，SHA256 1b56806dc4bb76be7b865dd6add8a64a94ed635351d068df8f1e851252e2fb89，8716 bytes / 33 LF。先取得四篇非 PIE-D 主文与 PIE-D 锚点的完整阅读证据后调用；补读第四篇时尚未启动，故只修订未使用提示、没有丢弃某次意见再重跑。

实际命令（每个 shell 仅 rtk，已授权正常升级；工具空、skills/slash/MCP 禁用、不持久化）：

```text
rtk proxy timeout 480s claude -p --safe-mode --model opus --effort high --max-budget-usd 5 --permission-mode dontAsk --tools '' --disable-slash-commands --strict-mcp-config --mcp-config '{"mcpServers":{}}' --no-session-persistence --output-format json < 73C11_PAPER_ARCHITECTURE_COUNTERFACTUAL_CONSTRUCTION_PROMPT_20260910.md
```

functions cell 138 → exec session 19355；首次 yield_time_ms=1000，获得空输出的真实存活句柄后，只继续该 session。观察 functions cell 139 / 141 仍返回原 session、空输出，不当意见。最终原 session 返回 chunk 6b52a4 / exit 0，完整 stdout JSON 已存 73C11_PAPER_ARCHITECTURE_RAW_RESPONSE_20260910.json。没有 PID 推断、同名进程代证、重试或并发重复调用。

真实 CLI session_id：da1b98f6-aac8-4938-b285-9574f749fc49；uuid：694086f9-720e-497f-8149-43bb7c71f9c2。type=result，subtype=success，is_error=false，terminal_reason=completed，stop_reason=end_turn，num_turns=1。duration_ms=144019，duration_api_ms=143984；total_cost_usd=0.25041。末行实际为 END-C11。

modelUsage（完整保存，不以请求别名充实际模型）：

```json
{
  "claude-opus-5": {
    "inputTokens": 2,
    "outputTokens": 8413,
    "cacheReadInputTokens": 0,
    "cacheCreationInputTokens": 6412,
    "webSearchRequests": 0,
    "costUSD": 0.25041,
    "contextWindow": 200000,
    "maxOutputTokens": 64000,
    "canonicalModel": "claude-opus-5",
    "provider": "firstParty"
  }
}
```

usage（完整保存；permission_denials=[]，api_error_status=null）：

```json
{
  "input_tokens": 2,
  "cache_creation_input_tokens": 6412,
  "cache_read_input_tokens": 0,
  "output_tokens": 8413,
  "output_tokens_details": {
    "thinking_tokens": 0
  },
  "server_tool_use": {
    "web_search_requests": 0,
    "web_fetch_requests": 0
  },
  "service_tier": "standard",
  "cache_creation": {
    "ephemeral_1h_input_tokens": 0,
    "ephemeral_5m_input_tokens": 6412
  },
  "inference_geo": "",
  "iterations": [],
  "speed": "standard"
}
```

## 2. 顾问原始 result 全文（原字保留，未修正）

以下围栏内恰为 JSON result；其中四格缺口、权重反例等说法的有效性由 L12 第 6 节独立分析，不能只凭完整回执当真。

```text
## 1. 论证阶梯（主张—对照—证据类型的绑定）

架构的核心不是章节顺序，而是**每条主张只许引用一个「只差一个轴」的对照**。建议主文以四级阶梯呈现，且阶梯不可跨级借证：

- **L0 语义闭合（前置条件，不是结果）**：问题陈述、固定 PIE-D 原 MOVE 提议不变、实体尺寸与真实横向跟踪偏差的语义、全费用世界钟（INIT/预处理/成功与失败查询/原源工作/控制与协议工作按实收费；t=0 起的合法待命世界；HOLD 不自动给零）、事前 E 为主分母、留 roster 规则、无可信服务前缀即无分数、缺日志不填零。L0 不闭 → 后续各级只能作描述，不作数值因果推断（这是既有定义，不是我新加的）。
- **L1 主量**：固定公共 roster/权重/评价窗口下 NATURAL 的 D-S 减 R-S。可支持的最强表述仅限：**在同一付费查询制度、同前缀、同源历史策略下，公共阻塞分数最大者的分配规则相对合理轮询的服务差**。
- **L2 制度内购买轴**：D-S 减 E0（同平台/控制/终点推送，不买途中 POSITION，不做无用途索引）。这一级才是「买可信进度是否值钱」的唯一许可对照，且结论以 D 的分配规则为条件。
- **L3 预列机制比较**：STRICT、S/F_cap、G/N、前缀/整边。全部预列、全部报，**不因显著性改主量、不选优**；F_cap 明确为次要释放因子（保留已授资源至原 END），且不是初始占整边的 WHOLE_EDGE。
- **L4 外部组合**：H19 原样与 H19 守卫共同适配必须分名分报；不得靠弱化原法或删外部域取胜。当前此级未闭，主文只能陈述未闭与分名协议本身。

**已读论文在此架构中只授权论证形式，不授权结论**（这点需在主文用一段明写，避免读者误读为继承结论）：
- Li 等 RHCR：模块贡献与运行代价分开论证，并允许在正文放不完整性/停滞反例（其 §4.3 的形式）——我们的环状组停滞见证只能放在这个位置，作为存在性见证，不作效应。不继承完备性与任务设定。
- Chen 等 Traffic Flow：吞吐、逐步响应时长、初始化/引导收益分列的报告形式可以借；**其「仅在共同求解成功者上比较解成本、另报成功数」以及按前图峰值选团队规模，属该文口径，明确不移植为我们的运行后筛选或规模择优许可。**
- Zhang 等 2025（项目锚点）：组合兼容性的先后顺序（先策略组合与有无引导，再对强机制基线）与失败解释结构可借；其随机 no-start/持续延迟**不是**真实横向跟踪偏差，不得据此把随机延迟改名误差；其 Hönig 相关基线不自动等同原完整 H19。
- Hönig 等 RA-L19：系统证据与局部机制证据分开、commit cut 闭合与假设显式化可借；其同步基线一次换多项机制，正是我们必须用 E0 隔离「观测选择」单轴的理由。
- PIE ICAPS24：目标失配先行、单次到达与长期服务分开、组件逐项回答原因的形式可借；**其按图选最佳承诺长度不构成我们给任何保护参数取值或在主数据上择优的许可**；单次到达改善不代替长期服务收益。

## 2. 主文 / 附录分工

**主文**：L0 全套定义与费用规则；臂能力等价与轴分解（见改良 1）；L1 与 L2 同表并列（不得只报 L1）；L3 预列机制表（原样呈现，无提升）；结构性正/负见证及其外推禁令；「本文不支持的主张」小节；未闭接口清单（精度、源后端、素材许可链、H19 外部组合、动态映射/源语义/评分闭合状态）；失败/合法超时/围栏锁死的留 roster 记账规则。
**附录**：逐角色逐消息全费用台账；roster/权重来源与冻结证明；日志完整性审计与缺失清单（不填零）；预注册全文与冻结分析；各 block 创新根独立的显式条件说明与块内臂相关的承认（并明确**不以随机换运行次序充作独立证明**）；环状组构造与其非公共 R0 实际输出实例的界定；H19 原样/守卫适配差异逐项；口径差异对照表（改良 2）。

## 3. 最危险的归因错误与可直接预注册的修复

**错误**：把 L1 当成购买轴的证据。按当前定义，D 与 R **共享同样的前缀、查询能力与源历史策略**，差别在选择规则（公共阻塞分数最大 vs 合理轮询）。因此 D-S 减 R-S 是**分配规则对照**，其中付费查询制度是被固定的共同条件；用它论证「付费买可信进度增加全窗口真实任务服务」是把两个轴混成一个标签。同时四格设计不完整：{阻塞分数选择, 轮询} × {买 POSITION, 不买} 中，现有为 (D,买)=D-S、(R,买)=R-S、(D,不买)=E0，**缺 (R,不买)**，故无法检验购买价值是否依赖选择规则，也无法区分「购买有价值」与「购买只在 D 的选择下有价值」。

**修复（预注册两段，第一段无成本、强制；第二段可选、有成本）**：
1. 在预注册中把 L1 的**标签**固定为「同付费查询制度内的分配规则差」，并写入绑定规则：任何购买价值主张只能引用 L2，且必须附带「以 D 的分配规则为条件」的限定；主文表格中 L1 与 L2 必须同表同时出现，禁止单独呈现 L1。此段**不改目标量、不新增费用、不依赖未闭接口**，纯为主张纪律。
2. 若确实要作非条件化的购买价值主张，则预注册补齐第四格（轮询 + 不买途中 POSITION，且同样不做无用途索引），并预注册可加性/交互的描述性检查（报数不提升、不改主量）。**明确代价：这是新增臂，其 INIT/预处理/成功与失败查询/控制与协议工作全部按实收费，占用同一世界钟与事前 E 分母；其能力集需在预注册阶段把 E0 的能力剖面对轮询侧写清（这是设计层接口，须在冻结前闭合，不能留到运行时解释）。** 目标量不变，主量仍为 D-S 减 R-S。

## 4. 两项改良（均为证据组织；唯一的方法新增在上节第 2 段）

**改良 1：臂能力等价与轴分解台账 + 主张映射表（主文）。** 逐臂列出能力集、被收费项、与其他臂相差的轴；再列主张→对照→差异轴的映射，任何跨过一个以上轴的对照不得被引用为该主张的证据。**共享稳健控制、同前缀/同查询能力、全部按实收费已在当前定义中；新增的只是把轴分解写成可审对象并让主张受其约束。** 无新增费用、不改目标量、不依赖未闭接口。

**改良 2：口径防火墙对照表（附录，主文引用）。** 逐条列出四篇已读论文的筛选/择优口径（仅共同成功者比较解成本、另报成功数、按图选最佳承诺长度、按前图峰值选规模、重规划成本不含路径改善、解成本只含成功重规划、同步基线一次换多项机制），并对每条声明我们**不继承**及替代规则（事前 E 分母、留 roster、无前缀无分数、缺日志不填零、预列不择优）。**这些替代规则已在当前定义中；新增的是把「不继承」显式化，防止读者按熟悉的领域口径回填我们的空缺。** 无新增费用、不改目标量。

（附带说明而非第三项改良：真实横向跟踪偏差与随机延迟的分名，仍受精度/动态映射/源语义/评分未闭的限制；按既有规则，未闭即不作数值因果推断，主文只报未闭状态与验收标准，不安排为主实验门槛。）

## 5. 即便 D-R 为正也不能支持广泛主张的具体反例

设固定 roster 的权重集中在少数高阻塞分数原边上。D 的最大阻塞分数选择恰与该权重集中重合而获得退休分数，但被退休空间随原边单调累计 cap 立刻被其他单位吸收；R 的轮询把**同样可用**的付费查询分散到低权重原边。此时 D-S 减 R-S 为正，来源是**这一 roster 实例上的分配—权重耦合**，不是付费可信退休本身增加服务；换一个同样事前固定的 NATURAL roster/权重剖面，符号可能消失或反向。更强的情形：同时 D-S 减 E0 ≤ 0，说明途中 POSITION 的购买没有买到任何东西，而 F_cap 保留已授资源至原 END 使释放只是次要因子——正的主量可能只是单实例的调度次序产物。

**文稿报告方式**：主文同表并列 L1 与 L2，并用预注册固定句式说明「单一固定 roster/权重/评价窗口实例，不外推至其他 roster 与更大规模」；全窗口服务与成本总量与差值一并报（其他服务/成本可能抵销）；在「本文不支持的主张」小节逐条写明：无 L2（及必要时第四格）支持则不作购买价值主张；环状组停滞仅为存在性见证、非公共 R0 实际输出实例、不外推为大规模效应；不作完备性主张；H19 原样/守卫分名比较未闭前不作外部组合主张；roster 敏感性未测，作为声明的限制而非零值。

当前未生成任何实验结果；本节全部为条件化的报告协议，不预言符号或结论，亦不涉及新颖性认定。

END-C11
```

## 3. 完整性与边界

JSON 保存实际返回 stdout 的单行正文和末尾 LF；没有把摘要替代 result，也未改原顾问措辞。模型报告未用 web 工具、无 permission denial；没有提供其私有推理过程，usage 中 output_tokens_details 如上。成功终态证明收到这一意见，不能证明其数学论证正确、论文全文由顾问亲读、候选可实施或资格通过。实际论文完整阅读者为本子代理，Claude 只收到已核精简证据及问题。

本次未调用任何 skill，未改 73、旧报告、行政页、Git、桌面；未实现/构建/测试/仿真/调参/保护参数赋值，未读受限载荷。原样结果已终止，无继续运行或重试任务。
