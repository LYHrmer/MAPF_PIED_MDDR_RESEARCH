# 73C2：动态阻塞计数的精确增量索引构造

请不用任何工具或skill，不读文件/网页，只分析下面抽象有限动态集合问题，中文约2000字，结尾ADVICE-COMPLETE。非正式科研审查，仅构造数学算法及证明，不给代码、可执行伪代码、运行命令或实验数据/参数。

一个中心顺序处理公开、已验证的资源账本变化，不读取隐藏物理状态。有限活动事务集合T；有限等待请求集合P，每个请求p有不可变规范身份、首次加入序号a(p)、所需资源集合M(p)。每资源r有唯一owner(r)∈T∪FREE（resident不是T，仅当占用者是活动事务才可计）。每个活动t有一个公开、已验证的可提前释放资源谓词eligible(t,r)，该谓词只能由已经付费获得的静态几何/中心cap/资源owner计算，不能由真实进度推断。定义n(p,t)=|{r∈M(p):owner(r)=t且eligible(t,r)}|，score(t)=|{p∈P:n(p,t)>0}|。选择score最高的正值t，平分先按h(t)=min{a(p):n(p,t)>0}，再按t规范key；全部score0则走独立已定义的轮询，空T无查询。一个p多个阻塞资源仅计一次。

事件为：插入/删除请求p；owner(r)改变；eligible(t,r)翻转；活动t插入/删除；每个事件的输入处理、全部枚举/索引维护/计数/树更新/内存都收费。P和T按当前已提交状态定义，中心单writer，没有并发部分提交；作业期间物理继续但不能免费更新公开索引。资源集合及变化可能稠密。

任务：
1. 构造与每次全扫描精确等价的增量表示，涵盖反向索引、n的零/非零迁移、请求去重、h的删除最小值、score0和新活动成员，字节序tie唯一。
2. 给按原始事件归纳的证明，不用“索引自然正确”。指出哪些更新必须原子同提交，失败/回滚如何保留旧选择结果，不能预先读未发布状态。
3. 给符号级输出敏感总工作界与内存界，明确输入规模Σ|M(p)|、实际资源—请求关联触达次数、活跃正n对数、平衡树成本；不把所有复杂度都写成O(1)，不假设固定最大度。查询提取top可如何降低成本，代价转移到哪里？重复/失败处理仍额外收费。
4. 最少四个反例破坏天真优化：按资源计分重复、最早请求删除后h不更新、eligible变但owner不变、owner移走后孤儿n、删t又复用身份。
5. 这类索引若采用到机器人论文，能支持“相同选择函数的计算组织改进”还是新观测策略本身？如何设计公平对照，避免只对新方法免费赠送预处理/维护？

不要假称已证明新颖性/吞吐/真实安全。不赋值任何实验规模、预算、窗口或参数。数学复杂度变量只是任意有限集的规模。
