# 73C10R：根追加记录恢复与付费重传独立分析

2026-09-10，根 /root。NO-SKILL / CONSTRUCTIVE / NONBALLOT。根已完整读C10实际result及原JSON；模型为claude-opus-5，exec83561实际exit0，END-C10完整。这是成功顾问调用，不是审查通过。**HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT**。

输入是根L11的APPEND投送子合同，L11当时c845f894147dc2e3cdcf6def1df06fb8478298e82d4d670f9799296f00535046，18880 bytes /89 LF；C10没有读取整篇L11，也没有检查其名义时长、公共PLAN_CHECK或cut。根在本件选择以下修订替换L11§2/3的相关重传条款；L11及C10原件保字节，待与73整体合并，不能说当前0bec...正文已有这些规则。

## 1. 逐项区分反例、已知条件和错误推断

C10 CE-A指出：已消费WAIT的CURSOR尚未交付时，按最早“无消费证据”条目固定重发，确可一直重发旧APPEND而不修后继缺口。根确认这个轨迹合法、费用负例成立。但提示已明确新鲜CURSOR是原恢复条件之一；把缺它说成“其它必要条件成立”并不是对原条件命题的反例。正确结论是原APPEND恢复依赖额外消费知识，单独可靠重传通道不足，而非满足原全部条件仍不能恢复。

CE-B的长期GRANTED使cap独占，也确会阻止后继预取重传。但是后继原MOVE本来就不能越过该未完成动作；若它永久no-start/被MODE阻止，不能据此证明已满足执行前提而新出现物理死锁。它说明恢复和预取被不必要地绑定到原动作完成，值得改善；不证明本项目吞吐必因改动提高。其“完成速率”等表述还需区分MOVE终点、游标消费与真实TASK三种事件。

C10安全推导在可信中心/gate不分叉、记录不可改、真实c单调及原PRECHECK/END合同成立时支持“重复追加不再执行/不跳槽”。称“无条件物理安全”过强：本题只检查重放接口，没有重新证明全空间覆盖、控制、公平服务或源定义域；可信故障和非法同槽签发不由它解决。

F3把FINISHED列为“中心消费证据”是明确语义错误。中心可在原END之后、READY仍未到达时FINISHED，而本地c仍为k。它不能推进消费前沿、删除仍被pending/READY/原动作证明引用的执行记录。另一方面，合法FINISHED确能证明该原动作**已经安装并执行过**；这个较弱安装事实可合法用于停止重复投送，不能混成CURSOR知识。

F4另设逻辑入队窗口，却没有给窗口合同/来源，不能据“未触及保护参数”默许一个新自由阈值或剪掉原H19的合法预取。根拒绝此准入窗口限制及固定缓存界主张。有限重传轮次可以直接冻结当时已提交尾长，不需要限制源路径或赋新预算。F1的“代价有界于窗口长度”也只能在具体足额服务/成功交付条件下讨论一次扫描；丢失、乱序、排队和多轮重复不存在由条目数单独给出的总费用界。

## 2. 采用的最小修订：分清安装、消费和持久引用

对每agent保留三个不同量：通用Kc按原CURSOR规则更新；生命周期消费下界f_cons只取合法CURSOR及后继PRECHECK所严格蕴含的c值；已安装记录长度下界f_inst来自下述实际已付证据。它们都不是当前真实c的免费读数。所有更新与字段验证、索引及GC准备均在既有中心收费作业内完成，不产生新的消息或authority角色。

CURSOR给c=x说明槽小于x曾按连续记录合法消费，故可令f_inst至少x。对槽k的完整成功PRECHECK说明该原动作已安装，可令f_inst至少k+1；它最多给f_cons至少k而非k+1。对槽k的合法FINISH_RECEIPT说明曾执行到该原动作END，可令f_inst至少k+1，**不**据此增加f_cons/Kc。相同episode/动作/槽及权威身份必须匹配，错误、普通或未验证消息不更新。所有前沿取已证事实的单调最大值。

若本地历史安装长度为L_E、中心已追加长度为L_C，则每次事实都有f_inst≤L_E≤L_C；单调消费给f_cons≤c。初始前沿来自同一付费INIT，不用缺省未核状态填充。安装证据是历史事实，GC不把安装长度倒退；不能由f_inst推出下一动作已执行、服务完成、q增加或中心准入已合格。

APPEND重传集合改为中心已经原子追加且槽k≥f_inst的缓存记录。某记录被证明安装后即可退出APPEND重传候选，但释放消息封装缓存仍需全部有效引用结束。独立执行记录、cut/ADG、原MOVE及pending/READY依赖继续按各自消费/关闭条件保留，不能一并回收。没有安装/消费证据时承担真实保留内存费用；不保证无限运行的固定内存。

本地持久保存已安装长度及当前安装尾的身份锚点；前缀GC不能删除验证下一次连续追加所必需的尾锚。新槽只有绑定当前尾并连续时才安装。已安装旧槽的重放不重建记录或控制状态；若旧正文已经合法GC，只按水位拒绝重装，不冒称仍比较了不存在的旧内容。真实c、END关闭及tid水位继续独立拒绝旧MOVE，不靠APPEND缓存存在来维持安全。

## 3. 采用的付费轮询重传：不设额外窗口预算

APPEND分支为每agent保留扫描游标及本轮封存尾界，均是已提交记录的身份/索引。开始一轮时捕获当时L_C作为尾界；本轮按槽序只考虑该尾界之前仍需重传的记录，游标随实际选择/发送意图成功提交前进。到尾后才开始下一轮、读取新的L_C；新追加动作不延长进行中的这一轮。只有有限已提交记录，故稳定未确认条目不会被不断增长的新尾永久挤在回绕之后。

这些是队列状态，不是新的规划窗口、H、容量或入队限制；全部界/游标位长和缓存扫描收费。一项在扫描前已获安装证据便可跳过；没有候选返回收费空结果。私有选择失败或未发布不推进游标，不把公共机会出现当作实际发送成功。选择提交以后即使网络丢失也不回退；重试由后来轮次覆盖。每token至多产生一条消息，没有递归发满整轮。

RESEND_CAP对外部的投送选择修订为：cap与APPEND都有合法候选时，用同一agent持久二态在两类之间交替，初态为cap。只有一次实际选定发送意图成功提交才切换；某一类无候选时发送另一类，保留二态。各类都无则收费空结果。二态不因tid/图generation更换重置；cap内容仍是当前合法最大已提交授权，APPEND仍按上述封存轮次。一次只发送一类，不增公共角色、容量或优先级。内部现有重传规则不被本件自动更改；H19两条外部臂共同使用该规则并分别付实际费用。

这样在两类持续非空且有足够已完成发送选择的稳定区间内，两类都被选择；APPEND的一轮不会因新尾增长无止境。这个选择公平性只数实际提交，不能变成任意有限O、网络成功或wall时间的界，也不把仅一次被选等同丢包后的恢复。

## 4. 独立条件证明与完整负例

安全归纳：中心只追加一个绑定原动作/槽并保留前一身份的记录；本地只接连续合法记录，重复旧槽不重装。因此不同消息次序只影响安装延迟，不改变每槽原动作。真正执行另需同槽真实c、resident/parent、唯一tid及原控制授权，消费后c单调越过旧槽，END永久关闭。安装前沿f_inst仅决定重传候选，不授资源、不改c、q或finished，所以它不能使新旧动作双执行。这一归纳仍以共同原安全底座及可信身份为前提。

消息恢复的条件可以与CURSOR分离：对一个已承诺、尚未安装的有限目标槽，若必要前驱记录仍可用，中心/本地未fail-stop，后续有足够成功付费选择、服务/发布及重复投送，其中按连续前驱次序各有一次成功安装，则按前驱槽归纳最终安装目标槽。f_inst不会越过实际未安装槽，因此它不会错误跳过该缺口。封存轮次在每次实际完成扫描后回访仍未证安装者；不需要先取得该槽之前WAIT的消费证据才能再尝试它。

上述消息恢复并不消除执行知识依赖。SOURCE_OR_EXTEND仍用Kc，WAIT的ADG finished仍需付费CURSOR或严格后继消费事实；MOVE还需资源许可、PRECHECK、合法RUN/no-start解除、到原端、END/READY及LOCAL_POLICY；真正任务完成另需原SERVICE_TRUE、TASK_SERVICE及任务实例绑定。不能把“安装成功”传播为“已完成任务”，也不能从没有CURSOR的消息恢复推主lifelong进展。

公平收费负例保留且新增一类：只有有限共同服务窗口，cap首次/后续消息丢失，交替给APPEND的那些实际付费机会可能推迟本来能成功的CUM_CAP重传，错过已有RUN或TASK_SERVICE，最终真实Q低于旧cap优先规则。另一方面大量已安装但未确认的APPEND仍会多轮重传，可能只有成本。两条外部臂使用同一修订，费用、失败与全roster均报告；不预先保证新规则净优。

## 5. 根取舍与当前状态

采用F1的轮询思路，但以本轮已提交尾界防止增长尾饥饿；采用F2的两类实际发送交替；纠正F3并用已有PRECHECK/FINISHED导出独立安装证据；拒绝F4未定义窗口及“无条件安全/固定费用界/吞吐恢复”过强结论。它们构成L11的指定修订，须与外部机会、执行记录、cut及收费全稿合并后再审，不是直接发布执行协议。

所有原C10正文/JSON保字节。未调用科学skill、未重跑C9/C10、未执行研究算法或测试；全部根文档apply_patch，shell每段rtk，保护参数原义未赋值，冻结/禁读/桌面旧备份边界保持。当前73仍0bec...，正式顺序审查及六门未闭。用户最新要求的LMAPF代表论文架构任务已排给原代理在P10/C9交付后接续，不能把排队任务计作已读论文或已得新Opus意见。
