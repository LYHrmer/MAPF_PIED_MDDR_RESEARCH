# 73L5：H19服务公平性与有限搜索域组合

2026-09-09。`/root/source73_external_comparator`。**NO-SKILL / CONSTRUCTIVE / NONBALLOT / NOT_IMPLEMENTED / NOT_RUN**。仅给L4两处接缝的独立纸面修订，不是正式逻辑审查、资格票或73定案。保持 **HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT**。

## 1. 结论及实际输入

唯一选择是：**删除L4外部专属的TASK确认离站屏障；保留visited标记作为预测访问；将J的覆盖命题严格限制于冻结、无外生时间条件、ADG可执行的几何访问问题。**原ADG环分支在这一限域内可以得到覆盖论证，但不能覆盖所有无碰撞同步MAPF运动，更不能证明真实服务覆盖。

L3/L4沿用本代理此前实际全文阅读，本轮重读L4全文；P4本轮全文阅读。73只读§3、§5–6、§8、§11–13相关窗口，没有冒称本轮全文审查。写作前实核身份如下；这是所读快照，根后续整合不由本文预先背书。

| 文本 | SHA256 | bytes / lines |
|---|---|---:|
| 73L3 | `e0c21289ea3047ff3aa72b1871dc4f39044074a7227268b2572d9a0c609f83f9` | 32022 / 201 |
| 73L4 | `ace2f5365a73ade6650a3a1b02637c226d2229b0821b786f2e37d90f6e9e52d8` | 27882 / 174 |
| 73P4 | `70ddcb01292a8e74773113403084f7b146ddcdb29ba237b4cac88222634cf1e1` | 33281 / 214 |
| 当前73快照 | `decef3a8908b21796c3ff86eaf2a8361289c76d13cbd09b1f45d66c1b138b38f` | 109595 / 418 |

P4输入表所写L3哈希末段与实核不符；以上为实际值。本次不修改P4。没有新增文献或源码对象；源码机制只引用L4已完整亲读的固定对象。

## 2. SERVICE_TRUE相同，知识等待不能只加给外部

73§8的真实服务要求：任务确为该agent当前未服务队首；参考处于合法初始驻留或完整原MOVE的终点驻留、顶点匹配且参考速度为零；真实整个实体包含于ServiceRegion；遇到公共TASK_SERVICE机会且未Stop。原终点尚未消费也可服务，中心READY、TASK回执和算法是否知道任务完成均不是计分前提。中点HOLD不算终点服务，实际实体绝对静止及额外驻留时长也不是现有前提。

L4§6却要求外部在真实任务完成且付费TASK知识到达后才离开。纸面反例是：任务已在共同机会实际完成，END/READY链已允许下一原MOVE，而TASK回执仍排队或失联。主方法可继续，外部被单独锁在目标。这既增加外部等待，也可能堵住其他agent的共享目标；它不是原ADG依赖、共同安全要求或SERVICE_TRUE所必需。

因此去掉这道强制屏障。H19满足已登记的source/cut、ADG、共同终点交接/消费及运动授权条件后即可执行后继；没有额外TASK确认条件，也不新加服务驻留时长。首次规划访问后可以按合法后继离开。若离开前没有真实服务机会，或实体未被服务区域完整覆盖，任务仍未完成，损失照计；不能用visited代替Q，也不能偷偷插入即时服务机会弥补。

D/R/E0/H19使用完全相同的SERVICE_TRUE、任务派发、TASK_SERVICE机会及付费TASK证据接口。P4的付费END推送只提供原动作完成和终点交接证据，不赠送TASK证据或下一任务。查询、重传、源规划、ADG和等待均按实际共同费用计；公共能力相同不要求各臂工作量相同。该组合以根实际纳入P4为前提，当前不能写成已闭。

保留L4单次只处理首个已知任务的访问目标；visited仅描述冻结计划中经过目标端点，不能更新真实派单或任务账。未知完成导致的重复访问属于本外部策略的可能代价，主方法PROJECT的队列预测另列为系统差异。比较仍名为H19-PR-COMMON-SYSTEM / ECBS-FA，不声称与PIE仅相差一个选择器，也不强迫其使用PIE生成的原动作。

## 3. 第一个覆盖反例：同步轮转不属于原ADG搜索域

考虑一个全部顶点被占据的简单环，每个机器人同时走向下一顶点。离散端点无重合，且不存在对向边交换；这是允许following的标准同步几何路径。但按原Algorithm 1，每个离开动作阻塞进入其源点的同层动作，形成有向环；执行时各动作都等待另一个finished，不能启动。[H19作者全文，Algorithm 1](https://whoenig.github.io/publications/2019_RA-L_Hoenig.pdf)。这是符号反例，未制作或执行实例。

最小修订不是删去环检查来扩大源成功率，而是明确承认它排除了原ADG无法执行的同步路径。source在该受限域无解，不能写成原图任务一般无解，更不能据此删除主roster或归功于新方法。若以后改造ADG以支持集体同步授权，须另命名新执行算法并另证，不属于本轮修订。

## 4. 同层环分支为什么可以保留

L4的单位层动作具有严格递增的Type-1时标和不倒退的Type-2时标，因此有向环不可能含Type-1边或严格增时Type-2边；其所有动作必须同层，每agent至多出现一个动作。

原Algorithm 1的“取另一agent首次满足者”不在这里破坏分支：对同层边，另一个agent每层只有一条原动作，不可能新增一个更早、同时又不早于当前层的竞争动作。若环内各agent都保留原层上的原几何转移，同层边仍全部存在。故任何消除此具体环的候选必须改变至少一条参与转移；分别禁止各参与者该层转移的孩子，其并集覆盖所有能消除此环的方案。禁止对象是几何转移及时间，不能只禁某个visited布尔值。

这是一条限于上述时标和原构图规则的覆盖证明，不是ECBS实现已通过。非单位层、同agent同层多动作或另一套Type-2规则均须重新证明。低层需真正遵守约束，环检查需使用实际原动作，不能把终端延拓凭空变为已执行动作。

## 5. J可以证明什么，不能证明什么

把冻结联合状态严格定为互异顶点配置与各agent单调visited标记，合法一步要求图邻接/WAIT、无顶点重合、无交换、无同层ADG环。终态要求所有本轮访问义务已标记，并允许各机器人永久停在各自互异末端。此时合法性只依赖相邻联合状态；J仍是L4的有限状态数上界，没有新增预算值。

在这个问题中，有解路径若重复完全相同的联合状态，可删去中间闭环：前后位置、标记及后继转移均相同，同层环约束不受整体重新编号影响，终态不变。故存在长度不超过J−1的解。这保留的是**几何访问和末端停车的存在性覆盖**；未证明有限付费worker必能找到，未继承真实lifelong吞吐或服务完成保证。

第二个反例是将这条证明误用于服务：机器人已在目标，visited为真，但首个合格TASK_SERVICE尚未发生。删除重复驻留状态可能删除等待该机会的时间；若服务机会比任意几何访问路径更晚，J没有理由约束它。实体包含、任务修订、通信和实际控制状态也不在该有限状态中。删除TASK确认屏障后，服务只由共同执行实际判定，从而不再需要把这些状态伪装进J证明；无法服务仍如实失败。

还有一个必须收窄的分支命题：CBS孩子含绝对层约束时，重复状态不能任意删。例如机器人先在起点等待再进目标，恰避开目标被禁的层；删去重复起点状态便可能让到达落入被禁层。若目标前J层均被禁，该低层孩子可以只在J之后有访问解。不能宣称每个无界孩子的所有可行解都能缩到J内。

所需且足够的较弱覆盖命题是：若根问题有解，则存在一条J内的简单见证；每次顶点、交换或上述环冲突分支，至少一个孩子保留这条见证。整个搜索自始限定在J内，孩子只覆盖这个预先固定有限域，无需给各无界分支重新证明压缩。每次添加的约束须确实被当前候选违反且此前未包含，才有严格进展；浮点、id、堆和失败对象的源码定义域仍沿L4保持UNKNOWN。

## 6. visit-then-leave的终止及拼接最小修订

终止必须同时满足visited和从该层起的驻留不违反本孩子全部未来约束；跨agent检查将短路径延拓到共同末层，末层后永久驻留。visited变真不会强制低层立即返回：后续驻留受禁时，应继续搜索离开、等待和另选停车点。无任务agent同样占有空间，也可由自己的外部求解器移动让路，不能从冲突检查中消失。

低层原件会在Environment报告目标满足时返回，因此未来约束检查必须位于这个终止谓词，而不能仅在返回后拒绝，再无限重复同一早停解。[固定a_star_epsilon.hpp](https://github.com/whoenig/libMultiRobotPlanning/blob/4c75fa20c435c440d8b6bd6dc81668ddc7296ba0/include/libMultiRobotPlanning/a_star_epsilon.hpp)。未来约束被完全检查、短路径被正确延拓，是上述数学覆盖的前提，不是本次已完成的实现验证。

L4§6及§8中的“服务等待条件”“服务屏障继续”“服务条件满足后进入后缀”同步撤销；cut封存仍保留全部不可撤回动作及ADG前驱闭包。visited与任务身份随冻结job保留，END/READY不能修改它为真实完成。拼接的访问绑定检查只核路径义务，真实服务始终交给公共环境。末端停车、cut依赖和共同空间授权继续保留；没有由新任务或任务完成撤回旧承诺的例外。

## 7. 回执与剩余边界

本报告关闭的是纸面选择歧义，并给出环分支与有限域覆盖的条件论证；未给正式PASS。根仍须把这些替换句与P4及73逐节合并，核新旧图拼接和服务输入的同一语义，再走既定正式审查。L4的NominalDuration、源因子、数值/依赖和来源资格未被本轮关闭。

本轮未调用任何skill、Claude或子代理；新增源码对象为零，未读禁区或实验载荷。唯一新件为本文，仅apply_patch编辑，全部shell/链段使用rtk；旧报告、正文和Git不改。没有实现、伪代码、构建、测试、仿真、调参或结果。H、T_delay、B_CAL、B_max、B*、P_active、density N原义未变、未赋值。全文及hash/bytes/lines只作静态回执，继续HOLD。
