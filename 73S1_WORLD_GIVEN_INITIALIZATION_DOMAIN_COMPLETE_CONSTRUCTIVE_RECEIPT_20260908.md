# WORLD_GIVEN 初始化域：第二份完整构造性意见归档

- 原任务真实 canonical handle：`/root/construct73_initialization_estimand_domain`。
- WORLD_GIVEN followup 真实 canonical handle：`/root/construct73_initialization_estimand_domain`。该 followup 复用同一 agent，没有独立的 canonical handle。
- 本次仅归档已经实际完整返回的第二份 WORLD_GIVEN 意见；第一份早期方案不据摘要补造原文。
- 状态：**NONBALLOT；非正式审查**。
- 意见形成时未使用 skill，未读取文件或网页，没有 skill／工具来源阅读，未调用 Claude，未编写或执行研究代码、测试或仿真。本次仅进行归档及文件完整性统计。
- 根已知结论：**已把主要条件写进未固定73，尚待全稿/正式审查，不宣称G6通过。**
- 本归档不修改73或其他文件；保护参数未赋值。

## 第二份完整意见原文

这个修复在数学上可成立：若 WORLD_GIVEN 已确定实体轨迹的初态，软件 INIT 失败只让机器人继续既有待命行为，真实服务事件仍可计数。这样无需对 INIT 失败赋处罚分。但“有合法初态”尚不足以推出“所有方法轨迹上的 \(Q\) 都有定义”，还需补齐以下条件。

**第一，世界演化必须独立于软件 READY 而完整定义。** 任务账本不能只是初始列表；还须规定服务成立条件、完成后的任务推进、同时事件顺序，以及依赖完成事件的任务生成规则。若下一任务必须由未完成 INIT 的中心创建，后续服务语义仍会断裂。可以让可信世界状态机承担这些规则，但必须对所有臂一致。还需保证有限窗口内服务事件有限，排除连续零耗时任务造成的无限计数。

**第二，bootstrap HOLD 必须是真正可执行的持续行为。** 初始包络两两不交只证明初始分离；还须证明在允许的扰动、时延和执行器状态下，HOLD 在所需期间保持包络不变性，并避开障碍。若 HOLD 其实要先完成算法 INIT 才能获得标定、状态估计或控制器，这是循环。可以声明这些属于已经工作的可信底层，但要明确其接口、误差假设及资源。该假设较强，却不是纯仿真模型的矛盾。

**第三，原子转交不能留下覆盖空档。** READY 仅代表软件状态就绪；接管还须依据接管时的有效状态界，证明新运行覆盖承接当前 bootstrap 覆盖。仅用 \(t=0\) 的状态证书不一定足够。转交失败就继续 HOLD；成功后也须明确中心失效时现有运动如何继续、停止及保留覆盖。否则只修好了 INIT 前分支，没有得到“全部方法轨迹”的完整语义。

**第四，真实状态评估器与执行基础设施必须分开。** 离线计分器读取真实状态不构成算法 oracle。可信底层若免费利用真实状态验证新 MOVE、完成避碰或提供中心尚未计算的认证信息，则可能承担了待比较的方法工作。应枚举底层究竟拥有何种信息、执行何种检查、是否反馈给算法，并说明运行费用归属。“算法不能读取 WORLD”本身不能排除通过动作许可得到信息。

**第五，费用公开不能替代时间起点说明。** 从“已合法待命”开始计算固定 \(E\) 是有效任务定义；它支持的是系统进入该状态后的性能结论。若建立 bootstrap 覆盖需要大量真实计算或部署准备，将其另报并不会使窗口内指标自动成为从上电开始的端到端性能。世界构造、可信底层准备、软件 INIT 和运行成本须分别可归属；不能把某臂的必需预计算移进公共世界准备。共同规则也不要求不同臂费用数值相同。

**第六，是否保留原 \(\theta\)，取决于支持集和服务语义。** 若原任务完成本来就是实体条件自主触发，73 是补全 INIT 失败时的世界语义；若原规则要求软件确认、交互动作或中心接单，改成自动服务便改变了目标。类似地，bootstrap 包络不交若比原合法 source 条件更强，就可能排除原 roster 中的拥挤实例；不能仅凭“大规模仍在”声称总体未变。需要证明原先冻结的合法 roster 全部满足新初态条件，而非按运行结果筛选。

静态合法性应在方法运行前按同一规则判定。已纳入 roster 的 WORLD 未绑定、宿主未启动或缺日志，应保留为不可验证记录；不能事后改记为非法 source 来完成评分。源代码 UB 若没有确定执行语义，仍不能凭世界模型推导该臂的潜在轨迹。

因此，73 可以使 INIT 失败下的原实体任务计数自然存在，且无需缩成“INIT 保证成功族”。但它需要一套明确的可信待命与任务服务基础设施；保留同一主总体，还须证明该基础设施没有改变既定 source 支持集、任务完成规则和比较时钟。
