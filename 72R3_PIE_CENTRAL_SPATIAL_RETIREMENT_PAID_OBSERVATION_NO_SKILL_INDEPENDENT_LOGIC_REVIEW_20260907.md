# 72R3：固定 72 候选的无 skill 独立逻辑审查

日期：2026-09-07。身份：固定顺序第三阶段的独立逻辑意见；本报告不是根验收，不授予六门，不启动后继候选。

结论：当前字节不能记为逻辑已全部闭合。R3-01 是 P2 字面时间量词的反例；R3-02 是真实服务充分条件的证明缺口；R3-03 是需要明确判定的状态机规格 UNKNOWN。没有据此声称已经发现接收后退休造成的界内实体碰撞，也没有证明主总体效应为负。维持 HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT。

## 一、唯一输入、完整阅读与独立性

唯一科学输入为 `/home/lyh/MAPF_PIED_MDDR_RESEARCH/72_PIE_CENTRAL_SPATIAL_RETIREMENT_AND_PAID_OBSERVATION_PREEXPERIMENT_CANDIDATE_20260906.md`。读取前和完整阅读后均实际核得：

- SHA256：`d2b7ee412b59b070f33cd80036f1e79b55ffb6fa6b79aa0a3d39e8b359f7bc84`。
- 字节：86528；行数：353；存在末尾换行。
- 原文分六段真实完整读取：1–65、66–125、126–185、186–245、246–305、306–353 行，随后仅在同一文件中定位相关原句。不是摘要阅读、关键词阅读替代全文或顾问转述。

明确不使用任何 skill：未读 SKILL.md 或其参考，未采用技能框架；未读导师、Nature、Claude、其他审查或行政报告；未上网；未派子代理；未读其他本地科研材料。候选正文自己叙述的来源阅读、旧意见和根状态，只是被审对象的陈述，不算本审查者亲见那些材料，也不用于多数投票。

新报告路径在首次读取前及新建前两次确认不存在且不是符号链接，仅用 apply_patch 新建。候选与所有旧文件不改。下文的证明变量和符号顺序不是实验参数、O 表、种子、场景载荷或运行结果。

## 二、实质意见与最小纸面关闭条件

### R3-01：P2 对乱序证书取 max 后，不能把新后缀覆盖追溯到任意当前来包的采样时刻

定位：§4 第 66 行；相关规则为 §6 第 108–110 行的固定采样内容及重传、§13 W03 第 295 行。

第 66 行先定义接收时 `q_new=max(q_old,q_sample)`，随后声称“从采样起至该 tid 终结”实体一直位于 `U(q_new)`。如果这里的采样起点就是本次到达证书的 `t_sample`，该命题不成立。

满足模型的符号反例：同一合法、尚未终结的 tid 在两个采样时刻 t_a<t_b 具有严格前进的参考进度 a<b；两份可信证书分别准确给出 a、b。后采样的 b 证书先到达，早采样的 a 证书随后到达，故处理 a 证书时 q_old=b，q_new=b。本文允许这种延迟及乱序，并未要求证书按采样顺序到达。

实体在 t_a 不必位于 U(b)。令误差恒为 Z 中沿 e 方向取得最小支撑的 z_0，并取合法 w=kappa z_0；选择 F 中同方向最小支撑点。该实体点沿 e 的投影为 `e·u+a+min(e·Z)+min(e·F)`，而 U(b) 中每一点的投影至少为 `e·u+b+min(e·Z)+min(e·F)`。a<b 使前者严格在后者之外。F、Z 的紧性保证这些支撑取得；常误差满足原闭环和误差界。参考轨迹可取 P1 已允许的合法前进段，不需要界外运动、错误可信证书或参数绑定。

这反驳的是回溯到 t_a 的字面命题。它不反驳接收后或发布后安全：两份证书的采样都早于相应接收，在后一次接收时参考已经不小于 a、b，之后又不回退，所以从该接收时刻起使用 U(b) 仍有覆盖。也不能据这项反例宣称真实实施过一次错误删除。

最小纸面关闭：分别写清“单份证书 q_j 自其自身采样时刻起有效”与“接收前缀的最大下界自该前缀形成时刻起有效”。例如令 q(R) 为接收事件 R 前已验证同 tid 证书下界的最大值，则对 R 以后且 tid 未终结的时刻证明 `s(t)≥q(R)` 和 `X(t)⊆U(q(R))`；后继发布再继承此包含性。也可显式绑定取得最大值的证书及其采样时刻，但不能继续使用未索引的来包采样起点。P3 与 W03 应引用这个前向命题。

根验收需求：独立核对上述乱序顺序确属候选允许域，逐式核对反例；然后区分 P2 原句应改与 P3 前向证明可保留。不得把本项自动升级为碰撞反例，也不得用“只是延迟”免除量词修正。

### R3-02：参考终点的时间界尚不足以闭合 §13 的真实 Q 充分条件

定位：§8 第 138 行，§12 第 255 行，§13 第 275–279 行。

第 277 行共同初态、合法时间平移输入和未中断条件可以给相同的有限参考运动时长 tau_B。由 START 上下界直接得到的是参考到达端点的上下界。第 138 行的真实服务另要求任务顶点匹配、合法已分配队首、真实实体 X 处于服务区域；到达参考端点本身不推出最后这一条件。

例如合法常误差 z_0 可使终点实体 `v+z_0+F` 不满足预定服务区域谓词。这样的实例并未违反 z∈Z、参考端点 HOLD、资源覆盖、付费交付或先启动的时间关系；即使有处在两臂参考到达时间之间的 TASK_SERVICE 机会，先到臂也可能不能服务，因而没有这次 Q 增量。全文没有给“服务区域必含所有 E=v+K”的公理；也不应在审查时替作者添加它。

此项不是主效应 HYP-NET 的总体反例，也不是已发现虚报实验结果。若把“仅在……才有”严格读为只列必要条件，该句自身不承诺充分性；但第 279 行将整段称为“结构性充分条件”，该层级需要补齐，或明确降格为尚不充分的条件清单。

最小纸面关闭：在用于证明增量的共同服务机会处，显式要求 B 的同一合法队首仍有效、未被此前服务，reference 处于合法终点/驻留模式，且实际服务区域谓词成立；同时该机会位于可验证评价前缀并在 Stop 之前。可采用较强的 E 包含于服务区域作为简便充分条件，但不必强加给全部实验域。再把“参考到达界”和“真实服务事件”分别写出，保留其他 agent 与比较区间已有服务计数的相等条件。不得为满足这些条件事后挑窗口或删样本。

根验收需求：用 §8 的完整服务谓词审计 §13 推论；接受者须指出区域资格从哪个前提推出。不能只凭“目标已分配”或“可信 q 到终点”跳过真实实体条件。补齐后的见证仍是有条件命题，不能替代固定 R0 输出证据或主 roster 正效应。

### R3-03：FINISHED 已提交、READY 尚未消费时，PRECHECK 稳定性与总 POLICY 需要明确闭合

定位：§5 第 84–98 行、§7 第 124 行、§9 第 170–182 行；相关 W01、W14、W26。

合法 END 先关闭旧 tid 并建立终点 resident，真实 c 仍为 k；中心随后 FINISHED 并推进 d_i；READY 允许延迟或丢失，因此存在“中心已 FINISHED、gate 在终点 resident、c=k、匹配 READY 尚未消费”的合法状态。该状态由本文自己的分阶段协议产生，不需要可信故障。

总 POLICY 首分支要求已有匹配 READY，次分支只覆盖旧 tid 尚未中心终结。上述状态不满足两者，字面上会走到原 slot 仍为 MOVE 的创建/重试分支。第 90 行又允许已知旧请求关闭后换新 e，而旧 slot 仍未消费。此时必须明确：是等待/重取旧 READY，还是允许发出必被拒绝的新请求并照计费用。

关键不是仅有 `e>g_i`、`c=slot` 和 parent 身份相符就足够。旧 MOVE 的终点 resident 与这个未消费 MOVE 的起点不同；“对应 resident”若已严格包含“resident 参考锚点等于该原边起点，且不是该 occurrence 已 END 未消费的状态”，即可排除这种 PRECHECK。若只解释为匹配当前 gamma，则第 86 行“无 grant 不能消费当前 MOVE”的稳定性论证会漏掉随后交付旧 READY 的合法消费，因为该消费不需要新 grant。

本项定为 SPEC_UNKNOWN，不宣称已构成满足 P5 全前提的跳变或碰撞反例。P5 的同点拼接、合法原步和实际接纳检查可以拒绝错误重启；把错误起点重置强塞进模型并不合法。但依赖这些概括用语不能替代总分支与 PRECHECK 输入谓词，额外失败请求是否发生还会改变全费用队列。

最小纸面关闭：明确 END 后未消费 occurrence 的持久状态/判定；规定该阶段的 POLICY 输出和费用；形式化 PRECHECK 的 resident 起点一致性与未终结 occurrence 条件。给出 READY 在 PRECHECK 捕获之前、之后及重传时的纸面顺序，证明一次 MOVE 最多执行一次、READY 最多消费一次、迟到证书不能授予已经被消费的旧 slot。若根认为原文已有充分谓词，须逐项给出原句及蕴含证明，不可仅回复“合法二字已经涵盖”。

## 三、其他审查面的独立推导与边界

以下是能按正文重建的局部结果，均限于其前提，不是整体 PASS 或运行认证。

| 审查面与位置 | 独立判断及实际限制 |
|---|---|
| P1 连续控制，§3 第 46–54 行 | z 的变分式给盒不变性；LAUNCH 的交点存在界、APPROACH 中 r 的边界方向及 sqrt(xi) 的有限耗尽可重建；BRAKE 的 b_dot≥0 给不越原边。启动瞬间 v_ref=0 时 F_L 导数为零，之后为正，不影响交点证明。端点 HOLD 是明确混合公理。无据把参考零速当实体零速。 |
| P5 拼接，§3 第 56–60 行 | 世界 p 同点、世界 z 恒等重置及同 F/Z/闭环足以接续不变性；等待中 z 可变并不破坏证明。转向、缩盒、偏移新起点、驱动记忆或实体停车不能由本模型推出。它们是前提/范围问题，不是满足全部 P5 前提的反例。协议确实只准入这种拼接仍需 R3-03 的谓词核验。 |
| P4/P6 几何，§4 第 64–76 行 | 紧性加闭性可证明 I_r 闭且最大值取得；q≤lambda 的等价不需要 I_r 连通。lambda=q 必须保留，lambda=ell 对应 endpoint。有限有理多边形域的逐凸块 LP 有明确有限路径。一般紧集不自动可计算。精确 G/N 同集合不意味着 NATURAL 同轨迹。 |
| P3 资源与事务，§5 | 单中心私有作业占槽直到发布排除了并发读旧账本再双重提交；完整 M0 先授、parent 改 tag、终点留 E 及 expected-holder 拒旧释放可构成覆盖归纳。CANCEL 先/GRANT 先两分支、水位吸收性、d_i 与 g_i 分离是自洽结构。证明应引用 R3-01 修正后的前向 P2，并确认 R3-03 的准入条件。未证明灾后恢复活性。 |
| 不可信与可信信息，§6、§9 | 普通反馈不授 q/c/任务权限；CAPTURE、完成、投送和算法输入读取分开。等待、重复证书及原 tid 绑定不构成免费刷新。下界含真、不可伪造和持久状态是显式可信假设，不能靠普通传感器自行得到。 |
| 延迟控制与付费发布，§9 | writer 槽不锁住物理，END 不消费 c，实际 START/RESUME 另查离散模式，避免把 command_revision 当当前模式。物理推进、既有完成结果发布、当前行操作有全序；原子操作容量不足时不读取。未见必须通过免费快照完成的环。具体费用表、指令/浮点和容量合法性仍待授权后的实际绑定。 |
| D/R 总选择，§7 | Active 用中心知识定义；RR 前哨、严格后继、回绕和成功提交推进 last 足以处理插删。D 按不同 p 去重、排除 endpoint、tie 及 RR 回退可重建，不读取真实 blocker 进度。扫描和索引维护均需费用；策略可能反复选失联者，这是效果风险而非策略未定义。 |
| PIE 游标/任务，§6、§8 | Kc 下界和 executor c 分开，pure WAIT 不由 POSITION 免费观测。PROJECT 的所有权约束避免虚拟派发未分配任务；滞后快照与逐槽弹出可产生错误预测，但正文已不承诺预测准确，且不直接改 A/Q。固定尾追加与 pin/GC 不会把全部审计存储伪称有界。未证明终身任务活性。 |
| 冻结原源与调用域，§8–10 | worker 副本、冻结环境、完整目标重验和失败隔离有明确合同。世界经过钟与 CPU 服务钟分域，原预算比较未被统一硬截止替代。随机比较器、外部调用覆盖、ABI/依赖、原生运行与 guarded 外部法适用域仍有候选自承 UNKNOWN。本次未读源码，不能把候选列出的 blob/阅读记录认证为独立实核。 |
| 全费用队列，§9、§11 | 请求/采样/字节/验证/扫描/失败/丢弃/预处理/GC/日志与有限发布都列入；元调度不透明 token 的预付边界已写明。NATURAL 同容量而各付实际工作、STRICT 连空闲供给预付，两种问题不同。尚未实例化不等于纸面结构 FAIL，也不等于已有完整运行计费回执。 |
| 故障总函数与评分，§9、§12 | 分量停止不删除在途实体，最早合法全臂 Stop 与未知日志分开；INIT 前未知不填零，合法 INIT 后零服务可核零。Q 来自真实服务，分母固定 E；非法原源与能力不匹配不用于制造因果优势。分类主类之外保留 flags，可避免单标签抹去事故。实际数值不可认证必须 UNKNOWN。 |
| 全 roster 与统计，§11–12 | 静态合法枚举、动态失败不删行、固定类别/地图/来源/profile/重复权重定义了有限来源目标。按独立 block 的有界差应用指数矩界可得到给定 radius，块内配对相关不破坏它。两项均正的交并检验不需要把两区间说成联合覆盖；结果依赖的缺失只保留错误发布概率界，正文已排除条件于发布的虚假覆盖。 |
| 对照公平与主张，§1、§11–13 | 主 D-S/R-S 共用相同 S 与固定 K；G/N 是实现组织比较，F 不是所有固定裕量法的代表。E0 免无用途阈值费用且保留必要可信服务，能约束“主动观测值得买”的外推。来源/平台或评分不合法时不得发布相应推断；正 D/R 不能抵销安全反例、E0 负结果或活性失败。 |

封闭 resident 循环不能出现第一个 grant 的负例可由完整 M0 准入直接重建。正方形共同进度的连续几何可行见证与独立 no-start 域不同，不可混用。候选承认死锁、饥饿、中心队列拥塞与未运行，所以本报告不把这些已承认限制重新包装成推翻 P1/P3 的新反例。

## 四、根必须独立验收的具体项目

1. 对 R3-01 作“原句被反驳 / 前向覆盖仍可证明”的分层判断，并核对 P3/W03 使用的真实时间起点。
2. 对 R3-02 判定 §13 到底主张必要条件还是充分条件；若主张充分条件，核齐实际服务区域、合法队首、Stop 前服务事件及计数关系。
3. 对 R3-03 明确终点未消费状态的唯一 POLICY 和 PRECHECK 谓词；若认为正文已蕴含，给出不依赖免费真实状态或循环定义的推导。
4. 对上表各局部证明检查前提可同时满足、源适配确实保持前提；不能将“没有新增异议”直接计为六门 PASS。
5. 文献全文范围、固定源码、原法/外部适用域、完整依赖和既有行政任务的真实身份，应由根使用其有权证据独立核验。本报告没有读取它们，不替代它们的完整回执。
6. 未运行机械合同、未绑定 manifest、尚无主总体效果都是尚待完成的层级；未运行本身及没有两台 LIMO 实物证据不构成结构 FAIL。本次不以执行测试来关闭任何意见。

有实质修订需要时由根按既定冻结纪律决定另立编号；本审查不编辑 72，也不创建后继候选。任何正式验收应逐项说明采纳、反驳或仍 UNKNOWN 的证据，不做多数投票。

## 五、实际执行边界回执

本阶段仅进行指定候选读取/身份核验、独立纸面推导和本新报告创建。未实现、构建、创建或运行测试、仿真、调参、生成种子/O/manifest/tape/载荷/实验结果，未进行实车动作。未读 35–38、Q-CAL/Q-CONFIRM、受限载荷/results、桌面或其他科研材料；69/70/71/72 与旧报告保持不动。

H、T_delay、B_CAL、B_max、B*、P_active、density N 均未赋值、指定范围、设置默认或改义。报告全文完成后的实际 SHA256、bytes、lines 将随最终回执给出；文件不能嵌入其自身最终哈希作为自证。

本阶段意见完成不代表研究目标完成。继续 HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT。
