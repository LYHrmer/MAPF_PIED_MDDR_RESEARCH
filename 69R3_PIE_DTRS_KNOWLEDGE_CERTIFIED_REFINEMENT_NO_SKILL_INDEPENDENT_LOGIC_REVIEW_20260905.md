69R3 — PIE-DTRS-KC 无 skill 独立逻辑审查

日期：2026-09-05（Asia/Shanghai）

**裁决：候选 69 的实验前设计尚未闭合。本路不予科学通过；保留 `HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT`。**

这是正式第三路、完整且非空的独立逻辑回执。回执有效不等于候选通过。结论只针对下述固定候选字节；没有继承任何前代通过，也没有读取第一、第二路报告或其他意见。下列问题能够通过新的规格、定义、静态推导和预注册文本修复；本路不要求实施、构建、测试、仿真、效果数据、硬件或期刊录用。

**输入身份、阅读覆盖与边界记录**

唯一科研输入：[候选 69](/home/lyh/MAPF_PIED_MDDR_RESEARCH/69_PIE_DTRS_KNOWLEDGE_CERTIFIED_PROPOSAL_PRESERVING_TRANSACTIONAL_REFINEMENT_PREEXPERIMENT_CANDIDATE_20260905.md)。本报告的 `L` 均指该候选的原始行号。

| 核验项目 | 实测结果 |
|---|---|
| 开始 SHA-256 | `e07cc5c7dd3e87d4e03d6e7cd544dedad5f05b686585494630c497f4ae1eb80d` |
| 开始 bytes / 换行数 | `50975` / `787` |
| 注册身份匹配 | 完全匹配 |
| 全文读取方式 | 仅一次 `rtk proxy cat`，成功返回全部原文，缓存后分段展示；无输出截断 |
| 连续阅读覆盖 | `L1–200`、`L201–400`、`L401–600`、`L601–787`，无缺段 |
| 完成全文审查后的 SHA-256 | `e07cc5c7dd3e87d4e03d6e7cd544dedad5f05b686585494630c497f4ae1eb80d` |
| 完成全文审查后的 bytes / 换行数 | `50975` / `787` |
| 身份状态 | 开始与结束一致，未修改候选 |
| 输出目标先验检查 | 精确目标路径不存在，随后仅用 `apply_patch` 新建本报告 |

没有读取 `SKILL.md`、技能参考资料、其他候选正文、旧 35–38、任何他路意见、Q-CAL/Q-CONFIRM outcome 或 scientific payload；没有访问永久只读桌面备份，没有联网，没有递归扫描研究目录，没有派子代理。没有执行科学代码、构建、测试、模拟、调参、载荷或结果生成，没有修改 release 或封存旧工件。原文中的前代名称与外部引文仅作为候选自身内容阅读，没有跟随访问。

所有 shell 命令均以 `rtk` 开头，精确保真读取使用 `rtk proxy`。唯一新建文件是本报告，唯一编辑机制是 `apply_patch`。`H`、`T_delay`、`B_CAL`、`B_max`、`B*`、`P_active`、density `N` 保持原含义且未赋值；本报告没有给出其数值、区间、范围、默认、先验或调参方向。PIE 仍是唯一默认新 MOVE 提案源，修复建议不引入替代 MOVE。

**哪些论证可以在当前明示前提下接受**

- 问题合同具体：固定 PIE 来源动作，知识不足时只能保留原 MOVE、经证 WAIT 或既有事务 containment（L25–43、L78–90）。这不是要求重新设计路径规划器。
- 不可区分性论证 69-I（L170–176）抓住了真实边界：若清空与未清空历史在消息上不可区分，且没有独立执行互锁，消息本身不能兼顾争用资源上的安全授权与非平凡进展。这里不要求证明真实传感器可靠。
- 若集合只增不减且只在首次加入时入队，有限批次的反向传播能够终止；复制原 MOVE 或转为 WAIT/无新授权也确实不会产生替代 MOVE（L313–331）。但“最小闭包”和计算界还需要下面 LG-02 的具体判定关系。
- 69-S 是合理的条件式组合证明框架（L347–361）。若知识包含、资源排斥、版本验证、最终批次守卫和 linked-execution 前提全部已经建立，首个冲突的分类反证思路成立。本报告没有把缺少这些前提的执行拿来反驳 69-S；问题在于候选尚未把部分前提构造成可独立重建的规格。
- 69-B 在其等初始字节、等外生逻辑轨迹、同一确定逻辑调度器前提下可接受（L391–409）。它没有承诺墙钟恒等，也不能自动推出活动期间切换模式后的恒等性。
- 失 ACK 不释放、失联不自动转移 owner、没有可靠清空证据时保守阻塞、对中途边状态保留占用，均是明确且有用的安全边界（L234–244、L373–387、L470–492）。不把这些退化情形误判为违反无条件活性，因为稿件没有作该承诺。
- L719–730 已把移除信任边界、围栏执行、最终复核等的消融限定为负控。此类去掉明示前提的负控不是候选安全定理的反例。

**承重问题总表**

| 编号 | 问题性质 | 直接受影响的主张 | 裁决 |
|---|---|---|---|
| LG-01 | 抽象状态及验证谓词未形成封闭的可计算定义 | 69-K、69-S、独立证书、polynomial certification | 必须修订 |
| LG-02 | 最小闭包依赖条件、完整依赖图与计算成本未证明 | 69-C、69-L、线性复杂度 | 必须修订或收窄主张 |
| LG-03 | 初始 WAIT、已启动事务及最终复核失败的总分支不完整 | total outcome、安全保持、提交入口 | 必须修订 |
| LG-04 | PREPARE 承诺寿命与 COMMIT/ABORT/RECOVER 的分布式守卫不足 | `LIN_COMMIT` 组合契约、失联恢复 | 必须修订 |
| LG-05 | inactive/active 的状态交接与安全域没有定义 | 字节恒等的适用域、全流程安全 | 必须修订 |
| LG-06 | 方法标签被纳入“独立样本”，配对相关性未处理 | 独立单位、估计量、推断有效性 | 必须修订 |
| LG-07 | 相同报告字节、相同提案和闭环轨迹的公平性契约未区分 | 同信息比较、故障强度可比性、吞吐因果解释 | 必须修订 |
| LG-08 | 数据选择、故障事件模式、停止及证伪仍是待填合同 | 公共数据门、纯仿真预注册门 | 必须修订 |

**LG-01：知识包含关系与证书查询目前仍依赖未定义的运算。**

定位：L182–208 定义 `Q_i/lo_i/hi_i/holes_i/choice_i/txn_tags_i` 并给出消息处理步骤；L214–228 给出查询和包含引理；L295–301 给出 `CERT` 的语义要求；L413–427 要求独立重建；L456–466 声称多项式可计算。

明确规格缺口有三处。第一，`holes_i`、`choice_i`、`txn_tags_i` 没有有限编码语法、联合 concretization 或约束传播规则。“每个 agent 的区间”与“保留跨 agent 事务相关性”没有被合成为一个确定联合状态集合。因而 prefix/claim consistency、非空判定以及查询究竟扫描什么，不能由当前文本唯一执行。这里不能据此断言问题必然困难；可以说多项式界尚无一个已限定的输入语言可供证明。

第二，69-K 的证明逐项分析了报告到达，却没有给出 issuance、durable COMMIT、无需新消息的物理相位推进、重启接管和 checkpoint compaction 的抽象转移。L189 的“从已发命令仍可能到达的最大相位”是需要保持的语义不变量，不是更新算法。以一个已知在起点的 occurrence 为例：若后来已有授权可以在静默期开始，`K` 必须在授权可执行之前已经包括相应后继相位，或者由一个保守的 abstract-post 规则立即扩展。只按 L198–208 的入站消息步骤执行，不能确定何时完成这一步。这个例子指出维护 69-K 前提的接口缺失；它没有把失真的 `K` 冒充满足 69-S 前提的反例。

第三，`CERT` 尚未给出带参与者与相位的兼容关系。一个单独 agent 在已证起点执行原 MOVE 时，自己的 `MAY_PHYS` 起点占用必须与自身 origin 权利兼容，而另一个 agent 的占用必须排斥。当前“exclusive against MAY_PHYS/MAY_CLAIM”（L298）没有展示这一区别及 linked component 的例外如何计算。L427 所要求的 verifier 也只得到摘要和证据键的字段清单，尚未规定如何取得完整、哈希绑定的前态、相位事实、资源模型及信任根。摘要相等不是上述缺口的替代物；候选对此已有正确原则，但尚缺完整输入合同。

最小可执行修复：给出有限记录语法及联合 concretization；给出初始化、发授权、物理可达后继闭包、证据 join/shrink、重启和压缩的总函数；将清空事实绑定到 resource、occurrence、version/epoch，避免把一次清空当成该资源永久为空；定义兼容谓词和确定的 `CERT` 判定步骤；规定 verifier 的全部规范化输入及证据解析路径。可以选择保守、能力较弱但定义完整的抽象，不必解决一般 consistent-cut 或相关性恢复问题。

客观闭合条件：仅凭新稿就能手工重建一个无争用原 MOVE、起点/边上不确定、错序普通报告、重复清空证据、跨重启 occurrence 和相关事务的全部抽象更新与查询；逐类证明每种抽象转移覆盖所有允许具体后继；给出非空/一致性算法对明确编码长度的上界。无需运行这些例子。若保留 L371 的重排收敛结论，还须证明普通 join 与持久真证据约束的规范化具有顺序无关性，而不仅是“不会不安全地缩小”。

**LG-02：闭包的最小性和线性界不能只由集合单调增长推出。**

定位：L295–329 的 `CERT(i,S,K)`、Seed 与 worklist；L431–435 的 `Affected` 和局部性；L458–464 的计算界。

集合只增不减证明的是终止性，并不单独证明最小性。当前 `CERT` 同时读取 `S`、动态 holding、已有 claims 和同批 releases。要使 L329 的归纳有效，必须证明：每次失败确实是所有可接受扩展都必须保留的抑制，而不是一个之后删除其他冲突 claim 便会消失的暂态失败；并且任何可能导致后续失败的关系均已进入依赖图。候选对“leastness relative to declared relation”的收窄是正确的，但该 relation 还没有被定义到足以检验上述性质的程度。

另一个具体缺口是 L320–323：不安全 WAIT 会把“component”改成 containment 并传播释放，最终独立复核也可能发现新失败，但没有给出组件的精确定义、这些失败怎样加入 `X`、是否重新入队以及何时算真正固定点。没有这层封闭性，L435 的“最终复核看到同样独立 claims”只是所需条件，尚不能从图中不可达推出。

复杂度也需要单独核算。L318 的文字算法对每条到达 `j` 的边重算 `CERT(j,...)`；其成本原则上为各次证书检查成本之和，并非仅为边数。稿件未明确跳过已被抑制的 `j`，也未给出增量失效计数或对检查开销的摊还证明。“建完索引”不自动消除遍历 `j` 全部义务的成本。这不意味着方法不是多项式；意味着 L462 的线性界还没有由所写算法推出。

最小可执行修复：定义一个固定、有限、完整的必要依赖关系及明确的抑制/containment 算子，证明其适当单调性和固定点条件；把没有 linked-execution 证明的组件失败、初始静态失败及最终新增失败纳入同一总流程；首次加入才排队，明确已抑制项的处理。若不能证明最小性，删除最小性而保留确定的保守安全算法；若不能证明线性，则给出真实可证明的多项式界，不必制造更强结论。

客观闭合条件：对固定版本和固定提案，给出闭包算子的定义与最小固定点证明，或明确收窄后的主张；说明工作队列顺序是否影响输出；静态推导跟随链、含不可靠 holding 的组件、最终复核新失败三个情形，表明结束时全部守卫成立或转为明确无新授权。成本按每个 occurrence、claim、义务、边的访问次数逐项计账。69-L 必须针对同一个已证明完整的 `Affected` 定义成立。

**LG-03：最终输出还不是对所有输入类的总算法。**

定位：L80–88 允许 PIE 本身给出 WAIT；L271–287 规定 safe hold 与结果类；L293–323 只以 MOVE 集 `M` 为 worklist 对象；L497–506 给出事务状态表。

初始 `mu_i=WAIT` 不属于 `M`，L320 只明确检查“induced WAIT”，但 L282 的 `UNCHANGED_WAIT` 没有对应的守卫分支。若已有事务尚未终结，当前预测位置上的 WAIT 并不自动满足 `MUST_AT` 或 `SH_BRAKE`。例如消息缺失使实际状态仍可能为起点或边上，而本轮 PIE 在其预测 occurrence 上给出 WAIT：输入合法性、普通 WAIT、继续旧事务和无新授权必须有确定分流。仅对 MOVE 建 Seed 不能完成这项分类。

同样，L323 要求最终独立复核，却没有规定其失败后的返回值、是否撤销本轮 PREPARE、是否扩大组件或怎样处理已经开始的旧事务。L297 允许起点由“existing transaction”保护，L284 又提供 `CONTINUE_EXISTING_TXN`，但没有明确何时只能继续该原 `tid`，而不能把同一 MOVE 当成新授权再次提交。

这是总规格缺口，不是指控任何失败 WAIT 已经满足 69-S 的 safe-holding 前提。最后一道检查如果理想地拒绝当然可以安全；仍须给出拒绝后可执行、无歧义的控制结果。

最小可执行修复：在闭包前后均对全体 agent 分类，明确原 WAIT 也须具备当前 occurrence 的 holding 证明；已启动事务只返回绑定原 `tid` 的继续/containment；最终复核失败进入确定的预提交 ABORT 或无新授权路径，并区分本轮准备权利与不能释放的旧物理/事务权利。不得通过把不确定 agent 标成 WAIT 来补齐向量。

客观闭合条件：新增总决策表覆盖初始 MOVE、初始 WAIT、未启动旧事务、已启动旧事务、空知识/恢复、最终复核失败；每行都有唯一结果类、提交资格及保留/可释放资源集。所有产生新离散授权的分支均引用同一最终 verifier，所有其他分支保持已存在的安全责任。

**LG-04：需要把稳定 PREPARE 与不可逆 COMMIT 写成明确的底层契约。**

定位：L234–244 的 owner/fence/日志假设；L248–267 的线性化与部分交付；L500–508 的 COMMIT、ABORT、RECOVER 状态表。

一个协调者的原子 durable append 可以作为决策线性化点，但“所有 owner 曾发出版本绑定 PREPARE token”还不等于“这些 token 在 append 当刻仍是不可撤销的承诺”。L261 要求版本变化在 append 前导致 ABORT，尚未说明参与者如何维持承诺、协调者如何与远端 ABORT 串行化，或哪些变化被锁阻止。

需要裁决的最小交错是：owner 已持久 PREPARE；协调者已收 token；owner 进入 RECOVER 且尚未见到全局 COMMIT；本地尚未物理启动。L505 的“pre-start”表述看似允许 ABORT，L506 也列出 RECOVER→ABORT，而协调者可能仍按已收到的 token 尝试 append。正确协议必须在该交错上禁止一种结局。这里没有断言它是满足 L261 理想验证前提的反例；恰恰是当前文本没有把这个前提落实为参与者的可执行守卫。

L501 的“no postcommit abort”原则正确，但“本地不知道 COMMIT”与“全局已证明没有 COMMIT”不同。只写“丢 ACK 不能 ABORT”（L508）还未覆盖提交记录在网络中不可见的恢复情形。单 writer、epoch 字段以及意图日志都不自动替代这一区别。

最小可执行修复：允许把该能力作为明确的可信事务底座接口，无需声称发明共识。合同至少规定 PREPARE vote 的持久性与锁定范围、COMMIT/ABORT 的唯一决策记录、提交可成立时 token 不可单方撤销、未知决策时继续持有 claims、ABORT 只能由可核验的全局未提交/已中止事实触发；说明版本失效与已准备决策如何互斥。执行器检查的是当前仍有效的完整授权，不只是 epoch 数字。

客观闭合条件：给出协调者和资源参与者分开的守卫表，分别处理决策已 COMMIT、已 ABORT、未知；用上面的交错及 lost ACK、重启、分区三个静态时序证明不能同时产生 COMMIT 与释放同一准备权利。若选择把全流程原子性作为底座公理，须精确列出该公理，69-S 明确条件化于它，而非以消息算法已实现该公理的口吻陈述。

**LG-05：inactive 的字节恒等不能充当模式交接协议。**

定位：L401–405 的外部激活谓词与 fast path；L248–267 的未完成事务；L413–425 的非 inactive 证书；L496–506 的状态机没有模式维度。

69-B 的等初态 inactive 重放论证本身可以成立。但 L401 允许“外部激活谓词为 false”便返回原 preauthorization 缓冲区，没有规定此时是否存在 Candidate 69 创建的 PREPARE/COMMIT、未清空 swept 资源、抑制后改变的逻辑状态或尚未接管的旧执行权利。第 21 节（L494–508）的事务状态机也没有 INACTIVE 与活动事务的乘积状态或转换守卫。

一个稿内尚不能决定结果的时序是：活动模式已经提交 `tid`，ACK/终态仍不确定；外部谓词随后变 false；下一次 baseline 提案触及该事务仍保留的资源。此时必须由谁拒绝、保留哪些 claims、fast path 的“final authorized bytes”如何解释，均不能从 L401 单独得到。若所有 baseline 授权也经过同一强制底座，安全可以保留，但该共同接口必须写明；若 inactive 完全绕过底座，则必须把安全域限定到 baseline 自己有保证的条件。两种方案都不能通过引用 69-B 的等初态前提来省略交接。

同样，激活时如何从 baseline 的已发行 occurrence、durable intents 与当前围栏重建保守 `K` 尚无规定。外部激活谓词不能被默认为无漏报的故障检测器。本报告不要求给 `P_active` 赋值。

最小可执行修复：用符号守卫定义启用、活动、排空/移交、停用状态；启用之前导入或围住全部在途授权，停用之前证明静止可移交或让共同底座持续保留并执行旧 claims；无法证明时延迟转换并保持 containment。明确 69-B 只用于哪些冷启动/重放比较，哪些跨模式运行只能主张安全而不能主张与未曾介入的 baseline 字节恒等。

客观闭合条件：逐个列出模式转换允许条件、读写状态、未完成 `tid` 的唯一接管者和围栏行为；对活动提交后失 ACK 再停用、baseline 旧动作仍可启动时启用两个静态时序，唯一决定授权或阻塞。全流程安全定理的量化范围须与 fast path 一致。

**LG-06：实验方法是配对处理标签，不能把它本身当作独立性的来源。**

定位：L647–649 把 map、task、initial state、planner stream、delay、feedback-fault 和 method 的完整元组称为独立单位；L409 又明确成对 replay；L637–643 要求公共输入；L686–711 列出结果与证伪项目。

共享同一外生场景的不同方法运行通常相关。最简逻辑反例是 baseline 与 candidate 的结果都等于共同场景效应 `U`；更换 method 标签并不使两者独立，二者仍共同随 `U` 变化。这个反例只涉及统计设计，没有生成结果或指定任何保护参数。L649 正确排除了 agent/tick 伪重复，却仍把同一配对块内的 treatment runs 称为独立。

不同 map/task 元组也不因字段字符串不同便必然独立；如果复用同一个上层随机场景或抽样层级，需在声明的推断范围内处理其聚类。当前文本还没有选定配对估计量、聚合权重、重抽样/推断单位，以及多项“primary”结果的主次关系。

最小可执行修复：独立抽样单位定义为不含 method 的外生场景/独立复制块；method 是该块内的处理，方法间比较使用块内差值或另一明确的配对估计。标明地图/任务是固定分层条件还是被抽样总体；若存在更高层共享生成来源，在该层处理聚类。保留 agent/tick 为重复观测，并冻结失败与删失进入目标量的规则。

客观闭合条件：预注册给出场景键、配对键、独立随机来源键、分析/重抽样单位、主比较和估计量；同一配对块不会被当成两个独立复制来增加证据。对全部发起单位的失败、阻塞和删失，能在没有结果的情况下唯一决定进入哪个分母及如何进入效用估计。无需在本阶段赋样本数、运行期限或任何受保护符号。

**LG-07：共同外生扰动不等于把闭环方法喂给同一组状态报告字节。**

定位：L609–620 的数据与扰动清单；L635–643 的 comparator / information parity；L649 的轨迹单位；L668–682 的两类计算比较。

候选既要求相同 delayed/faulted report trace，又在允许时要求相同 nominal PIE proposal。对于固定快照的一次判定比较，这可以有意义；对于各方法改变实际状态后的闭环吞吐比较，这些字节是内生的。

明确反例：同一个 PIE MOVE 被某方法抑制为 WAIT，而另一个方法执行。随后一条“已到目的地”的报告，对后者可能为真，对前者可能为假。把完全相同的报告 payload 再喂给二者，会使方法获得不同语义的故障，而不是相同外生故障机制。冻结同一后续 MOVE 字节也可能令其中一方的起点 occurrence 失效。L642 的“when the interface permits”和 L643 的 domain mismatch 披露是好原则，但尚未选定每个比较实际采取的协议。

此外，让各方法“收到同一 truth channel”并不自动等于它们在其正式定义的域内采用了同一安全底座。官方只覆盖 delay 的方法如果直接被放进任意假状态报告域，其失效可以作为标明域外的负控，却不能据此认定候选在匹配的安全责任下提高吞吐。候选自己对消融的同一原则，也应落实到外部 comparator。

最小可执行修复：区分固定提案/知识快照的离线决策比较与各自闭环状态下的吞吐比较。后者共享任务来源和外生故障操作/随机来源的耦合规则，报告真实 payload 由各自状态生成，再按同一已定义故障机制变换；冻结无对应事件、被抑制动作、重新提案、查询回复和重启事件的键映射。明确公共扰动是按逻辑时间、发行 occurrence 或其他已定义地址应用，不能在观察效果后选择对齐规则。

为每个 comparator 增加输入/动作/真证据/互锁/计算收费的确定适配合同及适用域。若用共同安全包装，应明确称为适配版本，同时保留官方原版在其自身域内的比较；若只能作 claim-level comparison，则不将其作为已冻结的同义可执行 comparator。不得改变 PIE 是候选唯一 MOVE 来源的约束。

客观闭合条件：形成逐方法的比较域矩阵，并对上述一方 MOVE、一方 WAIT 的静态分叉例子，唯一产生下一步各自的 truth、故障变换、报告键、提案来源和收费项目；每个主比较要么保持匹配安全义务，要么明确限定为域外负控。没有真实数据也能完成这项定义。

**LG-08：预注册还缺确定的选择器、事件模式和裁决函数。**

定位：L609 表示未来会按确定 manifest rule 选文件；L611–620 列出“must bind”的字段；L655–662 要求符号停止合同；L704–711 给出证伪方向；L537 表示未来机械 witness 尚需冻结。

目前公共数据部分只提供 benchmark tree 的来源类别，既没有准确文件清单，也没有确定的文件选择规则。故障 trace 只列出需要 schema 与 generator version，没有给出事件字段、合法性约束、地址语义和被跳过事件的处理。停止部分只有“method-independent symbolic stopping contract”这一要求，没有一个能据状态与日志唯一决定继续/停止/删失的符号谓词。把这些列为未来必须填写，不能同时把当前门称为 `PROTOCOL-COMPLETE`。

L711 的“逆转预注册 locality 或 evidence-repair 假设的任何结果”还缺被比较的目标量、适用场景、聚合方向和裁决规则。受保护符号未赋值不是问题；问题是即便保持其未赋值，稿件仍可先定义确定的符号函数和比较关系，而目前没有这样做。

最小可执行修复：给出只读固定对象上的选择器规格，包括输入键、合法类别、排序、排除理由和去重规则；给出 feedback-fault 事件模式及它与 LG-07 的配对寻址关系；写出停止、删失、重试、失败归类和主假设比较的符号判定函数。清楚标记哪些内容已冻结，哪些将来仅在授权后实例化。可以把 C69-01–22 补为静态 witness 规格：初态、事件序、期望类、资源不变量、定理映射，不能在本轮运行或生成载荷。

客观闭合条件：在不给 `H`、`T_delay`、各保护预算、`P_active`、density `N` 任何值的情况下，新稿已经唯一决定“以后如何合法选、如何合法生成、如何合法停止、如何判为反证”；授权后只剩实例化和执行，没有观察效果后再作设计选择的空间。不能要求真实效果结果来填补这些协议字段。

**六项资格门：本路重新裁决，不继承 L744–751 的状态**

这里明确分开“稿内结构是否具备”与“外部事实是否被本路核验”。本路被限定只读候选且不得联网，因此源文献、仓库、license、blob 和样本的外部真实性均未核验；未核验不是判定它们为假，也不能标成已验证事实。

| 门 | 稿内独立结构裁决 | 外部事实状态 | 本路依据及闭合条件 |
|---|---|---|---|
| 1. 白话问题合同 | 通过，属于本路新裁决 | 不依赖前代意见 | L25–43、L78–90 对触发、输入、允许输出与非目标明确；算法闭合异议另列，不否定问题可理解性 |
| 2. 已发表锚点 | 引文与差异候选的结构具备；不认可继承 PASS | 未核验 | L58–72 有可定位 primary-source chart；是否真正支持差异、POD 全文排除和 novelty 仍 OPEN，不因表格存在而通过新颖性 |
| 3. PIE 官方 R0 | 实验前只读审计计划结构具备 | 未核验 commit、版本、license、代码 seam | L539–575 给出来源、冻结对象、构建入口、最小原法计划与介入位置；本路不要求现在 checkout/build/run，也不把未运行当失败 |
| 4. 已发表外部 baseline | 来源冻结与原法审计计划结构具备；公平适配须在门 6 闭合 | 未核验 tag、commit、license、sample | L577–605 提供可追查计划；L596–597 的缩写 blob 不是独立完整身份，不过完整 commit 若外部核实可定位其树，因此本路不据缩写直接否定该门；LG-07 的适配合同不能由此免除 |
| 5. 公共数据/场景 | OPEN：稿内选择器和故障模式仍缺 | 未核验 benchmark 实际内容 | L607–622 只有类别与待冻结字段；按 LG-08 补确定选择规则、schema、寻址与复现合同，不需要本轮生成 trace |
| 6. 纯仿真证据链 | OPEN：不是当前可认可的 PROTOCOL-COMPLETE | 本路没有且不要求效果或硬件结果 | L645–730 已具独立 ground-truth oracle、失败留分母、两计算轨道和负控原则；仍须关闭 LG-06–08，并使 oracle/证书对应 LG-01 的同一明确资源语义 |

不能把上表折算为“沿用两 PASS，等待四次签字”。第 1 门是本路重新给出的结构通过；第 2–4 门只是候选中审计结构具备，外部真伪仍未核验；第 5–6 门有明确稿内承重缺口。新颖性也保持 OPEN，与是否需要硬件实验无关。

**承重问题的处理顺序与最终控制状态**

先关闭 LG-01–05，使被认证对象、知识维护、最终向量、提交底座与模式转换形成一个可手工执行的总规格；再关闭 LG-06–08，使独立抽样、共同外生扰动、比较域、停止及证伪有确定合同。可接受的闭合材料是正式定义、符号伪代码、有限状态/守卫表和静态推导，不是补一段“已考虑”或要求实验来证明尚未定义的谓词。

候选 L774 自定承重异议需要新编号、新字节后继。本路只提出修复，不修改候选 69，也不自行创建后继、修改旧工件或更新 release。当前安全证明不能因负控失败而被不当否定，也不能因把最终安全性直接写成前提就被当成完整算法已经得到验证。

最终读取状态：`READ-COMPLETE / L1–787 / NO-GAP / IDENTITY-MATCHED`。

最终审查状态：`VALID NONEMPTY THIRD-ROUTE LOGIC RECEIPT / SCIENTIFIC NON-PASS / PRE-EXPERIMENT DESIGN OPEN`。

最终控制状态：`HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT`。
