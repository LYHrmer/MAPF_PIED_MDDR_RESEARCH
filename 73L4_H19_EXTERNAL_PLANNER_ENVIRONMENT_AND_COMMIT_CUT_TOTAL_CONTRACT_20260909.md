# 73L4：H19外部规划Environment与commit cut总合同

2026-09-09。执行代理 `/root/source73_external_comparator`。性质：**NO-SKILL / CONSTRUCTIVE / NONBALLOT / PAPER CONTRACT / NOT_IMPLEMENTED / NOT_RUN**。本报告接续根已读73L3的新有界任务，不修改73L3、73候选、账本或Git，不是正式审查或资格票。

## 1. 决策及其证据边界

选定纸面对象为 **`H19-PR-COMMON-SYSTEM / ECBS-FA`**：Hönig持久ADG/commit-cut框架按论文重构；任务由共同环境预先分配；本系统用自己的相邻MOVE/WAIT求解器，保留原方法的执行依赖和规划执行重叠职责。`FA`仅标识fixed-assignment。它是系统级外部比较，不是D/R选择器单因素消融，也不是作者2019整套原实现。

最初优先核对作者ECBS-TA组件，但本次亲读固定源码发现一个不能由Environment“只返回一个assignment”消除的失败域缺口。故本报告唯一最小替代是**同提交的纯ECBS组件**，不是另选新路线或静默修原ECBS-TA。保留外部框架的H19身份，求解器子名明确改变为ECBS-FA；不能在最终表中只写“Hönig原版ECBS-TA”。

固定组件仓库为 `whoenig/libMultiRobotPlanning`，commit `4c75fa20c435c440d8b6bd6dc81668ddc7296ba0`。该commit仍不是已证的2019实验版本。本报告提供Environment、有限数学搜索域、规划期望访问与真实服务的区别、唯一触发/desired-set规则、cut封存及拼接合同。费用与END采用共同接口，但73P4尚未在本代理中读取/组合，依赖保持显式。

**HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT。**本报告所说“总合同”指每种输入/状态有行为或错误类，不等于所有底层模板、Boost、数值表示和主协议组合均已证明通过。

## 2. 本次四个固定源码对象：全部实际亲读

全部通过GitHub只读 `fetch_file` 取得完整UTF-8内容、固定ref和blob SHA；随后完整阅读并对工具内存文本计数。没有下载源码副本或读取示例/实例。行号是该对象完整文本的实际行号。

| 对象 | Git blob SHA | bytes / lines | 阅读与使用 |
|---|---|---:|---|
| [ecbs_ta.hpp](https://github.com/whoenig/libMultiRobotPlanning/blob/4c75fa20c435c440d8b6bd6dc81668ddc7296ba0/include/libMultiRobotPlanning/ecbs_ta.hpp) | `43aef2a16f5cf86c2325efdd0c40cf44b9c51b05` | 17248 / 502 | 全文；分配回调、实际上下文签名、默认宏及失败路径 |
| [planresult.hpp](https://github.com/whoenig/libMultiRobotPlanning/blob/4c75fa20c435c440d8b6bd6dc81668ddc7296ba0/include/libMultiRobotPlanning/planresult.hpp) | `437392fd4367955813bc1c07bf7cdbc2998d183c` | 941 / 30 | 全文；states/actions及cost/fmin表示 |
| [ecbs.hpp](https://github.com/whoenig/libMultiRobotPlanning/blob/4c75fa20c435c440d8b6bd6dc81668ddc7296ba0/include/libMultiRobotPlanning/ecbs.hpp) | `d93fbe2bd60b472af61a77c0365a3140a8c9065e` | 14871 / 425 | 全文；固定分配的最小替代及高层循环 |
| [a_star_epsilon.hpp](https://github.com/whoenig/libMultiRobotPlanning/blob/4c75fa20c435c440d8b6bd6dc81668ddc7296ba0/include/libMultiRobotPlanning/a_star_epsilon.hpp) | `1b6b90a895ff14fbdb41c7eba33ecfe0b6b7a288` | 14047 / 389 | 全文；状态哈希、低层失败输出、启发式及数值接缝 |

四对象共47107 bytes／1346行；没有第五个源码对象/论文窗口下一跳。H19论文Algorithm 1/2原文阅读沿用本代理此前73L3已实际完整阅读的作者PDF，不冒称本次再次全文读取。[作者PDF](https://whoenig.github.io/publications/2019_RA-L_Hoenig.pdf)。

初始两个raw网页请求均Cache miss；随后两个curl实际句柄59319/98397最终exit28，升级后的42139/46240也最终exit28，均为SSL connection timeout。全部真实终态已轮询，没有把超时重名为通过。之后用GitHub只读接口恢复的是相同两个对象，并取得新增两个必要依赖对象；不是重启失落旧任务。没有执行源码或构建工具。

## 3. 为什么不能直接固定ECBS-TA原字节

ECBS-TA实际调用 `nextTaskAssignment`，其任务容器为agent索引到Task的map；实际低层上下文调用带agent、constraints和可空Task指针，文档注释列出的两参数形式并不完整。固定共同分配可以在**一个新job的Environment**内只提供一次完整固定映射，后续返回空映射；无任务机器人走明确的空Task分支，不能将其位置当作新任务。[ecbs_ta.hpp L104、L307、L372–405、L451](https://github.com/whoenig/libMultiRobotPlanning/blob/4c75fa20c435c440d8b6bd6dc81668ddc7296ba0/include/libMultiRobotPlanning/ecbs_ta.hpp)。

但这不解决实际源码的控制流：默认启用STYLE_MINROOT；当前高层节点被移出open后，所有约束子分支可能低层失败而不入open。接下来L302在没有新判空的情况下访问open.top；L344也有后续top访问。这不是仅凭库名称推断的缺口，而是已读到的具体分支。[固定源码 L238–245、L267–344](https://github.com/whoenig/libMultiRobotPlanning/blob/4c75fa20c435c440d8b6bd6dc81668ddc7296ba0/include/libMultiRobotPlanning/ecbs_ta.hpp)。

纸面可达例：一条边的两个端点均由机器人占据，各自必须访问对方端点；禁止碰撞和对向边交换。两条各自独立的路径存在，联合任务不可解。在本报告有限搜索域中，每条CBS约束分支最终可穷尽失败；存在处理open最后一个冲突节点且无成功孩子的时刻。此时上述top读取不受while入口的旧判空保护。这里没有生成该实例、运行程序或观察实际崩溃，也不以此宣称某一未绑定Boost版本的实测UB。

纯ECBS同样具两层搜索，但没有追加assignment根及此处的无条件top分支；其外层while在下一轮入口判空，穷尽可走false。故选择它作为已固定任务时的最小替代。[ecbs.hpp L144–287](https://github.com/whoenig/libMultiRobotPlanning/blob/4c75fa20c435c440d8b6bd6dc81668ddc7296ba0/include/libMultiRobotPlanning/ecbs.hpp)。这只消除了已识别的那一处路径，不能据此授予完整源定义域PASS。

本报告不提出改STYLE宏来隐藏问题，也不修原文件。如果以后坚持ECBS-TA，必须另立显式修订组件身份、描述修复和检查失败域；不能沿用本报告纯ECBS轨名。

## 4. 冻结job输入与有限数学Environment

### 4.1 输入、任务和规划目标

每job输入为不可变快照：有限图及其规范顶点序；合法相邻MOVE/WAIT集合；agent规范顺序；cut后的预测顶点；每agent在已交付TASK修订中当前首个未完成任务的身份和目标；该agent的原承诺及计划代际；共同形体/控制/资源接口版本；源随机/clock/数值合同；该次公共worker额度引用。任何缺字段、重复agent身份、非法顶点、不同agent的cut锚点同占一顶点、任务归属不符或输入身份冲突，返回具名输入错误，不猜默认。

任务只用本臂已付费获得的共同分配。任务为空表示本轮没有访问义务，不赋假目标；不同agent允许拥有**同一目标顶点**，不得因此删掉主source或把共同任务改成互异目标。为了容纳依次访问同一目标，source目标不要求所有机器人最终永久停在自己的任务点，而要求本轮已知的访问义务已被规划覆盖，最终驻留位置互不冲突。末端驻留位置由外部求解器自己的合法动作产生；它不是追加任务，不产生分数，不是本方法PIE外层偷偷改目标。

单机器人规划状态是离散序号、图顶点和本轮“已规划访问目标”标记。无访问义务时标记从开始为真；有义务时，在预测cut顶点或后继状态首次等于该任务目标时标真，此后不回退。该标记仅是路径约束，**不是实际任务已完成**，不触发新的真实派单，也不允许读取未交付任务。

这比将所有未知未来任务塞进单次search更有限，也比要求重复目标永久互异更忠实于主共同任务域。每次只规划当前已知首任务及其必要离开/驻留；真实任务继续到来时通过后续job接续。与原仓库的货架stage不同必须在适配表保留，不能称完整仓库任务原样。

### 4.2 有限时间域由状态图推导，不能作为新调参预算

设图有有限顶点集合V，job有m个具名机器人。数学联合状态由互异顶点配置及各agent访问标记组成；其有限状态数上界记J，取“V上m个具名agent的所有单射配置数”乘“各访问标记所有布尔组合数”。m大于可驻留顶点数时输入不可容纳；不做数值实例化。

单次规划的离散时间域为从初始层到J−1层。J是上述有限状态图的推导上界，不是H、T_delay或任何新经验时域/预算。依据是：若这个冻结访问问题存在合法有限联合路径，可删除重复联合状态之间的循环，得到不超过J−1个联合转移的路径。访问标记包含在状态中，因此删除循环不会删除唯一尚未记载的目标访问。这个论证针对固定图/任务、下述合法转移和终端驻留，不推出真实服务最终发生或有限计算内一定找到解。

源枚举不实际建立J个层的载荷；J只界定纸面有限性。若未来具体表示无法表达J，不能默默截短时间域并仍声称是同一问题：须采用明确的精确表示或报告表示域不可用。计算额度仍是主协议共同worker已有额度，届满返回预算停止；不另给外部一个实验预算值，也不按结果放大J。

### 4.3 转移、终端与冲突

每个合法相邻MOVE或原地WAIT增加一个离散层，单位规划代价；这是离散图问题的动作定义，不是实际执行时长、保护参数或实验预算。MOVE遵守图邻接和静态合法性，WAIT不移位；超过上述层域没有后继。访问标记按4.1更新。低层约束可禁止具名时间层的顶点，或具名时间转移的有向动作；不得改变任务分配和图结构。

低层终端条件为访问义务已覆盖，并且从此层起保持当前顶点直到整个有限域结束不违反已有低层约束。即使物理动作序列已经结束，冲突检查也将该agent延拓为该顶点的永久驻留；不能在短路径末端把机器人从图中删除。两个最终驻留点相同会被检查发现。最终层后各agent均驻留，因此在最终层不交即可覆盖这组离散末端延拓；连续形体安全另由共同协议检查。

联合冲突检查的确定优先顺序是：最早离散层/转移，随后冲突类别和规范agent/顶点/动作序。类别顺序固定为顶点重合、对向边交换、原ADG有向环。对前两类用对应顶点/边的标准互斥分支；每个孩子只约束一个参与agent。不能仅从vertex/swap无冲突推定ADG无环。

对ADG环，按原Algorithm 1构图后选规范最小有向简单环。在本单位动作时标中Type-1严格前进，Type-2不倒退，因此环只能由同层跨agent动作组成。为环中每个参与agent建立一个孩子，禁止它在该层采取该原动作。任何消除该具体环的有效解都必须改变至少一个这样的动作，故这些分支覆盖该冲突的合法解，而不是把整类循环地图静态删掉。若检测结果违背“同层、每agent至多一个环动作”等前提，返回 `ADG_CONSTRUCTION_INVALID`，不拼凑分支。

本规则没有预先禁止所有following，也没有把同方向队列统一变成全同步停走；只排除原ADG不可执行的实际环。它保持纯ECBS以Conflict/Constraints控制几何可行性的接缝，但新增“ADG环冲突”的Environment逻辑是本适配，需要独立证明和未来验证。对终端延拓发现的冲突，禁止该agent相应时层的驻留即可；低层终端判定必须同步考虑这些未来约束，防止重复返回同一非法早停。

### 4.4 启发式、次优模式、计数和返回对象

访问标记为假时，admissibleHeuristic取当前顶点到本轮任务目标的静态最短图距离；为真时取零。静态不可达的访问任务直接给源不可达类，不将无穷距离塞进有限数值。它忽略其它agent和时间约束，属于可采纳且一致的下界。距离表的构造、查询、缓存与失效均付费。

focal状态/转移启发式按上述顶点/对向边规则，对当前已存在的其它agent候选路径延拓计冲突；完整方案的focalHeuristic取相同冲突数，加原ADG环检查确定的违反数。无解或尚未填好的单机器人结果视为“没有候选路径”，不能越界读取；这很必要，因为源在部分低层失败后仍调用完整启发式。相同启发值沿源堆规则处理，未来必须固定堆版本与插入序；本报告不声称跨平台逐字确定性已成立。

规划膨胀因子沿ECBS组件的原义，作为外部求解器参数槽明确登记，当前不赋值、不改成处理预算。未绑定则该job为 `UNBOUND_SOLVER_FACTOR`，不隐式取源码默认、旧论文实验值或内部PIE参数。数学合同要求它满足ECBS定义域，具体数值与精度绑定是未来统一资格步骤；其未知不影响上述任务/动作/失败语义的唯一性，也不能当作已经可执行。

PlanResult只承担有序states/actions及cost/fmin；source返回成功后仍须完整校验首状态、每步转移、标记、长度对应、代价、任务身份、终端延拓和联合冲突。零动作且满足终端的结果是合法驻留计划；转换ADG时保留状态锚点，不伪造完成动作。source失败时的partial states/cost不算计划，尤其不能把fmin字段当失败原因或未完成任务分数。[planresult.hpp](https://github.com/whoenig/libMultiRobotPlanning/blob/4c75fa20c435c440d8b6bd6dc81668ddc7296ba0/include/libMultiRobotPlanning/planresult.hpp)、[a_star_epsilon.hpp L85–91、L193–212、L284](https://github.com/whoenig/libMultiRobotPlanning/blob/4c75fa20c435c440d8b6bd6dc81668ddc7296ba0/include/libMultiRobotPlanning/a_star_epsilon.hpp)。

source入口一律提供空的旧解容器，使用新的job局部Environment；不触发只检查首状态便复用旧solution的warm-start分支。旧承诺通过cut输入保留，不能把旧计划私下塞入source当免费合法热启动。未来若增加warm-start，须另立完整旧解校验与成本合同。

## 5. 有限搜索不等于源码/数值定义域已闭

纯ECBS实例化仍需匹配实际回调，包括上下文、状态/转移启发式、完整冲突和约束分支，以及两层展开回调。采用普通ECBS时没有Task指针/nextTaskAssignment入口；固定任务存于job不变Environment，而不是保留一套永不使用的分配枚举器。

源的高层id是int，Cost参与浮点膨胀比较，堆比较与低层closed-set也有明确实现前提。不能把J有限等同于机器整数无溢出、舍入无歧义或所有库合同已闭。Environment/worker必须在将来的数值合同中界定精确状态和代价表示、混合比较、哈希/等价、指令计费及内存失败；不能把不够表示的输入静默裁小。

本报告优先固定原纯ECBS的独立编译作用域：不与ecbs_ta头文件宏污染同一作用域，不启用未经说明的宏分支。此处只是未执行的版本合同，不是编译设置已落地。若必须修改核心比较或计数保护，则需另加适配源码身份，不能继续称该组件原字节。外围已付费的展开回调可在到达已知危险计数范围前中止隔离worker，但是否覆盖所有运算仍需明确证明。

因此明确区分：数学有限域内穷尽无解、共同额度届满、内存不足、数值表示/源契约不可用、source异常和合法方案被接纳。它们均不释放旧cut责任，不删除主roster，不将source异常算新方法性能获胜。四对象阅读不能代替Boost/邻居类型和具体数值类型的全依赖验证；本有界任务不扩到第五对象。

## 6. 保留原ADG规则，真实服务只作为执行屏障

对源计划的每条原MOVE/WAIT建具名ADG动作结点；同agent顺序用Type-1边。Type-2严格按作者Algorithm 1：比较一动作的源顶点与另一agent动作的目的顶点及计划时标，并按原内层顺序保留对应首次满足者。不得换成只看目的相同或只检查交换；结果须通过4.3的环检查。原规则定位沿用73L3已读作者PDF p.4／L422–478。

中心对Type-1接受前驱已入队或已finished，对Type-2要求前驱finished。finished由共同付费可信END接口给出，不由reference-visitation标记、预计时间、普通通知或任务完成消息替代。逻辑入队不授予物理MOVE权限；实际运动继续受共同已证明的原动作/前缀执行底座约束。空间guard只延迟或限制同原MOVE，不替外部规划器生成新边。

每次规划覆盖的真实任务访问，在执行首次抵达该任务目标时增加 **服务等待条件**：在共同环境实际完成该具名任务、且相应付费TASK知识已交付前，该agent不开始离开目标的下一原MOVE。WAIT/HOLD保持现有责任；TASK查询/复制/重试照实际计费。若cut锚点已经等于目标，则服务等待条件附于该锚点，无需制造一条END动作。

该条件不假定真实服务必然发生：共同服务区域/实体保持假设不成立、服务机会不可得或证据失联时，机器人可以一直HOLD并计未完成。它避免规划标记已真时立即离开、错失真正任务服务，并允许其它agent在其付费完成/离开后访问同一目标。它是明确的共同任务适配，不是原仓库stage已免费等价实现。

任务完成不会删除已经不可撤回的后续合法动作；已计划的离开/驻留不是新的真实任务。新的真实任务只进入下一轮已付费TASK快照。因新任务出现而触发重规划时，旧承诺仍可为离开服务点提供合法动作，不能为减少换任务等待而抹去旧授权。

73P4的付费END共享接口在此只列为依赖：必须明确自主END事件、后继收费CAPTURE、传输、中心完成位、原终点持续占有与本地消费关系。**本代理没有读取或验收73P4，不声称服务屏障、END和前缀权限已经组合。**

## 7. 唯一触发与desired-set：用已知历史耗时，不新设lookahead预算

以下规则作为本稿唯一选择，不保留“稍后选一种前瞻”的菜单。它是对原ComputeDesiredSet/触发接缝的明确适配，不冒称论文经验设定原样。

中心为每次已终结规划attempt保留本臂已付费取得的周转时长：从该attempt的中心输入捕获到其结果成功/拒绝/失败终态在中心发布。时长只由已存在的可信clock接口记录，所需读时钟/记录都收费；没有终态的在途job不产生历史样本。网络故障导致结果长期未返回时仍保持job在途或等共同失败程序终结，不能猜一个完成时间。

定义当前历史前瞻量为最近一次已终结attempt的该时长。它是状态变量，不是新实验预算；不乘新调优系数，不看本次未来耗时或别臂结果。第一次初始化没有历史时只从当前锚点启动初始规划；首个attempt终结后就有相应记录。若已有非初始计划却缺失该历史记录，返回 `PLANNER_HISTORY_UNVERIFIABLE`，不补默认。

为将计划余量与历史时间比较，使用固定公共模型已经声明、由动作及其名义控制条件确定的非负预计时长函数 `NominalDuration`。MOVE的估计来自该固定动作模型，WAIT按同一模型的WAIT语义；它不读取真实位置、扰动或未来任务。该函数的单位和来源必须进入外部输入合同；若当前公共协议尚未提供它，返回 `NOMINAL_DURATION_UNBOUND`，不能临时用论文步数、H或新数值填空。这是一个已明确暴露的接入依赖，不是隐藏的自由调参旋钮。

每次公共外部PLAN_CHECK机会只做一次收费判定：有job在途则不重启；没有已知任务义务且没有需要更新的承诺/后缀时保持IDLE；否则在“尚无初始方案”“出现尚未处理的新TASK知识修订”“至少一个agent从中心已知首个未finished动作起的名义剩余时长不大于当前历史前瞻量”中任一条件成立时，发起一个新attempt。其它情况保持当前方案。同一机会不因返回失败递归再发job；后续失败重试只在新的PLAN_CHECK机会发生。

已有图上的desired-set对每agent唯一确定：从中心已知首个未finished原动作顺序累计完整名义时长，选最早使累计达到历史前瞻量的动作；整段仍不足时选其最后动作。再将该agent所有不可撤回动作纳入种子，不可撤回前沿更远则以其为准。无未finished动作则采用该agent当前已认证驻留锚点。历史前瞻量为零时选该agent首个未finished动作，不选一个虚构“动作之前”位置。

完整名义时长可能高估当前在途动作的剩余量；它只是前瞻启发式，不用于清空、授权或安全证明。超长通信/排队和不同动作动态可以使该前瞻不够，允许到cut后等待，真实费用和停滞完整保留。这个明确规则不保证无规划等待，也不能由使用历史量宣称必然优于论文原设置。

## 8. cut封存、后缀拼接与失效：所有状态有归宿

### 8.1 封存

中心付费CAPTURE当前已提交ADG、已知finished、逻辑队列、已发送/已授权/已安装/在途动作、服务等待条件、TASK修订和计划代际。逻辑上已承诺或实际上可能仍执行的动作都算不可撤回；不能因ACK缺失把它移出集合。

对7节desired种子及全部不可撤回动作取原ADG前驱闭包，再按每agent动作序取最末成员为cut。无动作的agent保留具名驻留锚点。中心在同一原子发布中登记cut版本、受保护旧前缀及后缀替换边界；以后不得再将旧cut以外的原动作入队、授予或发出。如果捕获后版本已变，作业丢弃并在未来机会重新付费，不安装旧快照cut。

中心只封存自身写域，不持锁等待worker或本地回复。已发权限继续按共同底座运行；服务屏障也继续。cut不是让实体跳到预测终态的命令。旧候选后缀可在私有内存保留，但没有新发布资格，内存/GC仍计费。

### 8.2 初态、时标与拼接

新规划输入位置为各cut动作完成后的符号终态；无动作使用锚点。任务输入仍用已交付修订，可标记哪些旧义务将在已承诺访问中处理，但不预填未来真实完成或派单。所有新计划使用同一规划时标接缝，接缝严格位于旧cut各动作的起始时标之后且覆盖其离散完成层；不足时插入**计划层的驻留延拓**作冲突检查，不作为已执行WAIT或真实任务完成。

原文通过共同时间起点阻止新到旧依赖；本合同仍实际对新旧合图验证，不只比较两个时间数。所保留旧动作和状态锚点具有唯一跨代身份，新后缀第一状态必须恰接相应cut终态。检查全部Type-1/Type-2、循环、终端驻留、访问/服务绑定及共同几何接入。源自身不同步真实执行时钟，不能用计划层时标授权运动。

成功接纳只替换未承诺后缀。旧前缀字节/权限不变；每agent在自己的旧序列完成、服务条件满足且新跨图依赖解除后才能进入新后缀，不要求全队在同一物理时刻到cut。后缀插入不能先给它未获得的资源，也不能把旧resident因计划代际改变视为FREE。

### 8.3 任务修订、失败及回收

结果到达时，中心付费接纳检查按语义处理任务修订：本轮已规划的旧任务实际已完成是允许的信息前进，但不得再次计服务；新到任务只使后续重规划条件变为待处理，不能改变本轮冻结输入。任务归属、目标或身份被撤销/改变且破坏本轮义务对应时，拒绝未提交后缀。已不可撤回动作仍执行其合法责任；不回滚到旧初态。

输入错误、source false、预算停止、source异常、数值域不可用、合图环、空间接入失败、过期版本和任务不相容各有原因码。所有失败保留旧cut及全部费用；到cut无后继则HOLD。失效结果不为旧计划造新边；只有新的外部job可产生替代后缀。

完成位/服务回执可异步推进，但历史原动作在所有旧消息、cut证明及新依赖引用结束前不能回收。GC的对象/引用检查与保留内存归本臂费用。中心fail-stop停止新发布，已提交prefix、已签发命令和实体继续遵守共同责任；源worker失败不让机器人消失。

## 9. 接受前须核的总返回类与剩余点

| 层 | 结果/状态 | 保留义务 |
|---|---|---|
| 绑定 | UNBOUND_SOLVER_FACTOR、NOMINAL_DURATION_UNBOUND、clock/数值/服务接口未闭 | 不启动实际job；不能将未知当默认 |
| 输入 | 非法图/身份/任务归属/锚点等具名错误 | 主source记录保留，不修改任务或agent数 |
| 数学source | 路径存在并通过后验验证，或有限域穷尽无解 | 不跨域认领真实吞吐、连续安全或最终服务 |
| 资源/实现 | 公共worker额度停止、内存失败、source/数值契约不可用 | 费用保留、旧cutHOLD；不当作新方法获胜 |
| 接纳 | 新旧图/任务/空间/版本检查成功或具名拒绝 | 只替未承诺后缀，不撤回旧权限 |
| 执行 | ADG入队许可、等待finished、等待真实服务、等待共同授权、END/消费依赖 | 这些状态不得合并成“已完成任务” |
| 失联/失败 | 在途且未终结、明确失败、不可核验分别记录 | 不以超时判已清空；不重派未知终态job |

现已给出唯一任务访问域、可重复目标处理、有限数学时域、源冲突分支、ADG保留规则、触发/desired-set、cut边界和拼接/失败合同。仍需根独立分析的实质点是：访问目标后服务屏障与73P4/原协议消费语义是否组合；公共NominalDuration及clock接口是否已存在且单位一致；外部ECBS膨胀槽的合法绑定规则；精确数值/堆/计数/依赖与worker停止覆盖；新环冲突分支和terminal延拓是否维持源的搜索性质；主来源实际输入是否满足共同图/服务条件。本报告没有实例化这些值或载荷。

即使未来证明ECBS在此冻结离散问题上的某种代价界，也不能把它传播为带空间误差、服务屏障、观测与处理费用的lifelong吞吐界。主对比仍保留固定全roster，源无解、停滞及任务确认等待不能事后删掉；共同来源执行轨单独命名，不能顶替本系统外部比较。

## 10. 完整回执和操作边界

创建前对精确73L4路径执行不存在检查，exit0。唯一写操作为apply_patch新建本文；没有修改73、73L3、行政记录、Git或其它报告。没有使用任何skill、Claude、新增子代理，也没有正式审稿。

本任务读取的外部源码对象仅§2四个；没有递归全库、打开例题、map/scen/config/agent/task/delay或结果载荷，没有35–38、Q-CAL/Q-CONFIRM或桌面读取。所有shell及链段均rtk，真实网络失败与恢复在§2记录。没有实现算法、写代码/伪代码、构建、创建或运行测试、仿真、调参、产生实验载荷或结果。

H、T_delay、B_CAL、B_max、B*、P_active、density N继续保留原义且未赋值。J是冻结数学状态空间的推导界，不是新实验预算；source单位动作代价是算法问题定义，不是保护参数赋值。本文写后只做完整文本及hash/bytes/lines静态核验，交付hash不写入正文自引用。保持HOLD。
