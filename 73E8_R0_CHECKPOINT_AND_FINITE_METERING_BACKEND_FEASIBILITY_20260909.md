# 73E8：检查点与有限计量后端的有界可行性裁决

2026-09-09，NO-SKILL / NONBALLOT。本件只作纸面分析，未实现、构建、测试、运行源码或生成载荷。HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT 保持。

## 1. 结论

**固定单线程 Linux 进程的 fork/COW，可作为私有内存分支机制；“fork/COW＋普通墙钟抢占”不能满足当前 73 的完整恢复和有限 PROCESS_SLICE 合同。** 这是对这一具体组合的拒绝，不是宣称所有本机执行方案都不可能，也不是把后端 UNKNOWN 改名后算闭合。

73 要求每行固定有限容量、只有已付费步骤推进、暂停不做算法工作，并把 chrono 映射为该公共行的物理时刻。事后测到总耗时，不能证明每行已经满足这些条件。E7 核得的 Boost 1.74 头合同解决堆的限定问题，不提供暂停、进程恢复或指令计量能力。

## 2. fork 分支能关闭到哪里

考察唯一具体结构：初始化成功的单线程进程在受控边界保留为基准；由它 fork 一个事务子代，子代安装冻结输入并执行规划。拒收时终止子代、保留基准；接纳时将子代登记为下一基准，原基准退役。子代内部指针仍指向其对应虚拟地址，无需 C++ 裸指针对象图的序列化。Linux 文档确认 fork 使用 COW；私有地址空间内容及写入分离，可支持这个**条件内存结论**。[Linux fork 手册](https://man7.org/linux/man-pages/man2/fork.2.html)

它不是完整进程快照。手册还明确：描述符共享 open-file-description；CPU 用量计数重置；计时器不继承；特定映射可被排除或清零；多线程父进程只复制调用线程，互斥对象状态却保留。因而资格前提必须覆盖单线程、映射、运行时钩子和外部状态，而不能只检查 planner 的 heap 地址。[同一 fork 合同](https://man7.org/linux/man-pages/man2/fork.2.html)

子代提升还有 E6 没具体落实的所有权问题：子代仍是旧基准的孩子，控制器不会因业务 CAS 自动成为其父进程。依据 E6 已核 wait 合同，控制器不能直接等待任意孙代。保留祖先充当回收者会增加进程、内存与管理费用；采用重新收养则需具体平台机制及证明。本件不把未读取的收养接口写成已有保证。故上述结构关闭内存分支思路，**尚未关闭完整提升／回收实现**。

## 3. 已知 R0 行为对恢复的约束

本节继承 E5、E6 和 73 的具名源码证据，没有另取源码，也不把有限关键词检索当全动态库覆盖。

| 已知行为 | 对 fork 分支的具体要求 |
|---|---|
| LNS 的 high_resolution_clock、LaCAM 的 steady_clock；mcp 的 CPU clock 日志 | fork 不会把宿主现在时变成公共行时刻。必须保留原转换／比较，同时由适配接口供应公共时刻和本 job 的收费 CPU 域；不能直接沿用子代重置的宿主 CPU 计数 |
| rand、默认 shuffle、局部 MT 构造／重置 | 经绑定的进程内 PRNG 状态可随私有内存分支；局部 MT 的源重建仍照常。新进程不得自动重新播种；任何外部熵入口仍须拒绝或明确映射 |
| 原 packaged_task／thread／detach | E6 的同步入口不进入它，但这不证明链接库没有线程／atfork 钩子。未证明整个基准满足单线程边界前，不能在 fork 后任意继续 C++ 求解 |
| 原计时／日志、文件输出入口 | 不能把子代日志写入与基准共享偏移的描述符后，认为拒收已经回滚。输出必须先进入本事务私有、有费缓冲；源退出不能顺带提交未接纳业务效果 |
| 检查点外的费用、首次事务资格、公共时刻 | 拒收不回滚这些记录；还要收费于页表建立、COW 页物化、状态保留、消息及回收，不能仅统计 source 的 plan 窗口 |

当前只掌握限定源集合，未取得完整外部调用解析表。没有据此声称 R0 必然执行某个未读 I/O，也不能把“未搜到”写成无外部作用证明。过滤或替换日志、时钟和系统调用属于适配身份，不能仍称未经修改的原生运行域。

## 4. 普通抢占为何与有限容量不相容

先考虑没有逐步闸门的墙钟方案：监管器请求暂停之前，worker 可继续执行；请求到实际停住之间又可能执行工作。该组合没有提供“恰在本行最后一个获准步骤之前停止”的合同。若超出的步骤已经改变内存、消耗随机数或读钟，补记费用会突破本行固定容量，删掉费用则留下免费状态；回滚整个事务也不能把已发生的用量和不明切点当作未发生。这是所给机制无法保持 73 不变式的具体原因，不依赖运行观察。

perf 也不能直接补出结论。官方手册区分计数和采样，明确存在事件发生至内核停止记录之间的 skid；precise_ip 描述采样地址精度，sigtrap 提供同步溢出信号。**这些接口文字没有给出本项目所需的整个用户态／内核路径精确步数停止证明。** 不能忽略同步 SIGTRAP 的存在而宣称所有通知都异步，也不能反过来把精确采样地址等同于容量闸门。硬件支持与实际配置本件均未核。[Linux perf_event_open 手册](https://man7.org/linux/man-pages/man2/perf_event_open.2.html)

这不是否定 perf 的测量用途；其证据不够将普通墙钟执行认证为 73 的确定步骤语义。若改成实测 CPU 时间服务模型，须公开修改容量、时钟、跨行进度与费用估计目标，并重新审查，不能继续使用当前合同名称宣称等价。

## 5. QEMU icount 给出什么、没有给出什么

QEMU 官方说明，icount 在翻译块将越过指令预算时重新生成合适长度的块，并对 I/O 点专门处理；这比墙钟信号更接近所需执行边界。但是文档明确它只用于**全系统模拟**，与多线程 TCG 不兼容，而且不是周期精确模拟。因此不能直接选“qemu-user 跑原 ELF＋icount”作为现成答案。[QEMU TCG Instruction Counting](https://www.qemu.org/docs/master/devel/tcg-icount.html)

其 QEMU_CLOCK_VIRTUAL 从已执行指令推导；73 的源 chrono 则取外部公共行的物理时刻，同一行内读钟可相同、排队时世界继续。直接接上该虚拟钟会改变停止语义，仍需独立时钟适配。客体指令计数也不自动包括宿主翻译、设备处理、检查点、页复制或所有消息字节费用。

若沿这条方向继续，可接受的 ABI 必须是明确的 x86_64 客体 ISA、Linux 用户态 ABI、固定编译器／C++ 标准库／Boost 与浮点环境，再列客体内核、机器模型、TCG 版本和外部调用表。理论上可保留合格 ELF 的字节，但目前没有该 ELF 身份；虚拟化、时钟接线及系统镜像已经形成新执行 profile。改编译为另一 ISA 或字节码则连产物和调用顺序证据都须重建，不能沿用本机 E7 自动通过。

本次只读文档，没有取得具体 TCG 构建、完整机器状态保存／恢复及对外逐片执行接口的实现合同，**不将 QEMU 登记为已合格后端**。它也不是唯一可能方向。

## 6. 保留 73 所需的最低接口

| 必需接口 | 必须给出的可核性质 |
|---|---|
| 执行前容量闸门 | 每个可见状态步骤先取得容量；耗尽前停止，准确返回已执行量和恢复位置；复合访存／系统调用不能隐藏任意内部工作 |
| 不可变基准与事务分支 | 明列内存、寄存器、PRNG、映射和外部状态；拒收不污染基准，接纳提升与回收所有权可证明 |
| 外部调用与时钟入口 | 只接收已交付输入，chrono 与收费 CPU 域分离；未映射调用返回不可认证，不偷用宿主时间／熵 |
| 费用与生命周期出口 | 指令、复制、I/O、管理和尾部清理分别归账；费用不回滚，退出未确认不释放／复用资源 |

更小的可采用修订是把 E6 的泛化“完整执行域检查点”拆成上述已知私有内存分支合同和明确未闭的外部状态／提升／计量接口。**当前后端资格不成立，应阻止固定为可执行主来源；不需要再做无终点全仓检索来确认这一点。** 后继应围绕一个候选执行器取得闸门、时钟和分支接口正文，或由根明确改协议；不能通过实际跑快慢来替代这些前提。

## 7. 身份与本件边界

本次亲读 E6 相关入口／生命周期段、E7 全文，以及 73 §8–9 的冻结输入、源时钟和 PROCESS_SLICE 原文。原件实核 SHA256：

- E6：`6f5205a871e6d8b2d45897283fd270d8c044fd48abeeac3b52acceab4b4a1dc6`。
- E7：`9c0e7effe9320ae5e91199b4162bd8d5f2a35369d28e045d4577056db5059a09`。
- 73：`decef3a8908b21796c3ff86eaf2a8361289c76d13cbd09b1f45d66c1b138b38f`。

新增下一跳仅上述三个官方文档页面。fork 阅读 DESCRIPTION／返回／错误／版本／COW NOTES；perf 阅读计数与采样、skid、同步信号、overflow／disable 及平台错误段，未称全文精读；两页属 Linux man-pages 6.18。QEMU 本页全文已读，页面标识 11.1.50，URL 属滚动 master。三者是 2026-09-09 取得的网页正文，不是本机实现实测，也没有伪造原 HTML 字节 hash。

本件不改 E6/E7/73、不操作 Git、不调用 skill、不派生代理；未读受限数据、桌面、35–38或 Q-CAL/Q-CONFIRM，不给保护参数数值、范围、默认值或新含义。新件仅研究目录、仅 apply_patch；所有 shell／链段均 rtk。完成本有界裁决不代表研究完成。

