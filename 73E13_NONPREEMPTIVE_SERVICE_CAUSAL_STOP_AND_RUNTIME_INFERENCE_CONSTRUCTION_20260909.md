# 73E13：非抢占规划的真实时钟、因果停止与宿主依赖下的推断构造

2026-09-09，`/root/source73_r0_remaining_contract`。**NO-SKILL / CONSTRUCTIVE / NONBALLOT**。只给根一个可审查的工作方案及其严格边界；没有实现、运行或资格票。**HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT**。

## 1. 唯一工作建议与代价

建议选择 **NP-LIVE**：规划 worker 按 E12 的 ALWAYS_ADVANCE／FAIL_STOP_SOURCE 持续存活；一次一个完整非抢占作业，实际与世界的共同单调钟同时运行，禁止先把未来整次规划算完再重放其时长。共同钟选 Linux `CLOCK_BOOTTIME`；公共机会表给出释放时刻，实际采样、服务、发布和故障生效各有真实时间与因果序。公开故障释放、权限撤销、实际退出明确分开，全部延迟和尾部工作付费。

统计上同时改用**固定来源行的逐行随机分配与条件平均效应**，不用“源种子相同／臂顺序随机”推宿主环境跨 block 独立。下文给出有限样本鞅界；它允许宿主热状态、负荷和既往运行造成依赖，代价是目标量及原来每 block 全臂实跑的配对设计必须实质改写。

这两个改动不能假装为现 73 的即插即用实现。NP-LIVE 放弃精确 Srv_P 指令脉冲，也放弃“公共故障名义时刻等于操作系统瞬间终止”的理想化；它保留大规模 lifelong、计算期间物理继续、真实任务服务、完整规划成本和强对照。**若根坚持原定时即时终止和原独立配对目标，本文不能宣布这两项已闭。** 选择应基于所研究系统与可执行性，不基于哪个规格容易通过。

## 2. 亲见材料与仅两项新一手依据

本轮重新完整读取 [73E10](/home/lyh/MAPF_PIED_MDDR_RESEARCH/73E10_ROOT_NONPREEMPTIVE_PLANNER_SERVICE_AND_SUBREAPER_ALTERNATIVE_20260909.md)，SHA256 `1e2a6c7ff03ae879656e1032f8d2bdff8b472226edfc9e25d48e5fd86ab48fa9`，12336 bytes／80 行。继承本代理已全文完成、根已全文接收的 [73E12](/home/lyh/MAPF_PIED_MDDR_RESEARCH/73E12_ALWAYS_ADVANCE_SOURCE_HISTORY_FAIRNESS_AND_FAILURE_CONSTRUCTION_20260909.md)，SHA256 `e4e6285012c53b3676a62bdab51b54798a8db225f3e95694f5a6a86b964ecc88`，24294 bytes／137 行。

根另告知：已在后继 73 采用 AA，源继续失败域、E0 同能力与单槽屏障已更新；终态由仍占槽的原 job 消费预先准备的控制收件槽释放，避免把释放作业排在自己后面。**本件亲见该消息，不冒称已全文读过这些后继字节**；以下接续这个释放原则，不重新引入跨 writer 等待或自阻塞。

新增一手读取仅以下两个对象，web 实际成功返回正文，未使用源码／载荷下载，也未把页面摘要作为运行证据：

| 官方对象与实际阅读窗口 | 可据此采用的接口事实 | 不由此推出 |
|---|---|---|
| [Linux clock_gettime 手册](https://man7.org/linux/man-pages/man3/clock_gettime.3.html)，本次页面标识 man-pages 5.07／2020-04-11；时钟说明、返回／错误、vDSO 说明窗口 | BOOTTIME 是含 suspend 的单调时钟；进程 CPU 钟另外计该进程线程所用 CPU；读钟可能经 vDSO | 精确回调调度、所有 chrono 实际链接入口已覆盖、已测时钟精度 |
| [Linux timerfd_create／settime 手册](https://man7.org/linux/man-pages/man2/timerfd_create.2.html)，man-pages 6.18／2026-02-16；DESCRIPTION、返回／错误及版本尾注 | 支持 BOOTTIME；`TFD_TIMER_ABSTIME` 以该钟设绝对到期；可读到期通知及累计到期次数 | 到期即用户回调运行、到期即 worker 停止、有限时间内必回收 |

这是 Linux 项目接口文档证据，不是本机实现／部署绑定。未取两页原始 HTML 字节，故不编造 raw SHA。下文状态和统计构造是本代理独立分析，不冒称由这两篇手册给出了科研模型或统计定理。

## 3. 一个共同经过钟；禁止提前运行源未来

每个已给定合法世界登记共同 epoch，世界时间定义为当前 BOOTTIME 与 epoch 的差。源 `steady_clock`／`high_resolution_clock` 的适配读数都来自该同一经过域，保持原 duration period、截断、比较、局部起点和预算覆盖语义；这是显式适配，不宣称 native chrono 使用 BOOTTIME。CPU 日志使用当前持续 worker 的实际进程 CPU 域，完整成本账另包含失败、管理与其他站。

本件替代 E10 的 RAW 选择，理由是让经过钟与绝对计时通知同域并使宿主 suspend 不抹掉观察窗口。suspend 期间应用没有进展，既有数学物理责任继续按世界时间定义；若无法重建这个区间的真实输入／物理前缀，给不可认证状态，不能删掉该间隔。该时钟不等于不受任何频率校正的理想物理钟，所研究时间尺度就是此明确测量域。

读钟／定时注册失败、clock domain 或 epoch 无法匹配时，不回退到未登记的 REALTIME／CPU 钟；按发生阶段保留未发起、初始化不完整或不可验证的实况。实际 ABI 的 chrono、库与 vDSO 入口仍须完整绑定到该映射；目前只有文档依据，没有构建产物证据。

Srv_P 在输入已付费封存、原槽可用且实际启动时开始源事务。原 source 仅可读冻结输入、私有历史和已登记的时钟／随机接口；不得读当前真实任务、实体、未来故障内容或其他臂。正常运行持续占站，不按旧 PROCESS_SLICE 机械指令边界暂停；排队和 OS 停顿增加 elapsed，CPU 只按实际用量增长。

返回后先付费完成 PRE_COMMIT_SHAPE 等既定资格、原 commit 和不可变输出封存；源完成、输出封存、中心接收、中心完整验证／CAS 是不同事件。只有真实已经发生的封存可产生后继接收，不能把宿主“算出来的完成时长”预先排进未来虚拟队列，再让别的世界事件从过去追赶它。AA 节点保留与失败会话停止仍沿 E12；终态控制收件槽由原占槽 job 自己在后继有费控制阶段消费，不另排到其后。

## 4. 公共释放、真实执行与评价器落后

公共 O 的每一项在本方案中首先是预先登记的**释放机会**；到期只使固定描述符可领取，不能免费选择主体或读取真值。实际领取、CAPTURE、计算结束和发布时刻分别记录。名义释放相同不保证宿主实际执行时刻相同；这是同一调度制度下的闭环结果，不通过倒填名义时间使之表面相同。原来其他站的付费字段权限、FIFO、单 writer 和有限已授权应用步骤继续保留，但其实际完成不得被安放到宿主尚未完成的早先世界时刻；全稿“本行即作用”的条款需逐项改写。

纯仿真世界的连续轨迹在数学上由已有控制及合法物理输入前缀决定，不因数值评价器暂时落后而 HOLD。评价器必须维护一个**已认证到何时的前缀记录**，以及按真实到达时间追加的命令／物理输入历史。它可以随后核算既有历史的物理结果，不能随后为过去安装新控制、补造过去 CAPTURE，或让算法在结果尚未实际形成时读取它。

可信 CAPTURE 在实际付费开始时固定当时的合法不可变历史前缀及采样时标；其物理值若尚未算出，后续解析／积分／几何认证继续收费，完成的消息仍是该旧时标的样本。仅有一个“请求已到期”的 token 不能叫已捕获物理真值。历史前缀不足以唯一认证所问事实时，返回 UNKNOWN／PENDING，不从未来补真值，也不能把响应结束时刻改称原采样时刻。

新运动／离散模式接纳不能依据评价器最后算到的旧 pose 自行视作当前 pose。仅在既有协议保持性证明覆盖从证据时刻到实际接纳时刻，并满足当前命令／END fence 等条件时才可接纳；必要条件不能认证则不发新权限。若采用覆盖一段时间的保守保持证书，其建立、验证和有效域必须明确，不能默填一个允许落后的时长。应用不得先动作、再用事后积分证明应当通过。

真实任务服务的计分依据仍是固定机会和真实服务谓词；若该环境机会改为实际服务站领取时才执行，就必须以该实际时刻判断并登记 A，不能选择名义时刻中较有利的真值。这里**选定后一种有费实际服务语义**：每个公共 TASK_SERVICE token 最多实际执行一次，未在观察界内执行的 token 不补计，仍可用公共 token 总数给 Q 上界。环境队列／计算不能给中心免费任务信息。世界物理连续，任务服务器及可信采样可以因计算慢而延迟，这些延迟全部暴露。

该服务的线性化点须由实际有费调用及 A 的单 writer 转移确定，并认证那个时刻的真实服务谓词与队首身份；不能把某次计算开始时“曾经够资格”自动带到更晚提交，也不能仅因结果算完就补计服务。跨越资格变化／Stop 而无法确定的调用保持不可认证，不能按希望的结果择取一个时刻。这里没有给机器人增加 TASK 确认后才可离站的屏障。

评价器 CPU、积分／认证、环境服务、共同控制记录与应用 CPU 分账；算法特有几何／选择／日志不迁入评价器。某臂负荷使评价器延迟增大，必须计为这个实测共同平台上的总系统效果；不能称为同一无负荷理想物理环境中的纯算法差。各臂拥有相同站图、基础设施制度和报告字段，单价相同本身不证明真实能力相同。

**本节是尚待资格证明的实时物理服务接口，不是 Linux 定时器已经提供的仿真能力。** 当前没有可据本件宣布合格的实时连续认证引擎。最低新增证明集中在一个边界：已认证历史、实际 CAPTURE、实际动作接纳及环境任务转移的时序相容。若做不到，应停止采用本服务方案，不能新增一个“瞬间知道当前物理状态”的免费 oracle。

## 5. Stop／WORKER_FAIL 的三个时刻与完整尾费

公开故障按固定 O 的时刻释放；本方案明确称其为**故障注入请求的释放时刻**，不是已经发生的进程死亡。监管器实际处理该请求时，在既定有费生命周期控制提交中撤销该 generation 的新发布／新作业权限，并启动真实终止；该提交才是 WORKER_FAIL 的生效点。最终完整执行域退出是第三个事件。三者分别记为释放、生效、退出，不要求它们相等。

生效后不能接纳未提交的新规划结果，也不能让未退出源域借名为后继继续运行。完成与撤销竞争由同一生命周期身份及提交全序裁决；已经合法提交的物理责任不取消。中心不能只根据一份先前读取的 generation 在撤销后发布，付费发布必须与该代际关闭形成可核的原子次序；本件不给一个“读钟后再写就一定原子”的虚假实现证明。未知排序或时间区间跨越关键边界时登记不可认证。

信号通知迟到、OS 停顿和实际终止延迟全部记录。释放到生效之间源可能已正常完成甚至在生效前被合法接纳；这是**本新故障服务模型允许的行为**，不能报告为原名义时刻即时 crash 的复现。生效后直到确认退出可能仍有工作；这些是实际失败尾部，不截断、不退费、不保留为可继续 AA 会话。未确认退出继续占用相应执行域，不重复启动替代 worker。

固定评价 cutoff 是计分边界，与监管器何时真正停止源分开：只统计该边界之前实际合法的真实任务服务；之后的计算、延迟关停和回收全部单列，不能换来评分。截止通知迟到不让窗口延长，也不能使已发生的 post-cutoff 工作从费用中消失。管理器不得主动利用迟到启动后继以取得任何窗内效果；所有实际发生的越界启动仍留证，不能改写为必要清理。

初始化 READY 同样必须在既定初始化截止之前实际完成中心合格提交；通知迟到不放宽资格，私有 initialize 已返回也不能倒写 READY。迟到返回按初始化超时／停止处理并保留全部费用，禁止开始 plan。原 plan 内部预算及其覆盖／重置仍是源语义，不另偷换为这个监管截止。

若内生物理异常后来才被认证，应分别记录最早可证异常触点、发现时刻和停止生效／退出；Q 按既定最早可证 Stop 前缀结算。触点之后已经消耗的真实工作仍是尾部；不能声称评价器在尚未发现异常时就已杀死 worker。若连异常前缀都不能认证，评分 UNKNOWN／UNVERIFIABLE，不填零。

同一经过钟实时驱动后，不再存在“宿主先运行了虚拟 Stop 之后的完整源未来”这种离散预计算安排；存在的是可测的真实关停延迟，必须完整付费。**如果需求是名义释放时刻立即停在某条指令前，本方案不满足，普通 timerfd 也没有给这个保证。** 这一不相容不能用更精确的 elapsed 数字掩盖。

## 6. 两个最小反例与适用性的诚实结论

离散预计算反例：源从 t_start 开始，正常返回所需经过量 d；世界在 t_start 与 t_start+d 之间有要求终止的事件。若宿主已把整次源算完，而评价器后来才处理那个事件，则将账面用量裁成停止前长度既漏实际工作，也可能留下本应不可继续的 RNG／对象状态。冻结输入只排除未来世界读取，并不修复这个终止反事实。NP-LIVE 用真实并行经过钟和实际终止记录避免它，未使用 E9 指令模拟器或新的快照。

实时落后反例：物理 END 已发生，但评价器仅认证到 END 之前；若此时免费把旧中途状态当作“当前”给新的控制接纳，随后补算 END，就可能把已关闭 tid 复活。只加 BOOTTIME 和计时器不能排除该反例。本件要求证据保持／当前 fence 或拒绝接纳，说明为何实时引擎接口仍须审查。不能为了让该引擎容易满足，删去原大规模负荷、并发物理或空间误差域。

因此唯一建议是把 NP-LIVE 作为**已明确边界的下一纸面服务选择**，而非现有软件即将可运行的承诺。它比完整指令仿真和每 job 快照少了两类复杂执行器；代价集中为实时评价延迟和故障实际生效语义。若根不接受这些代价，最简单诚实后退是保留 HOLD 和描述性来源分析，不声称原严格服务已被普通墙钟实现。

## 7. 宿主相关噪声下的唯一随机分配合同

不能继续假设各 block 因使用不同种子就独立。为给一个具体可用的替代，预先固定来源／地图／profile／重复行及顺序、权重、世界输入和每行观察窗口；不按运行速度或结果跳行、追加或改权重。每个行 j 开始前、此前已发起执行域完全终结后，才由独立分配机制抽取本行的方法 A_j，选自预登记的比较臂集合。各臂仍完整采用相同基础服务与 AA 制度；所需 D、R、E0 及已登记次要臂的分配概率都事前登记并为正，概率不依据本行未见结果，本件不绑定数值。

每行只运行所分配的方法。行的静态 WORLD 输入固定，真实任务闭环仍由该方法形成。源／世界创新与分配抽签分域，不让方法访问未来分配。剩余宿主热状态、缓存、以往负荷和既往行结果可以影响当前环境；不要求这些因素独立，也不以臂顺序打乱证明独立。跨行未回收活 worker 则不能冒充仅有热状态：那违反上述顺序启动合同，保留失败／缺失而不越过去继续搜集有利行。

**这实质替换原 73 的“每 block 全臂实跑、缺任一臂不能估计”设计。** 每行未被分配的方法是设计内未观察反事实，不是已经运行却缺日志；它仍在该行的目标定义里。已分配方法缺日志／未发起是实际缺失，不能用其他臂替补。若根保留原全臂逐 block 观察要求，就不能使用下文公式并假称原配对已修好。

分配概率必须是实际可核的抽签概率；本文选择它们为预先固定的行属性，以便下面使用确定的界，不引入结果自适应概率或事后权重。固定概率不意味着相同臂样本数必然相等；禁止按最终样本数重归一化。具体概率、行数、世界载荷和抽签结果均未生成。

## 8. 条件目标量、估计量与有界鞅证明

令 F_{j-1} 表示本行随机分配之前实际既往历史的数学信息集，包含宿主相关环境的过去及预先固定设计；不要求算法能读完这个信息集。p_{j,a} 是预登记分配概率。Y_j 为本行已分配方法的真实 Q／原固定设计分母 E_j，满足公共任务机会所给的 0≤Y_j≤U_j；U_j、权重 w_j 与 p_{j,a} 均由设计事前确定，不由本行结果改变。

定义 μ_{j,a}=E[Y_j | F_{j-1}, A_j=a]。这是在该实际过去下，当前行分配到方法 a 时的条件期望，包含当前方法诱发的规划耗时／平台竞争及后继真实任务效果。因分配在当前潜在环境／世界结果之前进行且无预知，若要称因果效应，还须满足分配独立性、方法一致性及同一预定系统干预规则；否则以下只能是条件统计对比，不是自动获得因果解释。

新目标为 Θ_DR=Σ_j w_j(μ_{j,D}−μ_{j,R})，沿**实际发生的既往历史**取平均。它可随既往抽签和宿主历史而变化，不是原 73 对独立创新的固定 θ_DR，不是“如果从头一直部署 D”减“一直部署 R”的长期制度效应，也不是所有未来机器的平均效果。

定义可观察加权差 Z_j=1{A_j=D}Y_j/p_{j,D}−1{A_j=R}Y_j/p_{j,R}，估计量 Δ_DR=Σ_j w_j Z_j。其他被分配臂对本比较的 Z_j 为零；这来自设计的逆概率估计，不是把其未知反事实得分填零。条件期望 E[Z_j | F_{j-1}]=μ_{j,D}−μ_{j,R}，无需跨行独立。

在 F_{j-1} 下，w_j Z_j 的支撑位于 [−w_j U_j/p_{j,R}, w_j U_j/p_{j,D}]，跨度 c_j=w_j U_j(1/p_{j,R}+1/p_{j,D})。令 M_j=w_j[Z_j−E(Z_j|F_{j-1})]。条件中心化有界变量满足 E[exp(λM_j)|F_{j-1}]≤exp(λ²c_j²/8)；理由与 73 有界变量的倾斜方差证明相同，只将每一步改为条件期望。逐次条件化得 E exp(λΣM_j)≤exp(λ²Σc_j²/8)。应用指数界并优化 λ，得到双尾概率不超过 2 exp(−2r²/Σc_j²)。

因而按既定置信约定的 α，取 r=sqrt{log(2/α)Σc_j²/2}，区间为 [Δ_DR−r, Δ_DR+r] 与 [−Σw_jU_j, Σw_jU_j] 的交。这里全部是符号规格，没有赋予保护参数新值或给分配概率默认值。全 U_j 为零时使用对应退化界，不除零。这个覆盖结论针对随机的条件目标 Θ_DR；不是条件于某次完整已实现历史仍有相同覆盖，也不是原固定 θ 的区间。

D−E0 用其两项分配概率作同样构造。若声称购买途中证据和定向分配同时有利，沿用预登记的两个单边正证据合取及各自有效错误界；不能任选较显著者，也不能由相关臂得到“所有区间同时覆盖”的额外结论。区间可能很宽，不能看到宽度后补行、提高负荷或改保护参数。

此证明允许宿主环境对过去有任意依赖；它不需要随机化臂顺序后环境自动独立。但它不容许隐藏的当前分配泄漏改变本行事前 WORLD，或研究者依据未来成绩选择抽签／行序。若宿主对当前方法负荷作真实响应，该响应包含在这个总系统干预的 μ 中；若外部人员按臂名改变机器能力，则违反共同方法能力合同，不能用统计公式洗掉。

## 9. 固定 roster、失败和实际缺失

全部预登记行保留原来源权重和 E_j。合法初始化失败、源合法超时／形状守卫触发会话停止、模型内通信／故障均按真实可证物理前缀统计，不能删除这些行或改成成功者条件均值。源 UB／语义未闭、服务能力不同或分配机制失效使对应因果推断 INFERENCE_UNAVAILABLE，不能依靠停止会话把它们自动合法化。

已分配行未实际发起、日志缺失或真实评分不可认证时，保留状态并将整项预定比较记为 UNESTIMABLE；不重抽方法、不填零、不用后继行替换。未分配的反事实则按上述设计处理，不要求实际生成。未知尾部费用不一定使已经认证的 Q 消失，但费用结果仍不完整，不能给“净成本／性价比已证明”的结论。

在全部潜在动态映射有定义的条件下，完整数据的鞅覆盖事件成立；只在证据完整时发布区间，使“发布且错误覆盖”的概率不超过既定 α。不能进一步宣称条件于成功发布仍具有相同覆盖率。任一关键动态映射本来未定义，就不能套这个概率结论。

若必须维持原全臂配对 roster，现有最简可立即诚实保留的输出仍是完整描述性差值及费用，而不是本节随机分配区间。本件提出的是一个明确替代，是否改变确认设计须根在未运行阶段选定并重新送审；不同时把两种目标量登记为可择优发布的主结果。

## 10. 根可取舍的闭合项与未闭项

| 问题 | 本件给出的具体处置 | 当前资格边界 |
|---|---|---|
| 宿主先算完整未来而虚拟途中已应终止 | 不做源作业离散预计算；真实共同钟并行执行，实际故障生效及退出留证，完整尾费 | 实时执行／生命周期实现未建，不能称已跑通 |
| 定时到期与真正停止混为一谈 | BOOTTIME＋timerfd 绝对释放；明确释放、生效、退出三时刻 | 官方接口存在；无零延迟或精确指令中断保证；原即时 fault 域须明确替换 |
| 评价器落后变成免费旧真值 | 认证历史、实际 CAPTURE、禁止倒装控制；任务服务取实际执行时刻 | 连续认证引擎及当前 fence 接纳仍需具体证明，未由计时器闭合 |
| elapsed 宿主噪声不独立 | 固定行序随机分配、条件目标、逆概率差和有界鞅区间 | 数学构造已给；实际抽签／共同能力／动态定义域尚未绑定；原配对目标改变 |
| 原费用与分母被时间模型偷换 | 全 elapsed／CPU／应用／评价器／尾费分账，Q 仍用原固定 E | 新量纲／服务 profile 必须统一登记，当前不赋值 |

本件不建议仅在 73 增加一句“计时器按时杀死 worker”便送审。根若采用，须连同公共机会作为释放、真实 TASK_SERVICE 时刻、源时钟、故障实际生效、评价落后、费用以及统计目标一起改写；若不接受某一实质代价，则保留本件为未采用备选，不把条件证明算正式通过。

目标文件创建前精确核为不存在。新工件仅本文件，编辑只用 apply_patch，所有 shell 均有 rtk 前缀。未重新进入 R0 源码、E9 指令工具或子代提升调查；未读 35–38、受限 Q-CAL／Q-CONFIRM、地图／scen／配置／任务／delay 载荷或桌面；未调用 skill、Claude、新 agent，未实现／伪代码／构建／测试／仿真／调参／载荷／结果／Git 操作。保护参数保持原义且未赋值、范围或默认值。HOLD 保持，本文完成不代表研究完成。
