# 73A3：Claude Opus建设性分工与实际回执规则
2026-09-09，/root。响应用户“考虑如何充分利用Claude”及明确Opus要求。本件是工作分工，NO-SKILL / NONBALLOT，不是研究验收。保持HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT。

## 1. 用在可独立验真的建设环节
根负责研究问题、原始来源身份、跨节组合、反例裁决与最终固定。Claude Opus适合承担三个不同交付物：提出一个具体机制/替代规则；从明确假设推导一个有限命题或最小反例；在已核原文证据包上重构外部基线的一个接口。不给它只做“是否有创新”的泛评，也不把它当已发表证据来源。
每题只含一个主要未知与相关定义/原文窗口，给明确边界、准确输入身份、输出要求及终态标记；不默认喂整份十余万字节73和所有审稿意见。需要组合判断时根先准备单独接缝表，再交一题。其意见必须能回到具体公式、状态转移或来源原句，不能靠多代理一致和长篇报告宣布通过。
当前第一次新题为C7“无调参的定向查询公平化”：多组正分需求可使纯D长期忽略其他Active；要求构造保留定向机会的确定规则、明确选择与有效证书公平的不同条件、处理丢包/tid更换/公共机会及全费用。它不重启C4环几何、C5总路线或C6服务选择，也不替P7约定回执。P9是另一Codex代理对根P8有限几何及组合证明的独立查错，二者交付物不同。
外部已发表基线的来源逐对象身份和精确原文仍由根实际核验；Claude将来只在该证据包上补一个明确接口，不允许凭记忆补作者实现。L7当前失落/未启动完成状态未解决，不能把同题换名再派Claude。日常构造保持无skill；固定候选的科研导师skill、Nature reviewer skill、明确无任何skill/不读他路意见独立逻辑及根验收仍按既定顺序，每次skill后根另作无skill分析。

## 2. 调用规模与上下文权限
只请求Opus、high，不设置fallback到其他模型；最终是否实际Opus由完整回执的modelUsage等实际字段核验，命令里写opus不足以证明推理完成。先确认一项短任务能返回，再在实际能力允许时同时发至多两项互不依赖的建设题；这是调用管理上限，不是实验保护参数或研究分配比例。若通道连续零token失败，不批量扩大发起数来制造进展。
新调用工具禁用、skill禁用、MCP空配置、会话不持久化，科学输入来自研究目录内apply_patch创建的明确提示，不允许它读取35–38、Q-CAL/Q-CONFIRM、地图/场景/任务/配置/delay/结果载荷、桌面旧备份或改变工作树。CLI的safe-mode据本机实际help仅停用自定义hooks/skills/plugins/CLAUDE.md等，管理员政策、通常认证和权限仍保留；它不是跳过安全审批的模式。没有使用dangerously-skip-permissions。
未给Claude文件写权限，完整原文由根接收后仅apply_patch存研究目录。新科学建议不在返回前预填报告，不创建实验配置/O/tape/种子/结果或运行算法。H、T_delay、B_CAL、B_max、B*、P_active、density N原义未赋值、范围或默认。

## 3. 实际核查与终态
C6两次实际exec95654/37657均已exit124，零token、空modelUsage、无意见；其完整失败回执保持。旧C4等NOT_CONFIRMED不因此改为成功、失败已停或重复许可。本次claude进程名查询见另两个长期进程；随后正常权限升级仅readlink核223338/225441工作目录，均属另一项目，不操作它们，也不能由它们的存在推出本项目有回执。沙箱内/proc看不见某PID不构成宿主进程已消失的证据。
本机实际claude auth status只输出过滤后的loggedIn=true、authMethod=oauth_token、apiProvider=firstParty，未读取或展示令牌/完整配置；这只证明本地认证状态，不证明网络请求可用。宽进程列表的初次输出含无关项目命令且被截断，不能用来判断本题进程终态；后续只取命令名/必要PID/工作目录，不记录无关载荷。
C7首调用真实exec59001，实际exit124，179602ms、零token/空modelUsage，完整无建议回执已独立保存。首次终态后，完全相同提示/命令正常权限升级，实际exec17370；本件初建时完整结果仍待回。没有第三次同题调用，不把中途沉默、CLI有JSON或结束标记单独当科学完整完成。
第二次实际exec17370现已exit0：is_error=false、terminal_reason=completed、stop_reason=end_turn，完整正文及END-C7实际返回；modelUsage明确claude-opus-5 / firstParty。完整JSON及意见另存73C7完整回执。调用通道本次恢复可用，不能据此声称旧失落任务成功或各科学建议正确；根另作73C7R逐项纠正，不改原回执。

根发现C7自足提示对“背景D”的tie作了抽象简写，写为最久未查询；当前73实际为贡献请求最早加入P行再tid key。两者不是同一基准算法。C7只用于新公平规则的构造，任何结果要整合时必须恢复73实际tie或明确改名，不将提示简写静默写回主D。输入原提示既已发出则保留字节，不追改成仿佛最初正确。

## 4. 返回后的根处理
完整回执需同时取得真实终态、全部正文、实际模型字段/错误字段及相应文件身份；保留原文，不用根润色覆盖。根另作不用任何skill的逐项分析，明确采用、修正、否决及仍未知。若建议改变研究问题、比较臂、服务时钟或任务谓词，必须显式选择并统一全文；不能挑取容易通过的局部。
例如本次根E14已经对E13非抢占实时路线作了独立复算：支持其停止反例和有限鞅界，但因会改任务服务时刻及随机条件目标，当前未采用NP-LIVE。类似判断不会外包给“Opus说可以”。候选全固定之前，所有此类意见都是建设证据，不是导师/Nature/独立逻辑正式票。
只有完整检查过的新工件及行政更新进入既有私有GitHub精确白名单。73目前529a67132eabf915401df097c0a78be91e2c1e47ce9f9205518acdef5ec7420c /137460 bytes /480 LF，未固定未送审未接受，六门仍2 PASS /3 UNKNOWN /1 FAIL。分工完成或Claude恢复可用都不等于实验前研究完成。
