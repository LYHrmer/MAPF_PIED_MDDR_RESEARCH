# Opus 有界建设性任务：单中心授权协议

只根据本提示推理，不用任何 skill、工具、文件、网络或子代理；不实现、测试、仿真、生成载荷或结果，不给任何项目参数数值/范围，不投票。请在 1500 中文字以内给完整建议，以 ADVICE-COMPLETE 结尾。

研究为大规模 lifelong MAPF 仿真，PIE-D 是唯一新 MOVE 提议源。单可信中心按序付费处理，检查完全部资源后在一个完成点原子预留，再发不可伪造 grant；低层 gate 只消费匹配 epoch/父 resident/原边的 grant。每 agent 最多一个未完成授权，已发 grant 不按超时撤销，资源继续保留；任务完成由独立真实判据计数。实际实体 X=u+s e+z+F，其中参考 s 单调，z 保持在已证明的紧盒 Z；可信付费采样给 q<=s，中心仅退休与后缀 U(q) 不交的资源，终点 E 永远要保留。原边到端点后 gate 先不可逆关闭旧 epoch，并进入对应 endpoint HOLD；中心收到付费可信终点/closed/latch 证据后将 E 转 resident 并清剩余。只有 READY 后才能消费原逻辑槽、再请求下一 MOVE。普通反馈可错乱、通信可丢失/重放/迟到；可信中心/gate不分叉，发生fail-stop时不清账本、不重新分配，不承诺恢复活性，既有运动仍在保留包络内。

请做建设性协议检查：1. 找两个最可能遗漏的安全竞态，若现有条件已排除就明确说明；2. 给出中心请求 CANCEL 先于首次处理时的最小吸收性关闭规则，不允许取消已经发出的grant；3. 解释每agent串行epoch关闭水位能否替代逐tid在线tombstone，区分字数与位数、在线状态与完整审计历史，并指出必要前提；4. 简述同能力四臂（导向/轮询 × 后缀/全段）应共同计入的费用与一项能推翻净吞吐收益的情形。不要扩成分布式owner协议或新规划器。指出仍需根证明的条件，意见仅NONBALLOT。
