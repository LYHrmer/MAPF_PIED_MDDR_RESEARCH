# 73P19 根独立分析：固定服务行的实体包含与有限空间驱动

2026-09-10，根 /root；NO-SKILL / CONSTRUCTIVE / NONBALLOT。本件由根独立核一手定理并作模型推导，不依赖尚未收到的 P18 报告，不是其回执、重启或正式独立逻辑审查。起始 73 已实核 SHA256 74f051d48924c9ce1373851f7264c07d083e071179e937bcb64984d55402bc65，197955 bytes/629 LF；HEAD 与私有远端 main 均为4bdd803f5b30b60ceacaa7cad619e2ccd2da9159。HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT。

## 1. 根结论与边界

在明确的有限实代数空间驱动来源子域中，现有 TASK_SERVICE 行上的全实体包含具有一条有限数学判定路线，包含恰好接触服务区边界的零值情形。它保留真实非零空间误差，不需要把 z 或指数响应当成代数数，也不把服务时刻改成区域首次交点。

这个结论不能使原任意有界可测 w 自动可算，也不保证某条原有限服务行具有足够容量。根将其作为优先名义参考备选的新增证据，尚未替换73主模型、实例支持域或世界引擎。完整后端、输入来源、事故/Stop前缀和同能力比较仍需证据。

## 2. 实际一手阅读与准确命题

根本轮独立检索，直接读取 [Steinberg 与 Redheffer，Analytic proof of the Lindemann theorem，Pacific J. Math. 2 (1952):231–242](https://msp.org/pjm/1952/2-2/pjm-v2-n2-p11-s.pdf) 的印刷231页定理及导言窗口。工具显示整包15个PDF页，根没有完整审读后续证明或所有页面；数学截图实际失败，原工具明确 screenshot 不可用，不声称完成版面公式核验。所需定理的正文文字明确给出了系数/指数条件。

根另直接读取 [Bezivin 与 Robba，Annals of Mathematics 129(1) (1989):151–160，DOI 10.2307/1971488](https://annals.math.princeton.edu/1989/129-1/p05) 的作者摘要及题录；摘要明确陈述：不同代数点的指数函数值，在代数数域上线性独立。这是作者原摘要的实际阅读，不是第二篇全文。

据这两项一手表述，本件只调用如下命题：有限个两两不同的代数指数 beta_l，以及不全为零的代数系数 c_l，给 **sum_l c_l exp(beta_l)≠0**。必须先合并相同指数；仅仅“各输入系数非零”不能替代合并后的条件，也不能把“有理系数线性独立”错用到一般代数系数。下文是根对该命题和现有模型的应用，并非这些论文讨论过 MAPF 或付费服务。

## 3. 明示的来源子域与权限

原模型仍是固定世界朝向平移 X(t)=p(t)+z(t)+F、z_dot=−kappa z+w。保留原 kappa>0、各分量 |w_j|≤kappa zbar_j、初态 |z_j(0)|≤zbar_j，以及不因换边重置/旋转 z。为下述有限表示，只新增以下资格类型，不给任何数值、范围、默认、分布或保护参数赋值：

- kappa、z 初态、有限驱动段的实系数及有限段界有精确实代数表示；每段在自己原时间区间取常值。对当前服务行只使用已经发生的因果前缀，最末区间截到本次时刻，不读取未发生尾部。
- 本次 TASK_SERVICE 的真实行时刻有精确实代数表示；这是 P17 对名义备选 O 的表示域。数学段界不生成新的应用 O、QUERY、ACTUATOR 或服务容量。
- F 采用主稿§4已经允许的有限凸多边形或给定有限凸分解，顶点保留现有精确有理输入；下述证明也适用于已明确资格化的代数顶点。此推广不表示本件把现稿有理几何输入全部改成代数输入。
- 服务区域在本子域为非空有限闭凸多边形，给出精确代数半空间表示；其几何、任务绑定、合法性和初始范围仍需来源核验。这里没有将其改成“必须覆盖整个Z包络”，也没有把真实包含改成只测名义中心。

有限分段常值 w 是原任意有界可测域的明确子域，不能声称原域等价或读到任意驱动就可自动编码。有限闭凸服务区域也是具体输入类，不能由一般 ServiceRegion 名称自动推出。非凸服务区域若要纳入需另给实际包含算法，本件不以只验顶点误判非凸区域。主稿允许的非凸 F 若有有限凸分解则仍可使用以下证明。

本域没有强制 w=0 或 z=0；在原包络允许时可有非零扰动、非零偏差和实体回摆。若某个未来绑定实例的包络本身退化到无空间偏差，必须如实标识，不能用它代表非零误差证据。合法非零响应与全部实现/分布效果尚未运行，本件不给实验例子或参数。

完整驱动/真实 z 仍只属于世界与相应可信物理角色；中心、查询选择器、E0 或外部基线不会由这份数学编码获得新增读权限。POSITION 仍不是 pose/z 服务。字段保留、读取及因果日志的实际成本不能因给出解析式而消失。

## 4. 从空间闭环到有限指数和

取任一既有服务行时刻 tau。将已发生驱动前缀写成有限个覆盖[0,tau]的正长度区间[a_k,b_k]，最后右端按tau截断；w_j在该区间为w_jk。段界值处的单点选择对连续解无影响，驱动无冲量。由变分公式逐段积分得

**z_j(tau)=z_j(0) exp(−kappa tau)+sum_k (w_jk/kappa){exp[−kappa(tau−b_k)]−exp[−kappa(tau−a_k)]}。**

所有系数和指数都属于实代数数，项数有限；但整个 z_j(tau) 一般仍为超越数。本推导不把 z(tau) 逐段重新当成代数初值，因而不会在第二个驱动段偷偷失去表示闭包。换原边不会重置这一世界坐标表达式，F/Z 也不变。

服务的离散必要条件可先按原总序判定：真实A中有效未服务队首、合法原终点/初始驻留、参考顶点匹配、零速、未Stop及本行共同机会。必要条件不满足时不能因实体经过区域就服务。条件满足时，p(tau)就是合法任务参考顶点，属于该子域已核的精确几何；P15/P17名义模型可另外确定其参考模式/接点先后。原服务仍不要求实体真速度为零，不增加驻留时间。

令服务区域为所有半空间 h_l·x≤d_l 的交，F的各凸分块顶点为 f_m。闭凸区域包含一个凸块，当且仅当包含该块所有顶点；有限并的F则逐块检查。所以全实体条件等价于全部有限个

**g_lm(tau)=d_l−h_l·[p(tau)+f_m]−h_l·z(tau)≥0。**

每个 g_lm 都是代数系数乘指数代数值的有限和，常数项用指数0计入。这一步来自区域的凸性，不能外推为任意非凸 ServiceRegion 的顶点判据。未知形状/分解不是判 false 的理由，而是输入/数值资格未证。

## 5. 零值与非零符号分别关闭

先在真实实代数数语义下将相等指数归组，把各组系数精确相加，并删除系数恰为零的组。如果所有组消失，则 g_lm 恰为零；这是真实边界包含，原闭区域应接纳这项不等式，而非用容差或概率零忽略。

若有组留下，不同指数与非零合并系数满足上述一手定理，故 g_lm 不是零。于是可以用带认证余项的有理外包去求其符号：代数系数/指数有任意精细的有理隔离，实指数函数在每个已界定紧区间上连续且有可算的有理外包，有限四则/求和的外包宽度可随输入及计算精度一同趋零。因这个固定非零值与0有正距离，必在有限细化后出现全正或全负外包。

这里可用初等指数级数的认证尾项支持可计算性：在有限绝对输入界内，足够高阶后的相邻绝对项比有严格小于1的有理上界，尾项受相应几何级数控制并趋零。这是存在一个逐步可认证的有限数学过程，不是选实验精度或承诺现有浮点exp满足要求。固定区间包含0而尚未完成，不构成已证明零；零分支必须由前面的精确代数归组合并解决。

每个服务行只有有限个facet/vertex对，因此可以得到全部符号后按原全包含谓词作真假判断。若某对严格负即可证明不包含；全部非负才证明包含。不能把只算完一部分或当前间隔较宽的情形算作未服务，不能等以后进入区域再补一个本行真值。

这条路线不需给所有非零指数和一个统一实用分离下界。它给固定合法输入的有限终止性，不给对任意位长/段数相同的运行时间、内存界或剩余服务容量保证。精度/复杂度增长可能使实际费用非常高，必须在方法选择中保留这一代价。

## 6. 真值、计量与Stop仍分开

上述运算属于世界环境对本次真实服务的资格判断，不是中心查询的免费副产品。其驱动因果读取、代数解码/归组/合并、指数外包、全部位长/内存/工作及分配/去重费用按原世界/应用分账。各臂遵守同一输入语法和费用规则，不能把方法臂已经代数化、基线仍用低精度结果作比较。给出数学可计算性也不批准宿主先免费算完、事后随意标一笔费用。

原TASK_SERVICE规定本行判断并转移真实A。若实际后端不能在所需费用/供给下可靠完成该行真值，不能偷偷改成false、延迟服务或把真实A冻结到证据算完。必须沿原数值不可认证/Stop及完整前缀规则处理，是否还能给本次或后续Q取决于真实证据；不能让UNVERIFIABLE等同零任务。世界计算和计量闸门的具体后端，本件没有资格化。

固定行取值也不直接决定连续首次碰撞或模型破坏时刻。把tau换成未知触点后，指数中的自变量不再是给定代数数；根没有证明可以逐个代数采样排除其间所有越界。原包络正不变性与覆盖/唯一owner证明可在自己的完整前提内给连续安全，但那是另一条证明链，不能从本次点值符号判定倒推出已经审查通过。条件外的事故/信任破坏及Stop前缀仍需独立认证。

同样，本件不关闭原任意w源、非凸服务区域、有限故障可用性、实际物理/参考接口一致性或整套R0/外部系统资格；只是将名义备选中一个此前宽泛的世界真值问题缩为有确切数学依据的输入子域和收费执行义务。

## 7. 实际采用状态

根接受本件有限固定行包含判定为一项条件数学证据；不将其宣布为已实现的服务引擎或效果结果，不因此改六门。当前主稿eta/APPROACH没有被替换，名义参考、有限空间驱动和对应服务区域类型还须作为整体备选比较其科学范围与实际可资格性，不能只挑方便部分混入原域。

P18仍为原有界任务：最近实际agent列表为running，约定完整报告文件尚未存在；根已向该代理发送自己核到的一手链接及准确阅读范围，未重启其任务或工具。本件不替代或伪造P18回执，也不把两者一致当成独立正式审查。后继应先收它的真实终态/完整文本，再对差异作根分析。

本件无任何skill/Claude调用，没有下载或创建驱动/任务/O/配置/实验载荷，没有运行科学代码、测试或仿真，没有给保护参数值/范围/默认。目标继续active；73仍DRAFT / NOT-FIXED / NOT-REVIEWED / NOT-ACCEPTED，六门2 PASS /3 UNKNOWN /1 FAIL，HOLD不变。
