# Claude Opus 构造性协作任务；NONBALLOT

这是一项新的、与文献检索及源码审计原任务不重叠的纸面构造协作。不是正式审稿、不是候选方法实现、不是69的通过票。根代理尚在收齐独立逻辑报告；不得提前替根代理锁题。你只会收到本摘要，不能声称读过候选69全文、任何skill或其他完整报告。

请不使用任何工具、skill、文件、网络或子代理；只依据下面的完整任务摘要推导。禁止实现、写代码、构建、测试、仿真、生成轨迹/实验载荷或结果、调参。H、T_delay、B_CAL、B_max、B*、P_active、density N 必须保持原含义未赋值，不给数值、范围、默认、估计或调参方向；不要用同名变量作别义。

用户希望回应导师的实际误差问题，可联合处理计算/通信延迟，保留PIE-D基线；允许联合改进，尚未最终选定定位/运动执行偏差或完成反馈错误路线。MOVE提案只能来自PIE；外层只保留精确原MOVE，或转为有证明的WAIT/保持既有事务，不得另造绕行MOVE。单tick内减速在标准离散MAPF中是语义空操作，不能改名为新方法；多tick慢行必须显式建模边上状态、区间占用、速度风险。

当前69的思想是：提交时维护每个机器人未压缩动作occurrence的阶段知识集；普通位置/COMPLETE载荷可能错误，不能靠签名或消息新鲜性当作物理真相；可信清空事实或已声明的物理互锁才可缩小知识；审核同一PIE批次，种子抑制沿失去原点释放的依赖反向传播；资源有唯一owner、epoch/fence、PREPARE/COMMIT；按缺失证书请求证据。它未通过，不应成为唯一答案。

已确认的规格缺口（根摘要，不提供审查票）：

1. 消息保守更新不能证明无消息的物理推进安全。须有具体/抽象 Issue、Advance、Observe、Recover、Compact 及包含性推导。
2. lo/hi/holes/choice/txn_tags仅有名称，无有限语法、具体化、join、相容表；CERT“所有资源安全”不是算法。自身PREPARE豁免过宽或过窄均有歧义。修复查询选谁、何时停止也未确定。
3. 有限集合只增不减仅证明终止，不证明leastness。抑制一个MOVE一方面保留其原点，另一方面撤回其进入要求，冲突会消失；若还坚持leastness，需要固定单调强制关系及每次插入必需性。允许撤回不实的最小性/多项式性主张。
4. owner PREPARE后，恢复者观察尚无COMMIT即撤销，协调器随后COMMIT，是典型竞争。须明确tid持久互斥决议、承诺保留、迟到COMMIT、同epoch旧tid不能再执行、写意图后崩溃不盲目重放、COMMIT后未启动不等于可ABORT。超时绝不释放物理资源。
5. 固定inactive整次基线字节恒等，与运行时触发不能混为一体。假COMPLETE未触发保护时不能偷用真值自称安全。激活要建立K包含性、接管旧事务；退出不能丢义务。
6. 同批跟随/旋转中原子提交不保证物理原子启动。若无linked语义证明只能抑制/等待；“全局停”也要真实可hold。holding需静态可停、动态无不可撤销incoming、制动可达。
7. 比较要分官方语义保真组与共同安全底座的算法增量组，不能把更强传感/互锁优势算作新算法效果。闭环动作分化后不能强行复用逐字同一正确报告；应复用外生扰动而各自产生符合自身真实历史的观测，并定义缺席事件、额外查询和重试的故障键。
8. 处理耗时期间状态应按显式事件语义演化；冻结PIE-D源码的主循环则先规划返回再模拟窗口，不能把新环境悄称R0原语义。strict same-compute与natural-runtime需要分别计时记账。
9. 独立配对block不含method；方法运行与tick/机器人观测不是新增独立样本。Stop/Exposure/Classify/Retry/Roster需要函数主体，可参数化但不能留自由选择。空分母、删失、全阻塞都要确定口径。

已核验的一手证据边界（不要求你检索或臆造引用）：Hönig等RA-L 2019已有ADG、commit cut、执行/规划重叠，假设不显著空间偏航且可及时报告完成。Zhong等Autonomous Robots 2026已处理主动观测、调度成本、实际运动和测量不一致、观测耗时；其部分异步推导依赖无漂移、各自动力学解耦和可holding。故主动取证、反向可达、一般信念盾、fencing和事务本身均不得认领为新意。POD2026全文尚不可得，不能声称它没有某功能。

你的具体交付：

 A. 给两套可选修复构造：一套以显式有限powerset为参考语义（承认必要的指数成本）；一套可行的受限表示/认证规则（若多项式性不能推导就明确不保证）。分别写有限状态、输入、转移、具体化、证书谓词和失败出口；不写实现代码。
 B. 对资源事务给单一明确方案：决议记录状态及CAS互斥、owner何时必须保留claim、同epoch撤销后的旧tid排除、迟到消息、partial start、COMPLETE仍保留目的点占用。可以假设持久单writer与本地物理门，但不能把想证明的安全直接放入证书前提。
 C. 给哪些部分可组合到有界定位/运动偏差路线，哪些只能覆盖反馈错误，指出真正需另建的几何/动力学合同，至少给一个“消息完全一样但物理世界不同”的失败构造。
 D. 提出一个公平且可证伪的增量效果假设，并说明相同安全底座下若互锁已保证安全，算法还必须改变哪个输出才有研究价值；禁止保证吞吐提升或期刊录用。
 E. 自己攻击你的两套构造，各给最强反例或未闭合假设，列出根代理应拒绝采用的部分。

请输出可供独立推导的完整中文意见，控制在约四千至六千中文字符，可含精确数学式和紧凑表格。不要只列待办或重命名术语。最后单独写 ADVICE-COMPLETE。所有主张均NONBALLOT，最终裁决仍HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT。
