# 73E14：根对 NP-LIVE 与条件推断的独立取舍
2026-09-09，/root，NO-SKILL / CONSTRUCTIVE / NONBALLOT。根完整读73E13：23432 bytes /132 LF，SHA256 13a6cbd64ad3482badfc1884e684c672e6402a367039e6fbc91abd4c3005081d；前部返回后另补读95–132行保证尾部完整。输入主73为529a67132eabf915401df097c0a78be91e2c1e47ce9f9205518acdef5ec7420c，137460 bytes /480 LF。保持原服务工作定义，**本轮不采用NP-LIVE**；这不是断言该路线无价值，也不是认定原指令服务已经可执行。HOLD。

## 1. 接受的推导与不接受的资格跳跃
E13将名义故障释放、权限撤销的线性化点、实际执行域退出分开，完整保留尾部费用，这是正确的真实系统区分。提前在宿主完成整次源未来、后来仅按虚拟Stop截掉账面用量的反例成立；冻结输入只能阻止读取未来世界，不能消除已经消费的随机状态/实际工作。因此E10式整job计时后离散重放不能无条件实现原中途硬停止。
根重新实际打开两项一手文档：[clock_gettime](https://man7.org/linux/man-pages/man3/clock_gettime.3.html)的时钟/错误/vDSO及版本段，[timerfd_create](https://man7.org/linux/man-pages/man2/timerfd_create.2.html)的BOOTTIME、ABSTIME及读取到期计数段。前页实显5.07/2020-04-11，后页当前接口页；未获取raw HTML，故不补造raw hash。BOOTTIME包含suspend；timerfd提供到期通知。这些接口事实不提供用户态零延迟执行、worker精确指令前终止或连续物理状态认证。E13没有把文档误写为运行成功，根保留这个限定。
AA正常完整调用保留源状态与NP-LIVE并非捆绑选择。现73已经单独采用AA；继续域/未知源行为/真实旧运动责任的要求不因本轮拒绝NP而回退。反过来，仅保留AA也不能补出PROCESS_SLICE后端。

## 2. 为什么本轮不替换主服务模型
原73的O行先推进数学物理并按行身份作用；固定TASK_SERVICE按该时刻的真实服务谓词转移A；源chrono映射为执行所在公共物理事件时间。E13改成释放token、实际领取/执行另有真实时刻，源与评价器并行墙钟，TASK_SERVICE转移取实际有费调用时刻。这至少同时改变公共信息到达机会、服务有效性、初始化资格、故障生效和源内部预算触发。
因此从原73切到NP-LIVE不能只替换一段“测耗时”代码或一个CPU单位。某臂多做几何扫描，可能使评价器和真实任务服务延后；其Q差值包含同机资源竞争造成的环境服务改变。这是可研究的总系统效果，但不能沿称原固定物理机会下的查询制度效应。候选是否更容易工程化不是偷换研究问题的理由。
E13自己给出的最小新资格缺口仍承重：连续物理认证前缀、真实CAPTURE时标、迟到控制接纳、A任务转移及Stop前缀必须相容。不能拿旧pose当现在、不能补装过去控制；实际任务服务也不能只凭过去计算开始时合格而任意选更晚提交。普通定时器没有关闭这些合同。采用此路线将需要全稿级的新候选与新审查，而不是本轮局部修补。
本轮据此将E13保留为**完整取得、根已分析、未采用的备选**。原服务执行资格继续UNKNOWN/G6不通过；不把“尚无实现”本身当失败理由，失败点是纸面接口与现实执行机制之间的承重映射仍未给出。

## 3. 条件推断公式的直接复算
在预先固定有限行、非负固定w_j、固定正分配概率p_{j,a}及固定上界U_j下，令Z_j=1{A_j=D}Y_j/p_{j,D}−1{A_j=R}Y_j/p_{j,R}。分配合同成立且各动态映射有定义时，条件期望等于μ_{j,D}−μ_{j,R}。给定此前历史，w_j Z_j落在[−w_j U_j/p_{j,R},w_j U_j/p_{j,D}]，跨度c_j=w_j U_j(1/p_{j,R}+1/p_{j,D})。
中心化条件变量的指数矩至多exp(λ²c_j²/8)，逐次条件化乘积给Σ项相同上界；指数Markov并优化λ产生2exp(−2r²/Σc_j²)双尾界。E13给出的半径因此成立。非负权重是此区间写法的必要条件；若允许负权重应取绝对值改写，当前不另引入该设计。全部都是符号推导，不设置保护参数、行数、概率或抽签结果。
根支持该数学结论，但它覆盖的是沿实际既往历史的随机条件目标Σw_j(μ_{j,D}−μ_{j,R})，不是原独立配对设计的固定θ，也不是从头始终部署D与始终部署R的长期差。一次只实跑所分配臂也实质改变原全臂配对roster；不能将没有分配的反事实视为运行缺日志，不能反过来拿实际缺日志套逆概率补零。
只在数据完整时发布区间，能保留“发布且错误覆盖”的无条件错误界；不推出条件于发布后的相同覆盖率。未知源行为或未定义动态映射不能靠停止会话或数学范围填补。当前本轮不把这个统计替代写进73，也不增加事后择优选择目标的出口。

## 4. 后续选择应提交的最小证据
保留原模型时，下一有效服务证据应具体连接源指令/外部接口、付费容量、源chrono/CPU读数、世界事件全序及停止前缀；API存在性或测完时间不足以替代该连接。无需重新调查已经取消的逐job回滚。
若将来选择NP-LIVE，应独立提交明确的实时系统研究问题和完整服务/评价接口，同时改任务服务/故障/成本/统计合同，再比较这种问题对MAPF读者的价值；不能与原模型混合选用容易通过的条款。无论何种选择，D/R/E0与外部已发表SYSTEM须同服务能力制度、承担自身实际费用，不能使只有新方法获得理想采样或环境算力。
本件未调用skill、未实现/伪代码/构建/研究测试/仿真/调参，未读35–38或受限载荷，未生成O/种子/配置/结果。原保护参数原义未赋值；72未接受，73未固定未送审，六门2 PASS /3 UNKNOWN /1 FAIL与HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT保持。
