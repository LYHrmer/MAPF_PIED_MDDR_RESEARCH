你是本项目的方法共同设计者。请完成一个有界的数学建设任务，不做泛泛审稿，不扩展治理流程，也不要保证阳性实验效果。项目是带真实横向跟踪偏差、计算及通信排队、付费可信进度观测的 Lifelong MAPF。目标是让审稿人能检验可行性与净收益。你只有以下文字输入，不能声称检查了源码/文件或运行过实验。

本次唯一问题：构造一个同时满足现有服务/权限/时钟合同的非空有限符号输入子族，使一条固定源给出的真正非零 MOVE 经付费软件 INIT、任务/游标知识、源计算和中心接纳、原 PRECHECK/grant/控制接入，实际 START、有限 END，然后在原 TASK_SERVICE 行产生真实服务。补一个已定义的固定外部 ECBS-ADG 路径可以使用同一公共设施的条件论证。不要用“所有工作有限，预算足够”作为答案。给出原机会类型上的有限依赖图、有限计算界的来源、发布/队列/原行 guard 约束，以及这些约束确实可共同满足的理由。若当前合同缺少足够定义，指出最小必要设计修订并说明改变了什么；不要虚假给出完成证明。

边界：
- 不写实现或实现伪代码，不生成地图/场景/任务/延迟数据、实际 O、manifest、种子、tape、配置或实验结果。
- H、T_delay、B_CAL、B_max、B*、P_active、density N 原义 UNASSIGNED，不能赋数值、范围、默认值或重新命名。可以写明确标作非实验赋值的纯符号可行性条件。
- 不修改主比较：GROUP_PREFIX 内 NATURAL D-S−强稳定轮询 R-S；必报 D-S−E0；H19E0 与 H19R-S 两外部系统保留。存在一条有用轨迹不是正效应、不是全 roster 资格，也不是整个求解器完备性。
- 不把公开输入的实际合法性或统计权重按有利结果筛掉。符号子族不是挑选主实验来源的规则。
- 回答建设结论和必要限制，建议英文不超过1100词，末行 END_C29_CONSTRUCTION。

已定模型（这是本次给定合同，不是已运行事实）：
1) 参考边 u→v，长度 ell>0。真实实体 X(t)=u+s(t)e+z(t)+F，有限凸多边形足迹F，非零闭盒误差域Z；z_dot=-kappa z+w，kappa>0，|w_j|≤kappa*zbar_j，z初值在Z且不因换边清零。允许有非零横向误差的有限代数常值驱动。所有几何/服务区和原公共时刻有限表示。合法原边的完整 swept envelope 不碰障碍，初始持有 endpoint 资源；瓦片闭边界接触计冲突。
2) 同一原MOVE tid内 0≤q_release≤s≤C≤b≤ell，b是中心授予cap，C是本地安装cap。可以选用普通完整初始授权 b=ell（GROUP_PREFIX平台允许，非另换算法）。只有付费已验证的显式RUN在公共 ACTUATOR 行实际接纳才启动。中途cap HOLD不算END，不自动RUN。已有控制证明：合法RUN且C固定正、控制不被中断时，在有限T_ref内达到C；只有C=ell的原终点才END。BRAKE和END优先不被晚命令覆盖。参考零速不等于实体零速。
3) 原任务服务条件：原真实账本A中有效未服务队首j的顶点等于实际合法原终点驻留；整个 X(t)包含于闭ServiceRegion(j)，未Stop，原 TASK_SERVICE 机会存在；每agent每行最多一个。END但c尚未消费也可服务。原初始目标须不等于起点，不能用初始Q绕开真正MOVE。软件READY、中心FINISHED或证书不是真实Q前提。A在每TASK_SERVICE全行先服务后补给原子转移，新补任务不在同一行再服务。算法不免费见A。
4) 初始世界已有合法BOOTSTRAP HOLD与真实任务，软件INIT另收费后才建立可读resident索引、c/Kc初始证据。K_task通过TASK_REQ→付费TASK CAPTURE全账本快照→通道→中心验证获得，c通过合法CURSOR证据，不能免费复制整表。PROJECT只虚拟弹掉已承诺路径经过的合法已分配队首，不赠未分配任务。
5) 固定R0修复来源内部唯一planner，首次合法 PLAN_CHECK需 all Kc≥S、无作业且PROJECT非空；初始共同尾L=S=c=Kc=0只是索引定义。冻结输入以Π[L]为当前位置，不是真实途中pose。源输出必须全agent合法共同非空延长、首点锚点相同、各边四邻接/WAIT且vertex/swap无冲突。原planner_commit之前有付费PRE_COMMIT_SHAPE；中心再核最新PROJECT/版本/锚点并CAS接纳才新增MOVE。中心STALE不回滚正常源历史。
6) 供你使用但必须标作“仍待根独立源码核实”的正常源分支候选：有限连通栅格，可有障碍外边界；有限个互不相邻起点，每agent有唯一相邻第一目标，目标互异且不占任何起点。LaCAM PIBT的第一步到各自目标，第二次搜索循环因全员到目标正常结束；原 LNS 将两层路径切分并用其原有末端驻留补至原 commit_step 正整数长度。原源码已有多项共享身份/目标访问/唯一所有权修复；最新发现 Instance.h 左下邻接guard和读取目标不一致，根在核最小纸面修复。本题不得把这个候选当已资格化源码，需把有限源工作/原输出与下述服务组合的义务拆清。外部H19 ECBS-FA在此无冲突一边邻接家族低层首目标可正常返回，ADG Type-2应为空，仍有实际图构造/入队/APPEND和guard费用，不能把R0提议作为它的输出。

有限服务合同：
- P_model为有限字确定性基本步模型。固定字读写/比较/算术/分支/槽分配/合法输入导入均先付费；变长整数/几何/容器/认证/复制/日志/GC按展开的基本步计。不能让“一次几何求值”“整个源体”“publish所有日志”恒价。
- 只有公开非减时间有限O的已有类型可用：INIT、POLICY(role,i)、ACTUATOR(i)、QUERY、CURSOR、ENTRY(i,kind)、TASK_REQ、TASK_SERVICE、PLAN_CHECK、COORDINATE、GROUP_RETIRE、NETWORK(channel)、PROCESS_SLICE(server)、END_LOCAL_RETRY、READY_CENTER_RETRY、MODE、FAULT、CUTOFF、STOP。不能生成具体O或为一次私有完成动态加公共行。
- 各行先推进物理与自主事件，再按规范job key用有限发布容量发布前行完成单元，最后处理本行类型。新生作业/消息/结果只能严格后继行可见；不能把同一行剩余容量递归运行新工作。相同时间允许严格后继row，不添epsilon。每行能力有限；各站FIFO，一个队首job/当前单元，占槽直到后继发布；普通sender发表消息后释放自身槽，不跨writer等待目的返回。批控制器例外是一个占槽有限批次，每记录仍需后继处理/发表，不同单元不能偷余量。
- 角色：Srv_C中心单站；Srv_E(i)本地单站；Srv_P隔离唯一planner；Srv_A任务快照；Srv_N(u,v)各预定通道。不增站、不跨站借能力。ACTUATOR当前guard用原agent控制容量。物理/任务世界为理想外部设施，其宿主求值成本逐臂另报，不能给算法免费真值或其他业务副产品。
- CAPTURE先付费取得当前不透明合法根和time，实际字段解码/数值推导随后收费。原发布/ACTUATOR动态guard必须在该原行完整重捕获、核当前身份/mode/END和提交；此内部不推进虚拟时间、不插别writer；不足则已费保留、无效果、无水位消费，下个原合格行重新尝试。源普通计算可在基本步边界暂停/后继原PROCESS_SLICE继续。
- 源chrono now等于当前虚拟物理t，经原duration period floor编码，原float/duration_cast、比较符号和预算覆盖/增量保留。LaCAM Deadline判断elapsed_ms > limit；原首plan源码覆盖time_limit=1，再以(time_limit−0.1)*1000建LaCAM deadline。这些是源码事实，不是研究参数赋值。不能统一成CPU超时或把等待时间去掉；若循环必须等deadline而无状态退出，同t无限工作不合法。
- 所有可能delay访问用同一固定外生表：按agent输入序号和公共ACTUATOR机会秩（包括空机会）取位，只有完整合法START/RESUME尝试访问当前位，无未来bit API；取位工作收费，完整潜在支持不得越界。不存在第二次独立no-start重抽。符号证明可以明确要求该实际尝试位允许启动，但不得把有利位筛选成主roster。消息可有丢失/迟到，见证只能作为给定有限故障实现的条件命题。
- 宿主计量/世界求值不推进虚拟时间但完整另报；应用控制计算仍受上述有限供给且影响后继发布/机会。真正主效应是在完整固定E上按原权重配对的Q/E，既不能按查询成功筛服务，也不能因证明而缩E。

请直接交付：一个可审查的条件构造（包含保留非零空间误差的服务区域充分条件和计费依赖链）；一个证明联合可满足所缺或已有的具体论点；以及你认为最小的应入稿段落。不要输出只有必要条件的清单后称已完成见证。
