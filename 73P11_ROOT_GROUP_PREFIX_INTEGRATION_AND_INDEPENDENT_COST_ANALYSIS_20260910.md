# 73P11：根前缀全稿整合与独立成本分析

2026-09-10，根 /root；NO-SKILL / CONSTRUCTIVE / NONBALLOT。本文记录实际作者修订及根独立推导，不是正式固定、盲审或资格票。**HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT**，六门仍2 PASS /3 UNKNOWN /1 FAIL。

## 实际身份与采用范围

本次起点为本地及远端main均实核的b8b444c4a21f3cfc898213da3f3346650d5b04a4。修改前73为SHA256 2f772fb65e9a9198fe30cae33647f7503c6c4eaf03831ca5eb0605ef0abb6844，141812 bytes /492 LF。根已在当前持续任务中完整读原正文及所需构造，逐段实际替换，并核整个落盘文本与所组装预期全文逐字相同；不是只更名主策略。

修改后73为SHA256 **0beccea730aced26203406f8ffa69f5ffa93247f80cec64f00ec9de265065965**，155562 bytes /531 LF。相对本轮起点正文diff为113插入/74删除，默认静态git diff --check实际exit0。这是供P10建设检查的固定读取身份，**不是正式科学候选固定**；全文状态仍DRAFT / NOT-FIXED / NOT-REVIEWED / NOT-ACCEPTED。

主工作平台选择GROUP_PREFIX：同一个原PIE MOVE、原tid、原slot，只提高不可撤cap；不增加路径、伪目标或中点END。WHOLE_EDGE保留为机制与经济参照。查询仍纯D，来源仍ALWAYS_ADVANCE / FAIL_STOP_SOURCE，收费仍原PROCESS_SLICE，独立配对主估计量保持。C7R交替查询、L9两项改良及NP-SEALED均未采用。选择前缀是为处理整边首授循环的明确限制，不意味着当前路线唯一或已有净收益。

## 根独立复算与实际全稿改动

1. 控制对象必须是本地已装C，而不是中心b或原终点ell。现§3采用0≤q_release≤s≤C≤b≤ell及v²≤2a_hi(C−s)，cap增大只增加允许的制动余量，不自动生成RUN。已有BRAKE优先到零；同一物理点先处理旧cap到达再装新cap。有限到达仅指满足持续活动等条件后的当前C，不自动推到ell或固定观察窗。原空间偏差连续，不因cap或END清零。

2. §4保持有限有理凸多边形资源或有限凸分解，不将旧域悄悄缩成矩形。每资源进度相交集合先合并成有限互不重叠闭区间[a_rh,d_rh]。在q≤b时，已结束区间d_rh<q必已满足a_rh≤b，因此
   n_r(q,b)=#{a_rh≤b}−#{d_rh<q}
   是非负的当前相交区间数，正值当且仅当资源属于Mask(U(q,b))。闭端点退出用严格小于，不能在q=d时提前释放。计数允许b向右启用而q向右退出，旧仅末端lambda规则只可用于全后缀。每事件一次仅限成功提交的单调路径；私有失败、取消和重复扫描另收费，不许据摊还式抹掉失败成本。

3. §5普通初始准入仅尝试整条原边。部分初始cap必须来自共同付费GROUP_ADMIT：中心先选合法未授请求的规范循环SCC，核初始resident、外国owner/活动/其它围栏排除，再从固定有限共享阈值族选出A_i两两不交且B_i两两不交的c_i及认证清除阈值。必须有既定精度来源下界合同；缺失则NO_CERTIFIED_GROUP，不临时选零误差或细调精度。所有失败候选、SCC、几何和提交均用原容量计费。

4. 围栏是新增准入排他能力，不是第二物理owner。每条新增占有入口均检查围栏；组内旧MOVE可以在他人尚未清除前做安全扩cap，新加部分属于该成员B_i。单中心先核完整事务及容量再一次提交，不能半组成功。终点交接可以合法完成，但组内下一原MOVE持续受阻，直到单独付费GROUP_RETIRE已关闭全组。退栏不由本地END免费触发，不靠超时，不删除仍合法的endpoint resident；历史引用GC另有条件。

5. CUM_CAP及控制尝试使用不同单调身份。中心发布cap不等于本地安装，安装不回传新的途中事实。RUN/STOP除control_epoch外绑定公共尝试行；已认证的no-start、无grant、C=s或模式不合等尝试消耗水位，重复包不得待未来cap变大后变成一次新启动。未认证包不能抬水位。MODE直接首命令与CONTROL_RETRY均有各自原公共行键，并按同一安装规则核验；这部分组合仍由P10独立检查，不据局部规则自动认证全协议。

6. §7把未授整边需求和已授未满CAP_EXTEND纳入明确P账本。组提交原子替换需求，保留首次加入行；只有满cap、中心END或绑定失效才移除。FOREIGN_FENCE阻塞的需求不作为D的owner清除收益。D按不同需求计分，tie仍是最早贡献需求加入行再tid；不把改协议趁机改成另一选择器。SOURCE_REQUEST原公共行按modulo 3分配来源/扩展、cap重发和控制重试，没有凭真实进度新增O或免费的采样/启动ACK。

7. SHARED-OFF及DRAIN后保留已经承诺的cap/控制/终点链；DRAIN停止新组、新occurrence和新规划。停止新POSITION可能使未完成组永久受阻，故不保证DRAINED，且DRAINED明确包括无未退围栏。失败分类要保留这一后果，不能把持久占有当成已安全关闭。

8. §11主2×2为D-S、R-S、D-F_cap、R-F_cap；F_cap的q_release固定为零而cap能力相同，q_evidence不取得释放权限。各臂共享能力、角色和供给，分别支付实际工作；不要求不同行为轨迹交相同费用。整边参照可省略无用的组计算和角色工作，须明示机会合同，不能故意加空开销抬高前缀表现。§13旧整边反例限定适用域，新增W59–67仅纸面合同，汇总W01–67全部未实现/未运行。

9. §15在当前正文给出有限候选、A/B分离及闭组条件进展。证明需要在清除时刻之后实际采样、付费交付并发布足够q，或完成全终点链；提前采样晚到达不足够。E0的首个满cap矛盾只针对该条件几何见证，R同样可能取得证据并进展，所以它不能证明D优于R。尚未证明该见证由固定源实际输出；粗资源、精度不足、长期围栏及额外费用的负面情况都保留。

## 仍必须关闭的接缝

源持续调用域、Boost实际合同及源失败处理仍有未知；严格指令收费执行外层与时钟/I/O合同未闭。证书精度来源没有由纸面epsilon替代。外部H19必须保留自身规划/ADG机制，其名义时长、PLAN_CHECK和新旧cut衔接仍缺全合同；内部前缀采用不自动赋予外部同名能力，是否只使用整边及对应公共角色映射必须说清。素材合法来源链及近邻全文排除仍未完整。因而不得冻结送审或运行实验。

P10已交给既有可运行Copernicus检查上述读取身份的组合，另限定Opus C9核退栏/控制/MODE接口；截至本文形成没有完整P10/C9回执，不计完成。P7/L7仍实际pending_init且约定文件缺，不重启、不重复派发；根本次独立整合不能冒充找回其报告。

正式步骤仍须在最终全文固定后依次科研导师skill、根不用skill独立分析、Nature reviewer skill、根不用skill独立分析、明确无任何skill且不读他路意见的独立逻辑审查、根验收。本轮未应用科学skill，不将过去其它用途技能阅读抹去。所有编辑apply_patch、shell每段rtk；保护参数原义未赋值，冻结与禁读边界保持，无实现、伪代码、构建、研究测试、仿真、调参或实验载荷/结果。
