# C24 — bounded constructive cost-model analysis, NO-SKILL / NONBALLOT

Use no tools, skills, plugins, MCP, browsing, code, implementation pseudocode, experiments, configurations or results. Answer in approximately 700 English words, ending exactly END-C24. This is a new mathematical service-cost question, not a retry of C22. No protected H, T_delay, B_CAL, B_max, B*, P_active or density N receives a value, range, default or changed meaning. B-symbol units are not established in the supplied safe documents; never identify them with the new cost unit. Keep HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT.

Input identity: current draft 73 SHA256 9e06199cc6045ddd750b861b3091ec239c7c6ff2ae7eeaebbdd0e7fde61ec113; E25 SHA256 c522b82f35d3ffffaec4c94e7c8c737ddba3f7258d471817518024f1a61fd36e. Those are evidence identifiers, not claims you have read full files. The parent supplies the complete relevant obligations below.

Current contract: finite common O rows ordered by (physical t,row_id), advance physics first, publish prior completed units second, execute the row type last. Each writer has one FIFO computation-or-pending-publication slot. Srv_C central; Srv_E per agent; Srv_A task snapshot; Srv_N per declared channel; Srv_P unique persistent planner. Private ordinary computations can pause across PROCESS_SLICE rows, and publish only in a strictly later row. Ordinary/GROUP_ADMIT publishes all-or-nothing; ADG is explicitly one prepared record at a time. A CAPTURE atomically fixes a consistent immutable authority root and sample row before further work; insufficient capacity for that atomic capture means no authority read. Variable-length copying, comparing, indexing, arithmetic, geometry, signature and logging all cost work. Current publication requires complete final validation and atomic effect or no effect, with attempts charged. ACTUATOR final guards must hold at its actual acceptance time, not a stale physical snapshot. World and authority may evolve between rows, but not during one row's mathematical evaluation. Host time used to evaluate world/true task service is reported separately and creates no online knowledge or application capacity. Protected budget units cannot silently change.

Source candidate stays P29: one nonpreemptive complete frozen-input AA body, real elapsed d, logical occupancy until a+d and original successor-row publication; no pre-run successor; center disposition and source release still needed. Revoke output/new bodies at original stop, charge full executed tail; do not claim exact instruction kill. Same lifecycle for internal repaired R0 and external ECBS, preserving each algorithm's own actual work. Old source instruction-supply prepayment is removed. No default price or added numeric capacity.

Proposed single common cost model to scrutinize: a finite-word capability machine. Word width/address domain are fixed architecture/representation choices, not experimental tuning; no concrete width is assigned here. Opaque root/row references are bounded words, whereas semantic job IDs, exact numbers and containers use as many words as needed and are never one-operation values. Ordinary work is a fixed finite set of bounded-word reads/writes/Boolean/branch/address moves, each with declared fixed positive abstract charge. Scans, bignum arithmetic, allocation initialization, comparisons and costs of constructing a charge description are ordinary charged steps. The metering state alone consumes supply; its bookkeeping is model administration, recorded in real host cost and forbidden from reading business data. This avoids recursively measuring the meter or computing a hidden-value-dependent charge for every step.

CAPTURE has a fixed bounded-word schema: current immutable authority-root reference, current immutable row reference and a preallocated private pin slot. Its whole opcode charge is known without reading authority. Retention, creating immutable versions, allocation and later traversal/GC are charged to their actual responsible station; a root swap is not a free copy of its contents. Single-writer publication prepares a complete immutable next writer-root including outboxes, then uses a bounded-word final root/authority-fence check and pointer swap after paying their full fixed charge. Never treat a variable semantic validity predicate as pointer identity.

The difficult case is a variable-cost guard on the current physical root at ACTUATOR/publication. Proposed rule: pay the fixed CAPTURE first, compute the guard privately using only this row's remaining abstract supply, and commit only if the full guard and commit complete in this same row. Exhaustion has no public effect/attempt-watermark consumption; retain actual costs, discard stale dynamic guard evidence and recapture on another original eligible row. Nothing recursively creates an opportunity or supplies control knowledge to the strategy. Thus it weakens only any interpretation requiring the whole unknown-cost guard to be affordable before even CAPTURE. Identify whether this is compatible with the stated existing capture rule and current-time acceptance, or give a minimal counterexample that requires a further explicit semantic change. Do not assume a hidden bound, unbounded scan primitive or free prepass.

Your main deliverable is a constructive, concrete mathematical contract you think usable for just three seams: authority CAPTURE, variable word length, and avoiding free or recursively charged fee computation. Start from this proposal, refine it once if necessary, and state directly adoptable clauses. Then use one strongest genuine counterexample to test and repair it, stating any necessary semantic change explicitly. The current-time dynamic guard is the proposed stress test, not permission for a broad manuscript review. Distinguish a defined pending/failed attempt from a model with no legal next state. Do not merely list blockers. The parent will integrate all stations and source/NATURAL/STRICT dimensional accounting separately; do not spend your short answer on a full-system verdict. Do not certify a usable backend, independence, effect or a gate PASS.
