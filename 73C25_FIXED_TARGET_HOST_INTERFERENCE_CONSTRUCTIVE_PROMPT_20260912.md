# C25-20260912: fixed-target paired inference under host interference

This is a bounded constructive consultation for the successor root, not a formal peer-review ballot. Work entirely from the self-contained statement below. Do not use tools, browse, read or write files, run commands, produce code or implementation pseudocode, generate any experiment inputs or outputs, assign parameter values or ranges, or contact anyone. The root will independently verify mathematical claims and primary sources. Give one complete answer of at most 1,200 English words, ending with `END-C25-20260912`. Do not claim that a mathematical assumption has been physically verified. A rigorous impossibility result with an explicit minimal research amendment is preferable to a fictitious executable design.

## Fixed scientific contract

The research studies large-scale lifelong multi-agent path finding in a fixed, legitimate simulated world, including spatial tracking error, paid planning/observation/control, real task service, failures, initialization, and all costs. It has a fixed roster of paired blocks indexed by b. Every required method is evaluated inside every original block, including D-S, R-S, E0 and two declared external systems. No single-arm randomized experiment can replace this pairing. The primary comparison is D-S versus R-S; D-S versus E0 must also be reported. The main denominator E_b is the original fixed virtual evaluation window. It is not actual host elapsed time, initialized exposure, or successful execution time.

Let X be the complete lawful design fixed before observations. Nonnegative fixed weights w_b sum to one and retain the original hierarchy of domain, distinct map identity, source identity, declared profile, and repeat index. For method a, Q_{a,b} counts actual physical-world task completions within that block's original virtual window and stopping rule. The complete data have d_b = (Q_{D,b} - Q_{R,b}) / E_b, with |d_b| <= U_b = Qmax_b / E_b, where Qmax_b is the original bound from finite allowed task-service opportunities. Define Delta = sum_b w_b d_b, Lambda = sum_b w_b U_b, and theta = E_P[Delta | X] = sum_b w_b E_P[d_b | X]. P is the predeclared full execution law, including host environment and execution order if these influence virtual outcomes. The required scientific target is this fixed theta, not a population of future maps and not a random target conditional on realized execution history.

All blocks, weights, windows, method identities, failure rules, and scoring definitions must remain. Unknown required scores make the entire associated comparison UNESTIMABLE; undefined method mappings or unsupported probability assumptions make inference INFERENCE_UNAVAILABLE. No dropping failed runs, replacing missing scores by zero, conditioning on successful initialization, selecting successful pairs, resampling until a desired result, or using a vacuous full-domain interval to declare the design complete is permitted. A valid confidence procedure need not produce a positive effect. Existing alpha is fixed by the design; use symbolic alpha in your reasoning without proposing numerical experiment settings.

The present draft assumes independent block innovation roots conditional on X, arbitrary dependence among arms within each block, and Hoeffding radius sqrt(log(2/alpha)/2 * sum_b (2 w_b U_b)^2). The defect is whether actual host elapsed time can satisfy the independence premise, not normality or identical distributions.

## Execution-model issue

One candidate source service executes a complete nonpreemptive planner body using only its frozen lawful input, the arm's continuing private source state, declared random state, and declared clocks. Each normal invocation advances persistent state even when its output is later rejected; resetting that source history within an arm changes the method. Its measured source clock may be virtual start a plus actual monotonic host elapsed. The output, new state, and measured cost are sealed until the original strictly later public row permits publication. Sealing prevents early use of an answer but cannot eliminate host interference. Logical service occupation, full body cost, later cleanup and failures must remain. Host execution is serial on the user's physical computer; no verified mechanism currently restores temperature, caches, operating-system load, allocator state, timing services, or other relevant hidden state between blocks. Pure task-world evaluation and logging can also affect the later host state even when their CPU is separately reported.

Randomizing D/R order therefore does not by itself establish independent blocks. Previous arms or blocks can change the host environment and then affect later elapsed, planner timeout decisions, returned paths, virtual publication times, and Q. It is forbidden to label all such state independent exogenous noise, to put the whole observed host history into X after the experiment, or to replace theta with sum_b w_b E[d_b | past]. Restarting processes, cooling, using another core, running on another day, or failing to detect autocorrelation is not independently a proof of independence.

## Already established in the predecessor analysis P30

Please do not spend the answer restating these results as new progress. If the original probability law P admits a predeclared partition C_g of the existing complete roster into independent cluster sums V_g = sum_{b in C_g} w_b d_b, with A_g = sum_{b in C_g} w_b U_b, then the unchanged Delta targets the same theta and has radius sqrt(2 log(2/alpha) * sum_g A_g^2). Labels such as machine or day do not establish the premise. A single cluster may give no directional information.

Alternatively, a Doob martingale of the complete final statistic, M_j = E[Delta | F_j], with F_0 = sigma(X) and Delta measurable at F_J, has M_0 = theta and M_J = Delta. A proven deterministic upper bound V_* on the sum of squared predictable increment spans gives radius sqrt(log(2/alpha) V_* / 2). This retains theta; subtracting ordinary sequential conditional means instead does not. A host variable affecting all later blocks can have span 2 Lambda. All host randomness must be included in the final sigma field. Independence of only the registered random seeds is insufficient. Neither a small influence bound nor an independent host-cluster structure is currently evidenced.

Changing execution law P to a block-reset law P' preserves the algebraic formula but not automatically its estimand. Equality requires E_{P'}[Delta | X] = E_P[Delta | X]; equality of each block's marginal law is a stronger sufficient condition. A plausible reset procedure is not such a transport proof. We have no calibrated runtime distribution, no admissible elapsed-time tape, and no empirical mixing certificate. No such object may be generated for this consultation.

## Precise constructive task

Identify what can actually be gained beyond P30. First evaluate whether an original-law-preserving statistical intervention exists under the stated resource and evidence constraints, with an operationally meaningful independent unit or a verifiable dependency certificate. Specify exactly which physical or probabilistic premise is missing. Do not claim that an assumption is implementable simply because it can be written as a function of independent seeds.

Second, examine an entire-campaign replication amendment: each new campaign contains every original block, with the unchanged original within-campaign execution order law, state propagation, w_b and E_b; complete campaigns are independently sampled from the same original P. Would averaging complete campaign scores target precisely the original theta? Distinguish preserving scientific target from changing the observation plan or expanding the repetition roster. Explain what must be true about the campaign starting-state law and cross-campaign interference; if unavailable on this host, say so. Derive the symbolic concentration statement only if justified. Do not choose campaign counts or silently adopt this amendment.

Third, strengthen the arbitrary-dependence obstruction if possible. Consider a permitted abstract score-law family in which one unobserved Bernoulli host state makes the whole d vector either (+U_b)_b or (-U_b)_b. Its probability p is unknown, so one complete roster reveals one Bernoulli draw and theta = (2p-1)Lambda. Can a deterministic closed interval procedure with uniform coverage at least 1-alpha over this family exclude any theta having probability of the observed corner greater than alpha? Give a precise consequence for attainable interval length or positive-effect inference at the two corners. State clearly that bounded-score compatibility is not proof that a legal MAPF source realizes those extremes. Explain the scope for randomized procedures or extra observed metadata without overstating the theorem.

End with one concrete recommendation: either a genuinely supported unchanged-target construction, or the narrowest explicit amendment plus unresolved evidence requirements. Keep separate mathematical validity, operational qualification, preservation of P, and preservation of theta. Do not expand into literature review, alternate scientific objectives, software implementation, or a generic catalog of concentration inequalities.
