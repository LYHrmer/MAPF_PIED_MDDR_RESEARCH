# 73C4：异步环形原MOVE的前缀可执行性，独立几何子题

NONBALLOT建设性数学咨询；不是正式审稿，不是以前任何失落任务/拒绝提示的重派。请不用任何skill、文件、网页或工具，只从以下抽象几何问题推导；不写代码或可执行伪代码，不给实验数据/规模/预算/窗口参数赋值。中文约两千字，末尾写ADVICE-COMPLETE；若不能证明，请给明确缺口或反例，不将直觉当定理。

四条既定有向线段沿边长为ell的正方形顺时针连接，分别由四个实体从各自起点执行，不能另选边或中点消费原MOVE。每个实体相对参考位置的整个可能占用包络为世界坐标轴对齐闭盒[-rho,rho]^2，其中rho>0且4rho<ell。各自参考进度s单调，从0到ell，误差可在上述盒内随时间任意回摆。参考控制能安全停在当前cap C，合法增加C后可在无后续中断的条件下有限到达它，但no-start和制动可能使各实体不同步；不要假设共同进度、同时START或绑定延迟上界。全平面以边长h的固定轴对齐闭正方形资源瓦片覆盖，边界算相交。资源由一个中心持久独占，各实体只可在已兑现区间[q,b]包络的资源内运动，其中已验证并发布的下界q≤s，gate已装cap C≤b，且s≤C。中心先占新增资源再发cap，迟到cap不撤回。只有付费POSITION证据能提升q；资源退休必须证明今后区间不再相交。所有开销为正、排队继续物理演化；可作条件性存在见证，但需明说证据、传输、扩cap和执行机会最终完成的条件。

任务限定：
1. 构造有限严格正的符号几何余量条件（h、rho、ell及有限阶段进度之间的关系，不填具体数值）和一个逐阶段可验证的顺序，证明在每阶段每实体进度可独立落在该阶段区间、有人停住而别人达到cap时，四个“全阶段区间包络”的资源仍可同时独占。因此只有阶段屏障依赖已付费下界，不能依赖隐藏s或免费全局同步。旧cap必须允许后方证据在可能不精确时最终足以进入下一阶段；说明需要何种可达到的证据精度条件，不能只说证书始终包含真值。
2. 区分：同步轨迹几何可行；这个条件性有限异步调度存在；特定贪心cap/查询策略一定会找到它。这三者哪些真正证明，若第三者不能推出要明确拒绝。
3. 若一个同能力E0策略不买任何途中POSITION，只允许原终点确认，因此q保持起点值直到原END，是否可能即使允许任意cap扩展也在这种资源独占环中无法完成？给严格证明或反例，勿将不购买证据说成没有同样的控制/授权能力。中间cap已安装回执不依赖到达且不得带进度；它本身是否可提高q？
4. 列出至少三个条件不能省略的失败见证，例如粗瓦片、证书下界永久无信息、永久no-start、晚到旧证书、过贪的cap已不可回收。不要宣称由局部环形见证推出大规模吞吐/普遍无死锁/新颖性。

这只决定前缀路线是否有值得整合的非同步机制证据；不生成实例、仿真或实验载荷。数学中的四是给定正方形几何子题的对象数，不是实验density N或任何保护参数赋值。
