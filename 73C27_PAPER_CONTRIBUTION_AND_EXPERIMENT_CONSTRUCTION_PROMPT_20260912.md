# C27: construct the contribution and experiment argument

Act as a research co-designer and scientific author, not a referee giving a score. Use only the supplied facts. No tools, skills, browsing, commands, files, code, implementation pseudocode, experiment generation, numerical parameter settings, fabricated citations or results. In at most 1,300 English words, design a concrete argument and falsifiable experiment structure, ending END-C27-20260912. We need useful decisions and scope control, not generic advice to read more literature.

The user's aim is a convincing publishable MAPF study, primarily large-scale lifelong simulation. Two LIMO robots may later provide optional mechanism evidence. The user permits combining or changing methods. No main experiments, implementation or tuning are currently authorized before design qualification. Physical tracking error must be real transverse spatial deviation, not random travel delay renamed.

The working question: robots with bounded transverse tracking error reserve conservative spatial resources. During one original planner MOVE, paid trusted progress evidence can prove some resources are behind the robot and cannot be reoccupied within that MOVE. Can selectively buying this evidence yield a net increase in true lifelong task service after computation, communication and all auxiliary costs? The discrete planner is a fixed official PIE-D/LaCAM entry with transparently documented shared repairs and adapters. The wrapper preserves each original MOVE, task, slot and endpoint; no detour, fake goal or replacement planner may be hidden inside it.

The body is X=u+s e+z+F, with bounded transverse z, compact footprint F and monotone reference progress s; true body position need not be monotone. Shared fixed error envelope Z gives reserved swept sets U(q,b), from verified retired progress q to granted cap b. Only a paid committed proof q<=current s can retire resources. A per-agent controller cannot exceed an installed monotone cap; reference END, centre FINISHED, READY and logical task/cursor consumption have separate meanings. A single centre owns reservations. A GROUP_PREFIX extension can grant compatible short caps to a group whose full original edges would block each other's starts; a future-resource fence is held until all group members finish and paid retirement completes. It can itself create delay and permanent stalls. No unconditional liveness or global throughput improvement is claimed.

D's current query policy counts distinct admissible pending MOVE/extension requests blocked by an active transaction's potentially releasable, nonterminal resources; a maximum positive score wins, with fixed age/identity ties and stable-agent round-robin fallback. It computes this from paid shared knowledge, not true progress. R is a strong stable-agent round robin with precisely the same paid information, safety margin, cap and early-release capabilities. D has no proven starvation bound when positive scores persist. E0 buys no in-motion POSITION but has the same useful cap/group/control capability and paid terminal notification; it need not build unused query indexes. Main comparison is D-S vs R-S; D-S vs E0 must be reported. A 2x2 D/R by spatial release/cap-held-until-END is a mechanism ablation; F_cap can omit unused retirement work. It is not a weak fixed-margin baseline since all arms have the same Z.

Geometry G uses interval-entry/exit events for exactly the same reserved sets as a reasonable direct geometry N baseline. Successful cumulative interval processing has a local amortized bound, but failed speculative work, global scans, sorting, bit complexity, ownership, messages and memory remain additional. G and N may change closed-loop runtime and hence Q even when their geometric function is identical. First-use/reuse/preprocessing and invalidation costs must be reported. D scoring is currently an explicit reference scan; an incremental exact reverse index would need full maintenance accounting and must not be claimed as already implemented.

The external comparator is named H19-PR-COMMON-SYSTEM/ECBS-FA, adapted from the published Hoenig 2019 persistent execution/ADG idea and fixed author ECBS code. It keeps its own planner, action-dependency graph and cut/recovery, with whole-MOVE caps rather than GROUP. H19E0 and H19R-S are both mandatory external systems. System-level differences cannot all be attributed to query selection. Neither comparator is represented as the author's entire original 2019 experimental program. Original native PIE-D is a separate fidelity/domain track; collisions from applying native outside its original physical-error domain are not evidence that D is superior.

Relevant known literature mechanisms: PIE-D already interleaves planning/execution with random delays; Hoenig has persistent execution/ADG and lifelong switching; Zhong/Rossi/Shell have planned synchronization and active observations with costs; B-UAVC handles position uncertainty; Shahar treats time uncertainty; S2M2 relates dynamics/disturbances to error bounds; WinkTPG has dependency-based scheduling and speed optimization; SCALE has swept occupancy and released prefixes; MAPF-X combines discrete planning with continuous control. Kim TII 2026 on lifelong asynchronous actions and two directly related AI/RAS papers have verified metadata/abstracts but their full mechanism texts are not available here. Therefore no strong novelty exclusion against those works may be claimed. You cannot infer missing features from titles, abstracts or absence in this prompt.

Current compute bottleneck: a strict finite-instruction service model lacks a qualified backend; making planner delay equal actual host elapsed creates cross-block host interference. A proposed explicit successor law may use deterministic simulated computation for every online operation, with real CPU/wall/memory separately reported. Such a law retains limited-compute waiting and spatial error, but its theta is not proved equal to a real-host deployment effect. The method comparison, fixed roster/weights and virtual observation windows stay fixed. No choice has yet been formally accepted. You may condition your recommended paper on a correctly qualified version of this model, without pretending qualification is already done.

Scores are true physical-world task completions Q over original fixed virtual E, not certificates or reference arrival. All paired methods are evaluated in each preregistered block. Main theta is the weighted expectation of D-S minus R-S service rates in a fixed declared population. Independence must come from a valid input/execution law, not simply random arm order. Unknown scores remain unknown and make the required full comparison unestimable; failed but valid executions retain actual Q and full denominators. Fixed observation horizon is not retrospectively cut at a favorable local gain. A local earlier service witness does not imply positive full-window Q. D beating R while losing to E0 cannot justify the practical value of paying for observations. External superiority is separately simultaneous and predeclared, not selected after results.

Please construct:

1. One clear proposed primary contribution, one supporting contribution at most, and a minimal mechanism chain from paid query decision to net service. Distinguish theorem, algorithmic design, systems engineering and hypothesis. Give a publication-credible claim without assuming the unknown near-neighbor mechanisms absent.

2. An explicit decision on GROUP_PREFIX: make it common platform, separate mechanism extension, or remove from the primary study. Compare inferential clarity, fairness, deadlock/stall risk, potential scope and cost. If retaining it, explain which comparisons identify D's incremental effect and which do not. Do not require success-selected scenarios or silently give R/E0 less capability.

3. A compact main figure/table sequence and experiment matrix organized by claims, including full-window Q, all costs, strong R/E0/external systems, negative regimes and scalability. Use symbolic conditions rather than numerical settings. State the patterns that would refute each claim, and which conclusions remain possible if net benefit is zero or negative. Keep sensitivity design fixed before results.

4. A minimal evidence-to-review checklist focused on scientific necessity. Separate conditions required to have a coherent design, source/backend qualifications deferred until legal implementation, and results that are hypotheses rather than entrance requirements. Explain how to avoid indefinite document expansion while maintaining meaningful independent review. Do not relax a known mathematical contradiction into a future test, or label all missing full texts fatal when an appropriately limited claim is possible.

Conclude with a proposed paper title and a concise contribution paragraph. No journal recommendation needing current calls, no promise of acceptance, and no fabricated empirical claim.
