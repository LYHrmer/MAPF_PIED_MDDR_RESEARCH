# 73P6：两阶段异步环见证的建设性查错

2026-09-09；NO-SKILL / NONBALLOT。本次独立全文读取 P5，并仅补读 P2 §5/7 控制规则；不是正式逻辑审查，也不是重派失落 Claude 环构造。新文件写入前确认不存在，未修改任何旧工件。

P5 实读 SHA256：187823da00cf20bf604686aed33b42b1aeb47cfd13f38a4b6581eadf2503b2c2，14541 bytes /124行。P2 实读 SHA256：5dfaa2adcbe6fcd11ecb16b9d093519f1733dc8fe947d7f64c7316a0db5ba5dd，36848 bytes /202行。

## 1. 结论及一处需要修补的证明

未找到反驳 P5 所声明“条件性付费执行顺序存在”和限定 E0 不可能性的模型内反例。但 §3 的等差阈值论证需要修补：§2 字面只要求等边、轴对齐闭方块的有限覆盖，并未明说来自同一个间距 h 的规则方格。这些条件不能直接推出某横排的进入阈值间距恰为 h。

前提反例是将边长 h 的方块沿横轴以间距 d_tile 重叠排列，其中 0<d_tile<h 且 h/d_tile 不是整数，再以有限横纵排覆盖所需域。进入阈值可按 d_tile 排列，没有相差 h 的一对。它仍符合等边闭方块覆盖条件。此例反驳的是原等差论证的隐含前提，**不反驳 G+ 本身**。

无需缩窄瓦片制度，就能补证 G+。记 A=r_geo+epsilon_obs、B=ell−r_geo。G+ 等价于 B−A>h，因此可取 A+h<s<B。完整包络覆盖提供一块包含前沿点 u+(s+rho)e 的瓦片。该瓦片沿运动方向的投影长 h，令其下端投影为 m，则

\[
s-h\le a:=m-e\cdot u-\rho\le s.
\]

故 a∈(A,B)。前沿点的横向坐标也在该瓦片内，而 K 是轴对齐盒，所以 a 确为沿原边首次包络相交的阈值；a>0、a<ell 排除了端点截断。它是一个非空 I_r 的真实左端点，必在 CAND 中。反向和竖向边只改变投影 e，证明不变。有限覆盖确保所用瓦片属于已登记有限集合，不能在证明中临时补资源。

因此 G+ 可保留；建议用此覆盖证明替换等差段，而非默认一个未写出的全局晶格。若根确实要限定统一方格，也须明示那个更强前提。

## 2. 闭瓦片分离与四条原边

投影严格间隙大于 h 时，同一个闭方块不可能与两个包络相交；等于 h 时可在两侧边界同时接触，所以 P5 的严格号必要。底边/右边的两个间隙分别为 ell−U−2rho、L−2rho，条件 S 正确。旋转用于转换坐标证明，不要求有限瓦片集合本身具有旋转对称性；每块仍轴对齐且每轴宽 h 即足够。

相对边横向间距 ell，因此 ell>2rho+h 足以分离其资源。G 已蕴含这个条件。四条边确为同一正方形上的四邻接单步边；第一阶段各自前缀包含在公共 [0,c_max] 阶段包络内，第二阶段各自后缀包含在 [q_min,ell] 内。两次使用的是整个阶段集合的分离，不依赖同时 START 或共同实际进度。

这不核验固定 PIE 源实际输出了这四条边，也不允许把别的 agent 或障碍从主 roster 删掉。P5 对闭合局部问题的限定必须保留。

## 3. 早证书扩 cap 与有限推进

公开 QUERY 可以在到 cap 前请求；有效使用条件是同 tid 下界已经付费验证/发布，而且 q 退休与相应 ledger 更新同一提交。只要全部已提交 q_i>r_geo，第二阶段集合分离立即成立，不必再查询隐藏的“是否已经停住”。未扩展者当前持有的 U(q_i,c_i) 也是其第二阶段需求的子集，串行扩展安全。

若屏障提前成立，某车可以在运动中扩 cap，并不一定真的在旧 c_i 零速停留。此时不能同时引用 §4 的“均到旧 cap 并 HOLD”作为事实；应将它标作较晚取证分支的充分路径。早取证分支直接沿 P2 的非零速 LAUNCH/APPROACH 接入：cap 增大不降低制动余量，BRAKE 不被取消；已 HOLD 者须新的显式 RUN，旧无作用尝试不能重用。

两种分支都只有有限次 cap 增长，但最终运行还依赖所需作业、传输及明确控制机会实际完成，并最终存在足够的不中断运行段。公开 QUERY 持续出现不等于每个 agent 必获信息充分的证书；永久 [0,ell] 回执或长期只服务一个对象都不能建立屏障。P5 已将 I 作为额外前提，不能在整合时删掉。

## 4. E0 的首个全长授权归谬

归谬成立于所写制度：同四条原 MOVE、q_release=0、已授权限不撤回、每资源唯一物理 owner。首次全长 b=ell 授予前不可能已有本组 END；而该授予必需后继 agent 的非空起点 Mask，后者在没有 END 或途中清空证据时仍持有它，因此冲突。

E0 的公开 cap/RUN、付费 END 推送或轮询均没有被禁用；原 END 服务不能在所需全长权限出现之前造出终点。原子多授也不能使重叠的完整需求同时具有不同唯一 owner。该论证不是对所有无 POSITION 方法的结论：联合占用、其他可信途中证据或更换原路径已经改变所列制度。E0 即使实际前进，仍保留起点资源正是其这里的差别，不能用安装 ACK 偷换空间清空。

## 5. 费用、时间与真实服务量词

可支持的是：在 G、信息条件 I、上述协议及必要服务/控制最终完成的条件下，存在依执行而定的有限完成时刻。它没有给所有合法延迟/排队一个统一上界，更没有保证落在任意预先固定 E 内。有限 O 可以耗尽必要机会；固定窗结果须保留未完成及已发生费用，不能为了让见证成功再选截止点。

从 END 推到任务服务时，建议把 §7 的使用条件写得更明确：保持该参考终点直到适用于该 agent 的 TASK_SERVICE 行；该行时同一 task_instance 仍是合法已分配、未计分的队首；实体满足其服务域且尚未 Stop。若允许其先离开或队首已经改变，仅有“某个 END 后的服务机会”不足。闭合见证可以选择保持终点满足这些条件，但这不是后继 lifelong 自动满足的事实。

即使这组有服务而 E0 无原 END，也未比较其他任务、队列费用和整个固定窗口，更未区别 D/R 是否都能买齐证据。不得推成主总体正效益、最优查询或一般活性。

## 6. 回执边界

当前最具体的修正是以覆盖点证明替换 G+ 的等差论证，并明确早取证分支与服务时刻条件。其余结论仅按上述前提保留，不授六门票。待根处理：候选选择/阶段调度的总规则、主证书信息来源与费用、实际原提案身份、固定 O/E 的适用性及正式审查。

全程只读指定文本、静态核身份并以 apply_patch 新写本文；没有 skill、Claude、新 agent、代码/伪代码、实验或载荷。保护参数均未赋值。HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT。
