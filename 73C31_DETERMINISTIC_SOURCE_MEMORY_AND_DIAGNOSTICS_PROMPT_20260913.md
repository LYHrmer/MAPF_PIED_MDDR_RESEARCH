You are Claude Opus acting as a constructive coauthor for a MAPF preexperiment design. Deliver a specific adoptable mathematical design, not a generic review. No tools/files/skills, no implementation or pseudocode, no source execution, no concrete experiment parameters, seeds, O calendar or configurations. This task is C31, distinct from completed C26 (choosing deterministic P_model) and C29/C30 (paid first-MOVE DAG). Do not redo their route debate.

Research aim and fixed boundaries: finite-size robots with genuine lateral z_dot=-kappa*z+w, bounded nonzero footprint/error, same original PIE-D MOVE proposals from fixed R0 commit 0b5b33649fc367abcb1cd0a7ad5ac03511bf9357, same GROUP_PREFIX/cap/END safety base, paid position evidence and blocking-oriented D versus strong stable-agent R, mandatory E0 and H19E0/H19R-S. Main net lifelong Q/E from same exogenous world/calendar under limited deployed computation. Host CPU/time/memory separately reported and NEVER changes virtual time/Q/source timeout/failure branch. All source repairs shared among its arms, explicitly not native. No positive effect promised, no failures filtered, no new arm matrix.

Current P_model is normative deterministic finite-word RAM, versioned word/address domain, fixed-word read/write/arithmetic/branch and one fixed-slot operation each paid BEFORE effect. Multiword/int/float/geometry/crypto/container/copy/allocate/free/hash/sort/diagnostic I/O all expand into basic operations, not a macro with a constant price. Meter is a limited ideal administrative facility, no business reads. Model storage exhaustion has a defined deterministic outcome; host OOM is missing evidence, never model failure. Every job resumes on allowed PROCESS_SLICE, output only strict-later-row finite publication, FIFO and one writer slot. Dynamic CAPTURE/guard must finish in same original admission row else spent work remains/no effect/no watermark advance. Real END autonomous; tasks true-world evaluator separately paid host cost cannot donate business work. Source clock maps existing native conversions/comparisons to public virtual row time, source PRNG stays private/persistent. No virtual host elapsed.

New factual evidence, do not claim you independently read source:
1. Root+independent source agent constructed FIRST pure-LaCAM normal branch on connected finite four-grid: nonempty n agents start at degree-1 leaves, unique neighbor first goals, 2n endpoints all distinct. Full frozen nonempty goal rows, repaired exact all-pair/strict comparators/goal identity/unique object ownership/bounds, stable frozen env, positive original commit c with c+1 and n(c-1) representable. All original Deadline checks unexpired. First DFS emits [S,G], next sees all goals and returns; no exchange helpers, inheritance recursion or second task read. commitPath keeps c copies of each goal after skipping initial start; caller curr_commits initially n empty vectors. PRE_COMMIT_SHAPE and full external anchor/conflict validation retained.
2. This is only first source subfamily, not whole roster or full ALWAYS_ADVANCE induction. Fixed first-call finite algorithmic counts are known: finite M grid/preprocessing, n PIBT calls, two HNodes/four LNodes in leaf subfamily, paths and c padding, original validation pair checks. Exact container/distance loop counts may be given symbolically by root, not assumed constant.
3. GNU libstdc++11 exact headers read. Two-element std::shuffle under the fixed uint32/uint64 optimized branch draws MT once: rejection threshold=0. Dummy random_shuffle calls rand at most K-1 times with no rejection (n<=K<=V_comp). Original MT seeding/draw are bounded loops. glibc-2.35 upstream TYPE_3 rand is straight-line plus finite if; srand when actually called has 30 fill+310 warmup steps. No lock wait only if ENTIRE C random state exclusive and no reentry, lock initial0. Local Ubuntu patch/binary identity not established. Another independent agent is now closing uniform_real_distribution<float>/log/nextafter, so don't redo RNG.
4. Paid first-service partial order already corrected: INIT/cursor/task chains -> PLAN_CHECK freeze -> C-to-P -> source work/output -> P-to-C -> centre receive verification/CAS append -> C-to-P disposition -> original job reserved in-job P receive/retire (not behind itself). append not grant. Original phase0 SOURCE_REQUEST -> PRECHECK full chain -> centre full grant -> CUM_CAP hop/E receive/prealloc/later install. Phase1 paid NO_GRANT only if preplaced before grant; phase2 only later creates RUN, E auth/publish, ACTUATOR current guarded START. START zero-speed with full C=ell; finite analytic END upper bound from controller, fixed later TASK_SERVICE true full-body containment, no READY dependency. Same-t strictly increasing rows possible. Not all-stage one-step price, not arbitrary DAG layer disjoint, not experimental O generated. Still conditional on actual predeclared work bounds/representability.

YOUR ONE CONCRETE TASK:
Construct the smallest explicit deterministic source MEMORY-ALLOCATION / DIAGNOSTIC-OUTPUT model binding that can close their remaining finite-work obligations in this first normal path, without auditing an entire OS or pretending future VM correctness has been proved. Propose a shared reference interface repair if needed; user authorizes combined/improved designs. Preserve container/source semantic control flow and actual incurred costs; explicitly enumerate any semantic change (e.g. diagnostic byte format vs full payload, allocator address/order). Do not outsource source work to a free host callback. Do not modify RNG, source timeout comparisons, physics, protected budgets or baseline abilities.

Need deliverables:
A. Exact states and total allocation/free/reallocation operations expressed mathematically (NO pseudocode), representability, deterministic failure, ownership/lifetime boundaries. A finite bitmap/first-fit model is one candidate, but choose and justify. State elementary upper bounds in terms of finite addressable cells A, word width b, requested cells r, number allocations L_a and live peaks; NOT just names W_alloc that assume the answer. Allocation scans/zeroing/copy/headers/free all charged; avoid allocating allocator metadata using allocator recursively. Need nonempty normal domain condition established symbolically from known finite first-source object counts; cannot simply assume the allocation returns.
B. A source-only diagnostic sink whose formatting/buffering/emit/failure costs are fully in-model and finite. Keep source message arguments and ordered emissions; if you change floating diagnostics to canonical finite encoding, explicitly restrict it to logs with no source/planner/authority readback and call it an adaptation (not native stdout equivalence). Do not silently disable logs or copy/format for free, or turn all I/O into an ideal host facility. A fixed known-byte expansion bound that supports multiword integers/strings and frozen float bits is preferred; discuss stream-error semantics and missing evidence.
C. Explain how library container algorithms and destructors still remain charged, what this interface actually closes now vs later implementation verification. If these definitions don't suffice for a predeclared total bound, identify the smallest precise remaining mathematical obligation. Do not create a universal SRC_WORK_CERT oracle or require proof of all deployed operating systems.
D. A short drop-in Chinese draft (around 800-1200 Chinese chars; extra formulas allowed) and one actual counterexample prevented by the design. Original primary method/roster/full objective unchanged.
No overclaim complete joint qualification. No formal approval vote. End exactly END_C31_CONSTRUCTION.
