你仅做独立纸面构造核验，不是正式审查票；不使用任何 skill、工具、文件、网络或子代理。你只看到本摘要，不能声称读过候选或报告。不要实现、测试、仿真、实验结果、数值参数。不使用/不重定义保护符号 H、T_delay、B_CAL、B_max、B*、P_active、density N。HOLD。请约1200–2000中文字，结尾 ADVICE-COMPLETE。
已固定候选70被正在进行的科研导师审查指出：使用制动裕量 h_dot>=-gamma*h 会使正初始裕量在任何有限时间仍>0，不能触发精确端点完成。这是进度/效用缺陷而非碰撞反例。70不修改；本次只为未来新编号构造，正式三路仍须收齐。

请严格核验以下根代理提出的有限时间替代，找真实错误，或者给完整短证明和必要边界：
直边长 ell，纵向状态 s∈[0,ell]、v>=0；ds/dt=v，dv/dt=u+eta，|eta|<=delta。横向/身体包络独立已证明保持，不要求你重做。只有PIE决定目标边，控制器只走同一边。端点 s=ell,v=0 为吸收驻留，局部门 latch 才允许新 tid。定理只关于声明的连续纯仿真模型，不验证真实差速硬件。
选符号模型量 0<a_lo<a_hi<=a_brake，a_launch>0，执行器可实现 u∈[-a_hi-delta,a_launch+delta]，输入可行性要求 v_cap²>=2a_hi ell（逐边检查，参数未赋数值）。
LAUNCH：初始 s=0,v=0，用 u=a_launch+delta，直到第一次 v²=2a_lo(ell-s)。此之前 v²<2a_lo(ell-s)<2a_hi(ell-s)，不会超制动界/速度限。eta任意可测有界，actualacc>=a_launch>0，所以有限时间触及下界曲线且在到达端点前。
APPROACH：d=ell-s>0；r=v²/(2d)；在带 2a_lo*d<=v²<=2a_hi*d 内用
u = -a_lo + delta - [(a_hi-a_lo+2delta)/(a_hi-a_lo)]*(r-a_lo).
在下边界 u=-a_lo+delta，actualacc>=-a_lo，导数(v²-2a_lo*d)=2v(u+eta+a_lo)>=0。
在上边界 u=-a_hi-delta，actualacc<=-a_hi，导数(v²-2a_hi*d)<=0。
所以连续流保持带；控制有界，不需要来回bang-bang/Zeno切换。v>=sqrt(2a_lo*d)给 sqrt(d)_dot<=-sqrt(a_lo/2)，从进入带开始有限时间<=sqrt(2d_entry/a_lo)到达d=0；上带保证v→0，可连续接端点吸收模式。到达点的控制公式不再求值，不做除零。
速度限由全程 v²<=2a_hi*d<=2a_hi ell<=v_cap² 保持。机器人可在任何时刻因外部停止而切到保有全部当前后缀资源的制动/驻留模式，此时不承诺任务有限完成；不允许倒退跨过已退休资源。

请回答：
1) 上述有限时间推导在有界任意扰动下是否成立？给反例或准确条件，包括LAUNCH切换、ODE解存在与端点极限。
2) 哪些假设仅是理想连续低层状态反馈？如果低层s/v也存在测量误差，能否直接沿用？请严禁“监控会发现所以安全”的泛泛兜底，也不要把统计测量界当确定界。
3) 如何把有限时间物理到达与异步消息/terminal latch的行政完成分开，不把物理到达定理错误当成永久分区下全协议活性？
只给这些有用结论，不扩展新路径规划、不跑任何东西、不设置实验量。

