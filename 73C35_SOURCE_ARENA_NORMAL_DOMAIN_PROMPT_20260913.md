你是Claude Opus，做具体构造C35，非正式审查。只用下面已给事实，不用工具/网络/skill，不写代码或实现伪代码，不运行实验，不赋实验参数。请800–1200中文字符加必要公式，给可直接采用的条件证明/修正，不泛审稿。
项目已选P_model有限字RAM，每个已合法固定字读/写/算术/比较/分支先收费，普通作业可在基本步边界暂停。所有内部PIE-D来源臂共用修订，非native等价。新静态证据已经明确批准纯LaCAM输出点、runtime初值0、未用文件构造器的regex可显式裁出；日志将转私有有类型收费sink，未定义I/O不在本题重查。C31旧构造没有模型正文，已取消；本题只解决分配，原源首轮正常算法分支已独立条件证明，[S,G]、有限图和两HNode/四LNode等，不由allocator保证算法求解成功。
根提出一个最小来源私有arena合同，请独立检验：A为X已有有限地址域划出的数据字槽数，w为已有模型字宽，均符号未实例化；占用数组used[A]与首址长度表len[A]各A个字，另独立保留于同一有限地址域，初始化和全部读写收费，不能递归调用分配器分配自己的元数据。程序栈/对象也必须有已计容量，不拿免费外部堆。一次分配原字节长度按冻结布局取r≥1个字槽（原零字节new也取1，标准容器不调用的操作不造调用），对齐q≥1个字槽，所有换算/指针/3A规模/计数可表示；不能折算布局为具名不合格。
取最小p使0≤p≤A−r、p mod q=0且[p,p+r)全未占。确定求取方式是按p递增检查对齐，每个对齐候选逐槽递增读取used至首个占用或成功；找到后标记r槽及len[p]=r。没有候选时确定分配失败，不借宿主heap。整个scan/标记允许在私有续体中暂停；同worker此时无其他arena writer，部分已保留槽不发布成源对象。返回raw storage不把源码未初始化读取修成0，构造/赋初值照原源另收费。free只接受原完整活块起点，核len[p]及边界，逐槽释放，清len[p]；double free/非首址/悬空或越界仍源码失败，不由归一化变合法。容器扩容保留原库allocate→逐元素copy/move→destroy/deallocate，无免费realloc。
根猜想充分容量条件：对于事前有限分配调用序列的每次r_j,q_j（包括实际容器重分配和构造临时），令S=Σ_j(r_j+q_j−1)。若A≥S且有合法空初态，则即便交错合法free，first-fit也不会因碎片失败。理由：在历史最高已占末位置h之后，总有不超过q_j−1个对齐空隙可放r_j；h按此量最多增长，因此h≤累计S。这里必须用累计请求量而非同时活峰值；请求量只能由已知对象/循环计数及固定容器容量规则事前上界，不能读一次成功结果补算。
交付1：证明或反例并最小修正上述容量条件，注意首址0、整数溢出、不同q、释放后历史h、零大小和模型元数据占域。
交付2：以A,r,q给逐类操作次数上界（对齐检查/used读取/标记/free/原扩容复制），不虚构未写出的机器指令常数，也不把总扫描叫1步；说明从这些次数到完整基本步数还缺什么固定映射。
交付3：本合同真正关闭哪些分配语义/有限性，哪些仍须首轮请求量界、容器源操作和实际后端对应。不得用有限RAM状态数跳过正常终止或剩余表示义务，不声称全AA/全净服务已闭。末尾END_C35_ARENA_BOUND。
