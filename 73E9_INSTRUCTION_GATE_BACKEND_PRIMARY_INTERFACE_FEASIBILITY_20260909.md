# 73E9：Unicorn 指令闸门及原 Linux 入口可行性

2026-09-09，NO-SKILL / NONBALLOT。只核固定 Unicorn 2.1.4 的三个官方源对象，不构造其他服务模型。HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT 保持。

## 1. 单一候选及取舍

具体候选限定为 **Unicorn 2.1.4 的 x86-64 engine、CODE/count 指令边界、同 engine 的 CPU＋MEMORY context，以及受控外部调用回调**。它确有可用 API，不应再泛写“指令计量器完全不存在”；但**当前不适合作为完整 R0 C++/Linux 入口的轻量后端**。所缺的是系统装载／调用语义、精确目标指令边界及完整费用状态，不能用少量通用回调视为已经补齐。

对已装入内存、调用边界封闭的机器码，该组合是具体可审查的工程方向；对现在的 MAPFPlanner／LNS／标准库入口，尚不能据此放行。此取舍不证明模拟器本身错误，也不宣布根正在另论证的服务模型已经通过。

## 2. count、停止与“执行前”的实际证据

[公开头文件](https://github.com/unicorn-engine/unicorn/blob/2.1.4/include/unicorn/unicorn.h) 的 uc_emu_start 将 count 定义为拟执行指令数，timeout 则是经过时间；另有 uc_hook_add、UC_HOOK_CODE、uc_emu_stop。原始行 1039–1041 还限定：无错误返回或错误已由回调处理时才保证内部状态正确，不能默认任何异常后都可安全续跑。

[uc.c](https://github.com/unicorn-engine/unicorn/blob/2.1.4/uc.c) L1043–1053 的计数回调先增加计数，超过请求数量时调用停止；L1201–1216 将它插在其他 CODE hooks 之前；每次 emu_start 都重置这一计数。uc_emu_stop 设置停止请求并调用 break_translation_loop。故 count 不是宿主定时器的别名，也不能把恢复后的本次计数当作整个作业累计量。

这里有重要边界：上述源码证明**计数 hook 在其他同类 hook 之前**，不能单凭这句证明所有 x86 指令副作用之前的精确位置。[通用 translator.c](https://github.com/unicorn-engine/unicorn/blob/2.1.4/qemu/accel/tcg/translator.c) 将 insn_start／translate_insn 委托给目标后端；本次未读 x86 目标生成器和 break_translation_loop 的传递实现。尤其不能把翻译循环的 max_insns 当成已经执行的步数，或看到“超过才停止”便宣称多执行了一条。**公开 count 合同已取得，全 x86 指令集的执行前收费证明未完成。**

唯一纸面接线应以已核指令边界为准：容量先归属本 job，暂停后保存恢复位置，累计账不随 emu_start 重置；未证明能在副作用前停住的指令不能进入合格集合。不能在宿主异步线程中随意调用 stop，就声称具有同一边界保证。

uc.c L1012–1040、1224–1246 明确 timeout 启动可 join 的线程、读宿主钟并 sleep 后请求停止。候选的虚拟指令计量不使用它作为 PROCESS_SLICE 容量定义。将它作宿主异常看门狗，也只能得到异常退出记录，不能据此恢复确定步骤执行。

## 3. 宏指令和宿主回调不能免费

count 的单位是该后端报告的客体指令事件，不是硬件周期、内存字节或 C++ 语句。REP／字符串指令究竟按宏指令还是重复迭代触发，当前三个对象没有给出完整目标级合同，不能填写臆测口径。一次复杂指令中的访存、故障及重复工作，也不能当然视为可以在任意字节处暂停。

公开接口具有内存事件、异常、interrupt 和有限特定指令 hooks；它们给调用者观察／介入的位置，**不替调用者执行一个完整 Linux 系统调用模型**。宿主回调若进行大量复制、路径检查或日志处理，这些本机工作不会因进入 callback 就成为 CODE 计数覆盖的客体步骤。

因此费用至少分开登记客体指令事件、客体与宿主间字节复制、回调实际工作、翻译／缓存建立、快照／恢复和清理。不能以 count 到额为理由认定其他费用已支付；不能将宿主 callback 内部任意长工作塞进一个收费原子步。对原子性和步内工作上界没有合同的调用，仍不足以满足当前 PROCESS_SLICE。

## 4. 新版 context 真有内存快照，但不是任意 worker 镜像

原始 unicorn.h L1106–1119 明确默认只保存 CPU，另可选择 MEMORY；**MEMORY context 含内部已分配内存指针，不能用于另一个 Unicorn 对象**。所以“context 只存寄存器”不符合本次版本，而“把 context 复制给新 engine 就获得独立 worker”同样不成立。[该版本头文件](https://github.com/unicorn-engine/unicorn/blob/2.1.4/include/unicorn/unicorn.h)

uc.c 的 save／restore 确有 flatview_copy、snapshot_level、内存恢复和 TLB 刷新调用，CPU 则复制状态或调用架构函数。这关闭了“内存模式是否实际接线”的问题；它不证明未读取的 flatview／memory 下游覆盖全部情况。

本候选仅选**同一个 engine 内串行试算和回滚**：已接纳状态作为基准，事务拒收恢复基准；接纳后才更新基准。不能声称这是已完成的跨进程分支。源异常或宿主故障要求更换 engine 时，旧 MEMORY context 不能直接移用；必须有另外可移植的客体映射／寄存器／系统状态恢复合同，当前未取得。

宿主 user_data、外部文件描述符、日志缓冲、系统调用表状态和费用账不是“CPU＋MEMORY”字面涵盖的对象。它们须分别定义所有权；费用、公共时刻及首次事务资格永不回滚。使用宿主内存映射接口时，还需证明外部 backing storage 不被其他使用方改写，不能把共享内存误称私有快照。

## 5. 原 C++/Linux 入口需要的系统层

原 R0 核心使用容器、分配器、随机库、chrono、异常和 I/O。即使只进入 E6 的同步规划入口，保持已编译 C++/Linux 产物也仍需处理 ELF 装载、重定位、运行库初始化、TLS／栈、内存分配相关系统接口及错误返回。动态链接需要相应装载与共享库状态；改成静态链接不是删除系统接口，并且改变构建身份。这些是承载原入口的必要语义，并非声称本次观测了某次实际系统调用。

所核 uc_open 接受架构和模式，uc_emu_start 接受机器地址与限制；它们没有交付“给一个 Linux ELF 即建立合格进程”的合同。对 SYSCALL 或 interrupt 能挂钩，并不说明 fd、mmap、时钟、信号和阻塞行为已经实现。直接把客体请求转给宿主，还会引入不可回滚副作用、地址转换和未计费工作。

故在当前证据下，采用该候选仍要补一层专门的 Linux 用户态装载／服务兼容层，且完整 R0 可达调用表尚未建立。这里的“不合适”是项目范围判断：不能为了保住精确指令模型，把这层建设隐藏成少量包装。若削减运行库或改写原核心来规避它们，则须重新命名来源及审查保真，不能再以 E6 的最小源码差异声称已经完成。

## 6. ABI、两类时钟与公平账

可保留的产物身份只能是未来明确构建的 x86-64 机器码及其完整用户态 ABI、CPU 模型、浮点模式、编译器／标准库／Boost 身份。理论上模拟执行无需改每条原指令，但当前没有合格产物，也没有证明它在该 engine 的全部所需指令上等价；本机 E7 的 Boost 1.74 头证据不自动绑定到模拟运行库。

源 chrono 应由受控入口返回当前公共行物理时刻，源 clock 日志应来自本 job 的收费 CPU 域；两者均不是 Unicorn timeout 或宿主实际运行时长。暂停期间前者随世界推进、后者不增加；局部随机库状态留在客体，宿主回调若持有随机状态须另外归属，不得通过恢复旧基准退还已发生费用。

实际宿主 CPU、内存、翻译和复制开销要完整记录，各臂使用相同 engine、回调和费用规则。但“记录宿主开销”本身不能证明它已经满足虚拟行容量；若把它映射进虚拟服务，须有独立可核的拆分和暂停合同。任何快照或日志优化收益属于共同适配，不归 D。根另核的 subreaper 回收入口由根承接，本件不重读，也不把收养进程当成模拟器状态迁移证明。

## 7. 来源身份与实际读取边界

三个对象均来自官方 unicorn-engine/unicorn 的 2.1.4 标签；未额外解析标签 commit。以下 blob 为完整 raw 字节按 Git blob 规则独立重算，并非冒称 GitHub 返回了该字段。Web 展示压缩空行，本文行号以 raw 为准。

| 对象 | bytes／行 | SHA256 | 重算 Git blob |
|---|---:|---|---|
| include/unicorn/unicorn.h | 52576／1456 | `66fdf8f39df34e507a163eb550549669cec3399c2103b8dc84bc4131f600e55a` | `b4ffc6bc75ff865f04fa8cfc12b6743404599ecf` |
| uc.c | 82028／3070 | `af8d8281594036b2d3e7d65940999b444761e4410d3b5801c998667f59331364` | `75b89a0176b4e89fdde43e3d26ce5af9a2b6adef` |
| qemu/accel/tcg/translator.c | 5869／171 | `04dcdbb2877f29c97d05395f6d4896e3c996dbb12101634126d5421fb4458e4d` | `72f21c410cbda3a19f1bc932c50f445eabafc328` |

头文件实际读版本、hooks、映射、start／stop 与 context 接口窗口；uc.c 读计时、计数、停止、context 的默认模式／保存／恢复／释放／设置及 snapshot 窗口；通用 translator 全文已读。完整散列不代表前两文件全文语义审查。GitHub 工具名虽在目录可见，实际不可调用，随后以同对象 Web／raw 读取；一次 raw 网络失败已按授权升级重试，空响应不算源码。

当前 73 实核 SHA256 为 `1881a85c7a21e5e5fe55e602e246cc057b964df4114dc6c8ab4bbde42f200a54`；另读取其两类时钟、PROCESS_SLICE 和 E8 状态段，没有冒称重读全文。目标 E9 创建前不存在。未修改旧件或 Git，未调用 skill、Claude 或新代理，未实现、伪代码、构建测试、仿真调参，未读 35–38、受限 Q 或任何数据载荷，保护参数未赋值。仅 apply_patch 写本研究目录报告，shell 各段均 rtk；本件完成不改变六门裁决。

