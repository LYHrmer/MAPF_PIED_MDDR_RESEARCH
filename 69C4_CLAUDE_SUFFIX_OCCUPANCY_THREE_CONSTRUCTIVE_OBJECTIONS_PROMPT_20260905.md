仅据本摘要做建设性研究协作，不用 skill、工具、文件、网络或子代理，不投票，不声称读过候选/论文。1500–2200 中文字，最后 ADVICE-COMPLETE。不要任何实验结果、实现代码或数值参数。保护符号 H、T_delay、B_CAL、B_max、B*、P_active、density N 完全不使用、不重定义。
已结束的上一请求 69C3 因 API504 未产生正文，本次是更窄的新请求。

请找出这个组合方案最可能被审稿人否定的三处，并给具体修复：
PIE 是唯一新离散 MOVE 源，外层只复制原边或驻留/保有旧 MOVE。每 agent 至多一个未终结 MOVE。直边实体中心 x=u+s e+z，ds/dt=v≥0，z 在有界盒内；v²≤2 a_brake(ℓ−s)，边界控制有足够制动力保证不越终点，允许边上停车。空间包络为从有证据的进度下界 q 到终点的全部身体/偏移包络。闭瓦片覆盖，完整未来掩码在 START 前排他准备。只有有界真实测量 ŝ±ε 得到向下取有限切点的 q 可以释放永远不会返回的后方瓦片。普通 COMPLETE 可以假，不能释放；测量服务可缺失/延迟但本定理内不能违背界。所有算法可用相同服务、相同查询机会并计费。
每 tid 有不可逆 D=UNDECIDED→COMMIT 或 ABORT。各 owner 的准备票不可超时撤销；COMMIT 必须有所有冻结票，entry gate 也为参与者。恢复 UNKNOWN 保有；ABORT 不释放 parent resident；COMMIT 后未启动也保有全掩码；终点+旧门 latch 后转新 resident。执行 START 绑定完整 tid/occurrence/fence/原 MOVE。拒绝依赖同批前车一定离开；可失去旋转/跟随并发。
主比较：同底座完整方案按阻塞原因优先查询 vs 同底座按活动 tid 轮询查询；另比较只在终点释放 vs 后缀分段释放，区别查询调度收益和共同几何互锁收益。保留 PIE-D 原版适用域轨道和官方时间独立 MCP；适配版明确标注。静态定理只说固定其他状态时后缀缩小不会增加阻塞，不承诺闭环吞吐。效用以所有发起配对运行纳入的完成数/预声明暴露计，失败不删，处理/查询/事务耗时期间实体继续动。
需要你特别指出：进度单调与坐标 z 的可信性、重启时已退休资源如何恢复、保守到无用的负例、让审稿人信服组合贡献所需最小对照和机制记录。只指出真正承重的三处并给修复，不发散为十几条泛泛建议。HOLD。

