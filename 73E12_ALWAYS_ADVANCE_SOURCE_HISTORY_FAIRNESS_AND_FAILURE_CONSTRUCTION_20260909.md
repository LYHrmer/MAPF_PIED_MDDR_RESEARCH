# 73E12：ALWAYS_ADVANCE 源历史、拒收、失败与公平比较的纸面构造

日期：2026-09-09。任务 `/root/source73_r0_remaining_contract`。**NO-SKILL / CONSTRUCTIVE / NONBALLOT**；本件是供根独立取舍的方法定义，不是正式审稿、候选固定或资格通过。保持 **HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT**。

## 1. 判断与唯一建议

**accepted-only 源状态回滚不是安全、非预知或有效因果比较的必要公理。它是当前 73 已选择、但可以显式修改的一项规划历史策略。** 可以改为 `ALWAYS_ADVANCE / FAIL_STOP_SOURCE`：同一来源会话只保有一个持续存活的私有内核；每个完整且合格的源事务正常结束后，无论外部计划最终接纳、过期或被撤销，都保留该次内核和随机状态，供合格后继事务使用。外部执行历史仍只在中心付费 CAS 接纳后追加。源事务不能证明可以继续时，停止该来源会话，不自动重启、重播或恢复。

这个替换能在规格上删除“每 job 从已接纳检查点恢复、保存候选检查点、接受后提升、拒收后回滚”及其子代提升机制。它保留初次隔离进程、输入与输出封存、全部费用、单作业串行和真实退出回收。**它不解决原 PROCESS_SLICE 的执行前有限计量，不建立 native R0 等价，也不证明固定源码已在该新增历史域闭合。** 若根选择 E10 的非抢占规划服务，必须把“服务模型”和“源历史策略”作为两个分别说明的变更，不能用本件替 E10 补齐时钟或宿主测量证据。

建议只采用上述一个失败规则：无暖恢复、无自动冷重启的会话 fail-stop。恢复功能若以后需要，属于另外的明确方法与费用合同；不在本件同时保留数个未定分支。这比以完整快照维护 accepted-only 更小，但代价是某些受控失败后不再有新规划，须在整个固定观察窗口中如实承担。

## 2. 本轮实际阅读与证据边界

本轮分段完整读取当前 73、E6 和 E10；一次并行输出被工具总长度裁剪，已通过覆盖该缺口的单独窗口补读。没有重新访问固定源码、系统接口或库，也没有重复根 E11 的源码检查。

根随后告知 73 已增加来源／服务备选段，而 accepted-only 语义未改。本件评估对象固定为下表中的输入快照，不冒作后继新增字节的全文审查；本件没有追改 73。

| 本轮亲见全文 | SHA256 | 字节／行 |
|---|---|---|
| [当前 73](/home/lyh/MAPF_PIED_MDDR_RESEARCH/73_PIE_SPATIAL_EVIDENCE_TERMINAL_HANDOFF_AND_PAID_SERVICE_PREEXPERIMENT_DRAFT_20260908.md) | `1881a85c7a21e5e5fe55e602e246cc057b964df4114dc6c8ab4bbde42f200a54` | 130281／466 |
| [73E6](/home/lyh/MAPF_PIED_MDDR_RESEARCH/73E6_R0_REPAIRED_ADAPTER_MINIMAL_SOURCE_AND_LIFECYCLE_CONTRACT_20260909.md) | `6f5205a871e6d8b2d45897283fd270d8c044fd48abeeac3b52acceab4b4a1dc6` | 34244／190 |
| [73E10](/home/lyh/MAPF_PIED_MDDR_RESEARCH/73E10_ROOT_NONPREEMPTIVE_PLANNER_SERVICE_AND_SUBREAPER_ALTERNATIVE_20260909.md) | `1e2a6c7ff03ae879656e1032f8d2bdff8b472226edfc9e25d48e5fd86ab48fa9` | 12336／80 |

根在本任务消息中另报告 E11 的亲核事实：`plan` 为 void，后继修复 bool 未完整传播；`planner_commit` 的清容器及 `skip_start=true` 路线需要在进入前做付费 `PRE_COMMIT_SHAPE`，不能等危险访问之后检查，也不能人工补 WAIT；PathTable.cpp 的空 `insertPath` 已有直接返回。本件据此限定继续域，**亲见层次仅为根的具名消息，不冒称已读 E11 全文或重新读过原函数**，不重复把空 insertPath 认成 UB。

源码身份仍为 [YueZhang-studyuse/LMAPF-delay 固定 R0](https://github.com/YueZhang-studyuse/LMAPF-delay/tree/0b5b33649fc367abcb1cd0a7ad5ac03511bf9357)，commit `0b5b33649fc367abcb1cd0a7ad5ac03511bf9357`。E6 所列入口替换、第五堆严格比较修补、具名 PIE-LACAM 核心及依赖前提全部保留；新增的是状态继承制度，不改原文件身份。以下证明是基于已读合同的独立逻辑分析，没有新增运行实证。

## 3. 将三种历史分开，避免把“保留”误作“执行”

第一种历史是实体与真实任务历史：连续物理状态、已承担的控制责任、真实任务账本 A 与服务 Q。源事务对它没有写权限；过期提议和私有预测不能使任务完成，也不能撤销已授动作。

第二种历史是中心已接纳的执行历史 Π、已交付 Kc／K_task、资源与授权账本。只有既有完整输出验证及当前版本 CAS 能追加 Π，后续 MOVE 必须回指该接纳记录中的原提议。ALWAYS_ADVANCE 不放宽任何前缀、资源、END／READY 或物理准入条件。

第三种历史是来源会话的私有求解历史：env、Instance／LNS／Agent、容器、缓存、源内标志、C PRNG 和实际存活的局部随机对象，以及正常完成事务的因果链。它记录“求解器曾做过什么”，不声称记录的每条路径都执行过。原 LaCAM 局部 MT 若按源码在某调用重新构造，就仍然重新构造；ALWAYS_ADVANCE 不是把所有 MT 改成一个永久连续主流。

费用、公共时刻、首次事务资格、生命周期与撤销记录构成外部单调账，三种历史均不能将它回滚。源状态版本与源代码／参数版本也必须分开：前者每次合格完成都演进，后两者在 profile 内固定。

## 4. “正常完成”必须有完整事务边界

本件的事务是已登记作业从输入安装，经原 loadPaths、plan、必要的调用前形状资格、原 planner_commit，到规定输出和完成记录封存的完整调用包。单独 `plan` 返回、某个 bool、日志成功字样或“进程还活着”均不等于完整事务合格。前置检查的位置与源输出形状由 E11 接口义务接续；本件不发明缺失的返回状态，也不按外部收益判断是否合格。

执行中，内核本来就在原地变化；本件没有隐藏的旧副本。正常完成时，封存结果及来源会话／作业／输入版本／起始与结束源状态版本的联系，登记一个新的可继承源历史节点。这里的节点是审计身份，不要求把完整内存序列化成检查点。中心之后拒收结果，不撤销这个节点。若事务中途失败，内存可能已部分变化，**只能隔离并停止，不能叙述为“没有改变状态”或把半成品接入正常完成链**。

继续域须在未来运行前通过固定 profile 的源码合同证明：对合法初始化状态，以及该 profile 允许的所有合格历史和下一冻结输入，原 loadPaths／plan／受保护 commit 调用仍有定义；正常事务留下满足下一调用前提的内部状态。这个归纳域必须包含“前次输出未被中心接纳”的历史，不能只证明原模拟器每次执行其规划后的路径。

这是一项具体的源资格义务，当前并未完成。外部输出非空、几何合法，甚至中心接受，都不单独证明全部内部容器和生命周期有效；反过来，外部因目标版本变化拒收，也不证明内核损坏。**不能设置一个未实现的万能运行时检查器，再把它总能识别内部有效性当作新前提。** 需要事前明确的源码保持证明与必要的有限输入／形状守卫；没有证明时整个适配 profile 仍未获执行资格。

## 5. 单作业状态语义与外部接纳

每臂只保有一个来源会话、一个活动事务或一个待处置结果。初始化仍按 E6 同步调用、付费后条件与中心 READY 规则；区别是 READY 后保有该活对象，不必建立用于逐 job 回滚的初始化检查点。初始化身份、预处理及所需费用不省略。

| 可辨事件 | 私有源状态与后继资格 | 外部执行／费用 |
|---|---|---|
| 尚未安装输入、也未进入任何源调用的输入不合格、NO_INPUT 或排队取消 | 源状态不变；合格新请求仍可随后进入 | 不追加 Π；输入核验、登记及取消等已发生成本保留 |
| 事务正在运行 | 仅该 worker 原地写入；中心和另一 job 不读取半成品 | 无新计划授权；冻结输入不随任务更新改变，全部工作收费 |
| 完整事务正常且在继续域内结束，结果已封存 | 无条件保留结束状态与原随机消费，登记源历史节点；仍等待结果处置 | 只产生候选，不能提前增加 MOVE 或 Q |
| 中心完整验证与当前 CAS 接受 | 使用已经保留的源节点；不再通过接受动作复制或提升状态 | 原子追加 Π，登记接纳及各版本；接纳工作另收费 |
| 中心判 STALE、目标改变、合法撤销或 CAS 失败 | 同一个已完成节点保留；不回退随机状态、缓存或源内标志 | Π 不变，失败和消息费用不退；后继须重新冻结当前允许输入 |
| `PRE_COMMIT_SHAPE` 拒绝、未形成合格完整提议或继续域无法保障 | 不进入危险 commit，不补路；会话停止，状态不再供下一事务使用 | 记录具体 `NO_PROPOSAL_SHAPE`／源失败原因；不把它自动称已执行 UB |
| 源异常、源退出、硬终止或状态身份不可信 | 会话失去新作业资格，进入停止与真实退出回收；没有回滚／重新播种 | 撤销未接纳发布；既有物理责任和费用保留，未知证据明确标记 |

正常内部超时若仍按原约定形成合格完整输出，就沿正常完成行处理；超时标签本身不强制回滚。源合法返回无提议但没有已证明的继续后条件时，采用上述会话停止规则。这个保守选择可能降低实用性，须保留在方法名称及结果中；不能把它隐藏成原作者算法必然停止。

输入安装一旦开始，env 等私有状态也可能已改变；此后安装失败不套用“尚未进入 plan，所以状态不变”，而按部分事务停止会话。首次资格仍沿 E6：首次合格事务发布即单调消耗，排队后取消、正常拒收或源失败不退还；未发布合格事务的输入拒绝不消费。它位于源状态之外，与保留的 initial_run 等源内标志分别记载。

若中心在结果封存后发现内生输出非法，记录 INVALID_OUTPUT 并停止来源会话；此前结束记录保留为实际审计历史，但不得再供后继继承。这不把状态回滚，也不把非法输出归入允许热继续的 STALE／外部目标变化。下一 job 等待中心终态的屏障，正好排除非法结果尚未查清就复用状态的窗口。

完整事务结束后，Srv_P 继续保留待结果槽；中心付费接受／拒绝，终态经既有付费交接到达并完成该槽的释放后，才允许后继公开机会启动下一 job。ALWAYS_ADVANCE 不附送“中心尚未检查就先跑下一个”的流水能力。中心也不持自身 writer 槽等待 worker。若中心 fail-stop，未接纳结果无发布资格，不能凭私有完成推进新规划；已有世界和任务服务继续。

中心 CAS 的版本合同需要同步修改：检查固定源代码／参数／会话身份、登记作业、输入锚点、当前任务投影及输出所属的结束节点；不能把“源状态仍等于本 job 开始节点”作为接受条件，因为它已按本方法正常前进。该更新只排除自我制造的版本冲突，不允许跳过当前 K_task／Kc／Π 的原接纳检查。

## 6. 任务更新、撤销与源失败的唯一处理

下一作业的 SharedEnv 仍完全由中心最新已交付知识及已接纳 Π 尾生成。`PROJECT` 只沿已接纳的未消费段投影合法已知任务；`unexecuted_paths` 只按现有合同复用已接纳 future_path，并通过 E11 所需入口资格。**不得把未接纳提议的内部 future_paths 当成外部已经承诺或走过的路径，也不得用其预测访问扣减真实任务。** 当前作业期间的新 TASK 快照只进入中心知识，不能偷换 worker 冻结输入。原有内部路径重用与新输入相容性属于上一节的继续域证明，不能假设 clearAll 会清除所有历史。

软撤销仅撤销输出发布资格。已进入的源事务按选定服务模型继续到合格终态，所有实际工作计入；若正常完成，状态照常保留。DRAIN 后恢复时不能重新获得旧随机初态或首轮资格。QUERY_OFF 本身不是规划会话失败，不给 E0 增加额外重启或停止惩罚。

硬 WORKER_FAIL、初始化截止、全臂 Stop 或无法维持来源状态定义时，停止整个会话；没有自动重启。E6 的 LIVE、STOP_REQUESTED、REAP_PENDING、REAPED、RELEASED 区分仍有效，信号发送、停止通知或一份不明终态不等于可释放对象。仍活的执行域不被析构或复用；尾部费用照记。采用一个直接拥有的持续 worker 可去掉逐 job 父子提升链，但不因此证明完整执行域回收已验证。

“撤销后让它算完”只适用于软撤销。若 E10 非抢占模型规定某个公开故障或 Stop 应真正终止计算，就必须取得与该时点相容的实际执行／终止证据，不能先让未来整段计算完，再把账面时间截短。当前 73 的有限 PROCESS_SLICE 条件若保留，则软撤销后的推进也仍受它约束；本件不以正常完成之名容许免费后台运行。

发生 `NO_PROPOSAL_SHAPE` 而守卫确实挡在危险调用前，且此前源执行有定义，可以是已规定的适配失败：会话不再规划，但世界 Q 仍按完整真实前缀统计。实际未定义访问、身份或适配映射不闭则须另外标为 PROTOCOL_OR_SOURCE_INVALID／INFERENCE_UNAVAILABLE；未知日志不是零分。不能把“不可继续”统统认成已发生 UB，也不能把真正 UB 洗成合法搜索失败。源会话停止不清空实体、不截短设计分母，不替代全臂 Stop。

## 7. 非预知性和安全边界的条件证明

以初始合法来源状态为起点，每次事务的输入来自此前合法交付与付费封存，源执行只使用该输入、自己的历史及规定随机／时钟映射。正常结束状态因此只依赖已经允许的过去；接受与拒绝是之后中心的合法操作。无条件保留这个状态不会凭空添加未来任务、隐藏实体位置或未交付证书。逐事务归纳即可保持非预知；accepted-only 的选择性保留不是该归纳的必要步骤。

证明要求结果与私有状态在所属服务完成之前不对后继决策可见。E10 若采用宿主提前完成的离散事件安排，结果、实测时长及完成后的状态均不得在规定结束事件之前驱动下一 job、查询或任务选择。已经在主机内存在，不等于算法在公共轨迹中可以使用。外部文件、时钟与熵仍须按既定映射，不能以“状态一直活着”省掉外部输入清单。

安全的归纳作用于已接纳 Π、资源责任及物理控制：未接纳源状态没有写这些对象的权限；只有原完整验证、CAS 和后续付费准入能产生新动作。故在原协议安全前提成立且来源隔离有效时，ALWAYS_ADVANCE 的状态演进本身不破坏该不变量。它仍可改变后续合法提议和停滞概率，**安全不推出活性、规划质量或固定窗口净收益**。

## 8. D／R／E0 的同能力与全部成本

D-S、R-S、D-F、R-F 和 E0 使用同一修复源身份、ALWAYS_ADVANCE／会话 fail-stop、初始化和首轮资格、单 worker／单待结果槽、请求屏障、取消语义、输入权限、继续域守卫及计费服务模型。初始随机输入和角色派生按同一公共合同；后继 C 随机消费、源内分支与调用数量可以不同。不能给 D 持久缓存、给 R 每次冷启动，或只让 D 的失败调用保留随机状态。

同能力不要求不同闭环产生相同次数的 job、相同随机流位置或逐字相同路径。为强制相同而额外执行无用调用、读取另一臂结束状态或跨臂接续随机数，会构成另一个制度。公共故障创新仍按其原语义 key 派生，不把某臂 source rand 的消费量用于顺移公共故障磁带。

取消逐 job 快照后，实际不再发生的 COW／恢复／提升工作不虚收；持续保留的内存、缓存、日志、输入复制与输出封存照实收。初始化、loadPaths、PRE_COMMIT_SHAPE、plan、commit、中心检查／CAS、撤销、失败、生命周期管理和尾部回收仍全部列入费用。结果等待占槽与内存保留单列，不假称等待一直消耗源 CPU；STRICT 的预付供给与实际工作向量继续分开。任何服务模型变化还须依 E10 重定占用与时钟合同，不能沿用旧单价名字掩盖能力改变。

共同取消快照或共同修复第五堆可能改善全部臂；这属于共同适配效果，不属于 D 的查询创新。H19 等不同源的外部方法不要求拥有虚构的 R0 内部对象，但其搜索历史、取消和成本语义必须同样公开，不能由“都用同一个服务站”推原法完整等价。

## 9. 必须保留的强反例

**过期计算改变下一次答案。** 设一次合法调用处理旧目标，其提议因后来的合法任务知识更新而被拒绝，但已消费 C 随机数或改变复用缓存。下一调用在相同当前可见输入上，ALWAYS_ADVANCE 从该结束状态起步，accepted-only 从先前接受状态起步；两者可以选择不同合法路线。若其中一路在后续服务机会前到达、另一路没有到达，真实 Q 可以不同。这是说明非等价的抽象状态见证，不声称已从固定 R0 找到对应输入或运行输出，也不赋值任何实验量。交换后续路线的有利条件即可改变效应方向，因此没有普遍优劣结论。

**多查询可带来更多随机尝试。** D 与 R 的信息到达时序不同，可能导致不同拒收／再规划次数；ALWAYS_ADVANCE 让这些正常拒收留下不同状态。D 的优势可能由额外探索或缓存暖化介导，代价也可能是耗时、过期与坏分支。它在完整付费且同规则时是合法闭环效果，却不能解释成“固定同一规划路径时仅选对一次观测”的直接效应。只报告成功接纳 job 的成本或随机历史会把该机制伪装成免费收益。

**预测历史越权。** 若把拒收提议的内部未来段安装为下一次外部已接纳路径，PROJECT 可能提前消费尚未执行的目标，随后给实体生成来源不实的新动作。这个反例不是随机性问题，而是把两种历史混为一谈；只有坚持 Π 权威和逐 job 输入重建才排除。

**正常返回掩盖不可继续状态。** plan 的 void 返回不能证明后继修复成功，更不能保护 commit 的危险形状。若见返回就继续热用对象，ALWAYS_ADVANCE 可能把部分更新带到后继；但 accepted-only 保存一个坏候选、随后接纳，也不会自动证明安全。需要 E11 前置检查及明确继续域；本件选择不合格就会话 fail-stop，不能靠回滚或“正常返回”四字代替源码资格。

**选择性取消成为无费筛选。** 看过结果后才决定该次算不算正常完成，保留有利随机状态、回退不利状态，或在中心判决前利用状态先跑下一 job，都不满足本定义。是否进入完整合格终态由预定源合同决定，状态是否保留不由结果价值或 D/R 身份决定。各臂自己的合法策略仍可影响请求和撤销，但不能获得另一套状态选择能力。

## 10. 轨迹、账本及可推断的效应

最低审计记录须把五条因果链连起来：来源会话与正常完成链；每个 job 的冻结输入／任务投影／已接纳尾版本；候选输出与中心接受或拒绝；全部服务和生命周期费用；实体、真实任务服务与全臂 Stop。每个 job 至少能回指源 generation、前一合格完成节点、输入身份、各阶段状态、受保护 commit 是否进入、结果封存事件、源结束节点、中心终态及释放事件。拒收也有节点，失败也有费用，均不从历史中消失。

RNG 留证应能证明采用的初态／后端及实际源调用的因果继承，但不要求中心免费逐 job 读取完整随机状态。若未来选择保存状态指纹、调用记录或审计副本，相应读取／散列／存储均收费且不向策略提供新能力；本件不生成种子、随机轨迹或任何载荷。完整内核快照不是这里最低审计记录的必要字段，也不能把一串版本号本身当作实际重放证据。

在共同 AA 方法、服务模型与源定义域均成立且完整评分可用时，原主 D-S−R-S 仍可估计固定 roster 中的闭环总效应；D-S−E0 仍判断购买途中证据的整体实用性。该效应包含查询引起的任务知识、规划触发、源历史、延迟和费用等后继影响。其名称应指明 `ALWAYS_ADVANCE / FAIL_STOP_SOURCE` profile，不能与 accepted-only 下的目标量混用。

若采用 E10，宿主运行时噪声、时钟映射和 block 独立性仍须另行满足。ALWAYS_ADVANCE 的非预知证明不会自动使原有区间有效。源定义域失效时，即使物理服务可数，也不把源码损坏造成的差异作为 D/R 因果效应；合法守卫失败或已规定会话停止则保留完整 roster、原权重和真实可证评分。

要区分“D/R 差异是否依赖源历史制度”，最小交叉对照是固定同一 S、同一共同源与服务制度，登记 D 与 R 各在 AA、accepted-only 下的组合，分别形成两个 D−R 对比，再比较它们的差。只加一个 R 的回滚臂不能识别 D 与状态制度的交互。这个交叉不属于本件已采用的新确认主检验，也不生成运行样本；若未来只采用 AA 而没有合格 accepted-only 后端，就保留“尚未区分该交互”的限制，不能伪造廉价回滚对照。

即使上述交叉可运行，它得到的也是含真实快照／恢复费用的制度交互，不能自动分离纯随机历史效应与后端开销。剥离后者需要另一个明确受控来源问题及相应可行计费合同，不能免费删掉快照费用。固定共同计划的有限执行比较可辅助观察执行层机制，但不能替代主 lifelong 闭环总体，也不能反推主效应只由查询造成。

## 11. 根若采用，必须同步改写的最小范围

当前 73 §8 的“丢弃内部状态不进入后继”、§9 的接纳副本／丢弃随机状态、恢复已接纳基准／CAS 提升，及 E6 所依赖的逐 job 检查点规格，需在新候选正文统一替换为本件的正常完成链与会话停止语义；旧 E6/E10 文件字节继续保留，只在新候选说明哪些条款被替代。E10 自身目前仍写 accepted-only，不能仅贴一个 AA 标签就称两件已相容。

相应修改规划源状态版本的 CAS、输出待处置屏障、任务／future_path 权威、软取消与硬失败、全过程费用、来源与方法命名、未来 W28/W29/W30 等未运行检查合同及推断解释。原物理安全、全部源资格和外部基线责任不由这一替换省略。必须明确是修改版 R0 核心适配，而非 native 全程序的运行等价。

本件实际关闭的是方法选择层：accepted-only 并非必要；一个没有逐 job 快照的、非预知且可公平比较的 AA 会话合同已经具体给出。仍未关闭的是固定源在拒收历史后的继续域、E11 接口资格、实际服务／时钟／依赖／回收绑定及全部候选审查。根可以选择它作为降低后端复杂度的下一工作路线，但不能据此给 G3/G6 或其余门改判 PASS。

本次仅创建本文件；目标创建前已精确核为不存在。没有读取 35–38、受限 Q-CAL／Q-CONFIRM、地图／scen／配置／任务／delay 载荷或桌面，没有调用 skill、Claude 或新 agent，没有源码修改、伪代码、实现、构建、测试、仿真、调参、实验载荷／结果或 Git 操作。所有保护参数保持原义且未给数值、范围、默认值。正式科研导师 skill、Nature reviewer skill、完全无 skill 独立逻辑审查及根验收尚未由本件启动或替代。研究仍未完成，HOLD 保持。
