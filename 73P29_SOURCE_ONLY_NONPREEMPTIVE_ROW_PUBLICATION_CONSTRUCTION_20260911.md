# 73P29：仅规划源非抢占、原行发布的时间与撤权合同

2026-09-11；/root/source73_r0_remaining_contract。NO-SKILL / CONSTRUCTIVE / NONBALLOT。候选名 SOURCE_ONLY_NONPREEMPTIVE_ROW_PUBLICATION。本件不改主稿、不授G3/G6资格；HOLD / NO-GO-IMPLEMENT / NO-GO-MAIN-EXPERIMENT保持。父任务转入新根交接时C22已经启动，随后只续原调用并完成既定四件封存；交接不是研究完成。

## 1. 有界结论与唯一建议

固定公共O不必加入a+d完成事件，也可定义因果一致的非抢占源服务：完整体的宿主返回、逻辑达到完成阈值、原行源发布、中心处置和源槽释放是五件不同的事。前提是输入/时钟/状态隔离真实成立；每个发表只用原后继行和原完整发布检查容量；所付完整体及取消尾费不回写虚拟服务时刻。无需凭“宿主先算完”直接否定模型，也不能因此证明当前R0/ECBS具有资格。

原稿有两个不能默默继承的句子：Srv_P的处置收件槽只由后继PROCESS_SLICE消费；硬WORKER_FAIL/INIT截止要求终止源。删除源指令切片后，前者失去原收费执行载体；非抢占完整体则不满足后者的中途指令停止。唯一建议是将**源job的完成、撤权、处置和行政回收作为一个显式源生命周期修订**：正常终态收件只作原发布容量可完整支付的本源有限控制检查；源外部硬撤权改为REVOKE_AND_DRAIN，立即禁止输出/新调用，保留完整体和真实回收。它改变源服务/故障能力，不是给旧合同换名字。

这套纸面选择不采用“只把收尾继续放回精确源指令片”的混合方案，也不让任意析构/清理在发布点免费执行。它仍依赖原公共发布检查可计量、实际隔离/时钟/来源继续域及物理后端；这些是具体未闭义务，不能把条件构造计作G3/G6 PASS。

## 2. 实际输入与调用身份

当前主稿73_PIE_SPATIAL_EVIDENCE_TERMINAL_HANDOFF_AND_PAID_SERVICE_PREEXPERIMENT_DRAFT_20260908.md实核SHA256 9e06199cc6045ddd750b861b3091ec239c7c6ff2ae7eeaebbdd0e7fde61ec113，216498 bytes/665 LF。父任务给出私有main52a798e2875772387514bbd0f63a6c6311bb6ea6，本代理未做Git/远端核验，不冒称亲验。

完整读73C8SR_ROOT_SEALED_SERVICE_CAUSAL_COST_AND_PAIRED_DESIGN_ANALYSIS_20260910.md；主稿读§8 L210–234、§9 L236–360、§10 L362–394及L418–444、§12 L477–505必要完整段。合并输出中的末部截断已补读L334–378与L499–505；没有据“关键词已搜”称源码已全读。仅读科学合同，不读模板所指实例或配置，不扩QEMU、统计或文献综述。

写前精确确认P29及C22提示/raw/完整回执四目标不存在且非符号链接。C21旧95740→6348b3真实exit0且完整回执身份保持；失落P7/L7不碰。C22固定提示SHA256 16835758832351e4fd74b279a7e906ca022850804c0968968fd21a855917b519；唯一正常升级调用functions cell323→exec81862，初始a149e5；a3399c仍活，e6165e真实exit0。JSON为completed/end_turn/is_error=false，实际claude-opus-5/firstParty、END-C22，duration52346ms，费用0.11914749999999999 USD。正文744空白分词，超建议约700，如实保原；完整原文和逐项独立纠错另在C22回执。本件不是C17/C14的重试。

## 3. 启动边界、完整体与权限

只改各臂唯一Srv_P，内部所有引用R0的比较及外部ECBS共享这一服务规则，不把不同核心工作数做成相同。Srv_C、Srv_E、Srv_A、Srv_N、控制以及其PROCESS_SLICE、FIFO、发布容量和公共机会均不免除。SOURCE、PLAN_CHECK/PROJECT、H19触发/名义量/cut/合图与中心全检继续在原中心收费；物理、真实任务账本与原TASK_SERVICE不等待源。

中心先按原付费事务冻结合法输入并通过原通道交付。a是**实际原Srv_P可启动行的公共时刻**，不是中心较早捕获时刻或宿主调用的UTC；源在该行固定本job/session/前一合格私有节点及不可变输入引用，未另付的安装从完整体起点计。排队取消在安装前不改变源；开始体后不能把它重记成NO_INPUT以逃掉已发生工作。

输入可读域只含冻结已交付知识、该源私有历史、原角色随机状态及声明时钟。冻结输入结构体不等于进程隔离，文件/描述符、共享内存、环境、库回调和外部调用仍须逐项可核；未来O、未分配任务、真实pose/z/eta、其它臂及未来中心处置不可进入。管理器能持有封存duration也不能用它事先改调度或控制。单独约束输入不变仍不足证明实际来源满足上述边界。

完整体包含尚未另付的输入安装、核心、PRE_COMMIT_SHAPE、受保护原commit、不可变输出/结束节点封存，以及只依已冻结输入/源状态的临时对象清理。测量末端必须在这些工作之后；不能只取plan返回点。上游已付复制不重复扣同一操作，未执行工作不虚收。核心失败或shape拒绝只走已登记合法清理/停止分支，不补WAIT，不称正常AA结束。初始化与预处理同样纳入这个来源服务定义，不能免费预跑。

源私有结果的封存/计量元信息采用事先声明的有限结束记录；采集和日志留证费用本身要归属，不能靠无限“测量测量器”递归追加新的过去d。实际边界/读钟遗漏尚未认证时，完整duration与对应来源资格未知，不能用大致函数计时替完整体。

## 4. 固定O中的完整阶段规则

设d为从完整体起点到约定末端的实际单调经过量，c=a+d只是管理器私有的“允许发表阈值”，**不是新增事件或row**。CPU是另一个实际计量量；d包含该体宿主调度等待，不能叫纯CPU服务。原PROCESS_SLICE(Srv_P)逐指令额度明确取消，以完整体非抢占的时间占用取代；原该类行只提供下一源启动的机会，busy期间不授并行体或预跑后继。其本体不靠中途公共行获得额外指令片。

源job从a起占原唯一源槽，宿主已得到结束节点也不得将其变为后继源输入。仅在某个原O行r严格后于启动行、t_r≥c、此前行内次序未撤权、且本源writer完整发布检查容量可支付时，才可封存结果envelope对原规划通道发表。不预先遍历未来O找最有利发布行；逐行到达按原规范key判断。检查失败/容量不足保留费用与占槽，后续已有行再按原规则处理；没有合格行就是未发表，不插行或追加“仅完成一次”的机会。

源发表不等于中心接纳。原Srv_N投送、中心后继接收/验证/CAS分别收费；中心不持槽等待源。中心返回ACCEPT/STALE或原合法处置，走既有通道至本job事前准备的有限控制收件槽。源在严格后于该回执合法可用行的本源发布检查点，完整支付job/session/结束节点/处置身份、模式/权限及幂等校验，才关闭当前槽。该检查只处理已限定控制字段；需要遍历路径/重算几何/执行任意析构不能塞入这个点，仍归原中心计算或下节行政回收。字段位长/复制/认证与失败实际收费，某行不足完整支付就保持pending。

这明确替换旧L350“必须在后继源指令切片消费”的一句，使用的是既有源writer控制发布容量，不借Srv_C空槽，不给第二worker、不新建指令额度。它是原有限发布语义的具体承接，不声称真实宿主计算零耗时；其实际计量执行能力沿原发布后端待证。下一源体只在槽释放后**严格后继**的原合格Srv_P行开始，不能在同一发布行顺手递归启动。

源内部正常完成可以保留S'及原RNG历史，但逻辑可用必须同时满足完整体到时、结果处置及槽释放；AA不等于让后继提前读S'。外部CAS拒收不会撤回nonce、首次资格或随机消费；后继输入仍从已接纳Π/合法知识重新冻结，不把被拒提议当外部承诺。

## 5. 零duration、同刻排序与源clock

d可能因分辨率显示为零，不添epsilon，不据此免CPU/复制/内存费用。r必须严格后于a的启动行，释放后又须严格后继启动；有限O因而不能在一行无限递归，但可以有有限多个同物理时刻的后继行。这只保证文法无同一行递归，不保证源函数返回或发布容量够用。

每次原chrono调用明确改为a加本体该次实际单调经过量，经同一冻结ABI/单位的非降转换；不是旧“读取当前公共row的t”。若该调用的经过量为u，必须有0≤u≤d；后继a_next≥前c并晚于结果处置/释放，则a_next≥a+d≥a+u。在同一epoch、相同转换规则、无溢出/时钟回拨前提下，跨job源clock非降；允许相等，不把row_id编码成额外时间，也不私自重置源累计CPU。

若主机末端d被截短、不同clock域混用、float日志不是可核准确记录或期间clock读值超过约定d，此证明不适用。实际测量tick若有有限整数与固定单位，可给c精确有限表示；本题未绑定这套接口，不能直接假定任意宿主d都是代数数。各原chrono别名、duration_cast/比较与预算增量保持原语义，值域及单位转换另待核；源输出改变属于修改版来源，不能称native逐结果等价。

相等时间的撤权不用“≤c一概拦截”代替行序。原一行先物理/自主、再发布、最后FAULT/MODE主体：若本行FAULT尚未生效，不能偷看它拦更早发布；若前行或更早同刻阶段已撤权则不得发布。数学Stop和公共故障按原全序生效，不用宿主求值先后推迟它们；没有新增c事件需要和FAULT竞争排序。

## 6. 唯一源生命周期修订：撤权后收尾

具体修订把源外部关闭（原WORKER_FAIL的本profile含义、INIT截止、全臂Stop及使后继源不可合法交接的中心fail-stop）定义为REVOKE_AND_DRAIN：在其原公共位置立即撤销该会话的输出与新体权限；已开始体仍按冻结读域完成或呈现真实源异常/未返回。正常完整私有结束可留审计节点，但关闭后的节点没有再启动资格，不能把取消后的状态称下一合法AA热状态。源“被撤权”“body已结束”“退出已确认”“引用已消失可回收”分开，关闭不等于内存可复用。

这**替换**原硬WORKER_FAIL的中途终止语义，不暗称两者相同，也不保留“硬故障精确停止后续源指令”的保证。反例是：一个源在a启动，某个原公共故障行位于a与c之间；宿主已执行故障之后才访问的私有对象。仅撤输出并全计d可以合法定义非抢占备选，却不能证明那些访问没有发生或源已按旧规则被杀；把两种叙述并用会产生矛盾。此处只有符号时间关系，没有生成真实日历/源轨迹/参数。

真正的宿主进程崩溃、异常展开、源未定义访问与计量故障仍按实际发生记录，不被REVOKE_AND_DRAIN改成正常完成。不能自造有限硬tail上界，不要求无限运行的源一定能被安全析构。其终止/退出未认证时一直不复用Srv_P，费用或依赖轨迹可能未知；不依进程消失就抹掉已执行局部析构和对象错误。

只因已发生撤销才需要的会话析构、回收/日志关闭，在该撤销之后、且对象没有活访问后才作为行政收尾实际执行，完整计CPU/经过量/内存/字节及失败。它不更新策略、Π或Q、不让新源体启动，也不冒充在a之前已经发生；不能倒填进原d。没有全臂Stop后的公共行不豁免实际行政尾费；但这些工作不创造应用行、不追加服务或让撤销失效。正常处置只释放控制槽并保留持续会话，不执行这种依未来撤销的析构。

若不接受这一明确的源故障语义变化，则本候选不能同时保留原硬终止合同；结论应是备选不采用，而不是继续从普通宿主stopwatch声称已恢复精确中断。

## 7. 费用、等待与失败的总规则

完整体实际用量、逻辑占槽和发表等待分账：a至c是非抢占服务elapsed；c至实际源发表是已完成未发表占槽；源发表至处置到达/消费释放仍占槽。等待可能到Stop仍未结束，已发生时间与内存保留可计，不预付未知未来CPU。初始化/输入安装/核心/shape/commit/封存/临时清理、原中心处理、通道、有限发布检查、行政尾部都有各自真实归属，不能只报已接纳规划或只计Stop前原源指令。

Stop前开始的完整非抢占体，即使c晚于Stop也计其完整已执行尾费；不得按Stop截成本以模拟旧抢占。不用尾部结果补计划或Q，未来取消才需要的清理另计且不能预读。CENTER_FAIL_STOP不是全臂Stop；旧合法物理控制/真实TASK_SERVICE可按原规则继续，只关闭中心/源新发表能力。INIT截止前未完成中心INIT_READY就是原INIT_INCOMPLETE，不能用宿主早算完或晚发布追认成功。

NATURAL记录全部实际体/等待/后续费用及真实竞争。STRICT中取消的Srv_P指令片配额不再虚构为预付空闲工作；保留同一个源worker服务可用性、原行启动/发表规则以及其它站原预付制度，同时另报真实体与尾费。若需给非抢占worker的保留时长/空闲一个单独定价，必须后续明确固定计费函数，不擅自把旧指令费率换成elapsed或给新权重；当前无数值。新模型的各臂能力同规则不代表elapsed、内存或总费相同，ECBS不虚收R0特有修复工作。

完整正常返回才有完整d和结束节点；异常有合法已测终态也不自动属于正常继续域。未返回、未取回异常、部分计量或身份丢失只保留已证前缀与未知项。候选允许状态保持pending，有限O耗尽不算超时成功。源异常/数值或映射不闭仍适用SOURCE_ERROR/UNVERIFIABLE/INFERENCE_UNAVAILABLE等既有分类；Q可否辨认另按已认证物理/服务依赖处理，不统一填零，不删roster或失败臂。尾费不完整不许捏造总成本上界。

## 8. C22的独立纠错与本题剩余义务

C22实际完整意见正确指出旧收件处置载体被删除；但其“没有后继所以不total”把可定义的pending与预期释放机制缺失混为一谈。它将源发表误归Srv_C，建议对后体保留精确源切片，又把取消精确配额说成“不增加能力”，均不作为本报告的方案。其硬WORKER_FAIL只记reap证据未知，未正面处理语义冲突。本件采用上面的单一源生命周期替换；原文不改，完整逐项纠错见receipt。

已关闭的是纸面阶段歧义：c可只是隐藏阈值；固定后继行发表不会自行新增O；d=0可按row序防递归；源不提前使用AA节点；完整体/等待/撤销析构各分账。条件非预知证明按已发表历史归纳：启动只取当前合法I与可用S，源只能看这些信息及声明计时，封存内容不影响c以前策略，后继发表又受原O/权限控制。该证明以源读域和宿主调度无偷看为前提，不从“未来世界还没算”推出。

尚未关闭且停止扩查的具体义务有三组：实际完整源读取/持续状态及返回域；同一clock/测量末端、CPU/内存及结束/退出回执；原剩余站及发布检查的实际有限计量、物理事件认证。无限源调用仍可阻断宿主证据取得，不能拿纸面pending保证实际跑完整窗口。配对统计与宿主顺序效应由根另分析，本题不授独立创新或任何CI。新模型相对严格源容量的算法/输出/费用差异必须重新声明，不能只以更易执行为理由宣布科学设计合格。

## 9. 交付与交接边界

本有界P29独立构造和唯一C22调用均完成；主模型尚未采用，源修订的实现、实际测试、资格审查与整项研究未完成。父任务要求交接后没有另起咨询/检索/后端任务，只续原cell323/session81862至exit0，保原JSON/result并完成本四文件。

所有新文字仅apply_patch写研究目录；旧C21、主稿、行政/Git、桌面与旧报告字节不动。所有shell链段rtk；无skill，无实现/伪代码/构建测试/仿真/调参，无保护参数赋值/范围/default，无35–38/Q/地图/配置/任务/驱动/结果载荷读取或生成。最终全文核对、raw-result逐字比较及SHA256/bytes/LF回执后结束；无活句柄，不将交接或本件完成当作研究完成。
